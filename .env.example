# 前端的构建期变量。复制成 `.env.local` 再填（`.env*` 已在 .gitignore 里，只有这份 example 进仓库）。
#
# 三个值都不填也能构建：同步功能整体关闭，设置页会说明原因，手动导出照常可用。
# 它们是**构建期**注入的，改完要重新 `npm run build` / `npm run build:native`。

# 同步后端的地址，不要带结尾斜杠。
VITE_SYNC_API_BASE=https://sync.gamestao.com

# Google OAuth 客户端 ID（Google Cloud Console → API 和服务 → 凭据）。
#   Web 应用类型的那个：web 版和 **Android** 都用它
#   iOS 类型的那个：只有 iOS 用
# Android 还需要在控制台建一个 Android 类型的客户端（填包名 + 签名 SHA-1），
# 但那个 ID **不进代码**，见 deploy/README.md。
VITE_GOOGLE_WEB_CLIENT_ID=
VITE_GOOGLE_IOS_CLIENT_ID=
