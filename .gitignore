node_modules/
dist/
dist-ssr/
*.local
*.tsbuildinfo
.env
.env.*
!.env.example

.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

coverage/
*.log
npm-debug.log*

# Cloudflare Workers 本地状态/缓存（wrangler deploy 会在本机生成，不用入库）
.wrangler/

# 学习素材：音频与文稿只存本地，永不入库（见 SPEC.md §3.1.1 R-5）
*.mp3
*.m4a
*.wav
manuskript/
backup-*.json

# 离线对齐的模型权重（FR-15）：约 200MB，可由 npm run stage:align 从 HF 重建，不入库。
# ORT 的 wasm 不在这里 —— 它走 Vite ?url，构建时自动进 dist/assets/。
public/models/

# 词典的**源**文件（FR-16）：de.sqlite3 一份就 1GB。不入库，npm run build:dict 会重新下。
# 注意 public/dict/ 里的**产物**是入库的 —— 它只有几 MB，换来的是 CI 完全不必碰这 1GB。
# 排除标准跟 public/models/ 是同一条：按体积，不是按原则。
.cache/

# 同步后端（server/）的运行时产物。SQLite 库里是**真实学习数据**（含 DW 正文），
# 与 backup-*.json 同一条理由：永不入库。node_modules 是它自己那套依赖。
server/data/
server/node_modules/

# 从 Google Cloud Console 下载的 OAuth 客户端文件（`google/` 是浏览器默认落点）。
# 这套方案**用不到 client_secret**（前端拿 ID token、后端验签，不走授权码交换），
# 而它一旦进了 git 历史就很难拔干净。需要的值已经在别处：
# 客户端 ID 在 GitHub 仓库变量与服务器 .env 里，iOS 的反向域名在 Info.plist 里。
google/
client_secret*.json

# 别的会话开在仓库里的 git worktree（.claude/worktrees/<分支>），是同一批文件的另一份快照。
.claude/worktrees/

# 原生签名材料。ios/ 与 android/ 两个工程本身入库（它们有手写的改动：Info.plist 的
# 权限说明、AppDelegate 的音频会话、build.gradle 的签名配置），但**密钥一律不入库** ——
# CI 从 secrets 里解出来用。生成方法见 .github/workflows/release-android.yml 头部。
# 各平台的生成物（ios/App/App/public、android/app/src/main/assets/public、build/、
# Pods/、DerivedData 等）由 ios/.gitignore 与 android/.gitignore 各自排除。
*.jks
*.keystore
*.p12
*.mobileprovision
*.p8
