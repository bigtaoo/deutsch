# 德语精听训练器 — 需求文档 v1

> 个人自用工具，单用户，不联网，不托管内容。
> 目标节奏：每周 1 篇 DW Alltagsdeutsch，一年约 50 篇。
> 工具的价值在于**降低单篇的摩擦成本**，不在于加速学习。

---

## 0. 相对原始规格的变更

| # | 变更 | 理由 |
|---|---|---|
| 1 | 运行形态定为 **PWA**，明确桌面/手机的**设备角色分工** | 复习必须在碎片时间做，绑死桌面浏览器等于复习不会发生 |
| 2 | 新增 **§3.3 能力依赖链**：时间戳 → 挖空 → 听写 → 音频卡 | 原规格中「听写点句子重播」和「手动稀疏打点」互相矛盾，必须显式约束 |
| 3 | 新增 **导入时的段落排除**（FR-1.4） | DW Manuskript 含标题、栏目说明、文末 Glossar，音频里没有 |
| 4 | 切句从「自动」改为 **自动 + 强制可手工修正**（FR-2.3） | `Intl.Segmenter` 对 `z. B.` 一定会断错，切错一次整篇报废 |
| 5 | `newPerDay` 默认 30 → **10**，`reviewPerDay` 70 → **60** | 每周 1 篇 ≈ 每天 3–4 个新词，30 会导致周一清空、之后六天空转 |
| 6 | 新增 V1 的**生词去重降级策略**（FR-9.3） | V1 没有 lemma，同一个词跨课会生成两张卡 |
| 7 | 新增**存储持久化与备份**为 V1 必做项（FR-11） | IndexedDB 会被浏览器驱逐；一年的积累不能靠运气 |
| 8 | 数据模型新增 `updatedAt` / `audioFileName` / `audioDuration` / `Blank.vocabEntryId` | 分别支撑：跨设备合并、手机端音频重绑定校验、听写错误回写 FSRS |
| 9 | 「可分享包」导出**函数在 V1 就实现并加测试**，UI 留到 V2 | 边界事后拆很痛；纯函数 + 断言测试的成本接近零 |
| 10 | 新增 **FR-13 来源与自动导入**，**纯前端实现，不需要任何后端进程** | 实测 DW 的 RSS、页面、mp3 CDN 三者**全部返回 `Access-Control-Allow-Origin: *`**（见附录 A），浏览器可直连，原先设想的本地 helper 整个不需要 |
| 11 | 新增 **FR-14 Glossar 候选词** | DW 的 `manuscript` 里用 `<span data-type="GLOSSARY">` 内联标注了生词，并给出**词条（含性和复数）+ 德语释义**。这把 V2 才打算做的语境释义在 DW 素材上直接白送（见附录 A.3） |
| 12 | §2.5 单一 origin（原「桌面 localhost + 手机 https 两个 origin」作废） | 没有 helper 就没有同源需求，桌面手机跑同一份部署，功能完全一致 |
| 13 | **§2.3 数据分层：缓存层 vs 标注层**，数据模型据此拆开，FR-3 重写为缓存管理 | 素材下载一次就缓存、永不重复下载；缓存不跨端，每台设备自己补齐。连带解决了 Q2（归档旧课程），并把备份要保护的数据量从 700 MB 压到几百 KB |
| 14 | **§2.6 重写为「备份与同步」，主次对调**：备份是首要需求，同步是副产品。~~方案为 GitHub 私有仓库自动备份~~（**运输方式已于 2026-09-02 换成自建后端，见变更 25；「备份优先、同步是副产品」这个判断本身没变**）（**不建服务器**），FR-11 相应重写并升为 V1 最高优先级的非学习功能 | 初稿把两者当同一件事（「手动搬一次顺带备份」）。**这对同步成立，对备份不成立** —— 备份的失效模式正是「你忘了」。且威胁模型里「写坏数据后覆盖备份」和「几年后工具跑不起来」两条，要求方案必须有**版本历史**且**格式能脱离工具被读懂** |
| 16 | **实现期回写（2026-08-31，§9 第 2–11 步完成后）**：`Sentence.index` 一律等于数组下标，**排除不重排号**（FR-1.4 修订）；听写「转写等价」放宽到「去变音符」写法（§7.4 修订）；`FSRSCard` 增补 `learning_steps`（§6 修订）；候选词接受同样受 §3.3 R1 约束（FR-14.2 修订） | 四条都是实现时才暴露的：第一条若按原文重排会让生词出处静默错位；第二条是 §7.4 的规则与 §10 的验收例子（`Hauser` → 转写等价）本身对不上；第三条丢了会让同日二次复习退化；第四条是两条规则的交叉点，原文没写 |
| 17 | **新增 FR-15 自动打点**：浏览器内跑 CTC 强制对齐（MMS-FA），从「已切好的句子 + 音频」直接算出句级与词级时间戳，结果一律标为**待校对** | 打点是整条能力依赖链（§3.3）的入口，也是唯一必须逐句手工做的一步 —— 一期 40+ 句，这是实际使用中最大的摩擦。强制对齐（文本已知）而不是 ASR（文本待猜）让这件事从「猜」变成「算」，且**音频不出设备**，§3.1.1 R-1 不受影响。刻意不叫「完成」：实测低置信句正好命中英语借词、被丢的数字这几类，必须留人工校对这一步 |
| 18 | **新增 §7.10 原生壳（Capacitor）**：iOS + Android 两套原生工程入库，CI 出包（iOS 自动上传 App Store Connect）。对齐权重随包带（**实测 490.2 MiB**，不是最初估的 200MB，见变更 19） | Q9 的手机那一半。FR-15 之后套壳有了三个纯 web 版给不了的东西：权重随包带（装完第一次用就离线）、iOS 上的数据寿命不再依赖「用户记得添加到主屏幕」、以及 `AVAudioSession` 一行修掉静音开关静音 `<audio>`。壳里跑的是同一份产物，业务代码只多了一个 `src/platform/native.ts` |
| 20 | **手工标注整节删除（2026-09-01）**：FR-4 的打点界面（含 `Enter` 打点、±0.1s 微调、清除时间戳）连同「标注」tab 一起去掉，时间戳只有一个来源 —— 下载完课程立刻自动对齐，进度常驻在应用底部。FR-4 里只保留 FR-4.4 的区间推断规则。同时把 FR-15 的失败面补齐：黑匣子（`src/align/journal.ts`）+ 后端降档阶梯 + 按 Range 分片取权重 | FR-15 之后手工打点已经从「每期 40 句」降到「偶尔核对一两句」，但那一整套界面还占着一个 tab、还是新课的必经动线，等于把一件不必再做的事继续摆在路上。删掉它的前提是自动对齐要**真的跑得完** —— 而它在 iPhone 上不跑得完（加载权重时整个应用被系统杀掉），所以这次连着做了三件配套的事：把死亡现场记下来（进程被杀时 JS 没有任何机会报错）、崩过的后端下次不再用、以及把峰值内存里那几份 187MB 砍掉 |
| 15 | ~~**不做 OAuth 登录**~~ **（已被变更 25 整体取代）**，改为「粘贴一次 PAT，其余全自动」：应用自动识别账号、**一键创建私有备份仓库**、校验权限、监控 token 过期 | 实测 GitHub 的 OAuth 端点不支持 CORS 且不支持 PKCE，纯前端拿不到 token（附录 B.1）。但 `api.github.com` 完全开放，所以「填 owner/repo」这一步可以彻底消灭，手工操作只剩「建 token + 粘贴」 |
| 19 | **iOS 发布回写（2026-09-01，流水线首次真跑之后）**：§7.10 的三处数字与两处判断按实测改正 —— 权重实际 **490.2 MiB**（`q4f16` 187.6 + `int8` 302.6 两份都带）、IPA **381MB**、装机 **553.6MB**；TestFlight 「处理完成」之后还有两道（出口合规、**必须有测试组**）；砍掉用不到的那份权重是唯一值得做的体积优化，为此在设置页加了对齐后端诊断 | 全部来自第一次真机链路：本机没有 Mac，此前所有数字都是估的。「200MB」这个数字在 SPEC / README / `capacitor.config.ts` 三处传抄了，实测差 2.45 倍。测试组那一道是原文完全没有的：文档原写「处理完之后内部测试员自动可见」，实测**一个测试组都没有时 build 不出现在任何人的 TestFlight 里**，处理完成 ≠ 可见 |
| 21 | **对齐后端阶梯实测改正（2026-09-02）**：第 2 档从 `wasm/int8`（302.6 MiB）换成 `wasm/q4`（230.3 MiB）—— 原来那一档**加载即被系统杀掉**，也就是「没有 WebGPU 的浏览器」以前是必死，不是文档写的「慢一个量级」；随包权重因此从 490.2 MiB 降到 **417.9 MiB**。同时补上降档的漏洞：Worker 被杀（主线程还活着）以前记成 `error`，不进 `crashed` 计数，那一档会被**无限重试**。并把 emissions / viterbi 之间切出一道缝（`src/align/emissionMatrix.ts`） | 三件事都来自同一次实测（Windows/Chrome、32GB）：`webgpu/q4f16` 真实一课 8:05 跑完 45/45 句、26 秒；`wasm/int8` 加载时渲染进程直接消失、JS 侧无任何报错；而 4-bit 那两份（q4 230.3 / bnb4 212.2）在 wasm 上都跑得通 —— 「int8 是 WASM 唯一能跑的组合」是错的。那道缝是给手机准备的：`q4f16` 已经是这个模型最小的变体，iPhone 13 两档都被杀，降档救不了；而 emissions 那一半**压根不看文稿**（CTC 前向只吃波形），所以它可以整块挪出 WebView（原生插件 / 远端）而**德语正文一个字都不出设备** —— §3.1 关于正文的约束因此完全不受影响 |
| 22 | **滑窗漂移修正（2026-09-02）**：`windowed.ts` 的窗口从 **30 秒改成 120 秒**，并按修完之后的分布把 `REVIEW_MARGIN` 从 0.8 重标为 **0.5** | 一课自动对齐后 **31/45 句**被标成低置信,查下来不是阈值问题而是滑窗真的错了：同一份 emissions，滑窗跑出整体 score −1.191 / 中位数 −2.99,整段对齐跑出 −0.604 / −1.17。逐窗打点看到漂移 −1052 → −554 → −25 → +455 → +565 → +684 帧,15 窗里有 3 窗喂进去的 token 装不进窗口，最后把 904 个字符（约 70 秒的话）挤进 19.7 秒 ——一句 45~53 字符/秒，物理上念不出来。根因是 30 秒窗口比 Alltagsdeutsch 的长句（437/475/625 字符）还短，而窗内 token 数只能靠全局平均速率估。三课实测 120 秒窗与整段对齐在「最差单句」上完全持平（−2.09/−1.77/−1.59），代价只有回溯缓冲 0.7MB → 11MB（整段要 143~387MB）。连带发现 `REVIEW_MARGIN` 的原始标定数字（最差 −3.50）正是这个 bug 的症状 |
| 23 | **新增 FR-16 内置词典 + FR-17 预置词库（2026-09-02）**：词典随包带（**实测产物 35.0 MiB**：词条 26.2 / 词形索引 8.9 / 牌组 0.5），由 `npm run build:dict` 从 WikDict + 一份口语词频表编译成 256 个分桶 JSON；预置词库分**六个口语词频档**，默认从第 4 档取词。连带把 §1.1 里「词形还原 / 离线词典」两条从非目标移出，并据此修订 FR-7.4（自动填性/复数/释义/IPA）、FR-9.3（去重从 surface 升级到词元）、FR-10.5（新增第四种卡「预置词」） | 「笔记不多时也能练」这个需求本身只需要一份词表，但**词表给不了卡背**，所以词典是前提，不是附加。三处判断值得记住：① **词典不是包体问题**，是分层问题 —— 装机已经 481MB（其中 418MB 是对齐权重），35MB 是噪声；真正的约束是 §2.3 的「可重建 → 缓存层」，所以词典产物入库而**源文件（1GB 的 `de.sqlite3`）不入库**，换来 CI 完全不必碰它。② **「A1–C2 六档」这个形状拼不出来**：官方 Goethe Wortliste 只有 A1(650)/A2(1300)/B1(2400)，B2/C1/C2 **没有官方词表**，且那三份是 © Goethe-Institut，塞进上架 App 属于再发布、还撞 EU 数据库权（§ 87a/b UrhG）与 §3.1 R-5 自己的立场。所以档位改成**词频名次**并在界面上明说不是 CEFR —— 把词频档标成 A1/B2 是编数据。③ **预置卡的正面只给声音、不给文字**：给了文字它就是「看词回忆意思」，而这个应用存在的理由是听觉识别。<br>同批还加了 **FR-13.12 回填最近几期** —— 预置词库解决「有卡可练」，但它的孤立词发音练不到连读；旧刊的 Glossar 才是带真语料的那一批。R-3 因此要重新读一遍：它禁的是「一键导入全部 100 期」那个形状，不是「有循环」（上限 + 只取已拉到的列表 + 复用 `politely()` 就满足它） |
| 24 | **开头的标题+导语不再自动排除，通听改为逐词高亮（2026-09-02）**：FR-13.7 从「自动排除首个 `<strong>` 块」改成**一句都不排**（那三句照常参与对齐与练习），「切句」页补一个「开头 N 句 → 恢复 / 排除」的批量控件；FR-5.2 从句级高亮升级为**逐词高亮 + 自动滚动 + 点词从那里播**（`src/lesson/karaoke.ts`） | 前一半是**原假设被实测推翻**：附录 A.3 写着那个块「音频里不朗读」，而真实素材（Alltagsdeutsch 45334084）里播音员把标题和导语一字不差地念了出来。这不只是少练三句 —— 排除句不进对齐目标（`target.ts` 那条硬规则），于是开头几十秒真实存在的声音无处安放，只能被挤给第一句正文。方向也不对称：把念过的句子排掉是把真音频挖成空洞，而把没念的句子留下只会让那一句自己拿到低置信度并被标出来。修的是默认值，不是能力：手动粘贴 PDF 时的文末 Glossar 仍然要排，所以两个批量控件（开头 / 文末）都在，而「恢复」那一半正是为了一次撤掉旧课里已经排掉的那几句。<br>后一半是**把已有数据接出来**：词级时间戳从 FR-15 第一天起就在算、在存（当时在 `LessonCache.wordTimings`，一课约 800 个；后来搬进 `Sentence.words`，见变更 26），却只有听写在用。句级高亮只能回答「读到第几句」，而这个应用要练的正是「听到的这一串音是屏幕上的哪个词」。渲染单位用 `tokens.ts`（与挖空同一套坐标）而不是 wordTimings 自己的罗马化词，否则 `Work-and-Travel` 会碎成三块闪、数字和引号会整个消失。FR-5.2 的「不做伪同步」原则一寸没退：没有时间戳的句子照旧不高亮，只有**两侧都有真锚点**时才把中间被罗马化丢掉的数字补上（`bridged`），一句词级时间戳全缺时宁可只做句级高亮 |
| 25 | **备份/同步从 GitHub 私有仓库换成自建后端 + Google 登录（2026-09-02）**：`src/github/` 整套删除（含 PAT 连接、一键建仓、FR-11.18 配对二维码与 `jsqr`/`qrcode` 两个依赖），换成 `server/`（Node + Hono + SQLite，跑在自己的 VPS 上，域名 `sync.gamestao.com`）与 `src/sync/`（Google 登录 → 会话令牌 → 按文档推拉）。§2.4 的合并规则、离线队列、状态常驻可见、手动导出这四样一字未改 | **换的是运输方式，不是备份策略**。PAT 那条路的三个代价在实现完之后才看清：① fine-grained PAT **强制有有效期**，到期即静默失败 —— 而「静默失败的备份」正是 FR-11.9 拼命要防的那类事故，FR-11.5 的过期提醒只是给这个缺陷打的补丁；② 新设备要么手敲一串 `github_pat_...`，要么扫一张**等价于明文令牌**的二维码（FR-11.18 那段安全提示本身就说明了这个形状不对）；③ 它从来没用真实 PAT 联调过（KICKOFF「大窟窿一」），也就是说这条路的收益一直是纸面上的。换过来之后登录是一次点击、令牌 90 天且服务器随时能撤销，代价是**多了一台要维护的机器**（§2.6.6 里原本以此否决自建方案）—— 但这台机器本来就在跑别的东西，而 GitHub 账号同样是单点，只是维护成本藏在别人那里。「有历史可回滚」这条不能丢，所以服务端每个文档保留最近 30 版（`revisions` 表）替代 git 历史 |
| 26 | **词级时间戳从缓存层搬进标注层（2026-09-02）**：`LessonCache.wordTimings` → `Sentence.words`（新类型 `WordSpan`，不再存对齐器内部的词序号）。备份、同步、合并、导出**一行没改** —— 它现在跟着 `Lesson` 走 | 用户手里的东西一律要备份。原来的分层判断（变更 17 时写的）用的是「有音频+文稿就能重算」，那句话**漏了一个限定词：在哪台设备上**。桌面能重算，iPhone 跑不动这个模型（两档都被系统杀掉，见变更 21），在那台设备上它压根重建不出来 —— 于是「桌面预处理、手机学习」只兑现了一半：句子能播，词不能。变更 25 之后代价也变小了：同步是按课分片的（`lesson:<id>`），只有改动过的那一课会推，一课约 800 条、约 30KB。<br>存**进句子**而不是整课一个数组，是因为它和 `blanks` 是同一套句内 offset，因此合并/拆分/重新切句要做的事完全一样（平移、按切点分家、按文本匹配搬过去）—— 三处都补了，漏任何一处都是**静默错位**：offset 指到别的词上，界面上看不出来。老库里那份 `wordTimings` 谁都不再读，也不迁移：缓存层本来就可以随时丢，而且开头三句不再排除（变更 24）之后每一课都要重新对齐一次，重跑就会把新字段写满 |
| 27 | **分层标准整条换掉：缓存层只剩音频与原始文稿，其余一切都备份且同步（2026-09-02）**。据此 `LessonCache.glossary` → `Lesson.glossary`；`Lesson` 新增 `audioSrc`（音频的原始下载地址），FR-3.5 补齐时优先用页面上的新直链、页面里没有就退到它。§2.3 / §6 的判断标准由「丢了能不能重建」改为「**它有没有一个记在标注层里的下载地址**」 | 用户的原话是「除了音频和原始文稿，其他都备份和同步；音频和原始文稿记录原始下载地址」。这不只是多同步两个字段，是**把分层标准换了一个可以当场回答的问题**：「能不能重建」得先问「谁在什么设备上重建」—— 变更 26 就是被这一条绊倒的（词级时间戳在桌面能重算、在 iPhone 上压根不能，于是「桌面预处理、手机学习」只兑现了一半）。而「有没有地址」是个事实问题，没有解释空间：音频有 mp3 直链、文稿有页面 URL，所以它们可以不进备份；其余任何东西都答不出一个地址，所以都得备份。<br>顺带解决了两件小事：① 候选词「还没接受哪些」现在是**跨设备的待办** —— 在桌面上看过一半、手机上接着看，这件事只有它跟着同步才成立；它的 offset 是句内的，不依赖 `plainText`，所以搬过来不需要文稿在场。② 换设备恢复之后，一课要补的只剩音频（照 `audioSrc` 取）和文稿（照 `sourceUrl` 取），别的一样不缺。<br>**这一份写了迁移**（`src/db/migrate.ts`，启动读库时跑一次，幂等）—— 词级时间戳那次没写，因为「重新对齐」就能拿回来；候选词**没有别的补救路径**，只有重抓一遍页面（连带重下 6~10MB 音频），而它带着「还没接受哪些」这个待办，不该静默消失。同理 FR-1.5 手工重切时候选词要跟着搬（`carryGlossary`：句号按 `carriedOver` 换、句内 offset 不动、认领不到的丢掉）。<br>**当时留了一件没做**：`Settings` 只进手动导出、不进同步；那件事在变更 28 里补上了 |
| 28 | **设置也同步（2026-09-02）**：新增 `settings` 文档（`src/sync/docs.ts`）、`Settings.updatedAt`、`mergeSettings`（§2.4 第三条：整体 last-write-wins）、`scheduleSettingsSync()`（5s 去抖）与恢复时一并拉回；顺带补上一个缺口 —— **备份文件里一直带着 settings，但导入这条路从来不读它**，也就是说「换设备恢复」以前会把设置默默留在默认值上 | 他的原话是「Settings 也同步吧，全部」，紧接着变更 27 那句「除了音频和原始文稿，其他都备份和同步」。原来不同步的理由（`docs.ts` 里那段注释：「对齐档位、跟读间隔跟机器走，跨设备覆盖只会互相打架」）**细看之下站不住**：真正跟机器走的那件事**压根不在 `Settings` 里** —— 「这台设备跑不动对齐」记在黑匣子（localStorage，`align/journal.ts`）里，与 `autoAlignOnImport` 这个开关无关，所以手机就算把它同步成 true，两档都崩过之后照样自己停（FR-15.10）。剩下的（每日新词量、跟读间隔、判分严格度、词频档）本来就该跟人走。<br>**为什么必须新增 `updatedAt`**：`Settings` 是个没有 id 的单例对象，合并需要一个定序键，而它一个时间字段都没有（`lastBackupAt` 是「上次备份」，不是「上次改动」）。只有 `useSettingsStore.update()` 写它 —— 绕过 store 直接 `putSettings` 会让合并失去准绳，这条写在了代码注释里。<br>**空设备恢复因此自动是对的**：`getSettings()` 在空库上给的是默认值（`updatedAt` 缺失 = 0），远端只要改过一次就赢 |
| 29 | **复习形式改为「听音四选一」，评分改为系统自动算（2026-09-02）**：FR-10.2/10.3/10.4 重写，新增 FR-10.8–10.11（四选一 + 卡龄渐进题型 + 干扰项 + 答对时的带冠词确认）；FR-17.3/17.4 从「加 N 个词」改为「整档报名 + 每日惰性激活」；新增 FR-16.8 IPA 近邻干扰项索引与 FR-16.9 例句 | 变更 23 落地时，复习页是照 FR-10 原文做的 **Anki 形状**（显示答案 + 四档自评），而这个形状与需求差三件事：① **自评是元认知任务** —— 每张卡都要判断一次自己的记忆强度，而用户要做的是学词；② **`presetBand` 那个「加 N 个词」把两件事压成一个动作** —— 「我要学哪些词」是一次性的选择，「今天学几个」是每天的配额，混在一个按钮上的结果是每十几个词就要回去点一次；③ **正面给了文字，卡就退化成认读**。三条修完之后有一个连带结论值得记住：**「整档报名」不需要新表** —— 「哪些还没学」= 档内词表 − 生词本已有词，是纯派生量，落库反而要处理跨设备合并。真正花时间的是例句（FR-16.9），因为 WikDict 没有例句表，它是这条管线第一次需要**第五个数据源**。<br>还有一处**必须记住的取舍**：答对时直接跳下一张（用户明确要的节奏），代价是名词的**性**在主路径上看不到 —— 所以补了 FR-10.11 那 600ms 的带冠词确认。没有它，这个改动会把 FR-7.4「名词不带性等于没记」的立场在最常走的那条路上悄悄取消掉 |
| 30 | **应用壳自动更新（2026-09-02）**：Service Worker 的注册从 vite-plugin-pwa 自动注入的 `registerSW.js` 换成虚拟模块 `virtual:pwa-register`（新增 `src/platform/pwa.ts`），新壳激活时页面自己刷新；另加「每小时 / 回到前台 / 刚连上网」三处主动探测。原生构建从「不把这个插件放进 plugins 数组」改为 `disable: native` | 症状是 deploy 之后在浏览器里刷新，看到的还是旧版 —— 而它跟「没部署成功」长得一模一样（这次是先把 CI、Variables、线上产物全查了一遍，确认两边都是新的，才回头看客户端）。原因在 §7.6 的 `navigateFallback: '/index.html'`：导航请求由 SW 从预缓存直出，所以那一次刷新拿到的**必然**是旧 SW 手里的旧 index.html —— 新 sw.js 是在这次加载的后台才被取到、装好、`skipWaiting` 接管的，而页面早渲染完了。插件注入的那个脚本只调一次 `register()`、拿到更新之后什么也不做，于是**必须刷第二次**；虚拟模块在 `autoUpdate` 下会监听新 SW 的 `activated`（`isUpdate`）自己 `location.reload()`，刷一次就够。<br>**定时探测不是可选项**：`register()` 只在页面加载时问一次，而装到主屏幕的 PWA 可以连着开好几天不重新加载（§2.1 的用法就是碎片时间点开就用）—— 没有探测，那种用法下永远等不到更新，而那恰好是最需要它生效的场景。<br>**唯一自己写的逻辑是「不在打字的当口刷」**：听写答案只活在 React state 里（`DictationTab` 的 `answers`，没落 IndexedDB），打了半句被静默刷掉比看到旧版更难接受。但**只等到光标离开、不等更久**：`skipWaiting` + `cleanupOutdatedCaches` 意味着新 SW 一激活，旧构建的预缓存就已经被删了，这个页面之后再按需加载旧 chunk（词典分桶、对齐的 onnxruntime）会 404 —— 所以「拖着不刷」不是安全的那一侧，反而更糟。刷新会保留当前 hash 路由，人看到的就是「同一个页面变新了」 |
| 31 | **iOS 上的 emissions 挪出 WebView（2026-09-03）**：新增原生插件 `native-plugins/align-native`（Swift + ONNX Runtime 的 SPM 分发）与 JS 侧的第二个 provider `src/align/nativeEmissions.ts`；`config.ts` 加 `NativePlan` / `NATIVE_PLAN`（`native/q4`）/ `NATIVE_PLAN_STEP = -1` / `planLabel()`；`alignLesson()` 在 iOS 上分出 `alignLessonNative()` 这条路；`useAlignStore` 的 `blocked` 不再拦原生 | 用户的原话是「自动对齐在电脑上可以，但是在 iPhone 13 上依然不行，可能需要用系统接口，而不是网页」。§7.10 早就把这件事的两条路写清楚了（换小一个数量级的模型 / 把 emissions 整块挪出 WebView），这次落地的是后者，而**能只改一半是因为那道缝（FR-15.13）在变更 21 时就切好了** —— viterbi、applyTimings、落库、同步一行都没动，换掉的只有「音频 → 矩阵」这一半。<br>三处形状值得记住：① **桥上过的是 mp3 而不是波形**，解码也交给原生（`AVAudioFile`）—— 反直觉，但 8 分钟波形 30MB、base64 之后 41MB，而同一课的 mp3 只有 7MB，而 Capacitor 的桥只能过 JSON；那 41MB 会在 WebView 里同时存在两份，正是要躲的峰值。② **原生那一档不在 `PLAN_LADDER` 里**，`planStep` 记 -1：阶梯是「WebView 里哪一档不会被杀」，对原生没有意义，混进去会让 `crashedSteps()` 把原生的失败算到第 0 档头上，桌面的降档判据跟着坏。③ **插件必须是一个 npm 包**（`file:native-plugins/align-native`）—— `ios/App/CapApp-SPM/Package.swift` 由 `cap sync` 按已装插件重写（文件头就写着 DO NOT MODIFY），手工加依赖会在下一次 sync 时被抹掉；包名 `deutsch-align-native` 经 CLI 的 `fixName` 变成 SPM 里的 `DeutschAlignNative`，Package.swift 里的包名与库名必须逐字对上。<br>`EmissionsEngine.swift` 是 `src/align/emissions.ts` 的**逐条移植**（20 秒块 / 2 秒重叠 / 只采用中间段 / 按块零均值单位方差 eps 1e-7 / 全局帧号 `floor(sample/320)` / 未覆盖帧填 `log(1/vocabSize)`）：同一课在桌面和 iPhone 上必须得到同样的时间戳，改一边就要改另一边。**这一份在本机验不了**（Windows 上编不了 Swift），只有 CI + TestFlight 能验 —— 所以它落地的标准是「出一个包并在 iPhone 13 上真的跑完一课」，不是 typecheck 绿 |
| 32 | **「切句」页两个批量控件的默认值改成 0（2026-09-03）**：`HeadControl` 从 3、`ExcludeTailControl` 从 5 一起改成 0，`min` 也放到 0，为 0 时按钮禁用（FR-1.4 修订） | 那两个非零默认值是把「上一次我想排除几句」当成了「下一次该排除几句」。代价不对称：**排除范围一变就要重新对齐整课**，而一个非零默认值加一次误点就够了。改成 0 之后想批量操作的人必须自己填一个数，而填数这一步同时就是确认 |
| 33 | **手机上自动对齐改成手动，并加断点续算（2026-09-03）**：iOS 原生壳上导入后不再自动对齐（`useAlignStore.enqueue` 的 `native` 闸门，`drain()` 里再问一次以避开启动竞态）；课程页多一条 info 横条，把「还没有时间戳」说成**正常状态**并给出两个出路（`AlignStatus`）；插件加断点续算（`Checkpoint.swift`：Caches 下 `<lessonId>.bin` + 带指纹的 header，每块落盘、只在成功时扔）、加 `cancelEmissions`（块边界生效，**不清断点**）、加 `isIdleTimerDisabled` 防锁屏；进度事件从「只有一个比例」改成三阶段 + 块数（`decode`/`model`/`infer`，带 `chunk`/`chunks`），`AlignBar` 加一秒一跳的秒表和由块速算出的「约还需」，黑匣子跟着记 `chunk`/`chunks` | 起因是「iOS 上更新版本之后，自动对齐在开始的时候卡了几分钟，没有进展」。**查下来没有卡死**：那条路上原本只有一种事件（每块算完报一个比例），而它前面排着四件在手机上要几十秒到几分钟的事（9MB base64 过桥、原生解码、加载 241MB 权重、第一块推理），于是最需要耐心的那几分钟恰好是唯一没有反馈的几分钟 —— 而这个功能真实的历史故障（§7.10，进程被 jetsam 杀掉）长得一模一样，所以「分不清」这件事本身就是 bug。<br>但补完进度之后，更大的问题露出来了：**一课估 10–15 分钟（27 块 × 20–30 秒，依据是桌面 1.4× 实时；真机还没跑完过一课），而手机不肯给这么长**。Info.plist 里没有 `UIBackgroundModes`，锁屏或切走 App 之后进程被挂起，而带着 400MB 常驻被挂起的进程正是 jetsam 最先挑的那一个；iOS 默认 30 秒到 2 分钟就自动锁屏。也就是说自动跑在手机上**十有八九跑不完**，只是白热一台机器。而这活儿手机本来就不必干：变更 26 之后句级 + 词级时间戳都进标注层并同步，桌面上同一课 26 秒（`webgpu/q4f16`），同步过去手机直接能练 —— FR-15.10 兜底那句「去桌面对齐」其实一直是**主路径**，只是它以前只在「两档都崩过」时才说出来，而原生那条路为了绕开那个闸门明确把 `blocked` 设成了 false。<br>三处形状值得记住：① 这道闸门**必须是设备本地的**，不能靠把 `autoAlignOnImport` 设成 false —— 它是同步项（变更 28），关掉会传染到桌面，而桌面上自动跑是完全对的；② 断点的键是 **lesson id** 而不是「这一次运行」，另配一个指纹（采样点数 + 音频字节数 + 分块参数 + 权重文件名）—— 混用一份不匹配的中间态会得到「看起来正常但时间戳系统性错位」的结果，那是最坏的一种坏法；③ 「停止」以前在手机上是个**死按钮**：`cancelAlignment()` 第一行看 `running`，而算 emissions 的那十几分钟里 Worker 还没起，`running` 是 false |
| 34 | **同步补上「拉」那一半：自动增量拉取 + 打开一课自动补齐（2026-09-03）**：新增 FR-11.19（`src/sync/pull.ts`：启动 / 回前台 / 网络恢复时按版本号增量拉，60s 节流、单飞、本地赢的部分回推）与 FR-3.5a（DW 来源的课程打开即自动补齐素材）；`syncNow()` 取代散在三处的 `drainSyncQueue()`；状态条与设置页把「上次推送」「上次拉取」分成两行；`useLessonAudio` 把 `hasAudio` 放进依赖数组。同时**去掉「补齐之后无条件重新对齐」**（`hasTimings()`：只有一句时间戳都没有、或音频时长变了才对） | 起因是一句提问：「现在有了后端，跨设备是自动同步的吗？我在电脑上把文本和音频同步了，但是手机上还得自己跑一遍。」答案是**只有推是自动的** —— 拉从头到尾只有设置页那一个「从服务器恢复」按钮（FR-11.13），而它是照「恢复到空设备」这个场景设计的，没人会为了看今天新导的一课去按它。于是桌面上导完一课，手机上什么也不会发生，而那个界面和「同步坏了」长得一模一样。<br>**真正的代价不是多点一次按钮，是它把人推上一条错路**：手机上看不到那一课，最顺手的下一步是**在手机上重新导入一遍** —— `importFromDw` 走的是 `generateId()`，于是同一期变成两门课，第二门标注全空、还要在手机上重跑十几分钟对齐（正是变更 33 刚判定为「跑不完」的那件事）。整个 FR-11 的收益就从这个缺口漏掉了。<br>三处形状值得记住：① **增量靠的是已有的东西** —— `GET /v1/docs` 的列表里带 `version`，本地为乐观并发一直记着 `syncVersions`，两个数一比就知道该拉谁，所以常态下这条路只有一次列表请求（复用 `restoreFromServer()` 会变成每次启动重下整个标注层）。② **「拉」不等于「远端赢」**：走 §2.4 同一套合并规则，本地更新的那部分留住**并回推**，否则手机上刚复习完的 FSRS 状态会被一次启动拉取悄悄按回去 —— 那是这个应用最不可接受的一种坏法。③ **刻意不跟随删除**：「本地记着版本号、远端列表里没有」确实是「另一台设备删了这一课」的强信号，但这条路每次启动都跑，判断错一次就是静默删掉标注，删除仍然只在本机发起。<br>**顺带修掉一个真 bug**：`rehydrateLesson` 之后原来无条件 `enqueueAlign()`，理由是「补齐等于刚下载完，时间戳可能作废」。同步落地之后常态恰恰相反 —— 标注层里那份时间戳正是桌面刚算出来的，重算一遍在 iOS 原生壳上被闸门挡住（看起来「没反应」），在 web 和 Android 手机上会真跑十几分钟再把同样的值盖回去。会让时间戳作废的只有音频变了，而那件事有 `audioDuration` 可比，不必靠猜。<br>**FR-3.5a 的代价说清楚**：它会在移动网络下直接开始下 6~10MB。取舍是「点开一课却练不了」比「省流量」更疼，且下载中横幅一直在，不是偷偷进行 |
| 35 | **emissions 挪到服务器上（2026-09-03）**：`server/src/align/`（ffmpeg 解码 + onnxruntime-node 跑 q4 + 任务队列，新增三条路由 `POST/GET/DELETE /v1/align/jobs`）与客户端第三个 provider `src/align/remoteEmissions.ts`；`config.ts` 加 `RemotePlan` / `REMOTE_PLAN`；`alignLesson` 加 `backend: 'auto' \| 'remote' \| 'local'`，**手机默认远端、桌面默认本机**（FR-15.18）；手机上自动对齐因此重新打开（变更 33 那道闸门现在问的是「有没有服务器」）。compose 的 `mem_limit` 256m → 2g | 起因是「wnet 的 VPS 根本没人用，资源放着也是浪费」。而这台机器正好补上手机端最后那个缺口：本地十几分钟且必须一直亮屏（变更 33），服务器一两分钟**且这期间手机可以锁屏、可以退出 App**。<br>**能只加一个 provider 就完事，是因为那道缝（FR-15.13）在变更 21 就切好了** —— 这是它第三次被兑现（wasm → 原生 → 远端），viterbi、applyTimings、落库、同步一行没动。<br>三处形状值得记住：① **只上行音频、下行矩阵**，Viterbi 留在设备上 —— CTC 前向压根不看文本，所以德语正文一个字都不出设备，§3.1 那一整套约束只需补一句「音频会上传到自己的服务器」（§3.1.1 已补）。② **任务 + 轮询，不是一次长请求**：一课一两分钟，而手机握不住这么久的连接（锁屏、切走、换基站）—— 同步请求会把变更 33 那面墙原样搬到服务器上。③ **一律不压矩阵**（不 float16、不 gzip）：3MB 一课，而压了之后同一课在服务器和桌面上会得到细微不同的边界，那是这条路最不该引入的差别；同理服务器用的 dtype 必须与原生那一档相同（都是 q4）。<br>**实测**：`onnxruntime-node` 1.29 的 CPU EP **认 `MatMulNBits`**。本机（Windows/3 线程）q4 那份 241MB 权重加载 0.6 秒、20 秒一块推理 3.4 秒、峰值 RSS 584MB —— 所以 256m 的 `mem_limit` 必须提，否则第一次对齐会把容器 OOM 掉、连**同步**一起带走。**那台 VPS 上（0.3.0 部署后实跑）**：ffmpeg 解码 0.1~0.2 秒、权重首次 60.7 秒（含从 HF 下 241MB）之后 0.6 秒、一块 4.1~4.5 秒 → **一课 27 块约 2 分钟**（实时倍率 4.5×），空载 104MiB / 2GiB；同一输入两次跑出逐位相同的指纹。<br>**刻意没做的**：「服务器每周自动抓新一期」。那撞 §3.1.1 R-1（不许有我们运营的中转去 DW 取内容），要改红线才能做，不能顺手带过去 |
%%36%%：新增 **§12 界面规范**（这份文档第一次有「形状」那一层）；导航从六个平级 tab 收成「三个活动 + 一个抽屉」，手机改底部标签栏；新增底部单浮层契约（`src/components/bottomLayer.ts`）；状态语言分三档（`Note` / `Banner`，`Banner.title` 必填，`tone="info"` 删除）；排版四级 + 颜色令牌 + 深色（`index.css` 的 `@theme`，并把 Tailwind 自带调色板清空）；一课的落地页从「切句」改成「通听」，切句移出 tab 条；`SyncStatusBar` → `SyncChip`（FR-11.9 的新落法，§12.3.1）；FR-11.12 改成两档升级（§12.3.2） | 起因是一句话：「现在的 ui 设计一团混乱。」而查下来**混乱有一个机械的原因**：文档把功能定得很死（FR-1~17，一千六百行），形状一次没定过 —— 于是每个页面各自发明一套，且只能借用文档里唯一存在的词汇，`存储（§2.2 / FR-11.16）`、`词典（FR-16 / FR-17）`、`L1 全自动` 这些字面因此直接进了产品界面。所以这一轮真正的产出是 §12，改代码只是把它落一遍。<br>**顺手修掉一个真 bug**：`AlignBar` 是 `fixed bottom-0 z-30`、`AudioBar` 是 `sticky bottom-0 z-10`，自动对齐一跑，进度条就正正压住播放键；`AlignBar` 上那个 `align-bar` class 本来是留给避让偏移的，而 `index.css` 里从来没有对应规则。同类的还有：**全新安装第一次打开就会看到「距上次手动导出已超过 90 天」**（`lastBackupAt` 只在导出时才写），以及导航项是 `<a>`、拿不到那条 44px 规则、手机上热区只有约 30px。<br>三处形状值得记住：① **`Banner` 的 `title` 必填是一个类型层面的强制**，它逼作者把事情在一行里说完，而设计理由归代码注释 —— 旧界面上那些三到五行的散文（故障域、jetsam、CEFR 词表版权）出现在课程页头部和首页，也就是每天要读一遍的地方；② **FR-11.9「常驻可见」的旧落法两头都不对** —— 它只在首页（而同步停摆最危险的恰好是连着三天只开复习页那几天），却又在一切正常时也占一行五个字段（于是不再有人读它）；新形状是每一页都在的芯片，一行只说四项里最坏的那一项，点开是四项全文。③ **底部浮层的高度不能只靠 `ResizeObserver`** —— 那一层的高度变化绝大多数是 React 自己引起的，所以每次渲染之后都量一遍，RO 只管转屏和字体加载；实测在一个不渲染的浏览器面板里 RO 一个回调都不投递，连初次那一发都没有。<br>**深色是这一轮的连带收益**：`dark:` 在改造前的 `src` 里出现 **0 次**，而「晚上躺着练听力」是 §2.1 写的主场景。做法是只重定义令牌 + `color-scheme` 跟着换 + `index.html` 两条带 `media` 的 `theme-color`，组件对主题无感。<br>**验收标准是 grep**：把 Tailwind 自带调色板整个清空（`--color-*: initial`）之后，`bg-neutral-200` 这类写法不再生成 CSS，所以「grep 不到原始色号」成了一个可信的判据，而不是靠眼睛扫 |
| 37 | **学习记录与分享图（2026-09-15）**：新增 **FR-18** 与 **§12.8**；`src/study/`（计时器 + 逐日记录 + 派生统计）、`src/share/`（语录 / 底图 / canvas 渲染 / 保存分享）、`⋯` 抽屉里的「记录」页；`StudyLog` 进标注层（`meta.studyLog`，同步文档 `study`，逐格取 max），备份文件新增可选字段 `studyLog`；`Settings.displayName` | 用户要「一张精美图片，上面一句德语鸡汤，下面我的名字、日期、学了多久、连续几天、一共几天」。做之前才发现**这个应用从来不知道你昨天学没学** —— FSRS 只留每张卡最后一次复习的时刻，课程只有 `createdAt`，所以「连续几天」在旧数据里根本推不出来。于是一半以上的工作量在**先把学习行为记下来**，图只是它的出口。<br>两个决定值得记住：① **计时口径宁可少算**（练习界面 + 页面可见 + 最近一分钟有动静或音频在放，三条同时成立才走表，每 5 秒跳一次）—— 这个数字要发给别人看，而「只要应用在前台就计时」会让泡咖啡的十分钟也算进去；② **记录按「日期 → 设备 → 秒数」存**，因为合并必须幂等：按天求和会在反复同步里越加越多，按天取 max 会吞掉另一台设备的时长，分设备存之后两条性质同时成立。<br>**不回填历史**：能用的只有 `createdAt` 和 FSRS 的 `last_review`（只有最后一次），凑出来的天数必然偏少，而连续天数无论如何只能从今天算起 —— 一个不准的数字发出去比没有更糟 |
| 38 | **译文与笔记（2026-09-15）**：新增 **FR-19**（逐句译文）、**FR-20**（课程笔记）与 **§12.9**；`src/lesson/translation.ts`（提示词 / 编号原文 / 解析 / 写回，纯函数）、课程页「⋯」里的**译文**页、通听与跟读上的「显示中文」开关、通听页底部的**笔记**折叠块；`Sentence.translation` 与 `Lesson.notes` 进标注层（跟着 `Lesson` 走，备份/同步/合并一行没改），`Settings.showTranslation` | 用户的原话是「我会用 Claude 对文章进行翻译」「我会自己写一些要点」——**应用自己不翻译**，它只负责把一整篇译文切回一句一句。<br>三个决定值得记住：① **逐句而不是整篇**：整篇译文要人在两块文本之间自己找位置，而听的时候手不在屏幕上；逐句之后跟读页也能用，而「跟着念了两遍却不知道念的是什么」正是跟读最难受的地方。② **用编号当锚，不按行数猜**：翻译工具多写一行说明、少一个空行，按行数对就整篇错位一句 ——而**错位的译文比没有译文更糟**（跟读时你看到的是下一句的意思，还不知道自己被骗了）。编号解析失败是显性的（「48 句认到 46 句」），行数对齐失败是静默的；所以没有编号的行**永远开不了一个新条目**，宁可少认几句。③ **笔记只做课程级**：句级那一层已经被挖空和生词本占满了，再加一层，过几周就想不起来某条东西当初记在哪一层。<br>顺带修了一处早就埋着的地雷：`Disclosure` 的展开状态原来直接钉在 prop 上，而它现在要出现在**每帧重渲染**的通听页里 —— 那样用户折叠之后下一帧就会被强行弹开。
| 39 | **低置信句可以人耳确认（2026-09-16）**：新增 **FR-15.19** 与 **§12.10**；`Sentence.timingChecked`（标注层，跟着 `Lesson` 走）、`flaggedByConfidence()` / `toggleTimingChecked()`（`src/align/apply.ts`）、通听里带 `?` 的行号变成按钮（点一下确认、点第二下撤销），`reviewQueue` 只报还没确认的那几句 | 用户的原话是「我能理解句子编号的？，我希望我可以自己在听完之后可以对其进行确认，然后消除？ 因为大部分时候都是正确的。」**这是 FR-15.3 的一个缺口**：那一条要求「低置信句数摆在课程页头部」，却没给这条提示任何**退出条件** —— 而低置信的常见原因（台标音乐、播音员交替、英语借词、被丢掉的数字）恰恰大多不影响句边界，于是听完一课、确认每句都对之后，头部那行「6 句置信度偏低」和那条 `Note` 一个字都不会变。一条听完也消不掉的提示，教会人的只有忽略提示，正是 §12.3「一切正常就静默」的反面。<br>三个决定值得记住：① **不复用 `timingSource: 'manual'`**：那个值的含义是「边界是人给的」，会让重新对齐跳过这一句（FR-15.5）—— 而这里发生的事只是「机器算的这一版我听过，没错」，下次重对照旧该重算它。所以 `applyTimings` 写入新时间戳时**把确认清掉**：确认过的是那一版边界，不是这个句子永远正确。② **确认不动阈值**：阈值按整课的置信度分布算（`flaggedByConfidence`），确认几句只改「谁还在队列里」，否则剩下那几句的判定会跟着人的操作漂。③ **确认过的行号和从没被标出来的行号长得一模一样**（不画勾）：消掉 `?` 换来另一个常驻标记等于没消；可撤销靠热区还在（点第二下），不靠画出来。<br>顺带把通听的每一行从内联排版改成 **flex 两列**（行号 / 文本）：触屏上 `index.css` 给 `button` 的 44px 最小热区会撑高它所在的行盒，把句子第一行推下去；作为独立的 flex 项它只影响整行高度。连带收益是**悬挂缩进** —— 折行之后的文本和译文都对齐在编号之后，不再回到最左边 |
| 40 | **生词本里的查词（2026-09-20）**：新增 **FR-9.5 ~ FR-9.10** 与 **§12.11**；`src/dict/view.ts`（两个来源合成一个答案）、`src/pages/vocab/DictLookup.tsx`（生词本页顶部的一块）、`online.ts` 重写成「一次解析、两个视图」（多出例句 / 同义词 / 反义词 / 词源 / 变形）、`VocabEntry.lookup` 与 `createFromLookup`；`cardAudioStatus` 的 `preset-word` 改名 `word-only`（`isWordCard`） | 用户的原话是「生词本里，加一个查词的功能，我现在的德语课很多时候要查生词。谷歌翻译的解释很多时候不太详细」。**这是一个此前没有入口的来源**：课上的词不来自这里的任何一课，也不在预置词库的名次里，而内置词典在这个应用里一直只在幕后跑（标记生词时 prefill、复习卡背），**没有任何地方能让人主动查一个词**。<br>三个决定值得记住：① **要的是「详细」，所以在线那一趟每次都问**，不只在内置词典查不到时问 —— 内置词典只给 1.7 万个牌组词带例句（FR-16.9），而课上要查的词大多不在牌组里；`Partizip II`、比较级、词源、同义词更是内置词典完全没有的一类。设置里那个开关因此从「查不到时联网」变成「联网查」，管两处。② **在线那一趟拿词头去问**：`abgewogen` 自己那一页只有一个 Partizip 小节，例句和变形全在 `abwägen` 那一页上 —— 不换词头，「详细」正好在最需要的那种词（变形）上少掉一半。代价是在线那一趟要等本地查完才开始。③ **加进来的词没有课**，所以要一个显式标记（`lookup`）：少了它，这张卡在复习页会走到「去这一课重新对齐」那条出口，而那一课从来不存在 —— 与预置卡当年要 `preset-word` 那一档是同一个理由，于是那一档改名 `word-only`，两种「没有原句的卡」共用。<br>**没做的**：查词历史、多词搭配（`sich einer Sache bewusst sein` 两边都查不到，界面如实说，并且照样可以原样收下）、整句翻译（那是 FR-19 的事，而且应用自己不翻译）
| 41 | **iOS 原生壳的热更新（2026-09-20）**：新增 **§7.12** 与 **§12.12**；装 `@capgo/capacitor-updater`（**手动模式**）、`src/platform/nativeUpdate.ts`（`decideUpdate` 纯函数 + `checkNativeUpdate` / `notifyNativeAppReady` / `runningBuild`）、`scripts/build-ota.mjs`（`npm run build:ota` → `ota-staging/`）、`AppDelegate.linkBundledAssets()`、设置页「版本」小节；deploy 流水线多两步（打包在 web 构建**之前**，搬运在**之后**）；`package.json` 升到 0.4.0 | 起因是一句提问：「为何网页上的版本更新之后，iOS 的版本没有更新呢？」答案是**设计如此**，而且代价已经攒了两周半：`webDir: 'dist'` 把前端冻在出包那一刻，SW 又在原生构建里被特意关掉（变更 30），所以 ios-v0.3.0（2026-09-03）之后的变更 36~40 ——界面重做、学习记录、译文笔记、人耳确认、查词——在 iPhone 上一个都没有。<br>**四处形状值得记住**：① **手动模式而不是插件自带的 autoUpdate** —— 后者要 POST 到端点，而 wrangler.jsonc 是纯静态部署、没有 Worker 脚本；改成自己 GET 一份静态 manifest.json 之后，「要不要更新」变成一个可单测的纯函数，而不是藏在原生侧的行为。② **`next()` 而不是 `set()`** ——立刻换 bundle 是整个 WebView 重载，比 web 版那次 `location.reload()` 更重，而变更 30 那条教训（听写答案只活在 React state 里）在这里加倍成立；所以后台下好、**下次冷启动**生效，界面上不给「现在就更新」的按钮。③ **buildId 用 commit 短 sha + 相等判断，不比 semver 大小** —— 比大小会让**回滚**（版本号往回走）被客户端拒绝，恰好在最需要更新生效时不生效。④ **`minNative` 门槛不能省** —— 热更换不了原生代码，把用到新 Swift 方法的 JS 推给旧壳，症状是「点了没反应」，比崩溃还难查（崩溃至少有 `notifyAppReady` 的回滚兜着）。<br>**真正的技术障碍不是下载，是 `/models/` 与 `/dict/`**：热更把 web 根整个切到 `Library/NoCloud/ionic_built_snapshots/<id>/`，而那两个路径是相对站点根的绝对路径，根一换就全 404。塞进热更包要每次传 458MB；改走 `convertFileSrc` 则赌 `_capacitor_file_` 支持 **Range**，而随包权重正是靠 Range 分块读的（§7.9 / `rangedFetch.ts`：187MB 整份进内存即被杀）。选的是第三条——**在新 bundle 目录里建符号链接指回 app 包**，web 代码一行不动，取数仍走同一个 WebViewAssetHandler。链接**每次启动都重建**：app 包路径里那个 UUID 每次安装都变，上一版留下的链接全是断链，而 `fileExists` 跟随链接、对断链回 false，「没有才建」会以 EEXIST 静默失败并留下一个永远坏着的 `/models/`。<br>**Android 这次不做**：`WebViewAssetLoader` 有 canonical path 检查，很可能拒绝指向 app 目录外的符号链接，要另想寻址方式。Android 照旧走发包 |
| 42 | **iOS 去掉本地对齐 + 换德语原生模型（2026-09-21）**：删掉 `native-plugins/align-native`（Swift ONNX 插件）、`src/align/nativeEmissions.ts`、iOS 发布流水线里的 `stage:align` 那两步、`AppDelegate` 里 `models` 那条符号链接；对齐模型从 **MMS-FA** 换成 **`oliverguhr/wav2vec2-large-xlsr-53-german-cv9`**（Apache-2.0，德语原生词表，35 token 含词分隔符 `|`），`vocab.ts` 整个重写（罗马化 → 直接映射 + 词间插 `|`）、`blankId` 从 0 变 32 并成为 `forcedAlign`/`alignWindowed` 的显式参数；服务器改跑 **fp32**（1204 MiB）并加**闲置释放**，新增**权重站** `GET /v1/align/weights/**`（`server/src/align/weights.ts`）与 `scripts/quantize-align-model.py`；`mem_limit` 2g → 4g | 用户的原话是「ios 版本上，把那个语言对齐的功能去掉吧。大部分时候根本跑不起来，还占用了 200 多 mb 的体积。桌面浏览器和服务器的功能保留。然后看看现在没有体积限制后，服务器版本能否采用更准确更优的方案」。<br>**第一半是承认一件已经成立的事实**：手机本地那条路从变更 31 建到变更 33 改成手动，一直没有真的跑完过一课 —— 一课十几分钟满载、必须一直亮屏，而 iOS 三十秒就锁屏、锁屏即挂起。它的代价却是确定的：随包 418 MiB 权重（装机 553.6MB）、一个要联网 resolve binaryTarget 的 SwiftPM 依赖、以及 §7.12 里那条最费劲的符号链接。删掉之后手机上时间戳只剩两个来源（服务器算 / 桌面算完同步过来），**装机体积 553.6MB → 95.2MB**（真机实测，少了 458MB / 83%）。**「送到服务器算」那条留着** —— 跑不动的是这台设备，不是这个功能。<br>**第二半是把一个一直凑合着的选择摆正**：服务器选 q4 从来不是因为它更好，而是为了「手机/桌面/服务器三条路算出同一份时间戳」，而那个约束的一半（手机）刚刚没有了。于是两件事一起做：① 服务器改跑未量化的 fp32；② **换模型** —— MMS-FA 是多语言罗马化对齐器，德语进去之前要丢掉 ä/ö/ü/ß 的区分，而且它的词表里**没有空格**，词间静音只能由 blank 吸收。换成德语原生模型之后这两笔税一起免掉，顺带解决 CC-BY-NC 那条许可问题。**实测（同一课 6:16、45 句、webgpu/q4）：句级 confidence 中位数从 -1.1 上下变成 -0.02，最差 -1.59，44 秒跑完**；被标出来的 3 句全是采访同期声（真的不好念），而不再是「模型看不懂的字符」。<br>**代价写在明处**：两条路不再用同一份权重（服务器 fp32、浏览器 q4），同一课换个地方重对会有几十毫秒级的出入 —— 这是主动放弃的，不是疏忽。<br>**一处非做不可的小手术**：浏览器要的 4-bit 权重 HF 上没有（自己量化），而 Cloudflare 单文件上限 25 MiB 放不下 230MB，所以权重只能挂在同步服务器上 —— 那台机器因此多了一个「权重站」的身份，`ALIGN_ENABLED=false` 之后它仍然要提供这一份。另外 `preprocessor_config.json` 里的 `processor_class: Wav2Vec2ProcessorWithLM` 必须删掉（入库在 `scripts/align-model/`），否则 transformers.js 会连着去加载一个这个仓库根本没有的 tokenizer，报错长得像「权重没放好」 |
| 43 | **桌面对齐完的时间戳真的到手机（2026-09-21）**：新增 **FR-11.7b**（推送队列里的 `deferred` 标脏项：`markPending` / `clearPending` / `alarmingCount`，三个 `schedule*` 入口进门就标脏）与 **FR-3.5b**（`Lesson.audioBytes`，补齐后要不要重对改比字节数）；`syncNow()` 里加一次性的 `repairUnpushed()`（把本地每一课补推一遍，`META_KEYS.syncRepairedAt` 记一次）；`RehydrateOutcome.audioDurationChanged` → `audioChanged` | 起因是一句提问：「我在电脑上已经对齐过音频了，为何手机上同步课程之后，还要再对齐一次？所有的操作和记录不是应该自动同步的吗」。答案是**设计上确实该同步**（词级句级时间戳自变更 26 起都在标注层），所以这是两个真 bug，而且两个都只在「桌面算、手机练」这条主路径上发作。<br>**① 去抖窗口是个黑洞。** 课程推送去抖 30 秒（FR-11.7），而那个定时器只活在内存里 —— 对齐一跑完就关掉标签页，那次推送连同刚算出来的时间戳一起消失。要命的不是丢一次，是**丢了之后没有任何补救路径**：FR-11.19 的增量拉取本来有一条「本地比远端新就回推」的兜底，但它只在版本号对不上时才取全文来比 —— 而远端那份自导入之后没人改过，版本号当然一致，于是那一课永远轮不到比较。结果是桌面本地有时间戳、服务器上那份还是空壳，手机拉到空壳就自己去对一遍（手机上那是走服务器的两分钟）。修法是**把「还没推」这件事落库**，而不是给去抖加一层 `pagehide`（手机上那个事件本来就不可靠）：进门就标脏，推成功才清；标脏项不计入「待推送 N 项」，否则每改一个字那个数字都要黄几十秒。**已经丢掉的那些不会因为「以后不丢」而回来**，所以另加一次性的 `repairUnpushed()`。<br>**② 判「音频换没换过」的两个数不同源。** 对齐完会把**解码出来的**精确时长写回 `audioDuration`（它比 DW 页面上报的整数秒可靠），而补齐素材时是拿**页面上那个整数**去和它比、容差 1 秒。于是一门在桌面上对齐过的课，到手机上补齐素材就可能被判成「DW 换过音频」而白对一遍。换成比字节数之后两边同源；老课程没记过字节数就退回比时长，但比的是这次解码出来的值。顺带把「补齐时无条件改写 `audioDuration`」也停掉了 —— 那等于每台设备各记一个值，下次比起来又像是换过音频 |
| 44 | **补测试 + E2E（2026-09-21）**：前端单测 **本次 +150 条**（595 → 745；与同日变更 43 的 16 条合计 **761**），12 个文件：`app/router`、`lib/base64` `lib/hash` `lib/id`、`components/bottomLayer` `components/AppNav`、`srs/fsrs`、`sync/docs` `sync/restore`、`state/useLessonStore` `useSyncStore` `useStudyStore`、`db/wordAudio`），服务端 66 → 120（5 个文件：`config`、`session`、`align/wire` `align/decode` `align/jobs`）；`tsconfig.e2e.json` 把 `e2e/` 纳进 `npm run typecheck`（Playwright 自己只转译、不查类型），`vite.config.ts` 的 vitest exclude 补上 `e2e/**`；新增 **Playwright E2E**（`playwright.config.ts` + `e2e/`，37 个用例，desktop + mobile 两个 project），进 `ci.yml` 成为**第三个部署门禁** | 用户的原话是「看看是否可以添加更多单元测试，并补齐 e2e 测试」。<br>**单测那半补的是「零覆盖但会静默出错」的那些**，不是凑数：`sync/restore` 对应 §2.6.5 点名的「最容易悄悄坏掉的那条路」；`useLessonStore` 对应文件头自己写的两条（漏 touch `updatedAt` = 改动同步回来就没了；漏搬 `sentenceIndex` = 生词出处静默错位）；`bottomLayer` 对应 §12.2 那个真 bug（进度条压住播放键）；`server/config` 对应三条「配错了就该拒绝启动」的规矩（密钥太短、`aud` 校验失效、白名单空着）。DW 的四个解析器、对齐的 `vocab.ts` 等看着没测的模块**其实早有覆盖**（在 `adapter.test.ts` / `target.test.ts` 里），所以没重复写。<br>**E2E 补的是 jsdom 根本问不出来的那一类**：真实 IndexedDB 持久化（刷新之后东西还在不在）、浏览器解音频时长、`<input type=file>`、`a[download]` 真的落一个文件、窄屏下的底部标签栏与 `--bottom-inset`。**跑的是构建产物**（`vite build` + `vite preview`）而不是 dev server —— 它守的正是「类型过了、单测过了、构建也过了，但打开是白屏」。<br>**三个形状值得记住**：① **种数据走界面**（导入一份备份 JSON），不用 `page.evaluate` 直接写 IndexedDB —— 后者绕过被测代码，而「导入备份」本身就是 FR-11.14 要验的一条路；② **不跑真的对齐**（fixture 里的时间戳是手写的）—— 一课几十秒起、还要下 230MB 权重，而对齐算法有它自己的 9 个单测文件；③ **retries: 0** —— 会随机变绿的门禁比没有门禁更糟。<br>**顺手抓到一个真 bug**：设置页点「确认导入」之后**不重读 store**，界面上什么都不会变，要手动刷新一次导入的课程才出现（从服务器恢复那一条 `RestoreSection` 一直是对的，本地导入这条漏了）。同时给两条都补上了 `useStudyStore.load()` —— 两者都会写学习记录。<br>**没做的**：WebKit/Firefox（CI 上的 Linux WebKit 与真机 Safari 不是一回事，虚假绿灯）、视觉回归截图对比（这个项目的界面还在动，截图基线的维护成本大于收益）、对 `align/` 与 `dict/` 的端到端（都要下权重或走网络，属于 §10 的人工复验） |
| 45 | **识词卡：一个词两张独立调度的卡（2026-09-21）**：新增 **FR-21** 与 **§12.13**；`VocabEntry` 增 `fsrsRead?`（读卡的 FSRS 状态，`undefined` = 还没开）与 `examples?`（cloze 用的句子，开卡时拷进标注层）；读卡三种题型 `read-gloss` / `read-form` / `cloze`；中译走 FR-19 那套「外面翻、粘回来」补齐。**§7.5 与 FR-10.2 的措辞同步修正** —— 原文把「看词选意思」归成了**生产方向**的卡，那是错的：它是**视觉识别**方向，生产方向是「中文 → 德语」（要自己产出德语）。§7.5 自己的论据反过来同样成立：听和看是两套独立表征，「听得出来但看到不确定什么意思」和「看得懂但听不出来」是两回事。直接起因是 FR-9.5 加进来的那批**课上的词** —— 它们是读到的、不是听到的，而在此之前它们在复习里只有「听孤立词发音」这一条路 |

| 46 | **改名 + 五件手机上的实事（2026-09-22）**：应用中文名 `精听` → **`努力学德语`**（web title / PWA manifest / iOS 三处 / Android `values-zh` / 分享图顶部那行 / 导出的 PNG 文件名）；新增 **FR-9.11 / FR-9.12**（标记一个词 + 「问 AI 补解释」的复制走 / 粘回来）、**FR-10.12**（复习三个音效）、**FR-18.7**（练习时屏幕常亮）、**§12.15**（顶部安全区兜底）、**§12.16**（问 AI 那一块的形状）；`VocabEntry` 增 `askAi?` 与 `note?`，`Settings` 增 `soundEffects` | 用户一次提了八条，其中两条查下来不是「没做」而是「做了但你找不到 / 按下去没反应」：<br>**① 分享图上的名字本来就有**（记录页 → 分享图 → 「图上的名字」，留空就不印）。不再加第二个入口 —— §12 反对同一件事有两个位置。已在浏览器里确认它真的画在图上。<br>**② 「查词查不到只能丢弃」是一个真 bug，不是缺功能。** §12.11 早就写着「照样可以原样收下这个词」，代码里那个按钮也在，但 `add()` 第一行是 `if (!result) return;` —— 而查不到时 `result` 恰恰是 `null`。于是按下去**什么都不发生**：不报错、不提示，看起来就像这个词只能丢掉。改成词形由调用方给。<br>**③ 「记录下来第二天用 AI 翻译」和「中文解释太简单，标记后去 AI 里详解」是同一件事的两半**，所以合成一块而不是两块：待办 = 查不到的空卡（`!meaning`，自动进）∪ 人手点的标记（`askAi`）。前者机器判得出来，后者只能由人点 —— 「这三个中文字看不出 `Zuversicht` 和 `Vertrauen` 差在哪」没有任何特征可判。动线、提示词、编号规则、解析器（`parseTranslations`）与 FR-19 / FR-21.9 逐字相同。解释单独存 `note` 而不是并进 `meaning`：后者要进读卡的四选一选项（截到 80 字符），一段三百字的辨析塞进去会把卡面撑爆。<br>**④ 音效用的是文件，不是现场合成的音**（用户选的）：Kenney「Interface Sounds」CC0 三个，转成单声道 WAV 入库（43KB，`public/sfx/`）—— Safari/iOS 对 Ogg 的支持不可靠，而这三段音要在原生壳里响。播放走 **Web Audio**，是 §3.2「所有播放走一个 `<audio>` 单例」的**唯一例外**：听卡上德语句子还在播的时候你就答了题，单例只有一个，谁后加载谁把前一个顶掉 —— 结果会是「答对的提示音把正在听的那一句掐了」。<br>**⑤ 屏幕常亮跟 FR-18.1 共用 `isPracticeRoute`**，不写第二份名单。<br>**⑥ iPhone 13 上顶部压住状态栏的成因没有验出来**：`viewport-fit=cover` 在、`.app-nav` 的 `env(safe-area-inset-top)` 也在，所以只可能是 env() 在那台设备上解析成了 0。最像的嫌疑是 `capacitor.config.ts` 的 `contentInset: 'never'`，但**没验过**，而另一个值 `'always'` 的失败方式更糟（系统再插一层 inset，和 CSS 叠起来空出一条，那是「看着像没坏」的坏法）。所以做的是一件不需要知道成因的事：启动时实测 env()，量到 0 就按屏幕尺寸查表补上，量到非 0 时 `max()` 让兜底**零效果**；同时把实测值摆进设置页的诊断块 —— 下次在真机上看一眼那一行，成因就定了 |
| 47 | **一天之内不重复（2026-09-22，FR-10.13 新增）**：调度改成 `fsrs({ enable_short_term: false })`，最小间隔 1 天 | 用户看到卡面写「下次 6 分钟后」，问这是不是正常的复习方式。**是正常的 FSRS 行为，但在这个应用里那行字是假的** —— `ts-fsrs` 默认的学习步骤把学习中的卡排到 6 分钟后，而 `buildReviewQueue` 的 `busy` 规则当天根本不会再发它（按词去重，顺带掐掉了「同一张卡再来一次」）。显示与行为分叉，且卡片永远停在 `Learning` 出不去。三个修法里选了最自洽的那个：让调度器承认「当天做完就清零」这个既成事实，而不是改队列让卡当天回来。存量卡下次评分自动毕业，无迁移。**测试 +7 条**（`srs/fsrs` 3 / `srs/queue` 4），钉的是这个开关和它依赖的那道闸 —— 两者被改回去的症状都轻到没人会注意。其中**重学那条路**（毕业卡被 Again，走的是另一个默认值 `relearning_steps: ['10m']`）原先零覆盖，漏掉的症状是「新词都好好的，偏偏背熟又忘掉的那些几分钟后就回来」。`ReviewPage.test.tsx` 有一条旧断言写的是「Again 之后绝不会跳到几天以后」（上界一天），口径改成「落在最短那一档」（≥1 天且 <2 天）—— 上界保留，它挡的是评分接错线。顺带回写了 `srs/queue.ts` 文件头那个「已知近似」：同一张新卡当天不会被重来了，`reps === 1` 与「今天第一次学」现在严格等价 |
| 48 | **问 AI 从剪贴板改成服务器实时调用（2026-09-22）**：`FR-9.11` / `FR-9.12` 重写，`§12.16` 重写；新增 `server/src/ai.ts`（`createAiExplainer`，调 Anthropic Messages API）与 `POST /v1/ai/explain`（要登录，缺 `ANTHROPIC_API_KEY` 时整块关闭）、`src/ai/explain.ts`（客户端调用 + `aiAvailable` 便宜判断）；删除 `src/srs/aiNotes.ts` 与 `src/pages/vocab/AiNotesPanel.tsx` 整块（`pendingAi` / `aiRequest` / `applyAiNotes` / `AI_PROMPT`），`VocabEntry.askAi` 字段随之删除；生词本每一行的「问 AI」改成直接调用并覆盖 `note`；查词面板两个词典都查不到时自动问一次 AI，成功就跟着 `createFromLookup` 一起收下 | 用户的原话是「我的 Claude 可以配置 api key，这样就可以用低价模型进行实时查询了……把这个功能放到服务器里」。**手动剪贴板那条路当初存在的唯一理由是没有自己的 key**（见变更 46）；有了 key 之后复制、切会话、粘贴、对编号这几步全是纯摩擦，换成一次服务器转发的 HTTP 请求，模型用 Haiku（够用、便宜）。<br>**不违反 §3.1.1 R-1**：R-1 管的是「服务器替用户去拿第三方版权内容」，而这里请求体只有用户自己敲的词/句，回复是模型生成的解释，没有任何第三方素材经手这台服务器——与「同步后端不在 R-1 的通路上」（§3.1.1 2026-09-02 补充）是同一条判断的另一次应用，详见该节新增的一段。<br>**一处真 bug，改的时候当场抓到**：查词面板的 AI 兜底状态最初写成 `Pending<string> | 'unavailable'`，而 `'unavailable'` 本身就是字符串——`typeof x === 'string'` 分不出它是哨兵值还是 AI 真给的解释，e2e 跑出来发现新卡的 `note` 字面量存成了 `"unavailable"`。改成 `'loading' | 'unavailable' | { note: string }` 这种判别联合类型之后不会再犯，成功值必须拆一层对象才能取到文本。 |
| 49 | **查到的词也有 AI 入口 + 答案缓存跨设备同步（2026-09-22 深夜）**：`FR-9.11` 再次扩写，`§12.16` 补一段；新增 `src/ai/cache.ts`（`AiCache` = 归一化词 → `{note, ts}`，逐词取 ts 更新的那条合并）与第五种同步文档 `aiCache`（`docs.ts` / `queue.ts` / `trigger.ts` 的 `pushAiCache` + `scheduleAiCacheSync` / `pull.ts` 的 `pullAiCache` / `restore.ts` 的一键恢复都补了这一支）；`explainWithAi` 每次成功都会 `rememberAiNote` 写入缓存；`DictLookup.tsx` 的 `ResultCard`（查到的词）新增 `AiBlock`（「问 AI」按钮，点了变「重新问 AI」），`NotFound` 那一支复用同一个组件；两支都先查缓存，命中就直接显示，不用再问一次模型 | 起因是他在浏览器里点开「查词」查一个词典里查到了的词（`das Anwesen`），发现**完全没有「问 AI」的入口**——变更 48 只把自动问一次接到了「两个词典都查不到」那一支，查到的词从来没有过按钮。他的原话是「不管是否查到，都给一个用ai查询的入口。查询结果记录到当前用户信息里并进行同步。以后再查这个词的时候，就把ai查询的结果对应的显示」——三件事：入口补全、结果持久化 + 同步、复用而不是重问。<br>**查到的词是按钮，不是自动**：内置词典/de.wiktionary 已经给出内容，每次查一个查到过的词都自动再问一次模型纯属浪费；查不到的那一支继续沿用变更 48 的自动问一次（那时候没有别的内容可看，多等一次请求换来的是「不再是一张空卡」）。<br>**缓存是新写的一份数据，不是塞进 `VocabEntry.note`**：查词这一步可能压根不会把词加进生词本（只是查一下），缓存必须独立于「有没有收进生词本」才能满足「以后再查这个词直接显示」——所以照抄 `study/log.ts` 那一套「标注层自己的一份 IndexedDB 记录 + 独立的同步文档」，而不是拐弯去更新某条 `VocabEntry`。合并规则**逐词取 ts 更新的那条**（不是像学习记录那样逐格求和/取 max）：一个词只需要一份解释，「重新问」本来就是想让新答案覆盖旧的。<br>**`explainWithAi` 写缓存是无条件的**：生词本行上的「问 AI」/「重新问 AI」也走这个函数，所以顺带也会把答案记进这份缓存——不用额外接线，两个入口自然共享同一份「问过的词」记忆。 |

| 50 | **更新器自己会把设备锁死（2026-09-22 深夜）**：`§7.12` 的「查更新」一行重写；`checkNativeUpdate()` 里那两个过桥调用改成**各问各的、各自 4 秒超时**（新增 `askBridge`，问不出来回 `undefined`，绝不挂住调用方）；`decideUpdate` 签名从 `(manifest, currentVersion, nativeVersion)` 改成 `(manifest, ctx: UpdateContext)`，三个数**都可缺**，而**缺了一律不否决更新**；新增不过桥的记号 `localStorage['ota.queuedBuildId']`（桥哑着时用它判断「这一版已经下过」，免得每次冷启动白下 10MB）；`runningBuild()` 两个数也各问各的、在原生壳上**永远回一个对象**（字段可为 `undefined`），`RunningBuild` 相应改成三个可选字段 + `queued`；`VersionSection` 改写：一个数问不出来时另一个照常报、桥哑掉时明说「热更可能停摆、连着几次不换版本就该发包了」、并报出上次下好等着生效的那一版。+14 条用例（`decideUpdate` 的「未知不许否决」5 条、`checkNativeUpdate` 6 条含**桥永不回调时照样下包**那条回归、`VersionSection` 3 条） | 用户报「问 AI 在网页上好使，手机上点了只是『已标记』」，而且**重启五次都不变**。查下来手机跑的是 `ios-v0.6.0` 的**内置 bundle**（那个 tag 指向 85d7cfc = 变更 46，`resetWhenUpdate: true` 装包时把热更包全丢了），并且它**永远也更新不了**。<br>成因是同一天早些时候 600fdb5 只修了一半：那次已经在真机上查实 `CapacitorUpdater.current()` / `App.getInfo()` **永远不 settle**（设置页「版本」块从 0.4.0 起一次都没出现过），并给 `runningBuild()` 加了超时——**但漏了 `checkNativeUpdate()`**，而那才是真正干活的那条路。于是每次冷启动：manifest 拉到了（服务端一切正常，`e209c83` 就在线上），然后卡死在 `Promise.all([current(), getInfo()])`，走不到 `download()`。不报错、不留日志、重启多少次都一样。<br>**这类 bug 的代价是不对称的**：门槛判错（放过一个其实不该下的包）= 「点了没反应」，下一版就能修回来；而门槛卡死（永远不下）= 这台设备**彻底脱离热更**，而修复本身也是 JS、只能靠热更下发——死锁只能靠再发一次 TestFlight 解开（这次就是这么解的，`ios-v0.6.1`）。所以定成：**未知一律按「不阻拦」处理，只有确实问出来了且确实不够才拦；更新器宁可多下一次包，也不能有一条会把自己锁死的路。**<br>教训与变更 46 那条「诊断块不能自己消失」是同一条的下半截：那次记住了「诊断不能消失」，但没有把同一个超时补到**依赖同一个桥去干活**的地方——**会消失的诊断只是看不见，会卡死的执行路径是真的坏了。** |

| 51 | **音效的 resume 手势链补了半截（2026-09-22）**：`playSfx()`（[src/audio/sfx.ts](src/audio/sfx.ts)）从「`resume()` 不等就直接 `start()`」改成**等 `resume()` 真的 resolve 了再 `start()`**；`sfx.test.ts` 两处断言从「点完立刻查」改成「等一个微任务再查」；FR-10.12 表述同步更新 | 用户报「按钮点击的音效播放，在网页上正常，在手机上没声音」——正是 §10「没验的（要真机）」清单里变更 46 落地时就留着的那条悬念，这次算是真被验出来了。**suspended 的上下文里排的音，Chrome 桌面上 resume 之后照样会响，WKWebView 不认**：那一下播放静默丢在原地，不抛错、不提示，跟音效模块本来的设计（失败一律静默）合起来就是「点了没反应」——和变更 45/50 那几次「查不到成因」的坑是同一种失败方式。**这一条还没在真机上重新确认过**：下一次热更到手机之后点一下选项确认真的响了，顺带看一眼德语句子还在播时答题两个声音会不会互相掐掉（这半条 FR-10.12 原文就有，没有理由跟着坏，但没有单独验过） |

| 52 | **热更诊断到根、常亮改走原生（2026-09-23）**：`§7.12`「查更新」一段重写，新增 `§12.12` 的诊断子段。`src/platform/nativeUpdate.ts` 新增 `probePlugins()`（同步读 `window.Capacitor.PluginHeaders`，不过桥，回答「这台设备上 `CapacitorUpdater` 到底注册没注册」）、`askBridgeVerbose()`（`askBridge` 的诊断版，`timeout`/`rejected`/`threw` 三态分开）、`CheckLogEntry` + `readLastCheckLog()`（`checkNativeUpdate()` 每一步都落 localStorage，`finally` 里无条件写，含 `probePlugins()` 那一刻的快照）；`checkNativeUpdate()` 补齐两个还没盖到的自锁点：`fetch` 本身没有超时（手写 `AbortController`，不用 `AbortSignal.timeout()`——那是 Safari 16.4 才有，部署目标是 iOS 15）、`notifyNativeAppReady()` 自己过桥也没超时。`VersionSection` 新增「现在就查一次更新」按钮（iOS 上才有）与两块新诊断（插件注册名单 + 上次查更新的逐步记录）。`ios/App/CapApp-SPM/Sources/CapApp-SPM/CapApp-SPM.swift` 新增对 `CapacitorUpdaterPlugin` / `SocialLoginPlugin` 的显式引用（防 SwiftPM 静态库死代码剥离）；`.github/workflows/release-ios.yml` 的 `cap sync ios` 之后新增一步，校验 `CapacitorUpdater` 确实进了这次构建。`SceneDelegate.swift` 新增 `sceneDidBecomeActive`/`sceneWillResignActive`，直接拨 `UIApplication.isIdleTimerDisabled`——FR-18.7 常亮从此有两道防线，Web Wake Lock 不再是唯一防线。`src/platform/wakeLock.ts`：被拒时记下 `err.name`/`err.message`（不能拿 `instanceof Error` 判——真机抛的是 `DOMException`，不一定是 `Error` 子类）；新增「下一次用户手势（`pointerdown`）自动重试一次」（`armGestureRetry`，一次被拒只挂一个监听器）；成功时清掉上一次的错误残留。前端 +42 单测（982），无 server/e2e 改动，五条验证全绿；**用户随后问「有测试可以加吗」，又补了 9 条**（991）：`probePlugins` 吃到畸形原生数据不炸（数组里混非对象元素、`methods` 字段形状不对）、`checkNativeUpdate` 日志分清 `rejected`（原生侧真报错）与 `threw`（过桥调用同步抛，两者都不同于 `timeout`）、`readLastCheckLog` 面对损坏的 JSON 回 `null` 不抛错、`wakeLock` 被拒的值不是 `Error`/`DOMException` 时不炸、`resetWakeLockForTests` 真的摘掉了手势重试监听器（用 `addEventListener`/`removeEventListener` 的函数引用断言，不是间接数调用次数——那条路会被 `requesting` 那道闸掩盖掉）；**这一轮顺带在 `VersionSection.tsx` 抓到一个真缺口**：`runCheckNow` 里 `await checkNativeUpdate()` 没有 `catch`，虽然生产实现自己已经把所有失败收进 try/catch/finally、正常不会 `reject`，但按钮不该依赖这个前提——补了一条防御性用例（mock 成 reject）当场证实会产生未处理的 rejection，顺手补了 `.catch(() => null)` | 用户带着上一轮（变更 50）的真机结果回来，指出两件事没解：**①** `App.getInfo()` 好、`CapacitorUpdater.current()` 哑，两者同一条桥队列，「慢」解释不通——更像是插件类没被链进二进制，桥找不到就直接丢调用（`CapacitorBridge.handleJSCall` 的 `guard ... else { return }`，读了 `@capacitor/ios` 源码验证）；**②** 「常亮申请被拒，且不是省电模式」是新暴露的问题，原来的 `catch {}` 连拒绝原因都没留。<br>**这次没法在真机上验**——没有 Mac，只能把「查不出来」变成「代码里有地方能查」：`probePlugins()` 分开「类没注册」和「调用超时」这两种成因（前者要发新包，后者可能下一版自己就好），`CapApp-SPM.swift` 那处显式引用是「最像的成因」的最低代价赌注（不引入新依赖，猜错了也不会更坏），CI 那道校验挡的是「配置里漏列」这一档最便宜就能查的错。常亮换成 `SceneDelegate` 接管是因为这份工程有 `UIApplicationSceneManifest`——**`AppDelegate.applicationDidBecomeActive` 根本不会被调用**，原来的实现如果试图挂在那里会是又一个「挂了个不响的钩子」。**这一整条都要等下一次出包装机才算数**：`probePlugins()` 的名单里有没有 `CapacitorUpdater`、`isIdleTimerDisabled` 接管之后屏幕真的不灭、以及最关键的——这次的赌注到底压中没压中 |

| 53 | **真机验出了插件确实注册成功、但 `download()`/`next()` 还是会卡死（2026-09-23）**：`checkNativeUpdate()` 里那两下裸 `await` 改成套 `askBridgeVerbose`——`download()` 45 秒超时、`next()` 10 秒超时（`§7.12` 补一段）；任何一步失败都不再假装成功：`download` 超时/报错直接 `skip`，`next` 超时/报错**不写** `queuedBuildId`、也不设 `pending`（否则设置页会显示「已下好等生效」但原生侧其实什么都没登记）；+3 条回归用例（51）。已发 `ios-v0.6.4`（这次没有依赖 OTA，直接把修复焊进 builtin bundle） | 用户装上 `ios-v0.6.2` 发来真机诊断截图：**`probePlugins()` 确认 `CapacitorUpdater` 注册成功**（12 个插件、`current` 在方法列表里）——变更 52 那个「类没链进二进制」的假设被这份数据**推翻**了；**常亮这次通过 Web API 直接就成功了**（「锁拿着」），FR-18.7 基本验过。但「现在就查一次更新」的按钮点下去**连着几分钟没有反应**——用户追问之后查代码发现：`current()`/`getInfo()` 早就套了超时保护，**唯独 `download()`/`next()` 这两下还是原来的裸 `await`**，是这次自己漏掉的。真机上大概率是 `current()` 4 秒超时后 `decideUpdate` 落进 `download` 分支，然后卡死在没有任何保护的 `download()` 调用上——和变更 50 那次死锁**一模一样的坏法，只是换了个调用**。<br>**这次没有让它走 OTA**：既然还不确定「热更这条通路本身通不通」，把修复只推到 OTA 等于赌「OTA 是好的」，而这正是要验证的东西；所以直接出了 `ios-v0.6.4`，把修好的版本焊进 builtin bundle，不依赖任何桥调用就能生效。<br>**教训**：给一类调用加了超时保护之后，要把"这一整条调用链上还有没有没保护到的兄弟"过一遍，不能只补眼前报错的那一个——`current()`/`getInfo()` 挂了会被立刻发现（症状明显），而 `download()`/`next()` 只在 `decideUpdate` 真的判定要下载时才会触发，不是每次查更新都会走到，所以更容易被漏掉、也更晚暴露 |
| 54 | **挖空题的音频放不出来（2026-09-23）**：`ReviewPage.tsx` 课程听卡自动播那个 effect 依赖数组从 `resolveRange()` 每次渲染新建的对象改成 `useMemo` 钉住的稳定引用；`FR-10.5` 补第四种静默失败（`hasMaterial` 说有、`audioBlobs` 里这一次真取不到 / 取到了解不了码，各给一条 Banner + 「去重新下载素材」）；读卡卡背「念一遍」三处修复：没有任何音源时按钮不出现（原来无条件渲染，按下去没反应）、`speak()`/`audioPlayer.load()` 的失败不再静默吞掉、**课程卡改成念原句而不是孤立词形**（用户当场选定的形状，§12.13 同步改写）。+5 条回归用例（999），server/e2e 不受影响，五条验证全绿 | 用户报「复习时遇到挖空的题，音频根本无法正常播放」。定位过程：写了个临时组件测试给 `getAudioBlob`/`audioPlayer.load` 配上贴近真实的延迟（不是零延迟的同步 mock），跑出真实调用序列是 `load → playRange → pause → load → playRange → pause → …`——**每次 `playRange` 后面紧跟一次 `pause`，不收敛**。根因定位到变更 45（FR-21）：那次为了在依赖数组里加 `isRead`，顺手把原来的 `[entry.id, audioStatus, lesson?.id, range?.start, range?.end]`（全是原始值）改成了 `[entry.id, audioStatus, isRead, lesson, range]`，而 `range` 是 `resolveRange()` 每次调用都 `return { ... }` 的新对象——effect 因此每渲染必重跑，清理函数把刚起播的那句音频掐掉，句子播几毫秒就被自己掐断。**为什么 847+ 条既有单测没抓到**：现有 mock 把 `getAudioBlob` 设成永远返回 `undefined`，代码走的是「取不到 blob」那条早退分支，根本进不了这段循环；写回归用例时特意钉住「延迟不能省，去掉延迟这条 bug 就自己收敛，测不出问题」。<br>顺手一起修的三处 FR-10.5 静默出口：`hasMaterial` 与 `audioBlobs` 分属两个 store 会分叉，原来取不到/解不了码都是无提示的永久灰按钮或未处理的 rejection；读卡卡背「念一遍」原来无条件渲染，没有真人音也没系统嗓音时点下去毫无反应，与变更 46 「原样收下」按钮那次是同一种坏法（[[report-symptom-not-cause]]）。课程卡卡背播放原句而不是孤立词形是问用户确认过的形状决定（§12.13 当场改写，[[spec-decisions-same-session]]）——那一句本来就在本机、也正是挖空题面用的那句，练它才练得到连读 |
| 55 | **卡背上的中文（2026-09-23）**：新增 **FR-10.14**；`ReviewPage.tsx` 的 `QuizCard` 在 `revealed` 且词条**既无 `meaningZh` 也无 `note`** 时自动走一次「先查 `ai/cache.ts` 缓存 → 没有再 `explainWithAi()`」，答案写回 `entry.note` 并触发 `syncVocabNow()`；`CardBack` 多两档状态（`loading` 的「AI 解释中…」、`failed` 的一行 + 「重试」），没配 AI 时**什么都不显示** | 起因是一张预置卡的截图加一句话：「这里能直接显示出中文的解释吗，如果查询过 ai，则 ai 的答案一起给出」。查下来卡背**本来就会**显示 `meaningZh` 和 `note`，只是 `erschlagen` 两样都空 —— 词典的中译只覆盖约 60%（德语释义 99.9%，FR-10.9 实测），而 `note` 要在生词本页人手点过「问 AI」才有。<br>**顺带补上一个一直存在的缺口**：查词面板（`DictLookup`）问到的答案只写进变更 49 那份缓存，**不写进 `entry.note`** —— 所以「这一课查词时问过这个词」的解释，在复习卡背上一直看不到。卡背先查缓存就同时把这条补了，而且不必在 `DictLookup` 那边再加一次写回。<br>**三处形状值得记住**：① **判据是「一个中文字都没有」**，不是「没有 `note`」—— 有 `meaningZh` 就已经有中文了，再问一次只是重复花钱；② **只在答错那条路触发**：答对走 `flash` 直接进下一张、根本不露卡背，在那里问等于给「已经会了的词」花钱（有测试钉住）；③ **没配 AI（没登录 / 服务器没 key）时卡背一个字都不多说** —— 这里没有任何下一步动作可做，每答错一次就唠叨一行「不可用」是纯噪音，登录状态在设置页。真的发出去又失败了才给「AI 服务暂时不可用 + 重试」，那时候重试是有意义的动作。<br>**卡背不等 AI**：词形、IPA、德语释义、例句立刻就在上面，中文回来了再填进来 —— 答错时最需要的是「这个词到底是什么」，不是一个转圈的卡。请求也**不随翻页取消**：答案已经付过钱了，落库比丢掉划算，而写的是 store，跟这个组件还在不在没关系。<br>**测试 +15 条前端 + 1 条 E2E**（`ReviewPage.test.tsx` 999 → 1014，e2e 43 → 44）。钉得最重的是**「什么时候不该问」**（有 `meaningZh` / 有 `note` / 答对 / 缓存命中 / 同一张卡重渲染）—— 少问一次在界面上看得见，多问一次看不见，只有账单知道。另外两条守的是「**钱花了答案却丢了**」这种纯静默的坏法：翻页之后才回来的答案仍然要落到**那个**词条上，且不会串到当前这张卡。E2E 那条守的是组件测试够不到的一段 —— 那里 `aiAvailable()` 是 mock 的，**它真实的返回值从来没在这条路上跑过**，而判错的代价是一行每天都看得见的噪音。<br>**补测试时抓到一个真缺口**：`getCachedAiNote()` 原先在外层 `try` 里，于是 IndexedDB 出一次问题，卡背上就只剩「不可用」—— 缓存只是一条省钱的近路，读坏了不该把「问一次」一起拖下水，单独 `.catch(() => undefined)` 掉。<br>**两处测试手法值得记住**：① AI mock 用的是**由用例自己决定什么时候回答**的 deferred promise，不是 `setTimeout` —— 后者会和 `waitFor` 自己的轮询赛跑（[[mock-delays-for-regression-tests]] 那条教训的延伸）；② 这一组**必须逐条把三个 mock 归位，而且用例散场前要等那次落库真的写完**。`vi.clearAllMocks()` 只清调用记录、不清 `mockResolvedValue` 设下的实现；而 deferred 的答案是在用例结束之后才写进 store 的，不等它就会落到**下一条用例刚 seed 好的库上**，把那条弄红。**同一种时序污染在这一轮里咬了两次**，两次的症状都是「单独跑绿、一起跑红」 |
| 56 | **生词本里 AI 解释默认折叠（2026-09-23）**：`VocabPage.tsx` 的 `Row` 把 `entry.note` 包进 §12.14 同款 `Disclosure`（`defaultOpen=false`），摘要写死「AI 解释」；新增 `VocabPage.test.tsx`（3 条：默认收起、点开才出现、没有 `note` 时整块不出现），前端 1014 → 1017，server/e2e 不受影响，五条验证全绿 | 用户截图反馈：变更 55/49 让 AI 解释越攒越长，不折叠的话列表里全是长文本，「看词就很不方便」。直接复用 `Disclosure`（`ZhPanel`/`LessonNotes` 已在用），没有新写组件；`defaultOpen` 特意给 `false`——该组件在 FR-20 笔记那边默认展开是因为「有内容就该露出来」，这里恰恰相反，内容长到会盖住一整块列表，折叠是为了让人扫得完这一页，不是为了藏。用真实浏览器验证折叠/展开行为时确认了一处 jsdom 差异：`<details>` 未展开时子节点在 jsdom 里仍然在 DOM 树里（jsdom 不做布局层面的 `display:none`），组件测试因此不能按「文本在不在 DOM 里」断言，改成查 `<details>` 元素本身有没有 `open` 属性 |
| 57 | **桥自检与诊断上报到服务器（2026-09-23）**：`§7.12` 新增「桥自检与诊断上报」一节、`§12.12` 新增三按钮那一组。`src/platform/nativeUpdate.ts`：新增 `COLD_BRIDGE_TIMEOUT_MS`（桥在这个会话里还没回过话时每个调用至少给 25 秒，回过一次话——resolve 或 reject 都算——就切回 4 秒）与 `resetBridgeWarmupForTests()`；`askBridgeVerbose` 导出、返回值多一个 `ms`、多一个 `allowCold` 参数（`askBridge` 传 `false`，**显示用的那条路不走放宽**）；步骤记录改成 `current:ok(37ms)` 这种带耗时的形式；顺手补了两处 `current.bundle.version` 的可选链（原生侧回一个没有 `bundle` 的对象就会抛，而这个返回值直接喂给 `void runningBuild().then(setBuild)`，抛出去就是没人接的 rejection、设置页永远停在「正在问原生侧版本号…」）。新增 `src/platform/consoleTap.ts`（环形缓冲抄 console，装在 `main.tsx` 最顶上）与 `src/platform/bridgeDiag.ts`（`runUpdaterSelfTest` 串行逐方法自检 + `collectDeviceReport` + `sendDeviceReport` + `reportToText`）。`VersionSection` 诊断区加三个按钮（跑一次桥自检 / 发到服务器 / 复制诊断）。服务端新增 `server/src/diag.ts`（文件收件箱，每用户 30 份上限）与 `POST/GET /v1/diag`、`GET /v1/diag/:id`（都要登录，2MB 上限）。**另加三条（用户在同一次对话里补了一句「没有『上次查更新』这个信息，也没有步骤的信息」）**：查更新的记录改成**每一步立刻落盘**（`startCheckLog()`，进门先写「开始了」，`nativePlatform()` 那一句也记一步）、`VersionSection` 在**一笔记录都没有时画一行 `warn` 明说**（推翻变更 52「不留空壳」那条约定）、「现在就查一次更新」加一道 150 秒总闸（`CHECK_HARD_TIMEOUT_MS`）。前端 1017 → 1043、server 133 → 145、e2e 44 不变，五条验证全绿 | 用户装上最新版之后症状没变：「一直显示查询中，提示版本号问不出来，原生桥没有回话」，并问「你有测试的办法吗，或者将错误日志汇报到 vps 上你进行分析」。<br>**读插件源码读出一个此前没考虑过的成因**：`@objc func current` 是纯同步的——拿到 `BundleInfo` 立刻 `call.resolve()`，**它不可能慢**。能让它慢的只有它前面那一步：Capacitor 的插件是懒加载的，第一次桥调用才 `loadPlugin()` → 跑 `load()`，而 Capgo 的 `load()` 在**主线程**上做一整套磁盘活 + 发一次统计到 capgo 的服务器 + `initialLoad()` 切 serverBasePath。所以「`current:timeout`」很可能不是桥哑了，是**我们在插件正初始化的当口就放弃等待了**——变更 50 起一路加的都是「别挂死」，这一版加的是反过来那一半：**别放弃得太早**。<br>**第二个发现改变了诊断的上限**：Capgo 的原生日志是 `webView.evaluateJavaScript("console.info(…)")` 打进 JS console 的（`Logger.swift`）——在 JS 侧抄下 console 等于**把 Xcode 控制台搬进了应用**，而这个项目从来没有过 Xcode 控制台可看（开发机是 Windows）。`load()` 里那几十行 `logger.info` 是回答「插件初始化到哪一步」的唯一证据。<br>**逐方法自检串行不并行**是刻意的：并行会让「第一次调用触发 `load()`」被后面的调用分摊掉，而分辨四种故障靠的正是「头一个特别慢、后面都快」这个形状。<br>**两条出口必须互不依赖**：发到服务器要求登录是好的，而诊断真正用得上的时候坏的可能正是登录——所以「复制诊断」是平行的一条，不是发送失败之后的兜底。<br>**写自检用例时撞出两件事**：一是 `runningBuild` 那个可选链缺口（上面已列，验证过「去掉可选链这条用例就红」）；二是「桥回过一次话就缩回 4 秒」是模块级状态，不在每个用例前清掉的话，前一个用例里一次成功的调用会让后一个用例的窗口悄悄从 25 秒变成 4 秒——那正是「单独跑绿、一起跑红」那一类泄漏，而且它会让「冷启动给足时间」这条用例在污染下**假装通过**（发现时确实是这样：那条用例推 5 秒就断言超时却绿着）。加了顶层 `beforeEach` 清记号之后，另有 7 条旧用例的假设当场露出来并改正。<br>**用户那句补充反馈本身就是一个证据**：「连『上次查更新』那一块都没有」= `checkNativeUpdate()` 从来没跑完过一次（记录写在 `finally` 里）。而这暴露了一个更根本的毛病——**诊断只在成功路径上落盘**。改成逐步落盘之后，「日志停在哪一步」直接就是「卡在哪一步」，不用再猜。按钮那道总闸同理：每一步都有超时 ≠ 整个函数一定会返回，而「按钮转个不停」是要报告的事实，不是一个能让人一直等下去的状态。 |
| 58 | **通听的当前词在手机上看不见（2026-09-23）**：`ListenTab.tsx` 里当前词的样式从 `bg-warn-soft font-medium` 改成 `bg-warn text-surface font-medium`（反白），并加 `data-active` 便于测试定位；`§12.17` 记下这条形状；新增 `src/pages/lesson/ListenTab.test.tsx`（3 条：当前词底色不等于当前行底色、高亮跟着时间走且同一时刻只有一个、落在句间空档时不标任何词），前端 1043 → 1046，server 145 / e2e 44 不受影响，五条验证全绿 | 用户带着一张桌面截图来问：「在网页上播放的时候，会有当前词的高亮显示，但是在手机上就没有。」<br>**查下来是一个纯视觉的低级错误，而且它是确定成立的、不必先复现**：词级高亮**永远**发生在当前行上（`activeToken` 只在当前行有值），而当前行的底色正是 `bg-warn-soft` —— 当前词用的也是 `bg-warn-soft`，两者同一个令牌、同一个色值，叠上去等于没叠。于是「当前词」这件事在屏幕上唯一剩下的表达是 `font-medium`，**一档字重**。Windows 上 400/500 一眼可辨（用户自己截的图里 `verbindet` 确实看得出来），iPhone 的 SF Pro 在 19px 正文上差别细到可以忽略 —— 同一份代码，两台设备上一个有高亮一个没有。<br>**所以这不是「手机上的 bug」，是这个样式从第一天（变更 24）起就只在字体渲染够狠的平台上勉强成立**。修法不是去调字重，是给当前词一个**自己的底色**：`warn` 实底 + `surface` 字色，两个令牌都跟着深色翻转（浅色是深棕底白字，深色是亮金底黑字），任何设备、任何字体渲染下都看得见，也正是卡拉OK/Spotify 的通行做法。<br>**测试断言写成两个类之间的关系**（当前词的底色 ≠ 当前行的底色）而不是「等于某个类名」：换配色照样过，退回同色就红 —— 还原成旧写法验证过这条用例确实会红。<br>**留一条待验**：如果手机上连**整行**的浅黄底色都不跟着句子走，那就是另一回事（时间源没推进，而不是看不见），要另查。<br>**随后他问「有测试可以加吗」，补了 6 条组件测试 + 3 条 E2E**（新文件 `e2e/listen.spec.ts`，e2e 44 → 47）。E2E 那条才是这次 bug 的**对口**判据：组件测试只断言得了 class 名，**断言不了这两个类最终算出来的颜色是不是同一个** —— 那要真的 CSS、真的层叠、真的令牌。还原成旧写法跑过一次，它报的是 `Expected: not "rgb(251, 241, 224)"`，正是 `--color-warn-soft` 的色值。<br>**为了让 E2E 能种出一课「既有音频又有词级时间戳」的课，`fixtures.ts` 多了 `timedLessonBackup(lessonId)`**：这两样东西分属两层（音频在缓存层，只能靠 `<input type=file>` 进来；词级时间戳在标注层，只能靠备份进来），所以先 `importLesson` 带 WAV 拿到 id，再用一份 `updatedAt` 更新的备份把时间戳盖上去。**另一条路（导 `sampleBackup` 再在课程页选本地音频）走不通得很安静**：绑音频会顺手 `enqueueAlign()`，而 E2E 里没有对齐服务器。fixture 里一句给 10 秒（真实素材远快于此）也是刻意的——断言要在「当前词还亮着」的窗口里跑完，句子短了用例就会随机掉进句间空档变红。 |
| 59 | **热更卡住的那一行找到了：取插件的那个 `import()`（2026-09-23）**：`§7.12` 新增「诊断给出了答案」一节。`nativeUpdate.ts` 新增 `withDeadline()`（给「取东西」那一步死线，和管过桥调用的 `askBridgeVerbose` 分工）、`acquireUpdater()`（**首选 `@capacitor/core` 的 `registerPlugin('CapacitorUpdater')` 造代理，一个新 chunk 都不取**；插件包那个动态 import 降级成 8 秒死线的退路；返回 `via` 说清是从哪条路拿到的）、手写 `UpdaterPlugin` 类型（不 `import type` 插件包，否则「这个包必须存在」又写回构建里）；`checkNativeUpdate` 的步骤从 `plugin-import:ok` 变成 `plugin:<via>`，拿不到插件时是 `skip` 而不是挂住；`notifyNativeAppReady` 改成**重试 3/10/30 秒**（跨过插件启动那个 20 秒窗口）、只重试「没人回话」这一种、结果落进新的 `AppReadyLog`；查更新记录与 appReady 记录各在内存里存一份（`readLastCheckLog()` 内存优先），localStorage 写失败的原因单独记进 `readStorageWriteError()`。`bridgeDiag` 改走同一个 `acquireUpdater()`（自检第一行就是 `acquireUpdater → ok(0ms) core`），报告 `schema` 1 → 2，多 `appReady` 与 `storageWriteError` 两个字段。`VersionSection` 多两行（「我起来了」成没成/第几次、存储写失败过）。前端 1046 → 1058，server 145 / e2e 44 不受影响，五条验证全绿。**已上线 `5cc8cea`，`ios-v0.6.6` 已出包上传 ASC**（构建号 17，6m44s；线上 manifest 是 `0.6.6+be58199`，正是这个 IPA 的那份代码） | `ios-v0.6.5` 的真机报告是四天里第一份决定性的证据。三条数摆在一起只指向一个地方：①`probePlugins()` 名单里**有** `CapacitorUpdater`、56 个方法齐全；②自检里七个只读方法 `getPluginVersion` 4ms / `current` 0ms / `list` 1ms，对照组 `App.getInfo` 2ms——**桥、插件实例、每一个方法全是好的**，变更 57 那张「四种形状」表里一种都对不上；③而「上次查更新」的步骤停在 `['platform:ios']`，下一步正是取插件。<br>**第三条还有一个独立印证**：同一次启动里 `notifyAppReady()` 根本没到过原生侧——原生日志里没有插件那句 `Current bundle loaded successfully. [notifyAppReady was called]`，只有回滚检查自己的定时器打的 `Built-in bundle is active. We skip the check`。而 `notifyNativeAppReady()` 在平台判断之后的下一步也是取插件。**两条互不相干的路停在同一行。**<br>**旁证在抄下来的原生日志里**（变更 57 那条 `consoleTap` 第一次派上用场）：那次启动打过 `Semaphore wait timed out after 20000ms`——插件 `load()` 正在主线程做磁盘活并 `setServerBasePath()` 切 web 根，而那个 4KB 的 chunk 是同一个 WebView 去取的。请求悬在那儿不成不败，`import()` 对此的表现就是 promise 永远不 settle。<br>**教训是同一条的第四次**：变更 50/52/53 一路把每个**过桥调用**都套上了超时，漏掉的是「过桥之前先把插件拿到手」——形状不是「过桥」而是「取东西」，于是搜「桥调用」的时候搜不到它。所以这次不是只补一个超时，而是**把这条路上唯一要取的那个 chunk 整个去掉**：插件包的 JS 层只是 `registerPlugin` 造的一层代理壳，而 core 在这一步之前必然已经加载完了（`platform:ios` 那一步就是证据）。<br>**`notifyAppReady` 那条是顺着这份报告捡到的第二个真缺口**：它原来只有一次机会、失手即止、一个字都不留。跑内置 bundle 时侥幸没事（`isBuiltin()` 直接跳过回滚检查），但热更一旦真的装上一个 bundle，同一个失手就是「新版起来了 → 没人说我起来了 → 下次启动回滚」，症状恰好是「下下来了、永远不生效」。<br>**诊断自己也补了一个盲点**：这一版之前，「记录停在第一步」有两种成因（真卡住 / 后面几步写不进 localStorage）在文件里长得**一模一样**——`writeCheckLog` 的 catch 是哑的。分不清它们，这份报告在最关键的问题上就不可信，所以记录改成内存优先、存储只管跨会话、写失败的原因单独上报。<br>**这次必须出包，判据和变更 53 的 `0.6.3` 恰好相反**：那次的结论是「没动 Swift 就不该出包，纯 JS 交给 OTA」，而这次改的同样是纯 JS，却**必须**焊进 builtin bundle——因为送 JS 过去的那条路正是坏的那条。判据不是「改了什么」，是「OTA 现在能不能把它送到」。 |
| 60 | **热更停在取 manifest：CORS（2026-09-23）**：新增 `public/_headers`，`/ota/*` 加 `Access-Control-Allow-Origin: *`；`build-ota.mjs` 把 `_headers` 排除出热更包；`deploy.yml` 发布后带 `Origin: capacitor://localhost` 线上检查放行头。本地用同一版 wrangler（4.104.0）`dev` 验过规则生效。`§7.12` 新增「走过了取插件，停在取 manifest」一节。**不必出包**。随后补测试：前端 1064 → 1071（`otaHeaders.test.ts` 守 `_headers` 规则 + `nativeUpdate` 三条取 manifest 的形状）；顺带修掉两处 CI 抖动——`diag.test.ts` 同一毫秒顺序不定（server 145 → 146），以及 `npm ci` 时 onnxruntime-node 的 postinstall 去 NuGet 下 CUDA EP 超时（今早 deploy 因此红过一次；五处 `npm ci` 都加 `ONNXRUNTIME_NODE_INSTALL=skip`，与 `server/Dockerfile` 的 `--ignore-scripts` 同理） | `ios-v0.6.6` 的真机报告：步骤 `platform:ios → plugin:cached → fetch:threw:Load failed`，`notifyAppReady` `ok(4ms, via core)`——变更 59 修对了，链条第一次走到网络那一步。壳的 origin 是 `capacitor://localhost`，取 manifest 是跨域，而线上响应没有任何放行头；网页版同源所以从没暴露。这条路从变更 41 起就是断的，只是此前每次都卡死在更前面。 |
| 61 | **热更走通；同一版只下一次、旧包要清（2026-09-23）**：`decideUpdate` 多一个 `nextVersion`（`getNextBundle()`），已下好待生效就跳过；`checkNativeUpdate()` 单飞（3 分钟过期防自锁），原函数改名 `runCheck`；新增 `bundlesToDelete()` + 每趟结束清理（两个 keep 都问出来才清、`next` 失败不清、最多 5 个）；`pendingUpdateVersion()` 认上一趟下好的那个；`UpdaterPlugin` 补 `getNextBundle`/`list`/`delete`。前端 1071 → 1085，四处修法各做了「去掉就红」的反证。§7.12 新增「走通了」一节 | 变更 60 上线后的真机报告是热更**第一次端到端走通**：download 770ms、next ok，冷启动后 `current()` 是新包、`notifyAppReady` 落在新包上。同一份报告里 `list` 有两个 `0.6.6+4f04891`：启动自动查与 9 秒后的按钮各下了一份，而代码从不删包。 |
| 62 | **导航角标「复习 N」改成跟复习页的「新卡 N · 复习 M」同一个口径（2026-09-24）**：`useDueCount`（`AppNav.tsx`）从「不设上限地数 `fsrs.due <= now` 的词条」改成直接调 `buildReviewQueue(entries, {newPerDay, reviewPerDay})`，取 `newCount + reviewCount`——套上每日上限与 FR-21.3 同日互斥去重，和进页面看到的数字保证一致。`AppNav.test.tsx` 相应改了两处：「未到期」那条从一张 state-0 新卡（新卡在队列里从不看 due，这条测不出「未到期」）换成一张 state-2 已毕业卡；「超过 99」那条把 `newPerDay` 显式调大，避免撞上新加的上限。新增一条「19 张到期新卡、`newPerDay=10`，角标应该是 10」的回归用例。**随后「有测试可以加吗」又补了 3 条**：`reviewPerDay` 单独封顶（不是只有新卡那条上限生效）、新卡与复习各占各的上限时角标是两边加总、以及桌面顶部导航（`TopBar`，另一段 JSX、跟手机底部标签栏是两条独立渲染路径）显示的数字同样对得上——四条新用例都做过「去掉这次改动就红」的反证（`git stash` 还原 `AppNav.tsx` 验证过）。前端 1086 → 1089，server 146 / e2e 47 不受影响，五条验证全绿 | 用户截图带两个红圈发来：顶部角标写「复习 19」，点进复习页却是「1/10 新卡 10·复习 0」。查下来两处压根是两套计数——角标是原始到期数，从没考虑过 `newPerDay`/`reviewPerDay` 这两个每日上限，而复习页从一开始就是套了上限之后的「今天真能刷到几张」。问用户要哪种口径，选的是「跟复习页一致」（Anki 的牌组列表也是这么做的：角标就是今天真正能刷到的数，不是总积压量）。 |
| 63 | **跟读每一遍都录音并回放（2026-09-26）**：新增 **FR-6.8**；状态机多一个 `echo` 相位与 `pass`/`recording` 两个字段，录音器以 `EchoRecorder` 注入（`src/audio/echo.ts` 的 `MicEcho`：getUserMedia + MediaRecorder，回放走 Web Audio）；`Settings.shadowingEcho`（默认开）；iOS `Info.plist` 加 `NSMicrophoneUsageDescription`（三语） | 用户原话：「没有重放我读的录音，没法看出差别」。形状是**把静默间隔变成录音，间隔结束立刻放一遍自己**，放完才进下一遍/下一句。**起初只录第 2 遍**（第 1 遍留作普通间隔「先听清」），当天用户就改成「去掉第一遍，仅保留第二遍就足够了」—— 听原句、跟读、听自己，一遍就是完整的一轮，那一遍不录的是多余的。于是 `echoPass`（第几遍录）改成布尔的 `echo`：开着就每遍都录；想每句只走一轮就把「每句重复」设成 1 —— 随后默认值也从 2 改成了 1。<br>**第三次改形状：录音不再有固定时长**。用户指出「很多时候在这个时间内根本读不完」（间隔 = 句长 × 1.2，长句的后半截录不上）。考虑过自动判「停了没有」（能量检测 + 0.8 秒静音），用户提议改成按钮，按钮更对：自动判会被外放的原句残响和环境噪音骗，而且误判时人不知道为什么不停；「念完了」只有人自己知道。于是录音那一遍没有倒计时，只有一个红色呼吸的「读完了」按钮 + `Enter`（桌面上手在键盘上），外加 max(20 秒, 句长 × 5) 的保底 —— 忘了点、人走开了，循环不能永远停在录音上；麦克风起不来时那一遍退回固定间隔，界面上不会挂着一个没在录的按钮。<br>三件值得记住：① **麦克风在整段跟读期间一直开着**（「开始跟读」打开、停止/切后台/离开页面/关开关时还回去）：每遍再 getUserMedia 要一两百毫秒，正好吃掉原句一停就开口的头一个音节；② **先起播再要麦克风**：iOS 的第一次 `play()` 必须留在点击的同步段里，等权限弹窗回来手势就断了，麦克风打开之前（首次授权弹窗那几秒）的那几遍按普通间隔走，界面不会写着「录音中」却什么都不放；③ **iOS 没有那条 plist 声明时调麦克风是被系统杀进程，不是报错**，而这份 JS 会经热更先落到旧壳上 —— 所以 `echoSupported()` 要求壳 ≥ `ECHO_MIN_IOS`（0.7.0），问不出版本就当不支持；Android 先不开。回放 `ended` 不来（上下文被挂起）时有「时长 + 1 秒」的保底，循环不会停死在回放上。<br>浏览器里用合成音频流替代麦克风走通了整条（原句 → 录 5 秒（远超原来的 2.4 秒间隔）→ 点按钮 → 回放整整 5 秒 → 下一句；第二句用 Enter 结束）、停止后 0 条活着的音轨、关掉开关后不再申请麦克风；**真麦克风、iPhone 上的音频路由（开麦后外放会不会被切到听筒）都未验** |
| 64 | **教材导入：整份 Transkript 按题导入、多轨拼接、说话人标记、按书分组（2026-09-26）**：新增 **FR-1.6**（`src/lesson/pdfText.ts` 的 `unwrapPdfText`：合回 PDF 硬换行、去页码/页眉/页边音轨号）、**FR-1.7**（`src/lesson/speakers.ts`：行首说话人标记从正文剥掉、记在 `Sentence.speaker`，清单存 `Lesson.speakers`）、**FR-1.8**（`BookImportPage` + `src/lesson/bookImport.ts`：一课 = 一个 Aufgabe；`src/audio/concat.ts` 把几轨拼成一个音频，`source.audioFiles` 记下文件清单）、**FR-1.9**（`Lesson.collection`，课程列表按它折叠分组）、**FR-3.6a**（`src/lesson/bindAudio.ts`：重绑音频只在「没有时间戳 / 换了音频」时才重对，整组一次补齐）；`createLesson` 顺手补记 `audioBytes`；`FilePicker` 支持多选；新路由 `#/import-book` | 用户在上德语课、买了 Aspekte neu C1，问「怎么把教材加进 app」。先做了一件事：**去看真实文稿的版式**（Klett 官网公开下载的 Lehrbuch Transkript），结果三处跟事先的猜测都不一样，方案因此整个换过：① 说话人不是 `Moderatorin:`，是行首符号 `●` `○` `△`；② **音轨号印在页边**，复制出来整堆挤在页尾（`1.8 1.9 1.10 1.11 1.12` 连着五行），跟正文的位置关系已经丢了 —— 所以「按 Track 切文稿」这条路根本走不通；③ 一个 Aufgabe 常跨好几轨（K1 Modul 2 Aufgabe 2a 是 Person 1~8 各一轨，文稿里下一题写着 `(Text wie Track 1.2-1.9)`）。于是**能可靠切出来的单位是 Aufgabe**（它有单独一行的标题），一课 = 一题，音频 = 那几轨按顺序拼起来；哪几轨归哪题由人在预览里对一眼（默认 1 轨，`Person 1~N` 的题默认 N 轨），这里不猜。课程模型、播放器、对齐因此一行都不用改。<br>**拼接两条路**：同规格 CBR mp3 按字节拼（去 ID3v2/ID3v1/Xing 头帧，**确定性**，换设备拼出来字节一样）；否则解码后拼成 44.1kHz 单声道 WAV（约 5MB/分钟）。VBR 不按字节拼是因为去了 Xing 头之后 `<audio>` 只能按码率估位置，seek 会偏，而跟读全靠 seek。Xing 头必须去：留着第一个文件那份，浏览器会把整段报成第一轨的时长。<br>**说话人标记由人确认，不由正则说了算**：`Das Problem: …` 与 `Moderatorin: …` 字面上一样。符号默认勾，名字出现 ≥2 次才默认勾、只出现一次的不列（在真实文稿里那一档全是正文冒号）。清单必须存进课程：FR-1.5 重切换一份清单，同一行切出来的文本就不同，旧句一句都认领不上。不给清单时切句结果**一个字都不变**（DW 与所有老课程）。<br>**顺手修掉一个真 bug（FR-3.6a）**：课程页「选本地音频」之后**无条件** `enqueueAlign()` —— 手动导入的课音频不同步，换台设备总要再选一次文件，于是桌面对好、同步过来的时间戳，在手机上重选一次同一个 mp3 就被送去服务器再算两分钟、原样盖回来。和变更 43 同一类，只是 DW 的课不走这条路所以一直没暴露。判据：字节数一样 = 同一份；不一样时比时长（WAV 兜底在不同浏览器上差几个采样，字节会变而时长不变）；两样都没记过才当作换过。而手动导入**以前根本不记 `audioBytes`**，这次一并补上。<br>**没做的**：不在应用里读 PDF（复制粘贴就够，多一个 PDF 解析库换不来什么）；不导练习题与课文正文（上课翻纸书，应用补的是听与复习）；`Person N` 这类小标题会切成一句，是否真的念要听了才知道，不念就用 FR-1.4 排除 |
| 65 | **听卡辨义题亮出目标词 + 题干跟题型走（2026-09-26）**：新增 **§12.19**；FR-10.2 / FR-10.9 补一条例外。`ReviewPage.tsx` 课程听卡的提示从写死的「听这一句，选出挖掉的那个词」改成按题型分：辨形题「听这一句，选出你听到的那个词」、辨义题「这句里有这个词，选出它的意思」；辨义题在播放键下方显示目标词（`entry.surface`，裸词形不带冠词，`GlossTarget`），孤立词发音那条路同样显示（没声音时已经亮过词形的不重复）。前端 +3 条（`ReviewPage.test.tsx`），三条都做过「还原旧写法就红」的反证 | 用户截图问「句子现在的选项不是词，而是词的解释，是设计如此吗」。选项是释义**是**设计（FR-10.9：进 Review 改考辨义），但查出两处真问题：① 提示是写死的一句，两种题型都显示它，而且两种都说错了 —— 句子音频是**整句播**的，没有挖掉任何词，「挖空」是读卡 `cloze` 的说法混了进来；到了辨义题它还在让人「选词」，这正是用户觉得不对的地方。② **辨义题从没说考的是句子里哪个词**，只能靠「四条释义各属于谁、谁在这句里」排除 —— 考的就不只是意思了。给了两个选项（只改提示 / 提示 + 亮出目标词），用户选了后者 |
| 66 | **Langsam gesprochene Nachrichten 能导入了；Sprachbar 换成 Top-Thema；来源页有了教材入口（2026-09-26）**：用户报「只有 Alltagsdeutsch 能导，后两个不行」+「发布的版本里没找到 Aspekte 的课」。实测三件事：① LGN 的期次是 **Article**（`a-<id>`）不是 Lesson —— 文稿在 `text`（每条新闻一个不带句号的 `<h2>` + `<p>`），一页挂两条 `Audio`（第一条慢速 = RSS enclosure，第二条正常语速直播版），没有词汇表；以前一律报「`__APOLLO_STATE__` 里没有 Lesson:…」。`parseLessonPage` 改成 Lesson 优先、退到 Article；音频按实体自己的 `audios` 引用顺序取；`manuscriptToText` 把 `<h1-6>` 当段落断开（Lesson 的 manuscript 实测没有 `<h*>`，存量课的 `manuscriptHash` 不变）；`fetchLesson` 只给裸 id 时 `l-` 页里找不到这一课（`LessonNotFoundError`）再按 `a-` 拉一次，网络错误不改拉；`parseLessonId` 认 `a-<id>`。② **Sprachbar 的 RSS 已下线**（`DKpodcast_sprachbar_de` 回 `Error: no feed by that name`），换成 `DKpodcast_topthemamitvokabeln_de`（Top-Thema，B1–B2，Lesson 结构，带词汇表，实测 17 条 Knowledge）。③ 「找不到 Aspekte」不是没部署（线上 bundle 里有 `import-book`），是**入口只在课程页**、他在来源页找 —— 来源页加「教材」一节与「导入整本教材」按钮，并说明应用里不带教材内容。浏览器里真拉了 LGN 79441515（41 句、标题各自成句、慢速音频 478 秒）与 Top-Thema 79412585；adapter +8 条测试 | |

---

## 1. 产品定义

**一句话**：把一份德语文稿 + 一段音频，变成可以逐句跟读、按自己的生词挖空听写、并长期复习听觉识别的训练材料。

**唯一用户**：本人。无账号、无多用户、无权限模型。

**成功标准**：完成第一篇 Alltagsdeutsch 的完整动线后，主观上愿意继续用第二篇。不设任何量化指标。

### 1.1 明确不做（非目标）

- 用户注册、账号、云端账户
- 内容托管、上传、分享（法律硬约束，见 §3.1）
- 自动强制对齐（V2）
- 自动挖空难度判定（V2）
- ~~词形还原 / 离线词典~~ —— **已移出非目标**，见 FR-16（变更 23）。仍不做的是**语境释义**：
  词典给的是脱离语境的通用释义，而「这个词在这一句里是什么意思」还得靠 DW 的 Glossar（FR-14）或人来写
- **CEFR 等级词表**（A1–C2）。不是不想做，是**做不成**：官方 Goethe Wortliste 只有 A1/A2/B1，
  B2/C1/C2 没有官方表；且那三份有版权，随 App 分发属于再发布（见 FR-17.2）。
  替代品是词频档，且界面上必须说清它不是 CEFR
- 中文 → 德语方向的卡片（见 §7.5）
- 连胜、徽章、学习时长统计等游戏化
- 云同步（V1 用手动导出/导入，见 FR-11）

---

## 2. 运行形态与设备角色

**形态**：Vite 构建的纯前端 PWA，无后端。本地 `localhost` 开发，构建产物可放任意静态托管（仅托管**代码**，不托管任何学习内容）。

### 2.1 设备角色

| 设备 | 角色 | 承担的步骤 |
|---|---|---|
| **桌面** | 制作工位 | 导入、切句修正、时间戳标注、跟读、挖空、听写 |
| **手机** | 复习工位 | SRS 复习；可选地跟读已标注的句子 |

分工的理由：标注和听写需要键盘和大屏；复习需要随手可及。工具在两端是**同一个 PWA**，只是使用的功能子集不同。

### 2.2 安装要求（必须写进 README）

- **iOS**：必须「添加到主屏幕」后使用。Safari 对普通网站的 storage 有 7 天无交互即清除的策略，已安装的 PWA 豁免。**不装到主屏幕 = 数据一定会丢。**
- 首次启动调用 `navigator.storage.persist()` 申请持久化配额，结果记录在设置页。
- 设置页展示 `navigator.storage.estimate()` 的用量/配额。

### 2.3 数据分层：缓存层 vs 标注层

**这是整个存储设计的地基。**

**分层标准（2026-09-02 改，§0 变更 27）：缓存层只装两样东西 —— 音频和原始文稿。其余一切都在标注层，备份且同步。**
那两样凭什么留在缓存层，不是「可重建」，而是**它们各有一个记在标注层里的下载地址**：
`Lesson.audioSrc`（mp3 直链）与 `Lesson.source.sourceUrl`（文稿页面）。地址在，另一台设备照着取回来就是了。

> 旧标准是「丢了能不能重建」。它错在漏了一个限定词 —— **在哪台设备上**。
> 词级时间戳在桌面能重算、在 iPhone 上压根不能（变更 26 就是被这一条绊到的：
> 「桌面预处理、手机学习」只兑现了一半）。「有没有地址」是个能当场回答的问题，
> 「能不能重建」得先问是谁在什么设备上重建。想往缓存层加字段，先答出它的地址。

| | **缓存层** | **标注层** |
|---|---|---|
| 内容 | 音频 Blob、原始 manuscript（HTML 与纯文本） | **其余全部**：时间戳（句级+词级）、切句边界、挖空、Glossar 候选词、生词、FSRS 状态、困难标记、排除标记、音频与文稿的下载地址 |
| 体积 | 大（一期 6–10 MB） | 小（一期几十 KB） |
| 丢了会怎样 | 照着地址重新下载，**无损** | **不可重建**，一年的功夫没了 |
| 跨设备 | **不跨端**，每台设备各自持有 | **必须同步** |
| 进导出包 | **永不** | 全部 |
| 可以主动删 | 可以，随时 | 不可以 |

由此得到三条硬规则：

- **R-缓存-1 下载过的不再下载。** 同一课程的音频与文稿，在同一设备上只获取一次，之后永远从 IndexedDB 读。
- **R-缓存-2 缓存不跨端。** 导出包里零字节音频。另一台设备要这份素材，自己照标注层里记着的地址去 DW 取（`Lesson.audioSrc` 是 mp3 直链，取不到时退回 lesson id 抓页面；§2.5 使这成为可能）。
- **R-缓存-3 缓存可弃。** 清空缓存对 DW 来源的课程是无损操作，标注层原封不动。

三条规则合起来的效果：**备份只需要保护那一小份标注层**，音频的存储压力和数据安全彻底解耦。词级时间戳（变更 26）与候选词（变更 27）搬进来之后，一课约几十 KB —— 一年 50 课仍在两三 MB 量级，而同步是按课分片的（`lesson:<id>` 一课一个文档），只有改过的那一课会推。

### 2.4 跨设备同步（V1 手动）

不做云同步。只有一个通道：导出 `backup-YYYY-MM-DD.json`（**标注层全部，缓存层零字节**），传到另一台设备导入。

对端拿到后，被引用但本机无缓存的课程显示为「素材未下载」，点一下照标注层记着的地址重新获取（音频用 `Lesson.audioSrc`，取不到再按 lesson id 抓页面；FR-3.5）。手动导入的课程无从重获取，则提示重新选择本地文件。

**合并规则**（导入时执行，不静默覆盖）：

- `Lesson`：按 `id` 匹配，`updatedAt` 较新者整体胜出。
- `VocabEntry`：按 `id` 匹配，比较 `fsrs.last_review`，较新者整条胜出（复习状态 last-write-wins）。
- `Settings`：**整体** last-write-wins，比 `updatedAt`（2026-09-02 新增，§0 变更 28）。不做逐字段合并 —— 那要给每个字段各配一个时间戳，把十来个布尔和数字变成一张带元数据的表，代价远超收益；设置一年动几次，整体覆盖最多丢掉「另一台设备上同时改的另一项」，看一眼就能改回来。
- **缓存层不参与合并**，各设备自管。
- 导入完成后展示摘要：新增 N 条 / 更新 M 条 / 跳过 K 条，并列出被覆盖的课程标题。

**约定的搬运方向**：标注永远 桌面 → 手机；复习状态回流 手机 → 桌面。避免两端同时复习产生分叉。

### 2.5 部署与 origin

**单一 origin，桌面和手机跑同一份部署，功能完全一致。**

因为 DW 的三类端点都允许跨域（附录 A.1），自动导入在浏览器里就能完成，不需要任何后端或本地进程。因此：

| 设备 | 访问地址 | 可用功能 |
|---|---|---|
| 桌面 | `https://d.gamestao.com/`（开发时 `http://localhost:5173`） | 全部 |
| 手机 | 同上，安装到主屏幕 | 全部，**含自动导入** |

**已确定的静态托管**：Cloudflare Workers static assets（`wrangler.jsonc`），GitHub Actions 在 main 分支 CI 通过后自动发布（`.github/workflows/deploy.yml`），部署方式参考同账号 funny 项目按本项目单一部署目标的体量做了精简。

这带来两个原本没预期的好处：

- **手机可以自己补齐素材**，不必搬运 mp3。这正是 §2.3 的 R-缓存-2「缓存不跨端」能够成立的前提 —— 如果浏览器不能直连 DW，缓存就必须跨端搬，那条规则也就写不出来。
- 部署面缩小到「一堆静态文件」，没有进程要启动、没有端口要记。

§2.4 的合并规则仍然需要 —— 两台设备各自导入、各自复习，标注层照样会分叉。

### 2.6 备份与同步

**首要需求是备份，不是同步。** 同步是备份做对之后的副产品。

一次澄清（推翻本节初稿）：本文档一度把备份和同步当成同一件事，说「每周手动搬一次，备份顺带就做了」。**这对同步成立，对备份不成立** —— 备份的失效模式恰恰是「你忘了」，而人会在认真做了八个月之后，恰好在设备损坏前的三周漏掉。依赖纪律的备份，总在最需要它时失效。

#### 2.6.1 威胁模型

| 威胁 | 说明 |
|---|---|
| 换手机 / 手机丢失损坏 | 主要动机 |
| 浏览器清掉 IndexedDB | 比换手机更可能，且两端都会发生。`persist()` 是请求，不是保证 |
| 误删、应用 bug 写坏数据 | 低概率但致命。**单份覆盖式备份会把坏数据一起覆盖过去** → 必须有版本历史 |
| 几年后工具本身跑不起来 | 时间够长就必然发生 → **备份格式必须能脱离本工具被读懂** |

后两条决定了方案形状：**要历史，不能只要最新副本；要纯文本，不能是不透明快照。**

#### 2.6.2 珍贵程度分级

| 数据 | 可重建性 | 一年体积 | 备份策略 |
|---|---|---|---|
| **`vocab`（生词 + FSRS 状态）** | **完全不可重建**。「该词记忆稳定度 47 天」无法从任何来源推回 | **~450 KB** | 每次复习会话结束即推 |
| `lessons`（时间戳、切句、挖空） | 可重建，但要重做一遍 | ~1.2 MB | 导入或标注变更后推 |
| 缓存层 | 免费重下 | ~700 MB | **永不备份** |

关键观察：**最珍贵的数据恰好是最小的**，三年也不过 1.4 MB。而它主要在**手机**上每天增长 —— 正是最可能被换掉的那台设备。

#### 2.6.3 现行方案：自建同步后端 + Google 登录

> **2026-09-02 换过一次运输方式。** 原方案是 GitHub 私有仓库（PAT + contents API），
> 逻辑全部实现、单测全绿，但从未用真实 PAT 联调过就被换掉了。换的理由与代价见 §0 变更 25，
> 被换掉的那套的实测数据留在附录 B。**下面这一段之外的一切都没变** ——
> §2.4 的合并规则、离线队列、状态常驻可见、手动导出，一字未改。

后端在自己的 VPS 上（`sync.gamestao.com`，Node + Hono + SQLite，代码在 `server/`，
部署步骤在 `deploy/README.md`）。登录用 Google：ID token 交给后端验签 + 查邮箱白名单，
换回一张 90 天的会话令牌。

「文档」是同步的最小单位，和分层（§2.3）一一对应：

```
vocab            ← 每次复习结束推       ~450 KB    完全不可重建
lesson:<id>      ← 导入/标注后推        ~25 KB     可重建但要重做
settings         ← 改过设置后推（5s 去抖）  <1 KB    重设一遍很烦，且没有任何理由不带走
（缓存层零字节）                                   免费重下
```

它同时满足威胁模型的四条：**异地**（不在任何一台设备上）、**自动**（没有要记住的动作）、
**有历史**（每个文档保留最近 30 版，可回滚）、**可脱离工具存活**
（服务器上就是一个 SQLite 文件，`docs` 表里存的是普通 JSON；再加上手动导出这条独立的路）。

- 每个文档一个单调递增的版本号，扮演原方案里 `sha` 的角色：PUT 带上你读到的版本，
  对不上就是 409，**并且响应里直接把远端现值带回来** —— 客户端一次往返就能跑完
  「合并 → 重推」，比 GitHub 那套「409 → 再 GET 一次 → 合并 → 再 PUT」少一趟。
- 合并规则仍在客户端（`src/backup/merge.ts`，§2.4）。服务器不理解业务语义，只管版本号 ——
  这样服务器坏了、被换了、被拆了，合并逻辑都不用重写一遍。

代价如实记录：

- **多了一台要维护的机器**。这正是 §2.6.6 当初否决自建方案的理由，现在它成立的前提是
  「那台机器本来就在跑别的东西」。它挂掉的后果是同步停摆（数据仍在每台设备的 IndexedDB 里），
  不是数据丢失 —— 但「同步静默停摆」和「备份静默失败」是同一种事故，所以 FR-11.9 更要认真。
- **引入新的单点：那台机器 + 那个 Google 账号**。因此手动导出（FR-11.11）**必须保留**，
  作为第二份、不同故障域的保险。
- **正文离开了设备**。它落在一台需要认证、只有白名单邮箱能进的私有服务器上 ——
  性质与放进自己的网盘相同，见 §2.6.7。这一条与 §3.1.1 R-1 的关系在那一节里单独交代。

#### 2.6.4 同步是副产品

备份做对之后，同步几乎不用额外做 —— 两台设备读写同一批文档即可。而且这不是真正的双向同步，因为两台设备**写的根本不是同一批字段**。

| | 桌面写什么 | 频率 |
|---|---|---|
| 桌面 | 新增 `Lesson`、新增 `VocabEntry` | 每周 |
| 手机 | 更新已有 `VocabEntry` 的 `fsrs` | 每天 |

桌面**新增**，手机**更新存量**。只要遵守 §2.4 的约定（复习只在手机做），这就是一个 **append-only 的交接**，不是真正的双向同步 —— 没有 CRDT、没有向量时钟、没有三路合并的必要。§2.4 那条 `fsrs.last_review` 比较的规则足够了，它唯一要防的就是桌面把手机的复习状态覆盖掉。

#### 2.6.5 恢复演练（不是可选项）

**没演练过的恢复不算备份。** 只写「能导出」而从不验证「能装回去」，等于把一个未经测试的代码路径押上几年的积累。

- 验收阶段必须在第二台设备（或清空本机数据后）**完整恢复一次**，见 §10。
- 恢复要能从零开始：空浏览器 → Google 登录 → 拉取 → 标注层齐全 → 课程显示「素材未下载」→ 补齐 → 复习照常。
- 每年提示一次重新演练。工具在变，恢复路径会悄悄坏掉。

#### 2.6.6 已否决的方案

| 方案 | 否决理由 |
|---|---|
| ~~自建 VPS / 小型服务~~ **（2026-09-02 改判为现行方案）** | 原否决理由是「为每周 400 KB 维护一台要打补丁的机器；且自己写的服务没有版本历史，防不住『写坏 + 覆盖』」。两条都不再成立：机器是**已经在跑别的东西**的那台，边际维护成本接近零；版本历史自己做了（每文档 30 版的 `revisions` 表）。真正推动改判的是 PAT 那条路的三个代价，见 §0 变更 25 |
| Firebase / Supabase | 功能远超需求，引入账号体系与厂商绑定 |
| PouchDB + CouchDB | 离线同步的经典答案，但要用它重写整个数据层，代价与收益严重不匹配 |
| 云盘文件夹 + File System Access API | 桌面上很优雅，但该 API 在 iOS Safari 上**不存在** —— 恰好是最需要自动备份的那台设备用不了 |
| 仅靠手动导出 | **本节初稿的方案，已推翻**：依赖纪律，而备份的失效模式正是「你忘了」。保留为第二道保险，不作为主方案 |
| WebRTC / 局域网直传 | 仍需信令服务器，要求两端同时在线，且不产生异地副本 —— 对备份毫无用处 |
| **GitHub OAuth 登录** | **纯前端不可行**（实测见附录 B.1）：授权码流程需要 `client_secret`，GitHub 不支持 PKCE，而 Device Flow 的两个端点在 `github.com` 上、预检 404 无 CORS，浏览器发不出请求。**注**：有了自建后端之后这条限制本身已经不成立了（后端可以持有 secret），但那时已经不需要 GitHub 了 |
| OAuth + 自建 token 交换代理（Cloudflare Worker 等） | 技术上可行，且**不违反 §3.1.1 R-1**（只交换 token，不碰内容通路）。当时否决的是工程理由：换来的仅是省掉一次性三分钟操作，代价是永久的部署依赖。**2026-09-02 回看，这个判断是对的但结论换了**：既然最终还是接受了「永久的部署依赖」，那就不该只拿它换一次登录 —— 现行方案里那台机器同时是存储、是并发仲裁、是版本历史，换来的是整条链路不再受制于别人家的 API |

#### 2.6.7 法律补充

备份载荷含 `Sentence.text` 与 `contextSentence`，即受版权的正文。放进**私有且需认证**的存储（自己那台只认白名单邮箱的服务器、你自己的网盘）属于私人存档，与放进 Dropbox 无异，**不构成 §3.1 所说的「向公众提供」**。

但这条豁免完全依赖「私有」二字，换成自建后端之后它落在三个具体的开关上，缺一条豁免就没了：

1. **`ALLOWED_EMAILS` 不得为空、不得放进不该有的人**。服务器在白名单为空时**拒绝启动**，就是为了让这条不可能被忘掉。
2. **不得有任何免鉴权的读取路径**。`/v1/healthz` 之外的每个接口都过会话令牌，且令牌只带 `sub`、邮箱每次回库里查 —— 从白名单里删掉一个邮箱，他手上没过期的令牌下一次请求就失效。
3. **不得转成多人共享的服务**。数据库按 `user_id` 分区只是工程上的整洁，不是可以邀请别人来用的许可。

若真要公开分享，走的是 §3.1 的 `ShareablePackage`（不含正文），不是这个备份文件。

---

## 3. 核心约束

### 3.1 法律：BYOC（Bring Your Own Content）

**原则：工具不托管、不传输、不经手任何版权内容。**

- 用户自行从 DW 下载 mp3、自行复制 Manuskript，文件只存在本地设备 → 属于私人复制，合法。
- 教材音频（Aspekte neu 等）同理。
- **不做任何分享功能。** 一旦托管用户上传内容并向公众提供，即落入 **UrhDaG**（欧盟版权指令第 17 条的德国落实法），平台承担**直接责任**，"收到通知才删"的避风港抗辩不成立。教科书出版社（Klett、Cornelsen）是该领域最积极的维权方。
- 若将来要做分享：**只分享标注层** —— 内容标识（`sourceUrl` 或 "Alltagsdeutsch, 2025-11-03"）+ 时间戳数组 + lemma 列表 + 挖空位置。**绝不含正文或例句原文**，带了就是分发原作片段。思路等同字幕轴文件。

**架构落实**（V1 必做）：

```
ShareablePackage = f(Lesson)   // 纯函数，白名单式构造
```

- 白名单构造，不是黑名单剔除。只显式挑选允许的字段。
- 配单元测试：把导出结果 `JSON.stringify` 后，断言**不包含**该课任何一句正文的任意 8 词以上连续片段，且不含 `text` / `contextSentence` / `surface` 字段名。
- 全量备份 `backup-*.json` **含正文**，文件顶层写 `"_warning": "Contains copyrighted text. Local backup only. Do not share."`

#### 3.1.1 自动获取内容的边界（FR-13 / §7.8）

自动从 DW 抓取音频和文稿**不违反上面的原则**，因为 BYOC 管的是「谁在托管、谁在向公众提供」，不是「谁点的下载按钮」。私人复制（§ 53 UrhG）不区分是手点的还是脚本点的。据此把红线精确化：

| 规则 | 说明 |
|---|---|
| **R-1 请求只能从用户设备发出** | 实现上就是浏览器里的一次 `fetch()`：用户自己的浏览器、自己的 IP、直连 DW，中间没有任何第三方。**代码库中不得出现任何我们运营的中转/代理服务，也不得引入公共 CORS 代理** —— 一旦有服务器代抓，就变成我们在复制和传输，性质立刻不同。这是本节唯一的硬红线。**（2026-09-02 修订）** 它曾经是**免费满足**的 —— 那时这个项目没有后端可写，也就没有红线可越。现在有了一台自己的服务器（§2.6.3），这条豁免不再免费，改由下面那段专门交代。 |

**同步后端与 R-1 的关系**（2026-09-02 补充）：R-1 管的是**内容获取通路** —— 谁向 DW 发出那次请求、谁在复制和传输受版权的素材。同步后端**不在这条通路上**：抓 DW 的 `fetch()` 仍然只从用户设备发出，直连、无代理，服务器不知道 DW 的存在，也从不代任何人去取任何东西。它在通路的**下游**，存的是用户自己设备上已经存在的那份私人复制（连同标注），且只对白名单里的本人开放（§2.6.7）。判断标准可以写成一句话：**R-1 禁的是「服务器替你去拿内容」，不是「你把自己的东西放到自己的服务器上」**。若哪天这台服务器开始代抓 DW、或者开始向第三人提供这些数据，那才是越线 —— 前者是 R-1，后者是 §3.1。

**2026-09-03 补充（§0 变更 35）：那台服务器现在还会收到音频。** 对齐的 emissions 那一半挪了上去（FR-15.17），
于是每对一课就有一个 mp3 上行。按上面那句判断标准，这**不越 R-1**：请求仍然只从用户设备发向 DW，
服务器仍然不知道 DW 的存在、从不代任何人去取任何东西；它拿到的是**用户自己设备上已经存在的那份私人复制**，
和它已经在存的标注层是同一个性质（§2.6.7）。变的是「经手」的范围，所以下面那段关于权重的话要跟着改。
两条界线一寸没动：① 服务器绝不代抓 DW；② 这些数据绝不向第三人提供。
另外**文稿仍然不上去** —— 不是「暂时没做」，是那道缝（FR-15.13）让它没有理由上去：CTC 前向只吃波形。

**FR-15 的模型权重与 R-1 的关系**（实现期补充）：自动打点要一份约 200MB 的声学模型权重。它**不是学习内容**，所以不受 R-1 约束 —— R-1 管的是「谁在复制和传输受版权的素材」。~~判断依据是：音频与文稿始终留在设备上，对齐全程在本机的 WASM/WebGPU 里跑，**没有任何一段音频或文稿离开过设备**。~~ **2026-09-03 改（变更 35）：后半句不再成立** —— 走服务器那条路时音频会上行到自己的那台服务器（FR-15.17）。改正后的判断依据是：**文稿从不离开设备；音频只可能到达自己的、白名单限定的那台服务器，且它不落盘、跑完即从内存里放掉**。权重这一段的结论不受影响（它本来就与内容通路无关）。权重的来路有两条：打包版随包带（`npm run stage:align` 放进 `public/models/`），纯 web 版首次使用时从 HF CDN 取一次，之后由 transformers.js 存进 Cache API。两条都只是取一份公开的二进制，与内容通路无关。

**FR-9.11/9.12 的 AI 补充解释与 R-1 的关系**（2026-09-22 补充，变更 48）：查词与生词本的「问 AI」从「用户自己复制去外面问」改成服务器转发一次 Anthropic API 调用之后，第一反应是「这不就是当年否决 DWDS 的那种自建代理吗」——**不是**，区别在于代理的到底是什么。R-1 管的是**受版权的第三方素材**（DW 的文稿音频、DWDS/Wiktionary 的词典数据）经手这台服务器、把「用户自己去拿」变成「服务器代为复制和传输」。而 AI 补充解释请求体里只有**用户自己敲的词或句**，回来的是**模型生成的解释**——不是任何人的数据库记录、不是任何第三方网站的内容，这台服务器也没有从任何第三方"拿"什么再转手给用户。判断标准与「同步后端与 R-1 的关系」那段是同一条：**R-1 禁的是"服务器替你去拿别人的内容"，不是"服务器帮你转发一次计算"**。真正类比对象不是 DWDS 代理，而是对齐那条已经在用的路（FR-15.17）——两者都是"用户的输入上行、服务器算一次、结果下行"，从未涉及第三方版权内容经手。
| **R-2 不绕过任何付费墙或技术保护措施** | § 95a UrhG。DW 无 DRM，本来也不需要绕；但这条约束对将来任何新来源都适用。 |
| **R-3 抓取要礼貌** | 串行请求、请求间隔 ≥ 1s、不做批量预抓。每周 1 次本来也构不成负载，但代码里不能写出可以被误用成批量抓取的形状（比如「一键导入全部 100 期」）。浏览器里无法自定义 User-Agent，这条不适用。 |
| **R-4 抓下来的内容性质不变** | 仍然受版权、仍然只存本地、仍然永不进 `ShareablePackage`。自动获取只是省掉手工步骤，不改变内容的法律地位。 |
| **R-5 代码可开源，内容零份** | 抓取器作为工具本身没有问题（性质类似 yt-dlp）。但仓库、构建产物、示例数据中必须**不含任何一句 Manuskript 或一个音频文件**，测试固件也不行（用自造的德语句子）。 |

关于使用条款：抓取可能触碰 DW 的 Nutzungsbedingungen，那属于合同层面而非版权层面。DW 是公共资金支持的广播机构，语言学习材料本就是免费提供给学习者的，这大概是抓取里风险最低的一类。但「风险低」不等于「获得授权」，所以 R-4 的本地留存约束不因此放松。（以上是工程判断，不是法律意见。）

### 3.2 平台约束（会直接影响实现）

| 约束 | 后果 |
|---|---|
| iOS 需要用户手势才能开始播放 | **全程复用同一个 `<audio>` 元素**，不要每句 `new Audio()`。首次点击后的 seek + play 被视为同一手势链，允许。 |
| iOS 静音开关会静音 `<audio>` | README 提示；或用 Web Audio API 的 `AudioContext`（不受静音开关影响）播放。V1 先用 `<audio>`，遇到问题再换。 |
| 手机锁屏/切后台会暂停跟读循环 | 跟读在手机上是次要功能，不做后台播放。`visibilitychange` 时暂停并保留位置。 |
| `playbackRate` 在移动 Safari 支持但过低会失真 | 变速范围限定 **0.7–1.2**（与原规格一致）。 |
| IndexedDB 配额与驱逐 | 见 FR-11。 |

### 3.3 能力依赖链（重要）

一期 Alltagsdeutsch 约 8–10 分钟、80–120 句。打点是**稀疏且按需**的——一期里真正需要反复听的可能就五六句。因此：

```
标注 startTime ──→ 可跟读
               └──→ 可挖空 ──→ 可听写 ──→ 生词入库 ──→ 带音频的 SRS 卡

未标注句子     ──→ 只能阅读、只能标记生词（生成【无音频卡】）
```

**规则**：

- **R1**：挖空操作只允许在**有时间戳**的句子上进行。没有时间戳的句子点词时，提示原因并提供一键「自动对齐这一课」（FR-15；手工打点界面已删除，见 §0 变更 20）。
- **R2**：`VocabEntry` 允许来自未标注句子（阅读时直接标记生词），此时卡片正面无音频，标记为「无音频卡」，在复习界面显式提示，并提供「去这一课重新对齐」入口。
- **R3**：UI 任何位置都不得出现「点了重播却没有声音」的死角。播放按钮在无时间戳时为禁用态 + tooltip 说明原因。

---

## 4. 用户动线

```
① 导入   粘贴 Manuskript + 选择本地 mp3
    ↓
② 切句   自动切分 → 肉眼扫一遍 → 合并/拆分误切 → 排除 Glossar 等非朗读段落
    ↓
③ 通听   完整播放一遍，不看文本（文本区域可折叠）
    ↓
④ 对齐   下载完课程时已经自动跑过（FR-15）；这一步只是看一眼低置信句
    ↓
⑤ 学词   阅读文本，点击生词 → 标记 → 自动挖空 + 入生词本草稿
    ↓
⑥ 跟读   逐句循环 + 静默间隔 + 变速；跟不上的句子按 D 标记为 markedDifficult
    ↓
⑦ 听写   对已挖空的句子作答，实时校验；答错自动入生词本
    ↓
⑧ 复习   SRS 队列（手机）：听句子音频 + 看挖空句 → 回忆词与释义
```

②③④⑤ 顺序可打乱；⑥⑦ 依赖 ④；⑧ 依赖 ⑤ 或 ⑦。

---

## 5. 功能需求

### FR-1 课程导入

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-1.1 | 新建课程，填写标题（必填）、`sourceUrl`（选填） | 标题为空时不能保存 |
| FR-1.2 | 粘贴 Manuskript 纯文本到 textarea | 支持 20000 字符以上不卡顿 |
| FR-1.3 | 选择本地音频文件（`accept="audio/*"`），读取时长 | 显示文件名与时长；未选音频也允许保存课程（可后补） |
| FR-1.4 | **段落排除**：切句后，每句可标记为「非朗读内容」并从正文中排除 | 提供「批量排除文末 N 句」与「开头 N 句 → 排除 / 恢复」两个快捷操作（前者对付 Glossar，后者对付历史上被 FR-13.7 自动排掉的标题+导语，见 §0 变更 24）；**两个 N 的默认值都是 0，为 0 时按钮禁用**（§0 变更 32）；被排除的句子**在界面编号里被跳过**、不出现在跟读/听写/复习 |
| FR-1.4a | **（实现期修订）** 原文写「不参与索引编号」。实现改为：`Sentence.index` 一律等于**数组下标**，排除**不**重排号；界面上的连续编号由 `displayNumbers()` 另算 | `VocabEntry.sentenceIndex` 与 `GlossaryCandidate.sentenceIndex` 都指向 `Sentence.index`。若排除也重排，勾一下「非朗读内容」就会让后面所有生词的出处静默错位。用户看到的效果与原文一致（排除句不占号），引用则不动。合并/拆分**仍然**重排 index（FR-2.3 明说要重排），故这两个操作额外返回 `indexMap`，调用方必须拿它同步 `VocabEntry.sentenceIndex` |
| FR-1.5 | 导入后可随时重新编辑原文并重新切句 | 重新切句时，**保留已有 startTime/blanks 的句子**（按文本内容匹配），无法匹配的句子列出来让用户确认丢弃。跟着搬的还有词级时间戳与 Glossar 候选词 —— 认领规则保证被认领的那一句文本一致，所以句内 offset 不用动；认领不到的丢掉（留着就是静默错位）|
| FR-1.6 | **（2026-09-26，§0 变更 64）整理 PDF 断行**：导入页一个按钮（整本导入时自动）把硬换行合回段落；行尾连字符 + 小写开头去掉连字符、+ 大写开头保留；`Ein- und` 这种省略式保留并补空格；页码、`Transkript zum Lehrbuch`、页边音轨号 `1.12` 整行丢掉；标题行、`Person 3`、括号占位自成一行；说话人符号开新段；`Name:` 只在上一段已经句末收尾时才开新段 | 规则一律站在「多断一行」那边：多断的代价是切句页多点一次合并，少断则是把标题拼进正文、被当成要对齐的字。`Name:` 那条限制来自真实文稿：`… sehr wissen-` / `schaftlich: Die Pilze …` 这种折行不加限制会被劈成两段 |
| FR-1.7 | **说话人标记**：行首的 `●` `○` `△` 或 `Name:` 从句子正文里剥掉，挂在那段话**第一句**的 `Sentence.speaker` 上，界面上在句首单独标出；哪些算标记由人在导入页勾选，清单存 `Lesson.speakers`，FR-1.5 重切沿用同一份 | 标记不念：留在正文里会被当成要对齐的字，也会出现在挖空题与复习卡的原句里。不给清单 = 不剥，老课程切句结果不变。已知的小瑕疵：把两段不同人的话「与下一句合并」时，合并句从原文切出来，会把第二个人的标记带进正文 —— 跨说话人合并本来就少见，不为它改 offset 契约 |
| FR-1.8 | **整本导入**（`#/import-book`）：粘整份 Transkript → 按题目标题（`Modul 4 Aufgabe 2c`、`Auftakt Aufgabe 1`、`Track 1.05`）切段，每段记所在章 → 按章勾要导的题 → 一次选这几题的全部音频，按文件名自然顺序依次分给勾上的题，每题「用几轨」可调 → 导入。一题多轨时拼成一个音频（`audio/concat.ts`），文件清单存 `source.audioFiles`。课程标题 `Kapitel N · <题目标题>` | 音轨号在复制出来的文字里已经没有位置信息（见变更 64），所以不能按音轨切；题目标题是正文的一部分，位置可靠。同一组里已有同名课时在那一行上提示，免得同一题导两遍（同步之后是两门课、两套标注）。对齐是串行排队的，一次导十课不会同时跑十个 |
| FR-1.9 | **按书分组**：`Lesson.collection`（自由文本，导入时填，输入框给已有组名做候选）。课程列表里没分组的课照旧平铺在最上面，每组一个默认折叠的块，组内按标题自然排序；组头显示课数、已对齐数、缺音频数，缺音频时给「一次选齐音频」 | 折叠：一本书四五十课展开着会把每周那篇 DW 挤到第二屏以下。展开状态记在本机 localStorage（个人习惯，不同步）。自然排序而不按导入时间：教材的顺序写在标题里，先导第 5 章再导第 1 章不该让列表倒过来 |

### FR-2 句子切分

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-2.1 | 用 `Intl.Segmenter('de', {granularity:'sentence'})` 初切 | — |
| FR-2.2 | 应用后处理合并规则（见 §7.1） | `z. B.` / `u. a.` / `am 3. Oktober` / `im 19. Jahrhundert` / `Dr. Müller` 均不被误断 |
| FR-2.3 | **手工修正**：在句子列表中「与下一句合并」/「在光标处拆分」 | 每句都有这两个操作；操作后重排 index，已有 startTime 跟随 |
| FR-2.4 | 每句保存其在原文中的 `charStart`/`charEnd` | 用于 FR-1.5 的重切匹配 |

### FR-3 素材缓存（实现 §2.3 的三条缓存规则）

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-3.1 | 缓存单元 = `LessonCache`，一课一条，存音频 Blob + 原始 manuscript HTML + Glossar 元数据，key 为 `lessonId` | 关掉浏览器再打开，无需任何网络请求即可播放和学习 |
| FR-3.2 | **下载前先查缓存**（R-缓存-1）。命中则直接用，不发任何请求 | 同一课程重复进入，Network 面板零请求 |
| FR-3.3 | 播放用 `URL.createObjectURL`，组件卸载时 `revokeObjectURL` | 不泄漏 |
| FR-3.4 | **缓存缺失态**：标注层有这一课，本机无缓存 | 明确显示「素材未下载」；播放控件禁用并说明原因（§3.3 R3）；给出补齐入口 |
| FR-3.5 | **补齐（rehydrate）**：DW 来源的课程，凭 `source.dwLessonId` 重新拉取音频与 manuscript。音频优先用页面上抓到的直链，页面里没有时退到标注层记着的 `Lesson.audioSrc`；抓到的新直链顺手写回去 | 手机导入备份后，一键补齐即可复习带音频的卡片。**两个地址都留着**是因为它们坏的方式不同：CDN 直链会变（所以以页面为准），而页面也可能改版或下架（所以记着的那个是最后一根绳）|
| FR-3.5a | **（2026-09-03 新增，§0 变更 34）** DW 来源的课程**打开即自动补齐**，不必先绕去「来源」页；下载中横幅显示「正在补齐素材…」，失败留在原地给出重试；文稿 hash 变了（FR-3.7）时把人送去「来源」页做那个决定 | 同步落地之后这条路是常态而非例外：桌面导入 → 手机上课程自己出现（FR-11.19）→ 缺的只有音频和原文这两样按设计不同步的东西。让人读一句「去『来源』页补齐」再自己找过去，是把一个纯机械的步骤留给人做 —— 而不做这一步的人会去重新导入，那会建出第二门课。**同一课在本次会话里只自动试一次**（失败不重试，否则切一次 tab 就是一次 6~10MB）|
| FR-3.5b | **（2026-09-21 新增，§0 变更 43）** 补齐之后「要不要重新对齐」的判据是 **`Lesson.audioBytes`**：下回来的音频字节数与标注层记着的不一致才算 DW 换过音频。老课程没有这个数时退回比时长，但比的是**这次解码出来的**时长（与对齐写回 `audioDuration` 的那个同源），容差 2s。补齐**不覆盖**已有的 `audioDuration`，除非音频确实变了 | 判错的代价不对称：判成「没变」最多时间戳偏一点，判成「变了」是在手机上白跑一次对齐（一课两分钟，走服务器）。旧判据拿 DW 页面上报的**整数秒**去比对齐写回的**解码值**，于是每一门「桌面对齐过、手机上补齐素材」的课都可能中招 —— 那正是「电脑上对齐过了，手机上还要再对一次」的第二个成因 |
| FR-3.6 | 手动来源的课程无法自动补齐 → 提示重新选择本地文件，并按 `audioDuration` 校验 | 时长差 > 0.5s 时警告「时长不匹配，时间戳可能失效」，允许用户坚持 |
| FR-3.6a | **（2026-09-26，§0 变更 64）重绑音频后要不要重对齐**：只有「一句时间戳都没有」或「换了一份音频」才重对。换没换的判据：字节数与 `Lesson.audioBytes` 一样 = 同一份；不一样时看时长是否在 0.5 秒容差内；两样都没记过才当作换过。拼过的课要一次选齐 `source.audioFiles` 里的全部文件（按清单顺序拼，缺哪个报哪个、不将就）；课程列表的组头可以一次选一整批文件，每课按自己记着的文件名认领 | 手动导入的课音频不同步，换设备总要再选一次文件 —— 以前那一步**无条件**重对，桌面算好、同步来的时间戳在手机上被白算一遍盖回来。比时长这一档是给 WAV 兜底留的：不同浏览器的解码器差几个采样，字节数跟着变，但那不是换了音频 |
| FR-3.7 | 补齐后校验文稿一致性：比对纯文本的 hash | 不一致说明 DW 改过稿，**时间戳与挖空 offset 可能全部失效** —— 必须显式警告，让用户选择「按新文稿重切」或「保留旧标注自行核对」，不能静默接受 |
| FR-3.8 | 缓存管理页：按课列出占用空间、单课清除、一键清除全部 | 与 `storage.estimate()` 用量一起显示 |
| FR-3.9 | 清除前区分**可补齐**（DW 来源，无损）与**不可补齐**（手动导入，音频要你自己再找回来） | 两者用不同颜色和不同确认文案；后者需二次确认 |

### FR-4 时间戳（手工标注部分已删除）

**FR-4.1 / 4.2 / 4.3 / 4.5 / 4.6 作废（2026-09-01，§0 变更 20）。** 时间戳只有一个来源：FR-15 的自动对齐，下载完课程立刻跑。手工打点界面（`Enter` 打点、±0.1s 微调、清除时间戳）连同「标注」tab 一起删掉了 —— 自动对齐把要动手的句子从「每期 40 句」降到「偶尔核对一两句」之后，那一整套界面还留在必经动线上，就只是在提醒你做一件不必再做的事。

**它的代价，写在这里以免以后忘了**：某一句真的对偏了，现在只能整课重跑一次（可能还是偏），或者在桌面上重跑。如果实测发现有一类句子稳定对不上、重跑也救不回来，那就该把这一节恢复成「只针对单句的微调」——不必把整套顺序打点界面找回来。

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-4.4 | **保留。** `endTime` 规则：显式标记优先；未显式标记时取**下一个有 startTime 的句子**的 startTime；若无后续标注则取音频总时长 | 自动对齐给出的终点是显式的，所以推断分支只在老数据与部分覆盖的课程上生效 |

### FR-5 通听

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-5.1 | 全篇连续播放，文本区域默认**折叠** | 一键展开/折叠 |
| FR-5.2 | 播放中，有时间戳的句子随播放进度高亮 | 没有时间戳的句子不高亮（不做假装的伪同步） |
| FR-5.3 | **（2026-09-02 新增，§0 变更 24）逐词高亮**：有词级时间戳时，当前句里正在读的那个词单独高亮；当前行自动滚到**文本框高度的三分之一处**（下方留出还没读到的几句）；**点任意一个词从那里开始播** | 句间空档（台标音乐、换播音员）里保留「刚读完」那一行的标记，不让高亮闪掉。**（2026-09-15 修正）跟随只有按钮能关**：手动滚动（滚轮 / 触摸）只是暂停跟随 5 秒，停手后自己回到当前句。旧行为是滚一下就把「跟随播放」永久切成「不跟随」，而展开文本后滚一下几乎必然发生 —— 于是默认值写着「跟随」，实际看到的永远是「不跟随」，文本从此一动不动，看起来像是自动滚动根本没做。数字这类罗马化时被丢掉的词，仅在**前后两侧都有真时间戳**时用中间那段空档补上；一句词级时间戳全缺时退回句级高亮，绝不按字数估。**（2026-09-23 修正，§0 变更 58）当前词是反白，不是加粗**：当前词永远在当前行上，而当前行本身已经是浅色底 —— 两者同色时「当前词」只剩一档字重可看，在 iPhone 的字体渲染下等于没有高亮。形状见 §12.17 |

### FR-6 跟读模式（最高频界面，优先级最高）

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-6.1 | 循环单元：`播放 [start,end] → 静默 gap → （重复 N 次）→ 手动或自动进入下一句` | gap = 句子时长 × `shadowingGapRatio`（默认 1.2） |
| FR-6.2 | 每句重复次数可配置（默认 1；2026-09-26 前是 2，有了 FR-6.8 之后一遍就是完整的一轮），或设为「无限，手动推进」 | 默认值只影响从没改过这一项的设备；已经存过 2 的不会被改 |
| FR-6.3 | 变速 0.7 / 0.85 / 1.0 / 1.2 四档 | 切换即时生效，不重启当前句 |
| FR-6.4 | 只在**有时间戳**的句子间循环，跳过没有的 | 课程页头部显示「已对齐 N / M 句」与低置信句数 |
| FR-6.5 | 键盘快捷键 | `Space` 重播当前句 · `→` 下一句 · `←` 上一句 · `D` 切换 `markedDifficult` · `+`/`-` 变速 |
| FR-6.6 | 静默间隔期间有可见倒计时（进度条或环） | 让人知道该开口了 |
| FR-6.7 | 可切换「只练标记为困难的句子」 | — |
| FR-6.8 | **每一遍都录音并回放**：播放原句 → 自动开始录音（**没有倒计时**），跟读完点「读完了」按钮或按 `Enter` → 把录下的这一遍放出来 → 放完再进入下一遍/下一句。开关 `shadowingEcho`（默认开）；录音有保底上限 max(20 秒, 句长 × 5)，到了当作读完；设备不能录（iOS 壳 < 0.7.0、Android、没麦克风、没授权）或麦克风还没打开时退回 FR-6.1 的固定间隔 | 录音中那个按钮是红色、带呼吸动画的整行按钮，取代倒计时条；录音只在内存里，不落库、不同步；麦克风只在跟读进行中打开；回放时显示「回放你刚才的跟读」 |

### FR-7 生词标记与挖空

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-7.1 | 句子渲染为可点击的 token（按空白与标点切分，保留原始 offset） | 点击任一 token 高亮 |
| FR-7.2 | 支持**多 token 连选**（拖选或 shift+点击）标记搭配 | 能标记 `hing ... ab` 这类不连续片段：一个 Blank 允许含多个 `[charStart, charEnd]` 区间 |
| FR-7.3 | 标记后：在该句创建 `Blank` + 创建 `VocabEntry` 草稿（`meaning` 待填） | 挖空在听写模式下显示为 `____` |
| FR-7.4 | **先由内置词典自动填** `lemma` / `gender` / `plural` / `meaning` / `ipa`（FR-16.4），填不上再手填 | 名词未填 `gender` 时在生词本列表标黄提示（德语名词不带性等于没记）。**DW 的 Glossar（FR-14.3）优先级更高**，词典只补它没给的字段 —— Glossar 给的是语境内的释义，词典给的是通用释义，用后者覆盖前者是把更好的那份换掉 |
| FR-7.5 | 取消挖空 → 询问是否同时删除生词条目 | — |
| FR-7.6 | 未标注时间戳的句子上点词 → 按 §3.3 R1 提示 | — |

### FR-8 听写模式

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-8.1 | 队列 = 本篇所有含 Blank 且有时间戳的句子 | — |
| FR-8.2 | 展示挖空后的句子 + 每个空一个输入框；`Space`（非输入焦点时）或按钮重播本句 | — |
| FR-8.3 | 校验分级（见 §7.4）：**正确 / 转写等价 / 仅大小写错 / 错误** | 每级有明确的视觉区分与文案 |
| FR-8.4 | 错误时展示字符级 diff（正确答案与输入的差异高亮） | — |
| FR-8.5 | 「我不会」按钮：直接判错并显示答案 | — |
| FR-8.6 | 结果回写：错误或「我不会」→ 对应 `VocabEntry` 记 FSRS `Again`；仅大小写错 → `Hard`；正确 → `Good` | — |
| FR-8.7 | 德语输入辅助：ä ö ü ß 快捷按钮（手机端必需） | — |

### FR-9 生词本

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-9.1 | 列表视图：surface / 释义 / 出处课程 / 原句 / 下次复习时间 | 支持按课程、按 FSRS 状态筛选 |
| FR-9.2 | 每条可编辑、可删除、可「暂停复习」 | — |
| FR-9.3 | 去重：新建时按**词元键**全库匹配，命中则提示「已有该词条（来自《XX》）」，给三个选项：合并到已有条目 / 仍然新建 / 取消 | **原文那条已知局限已被 FR-16 修掉**：原写「V1 无 lemma，只能靠 surface 匹配；`gelaufen` 与 `laufen` 匹配不上是已知且接受的局限」—— 内置词典带了 30 万条词形→词元索引，`gelaufen` 与 `laufen` 现在归到同一个键。两条路并用而不是替换：老词条没有 lemma、键退回 surface（即原行为），而 surface 那条路还能抓到词典里查不到的词（生僻复合词、多词搭配） |
| FR-9.4 | 条目粒度鼓励用**搭配/整句**：若选中的是单个常见词，轻提示「考虑连搭配一起标记」 | 不强制。`sich einer Sache bewusst sein` 比 `bewusst` 有用得多 |
| FR-9.5 | 生词本页顶部有一块**查词**：输入一个词 → 内置词典（离线）与 de.wiktionary（例句 / 同义词 / 反义词 / 词源 / 变形）合成**一张卡**，并可直接加进生词本 | 起因是课堂：他的德语课上的生词**不来自这里的任何一课**，而在此之前内置词典只在幕后跑（标记生词时 prefill、复习卡背），没有任何地方能主动查一个词。这一条要的是**详细**（原话：「谷歌翻译的解释很多时候不太详细」），不是「能查到」—— 所以在线那一趟**每次都问**，不是只在内置词典查不到时才问（FR-16.5 那条仍然管标记生词那条路）。形状见 §12.11 |
| FR-9.6 | 查词走**词形还原**，并把词头说出来（查 `abgewogen` → 词头 `abwägen`）；**在线那一趟拿词头去问**，不拿用户输入的词形 | `abgewogen` 自己那一页只有一个 `Partizip II` 小节，例句、同义词、变形全在 `abwägen` 那一页上 —— 不换词头，「详细」恰好在最需要它的那种词上少掉一半。代价是在线那一趟要等本地查完才开始（几十毫秒） |
| FR-9.7 | 两个来源**合成一个答案**，但如实标出这一份来自哪边；义项整份取内置那边，只有**记录级**的东西（例句 / 同义词 / 反义词 / 词源）才合并，逐义项的 IPA 与变形**按词性**对上去 | 义项不混排：内置的义项是按词性从 WikDict 拼的，在线的是页面上的词类小节，编号对不上 —— 与 FR-16.9 决定「例句挂记录级」是同一条。按词性配而不是按位置配：按位置会把动词的 `Partizip II` 配到名词义项上。**例句去重要去掉引号**：构建脚本洗过 `„…“`、在线这份没洗，不归一化同一句会并排出现两遍 |
| FR-9.8 | 结果卡上能**念一遍**：真人录音优先（Wiktionary），没有就系统合成音，都没有就明说；并写明这是**孤立词发音** | 与 FR-17.7 同一条理由，也同一套代码（`ensureWordAudio` + 全局单例 `<audio>`）。这个应用的痛点是听觉识别，查一个词而听不到它等于只查了一半 |
| FR-9.9 | 「加入生词本」建的是**没有课的词条**（`VocabEntry.lookup`）：没有原句、没有挖空，复习时走「孤立词发音」那一档；加词时就把发音下下来 | 那个标记不能省：少了它，这张卡在复习页会被判成「来源句还没有时间戳 → 去这一课重新对齐」，而那一课从来不存在。`cardAudioStatus` 的 `preset-word` 因此改名 **`word-only`**，预置卡与查词卡共用（`isWordCard`）。发音在加词时取，理由与 FR-17.6 逐字相同 |
| FR-9.10 | 命中已有词条时**不新建**，如实说它在哪（哪一课 / 预置第几档 / 查词加的）；查不到时也明说，并且**仍然可以原样收下**（释义自己填） | 这里没有句子可挖空，所以 FR-9.3 那三个选项里「合并」无事可做、「仍然新建」只会多出一张同词的卡（两张卡的 FSRS 状态各自漂移）。而「查不到就不让加」会把多词搭配（`hing ... ab`）和生僻复合词挡在门外 —— 那恰恰是最值得记的一类。**「原样收下」这条路 2026-09-22 之前是坏的**：`add()` 第一行看 `result`，而查不到时它是 `null`，于是那个按钮按下去什么都不发生 —— 没有报错也没有提示，看起来就像这个词只能丢掉（用户报的第 7 条） |
| FR-9.11 | 生词本每一行、以及查词面板（不管这个词查到没查到）都有一个「**问 AI**」入口：生词本行上随时可点（点了就是重新问、覆盖旧的 `note`）；查词面板里查到的词是按钮，两个词典都查不到的词**自动**问一次，都不用先加进生词本再去点。**问过的答案会缓存并跨设备同步**（变更 49）：同一个词下次查到时直接显示缓存的答案，不用再问一次模型 | **2026-09-22 起服务器实时调用**（变更 48），此前是「标记 → 排进待办 → 复制走 → 粘回来」。没有「待问」标记字段了：一次点击就是一次完整的请求，中间状态只活在页面组件里（哪个词正在等、哪个词上次失败了），不用持久化也不用跨设备同步 —— 这与「查到了但那几个中文字看不出区别，只能由人点」那条判断（本条最初的立足点）并不矛盾，只是不再需要一个持久标记来记住「点过」这件事。**查到的词当晚（变更 49）补上了同一个入口**——之前只有查不到那一支能问，用户点开一个查到了的词发现完全没有 AI 入口。答案本身单独存一份缓存（`src/ai/cache.ts`，归一化词 → 解释），不依赖这个词有没有被收进生词本 |
| FR-9.12 | 服务器转发一次 Anthropic Messages API 调用（`POST /v1/ai/explain`，模型 Haiku），提示词要求**中文回复**、**不认识就说不认识**（不编造）、不写前言客套；结果直接写进 `VocabEntry.note`（多行自由文本） | **应用自己不解释词**——这条立场没变（与 FR-19 一致），变的只是「谁去问」：以前是用户手动去问自己的 AI 会话，现在是服务器代为转发同一类请求，返回的仍然是模型生成的解释，不是这台服务器自己的知识库或任何第三方版权数据（与 §3.1.1 R-1 的关系见该节新增说明）。`note` 单独一个字段而不是并进 `meaning`：`meaning` 要进读卡的四选一选项（`shortGloss` 截到 80 字符）、`meaningZh` 要进卡背那一行，两者都有结构上的用途，而一段三百字的辨析塞进任何一个都会把卡面撑爆。`note` 只出现在卡背末尾和生词本列表里。**没有配置 `ANTHROPIC_API_KEY` 或没有登录同步账号时整块不可用**，界面统一呈现成「AI 服务暂时不可用」（不区分具体原因，用户能做的事一样：过会儿再试）；查词面板问出结果后跟着 `createFromLookup` 一起收下，不需要用户再操作一次。形状见 §12.16 |

### FR-10 SRS 复习

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-10.1 | 用 **FSRS**（`ts-fsrs`），不用 SM-2 | — |
| FR-10.2 | **听卡**（这一节讲的都是听卡）**正面只有声音**：进入时自动播一次，点播放键可重播，**不给任何文字** | 课程卡放句子音频，预置卡放孤立词发音（FR-17.7）。音频里包含答案是**设计意图**：目标是训练「听到 /ˈtsuːfɐˌzɪçt/ 能反应过来是 Zuversicht 且知道意思」，不是遮蔽听觉线索。而给了文字，这张卡考的就不再是听觉识别了。**例外（变更 65）**：辨义题在卡面上亮出目标词形，见 §12.19 —— 它已经过了辨形那一关，这时再藏词形没有训练价值，反而让题目说不清考哪个词。**原文这里写着「那是生产方向的卡」，那句是错的**（§7.5 同样，已一并改）：看词选意思是**视觉识别**，不要求产出任何德语。它值得练，但要练成**另一张卡** —— 见 FR-21 |
| FR-10.3 | **答错**时展开完整卡背：词 + 性/复数 + IPA + 完整德语释义 + 例句（预置卡，FR-16.9）或完整原句（课程卡） | 答对不展开卡背 —— 见 FR-10.11 那 600ms 的补偿 |
| FR-10.4 | **评分由系统算，不让用户选**（`gradeFromAnswer()`）：选错或「不认识」→ `Again`；答对且 ≥4s → `Hard`；答对 <4s → `Good`；答对 <1.5s 且这张卡历史无错 → `Easy` | 原文那条「评分四档 `Again/Hard/Good/Easy`、键盘 `1234`、四个大按钮」**已废除**（变更 29）。两条理由：① 手评要求用户在每张卡上判断一次自己的记忆强度，那是元认知任务、不是学习任务；② 手评与自动评分喂给 FSRS 的分布不同，**两条路径不能并存**，混着用会让调度参数失真 —— 所以旧按钮是删掉，而不是留一个高级入口。界面上**不给可选间隔**，只在答对时如实告知「下次 X 后再见」（那是信息，不是选择） |
| FR-10.5 | 无真语料音频的卡要**区分三种原因**，给不同出口：`hasTimestamp=false` → 「去补标注」；有时间戳但本机无缓存 → 「下载素材」（FR-3.5）；**`preset` 卡 → 用孤立词发音，并在卡面说明**（FR-17.7）。**第四种（变更 53）**：`hasTimestamp` 与本机缓存都说「有」，但这一次 `getAudioBlob()` 真取不到、或取到了 `audioPlayer.load()` 解不了码 → 分别给「音频文件找不到了」/「音频文件解码失败」，同一个「去重新下载素材」出口 | 三者都不能静默降级成纯文本卡。第二种是一键可解的，混为一谈会让人以为卡片废了。第三种是 FR-17 加的：它**有**声音，但那是孤立词发音、练不到连读，不说清会让人以为自己在练真语料。第四种是变更 53 补的：`hasMaterial`（`LessonCache` 元数据）与 `audioBlobs`（IndexedDB 里真正的字节）分属两个 store，会分叉——原来这两条失败直接 `return`/未接的 rejection，症状是播放键**永久变灰、没有任何解释** |
| FR-10.6 | 队列受 `newPerDay` / `reviewPerDay` 限制；今日无卡时显示下次到期时间 | — |
| FR-10.7 | 手机端复习界面单手可用，按钮在拇指可达区 | **列数按题型分**：辨形题（短词）在手机上也是 **2×2**，按起来省手；辨义题在窄屏上是**单列**（截到 80 字符的释义挤进半个屏宽会折成四五行，那时单列才读得下去），宽屏才 2×2。实测过：一套响应式断点走到底的话 430px 下两种题都成单列。选项卡片**高度固定、不随释义长短浮动**，否则每张卡的按钮位置都在跳 |
| FR-10.8 | 作答形式是**四选一**：2×2 网格 + 一个「**没听清 / 不认识**」 | 「不认识」这个出口**不能省**：四选一有 25% 瞎猜命中率，没有它，猜对会被记成 `Good`，FSRS 的稳定度被系统性喂高 —— 卡越来越晚才回来，而用户其实没记住。点它等于 `Again`，并直接展开卡背 |
| FR-10.9 | 题型按**卡龄渐进**：新卡 / 重学卡考**辨形**（选项是四个德语词形），进入 Review 状态后考**辨义**（选项是四条德语释义；卡面亮出目标词形，题干问意思 —— §12.19） | 音都还没认住时问意思，等于一道题同时考两件事，答错了也说不清错在哪一环。释义优先德语而不是中文，与 FR-16.3 / FR-14 同一条判断；另有一条实测约束：牌组词的中译只覆盖 **60%**，德语释义覆盖 **99.9%**（band-4 实测 2999/3000），中文选项根本凑不齐四个 |
| FR-10.10 | **干扰项**：辨形题取 **IPA 最近邻**（全牌组范围，构建时预算，FR-16.8）；辨义题取同档**同词性**随机 | 辨形题的干扰项必须音近，否则 `Zuversicht / Haus / gehen / rot` 这种题谁都不会选错，**辨音训练归零**，等于每天点十次「对」。实测的样子：`heilen → heulen / teilen / weilen / eilen`，`Halluzination → Faszination / Emanzipation`。辨义题反过来 —— 语义上太相近对使用者不公平。释义选项要截断到约 80 字符（`shortGloss()`）：词典里的德语释义是 Wiktionary 式完整定义，中位 60 字符、p90 126、最长 862，原样放进 2×2 网格会爆屏 |
| FR-10.12 | **三个音效**：点一下选项一声轻「嗒」，随后（隔 120ms）答对 / 答错各一声。设置里可整块关掉，默认开 | **走 Web Audio，不走那个全局单例 `<audio>`** —— 这是 §3.2 那条规矩的唯一例外，而且非例外不可：听卡上德语句子还在播的时候你就答了题，单例只有一个，谁后加载谁把前一个顶掉，结果会是「答对的提示音把正在听的那一句掐了」。iOS 的手势链约束仍然成立，所以点击那一声**必须在点击处理器里发**（它顺带 `resume()` 了 AudioContext），判定音才响得出来；**`start()` 还必须等 `resume()` 真的 resolve 之后才发**——suspended 的上下文里排的音，WKWebView 上 resume 之后并不会响，是静默丢失（变更 51）。**隔 120ms 不是装饰**：两声一起响会糊成一声，而它们说的是两件事（「收到了」和「对 / 错」）；也不能更长 —— 判定音要压在答对那 600ms 的闪之内，否则它落在下一张卡上。音源是**文件**不是现场合成的音（Kenney Interface Sounds，CC0，转成单声道 WAV 共 43KB，`public/sfx/CREDITS.md`）；进复习页就预取，等第一次答题才去取的话那一声会迟半秒到，而**迟到的反馈比没有反馈更分神**。给开关是因为这是每答一题都会发生的事：在图书馆里唯一的出路不该是把整台手机静音，那会连德语音频一起关掉 |
| FR-10.11 | 答对 → 顶部闪 600ms「✓ **der** Vorhang」（名词带冠词）→ 自动进下一张 | 这 600ms **不是装饰**。答对是主路径，而它不展开卡背 —— 不在这里给一次性，名词的**性**就在最常走的那条路上彻底消失了，而 FR-7.4 的立场是「德语名词不带性等于没记」 |
| FR-10.13 | **一天之内不重复**：调度关掉 FSRS 的学习步骤（`enable_short_term: false`），最小间隔 **1 天** —— 当天的队列做完就是做完了，不会有卡在几分钟后回来 | `ts-fsrs` 默认带 Anki 那套学习步骤（`learning_steps: ['1m','10m']`、`relearning_steps: ['10m']`），学习中的卡被判 `Hard` 排到 **6 分钟**后。但这个应用**当天根本不会再把它发出来**：`buildReviewQueue` 的 `busy` 规则是「今天做过的词今天不再进队列」（FR-21.3 同日互斥的实现，按**词**去重，顺带把「同一张卡再来一次」也掐了）。两者并存的症状是**卡面在撒谎** —— 写着「下次 6 分钟后」，实际明天才见；更深一层是那张卡**永远毕不了业**，它停在 `Learning`，每次回来又被推 6 分钟，间隔始终由学习步骤而不是 stability 决定。<br>**修的方向是让调度器承认队列的既成事实，不是反过来让队列当天重发。**「当天复习完就清零」本来就是想要的形状：这不是 Anki 的默认，但 Anki 的学习步骤服务的是「一次坐下来啃 50 张新卡」，而这里的主场景是每天十几张、跟着一周一篇 DW 走。关掉之后新卡四档是 `Again` 1 天 / `Hard` 2 天 / `Good` 3 天 / `Easy` 8 天。<br>**存量卡不需要迁移**：已经停在 `Learning` 的卡下次评分会直接毕业进 `Review`（有测试钉住）。`formatInterval()` 的分钟/小时两个分支**保留**，它们现在是防御 —— 真出现分钟级间隔说明这个开关被谁改回去了 |

| FR-10.14 | **卡背上的中文**：答错展开卡背时，如果这个词**既没有中译（`meaningZh`）也没有 AI 解释（`note`）**，就自动补一次 —— 先查 AI 答案缓存（FR-9.12 / 变更 49，跨设备同步），命中直接用；没有才真的问一次 AI。答案写回 `note`（落库、进备份与同步），下次再答错这张卡不会再问。没配 AI（没登录 / 服务器没 key）时**卡背上不提这回事** | 词典的中译只覆盖约 60%，德语释义 99.9%（FR-10.9 实测）—— 于是像 `erschlagen` 这样的预置卡，卡背上一个中文字都没有，而**答错正是最需要「这个词到底是什么」的那一刻**。FR-21.9 的「复制走 → 在外面翻 → 粘回来」补得了中译，但那是一次批量动作，答错当下补不上。<br>**判据是「一个中文字都没有」而不是「没有 `note`」**：有 `meaningZh` 就已经有中文了，再问一次只是重复花钱。**只在答错那条路触发**：答对走 FR-10.11 那 600ms 直接进下一张、根本不露卡背，在那里问等于给「已经会了的词」花钱。**缓存只是一条省钱的近路**：读坏了（IndexedDB 出问题）不影响照旧去问一次。它同时补上了一个一直存在的缺口 —— 查词面板（FR-9.5~9.10）问到的答案原先只写进缓存、不写进 `note`，所以「这一课查词时问过这个词」的解释在复习卡背上看不到。<br>**卡背不等 AI**：词形、IPA、德语释义、例句立刻就在上面，中文回来了再填进来。请求**不随翻页取消**（答案已经付过钱了，落库比丢掉划算）。**没配 AI 时静默**：这里没有任何下一步动作可做，每答错一次就唠叨一行「不可用」是纯噪音，登录状态在设置页（§12.3「一切正常就静默」）；真的发出去又失败了才给「AI 服务暂时不可用 + 重试」，那时候重试是有意义的动作 |

**这一节讲的是听卡。** 同一个词还有一张**读卡**（看词形/释义/句子，不放声音），独立调度、独立题型、与听卡同日互斥 —— 见 **FR-21**。评分（FR-10.4）、「不认识」出口（FR-10.8）、答对的 600ms（FR-10.11）两张卡共用。

### FR-11 备份、恢复与存储

> 实现 §2.6。**这是 V1 中优先级最高的非学习功能** —— 它保护的是几年积累里唯一不可重建的部分。

**登录同步后端**

> **2026-09-02 重写**：原文是「连接 GitHub」那一套（粘贴 fine-grained PAT、一键建仓、
> 监控 token 过期、扫码配对）。换成自建后端 + Google 登录之后，FR-11.1 ~ FR-11.5 与
> FR-11.18 这六条整体作废，由下面三条取代。理由见 §0 变更 25、方案见 §2.6.3。
> **FR-11.6 及之后的编号与内容全部不变** —— 换的是运输方式，不是备份策略。

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-11.1 | 设置页一个「用 Google 登录」按钮，三端（web / iOS / Android）同一条代码路径 | 没有要手抄的字符串。构建期没配服务器地址时整块关闭并说明原因，**手动导出照常可用** |
| FR-11.2 | 登录后显示账号邮箱与服务器地址；可退出 | 退出只清本机会话与版本号，服务器上的数据一个字节不动 |
| FR-11.3 | 服务器只认白名单邮箱，被拒时给出**明确原因**（不是白屏、不是「登录成功但同步不动」） | 白名单为空 → 服务器拒绝启动（§2.6.7）。令牌被撤销后，下一次请求即失效，界面切回未登录并提示 |

> **web 版的登录是「弹窗 + 完整 OAuth 重定向」，不是 One Tap**（2026-09-03 查清）。
> `@capgo/capacitor-social-login` 的 web 实现只有这一条路：`window.open` 一个
> `accounts.google.com/o/oauth2/v2/auth?response_type=token id_token`，Google 把令牌拼在
> fragment 里重定向回来。由此有两件事必须知道 ——
>
> ① **`redirect_uri` 要一字不差地登记在控制台**（deploy/README.md ② 那一条）。插件的默认值是
> `origin + pathname`，末尾带一条斜杠；`src/sync/session.ts` 的 `oauthRedirectUrl()` 把它写死成
> 裸 `location.origin`，跟部署路径与路由脱钩，只剩一个值要登记。实测三种写法的区别就是全部：
> `https://d.gamestao.com` → 正常登录页，`https://d.gamestao.com/` → `错误 400:
> redirect_uri_mismatch`。**Google 对这个值是逐字符比对的**，「已获授权的 JavaScript 来源」
> 填了不算，那是另一栏。
>
> ② **弹窗跳回来落在本站首页，那个窗口会再启动一遍整个 App**。收尾（postMessage 回主窗口 +
> `window.close()`）挂在插件模块的 import 副作用上，而这个 App 是懒加载插件的 —— 弹窗里没人
> import 它，主窗口就一直等到 5 分钟超时。`src/main.tsx` 因此先认「我是不是弹窗跳回来的」
> （`isOAuthPopupReturn()`），是就只 import 插件、**不启动 App**。这条与 ① 是叠在一起的两个 bug，
> 修好 ① 才会看到 ②。
>
> 原生壳不经过这一段：iOS/Android 用系统 Google 账号，源码里根本不读 `google.redirectUrl`。

**自动备份（主方案）**

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-11.6 | 推送 `vocab` 文档：**每次复习会话结束**触发 | 不可重建的数据不过夜 |
| FR-11.7 | 推送 `lesson:<id>` 文档：该课导入完成、或标注/挖空变更后触发（去抖 30s）；本地删课时删除远端文档 | 「删除」与「刚改过」不能同时留在队列里，否则一次重试会先删再建，把删掉的课复活 |
| FR-11.7b | **（2026-09-21 新增，§0 变更 43）** **去抖窗口一开始就把这一项标脏落库**（推送队列里的 `deferred` 项）；推成功即清掉。标脏项照常参与 drain，但**不计入**「待推送 N 项」 | 去抖定时器只活在内存里：窗口没到就关页面 / 被系统回收，那次推送连同它要推的改动一起消失，而且**没有任何补救路径** —— 远端那份没被别人改过，版本号一致，FR-11.19 的增量拉取连全文都不取，无从比较 `updatedAt`。真实症状是「桌面上对齐完就关掉，手机上拉下来还是空的，于是又在手机上对一遍」。不计入待推送数是因为它是每次改动后都会出现几十秒的**正常**状态，不是故障 |
| FR-11.7a | **（2026-09-02 新增，§0 变更 28）** 推送 `settings` 文档：改过设置后触发，去抖 **5s** | 去抖比课程短得多：改设置是「拖一下滑块、点一下开关」，几秒就停手，而且改完之后人很可能立刻换设备 —— 那正是要同步它的场景。30s 在这里只会让「桌面上改完、马上拿起手机」拿不到新值 |
| FR-11.8 | 用文档版本号做乐观并发；收到 409 → **响应里已带远端现值** → 按 §2.4 合并规则合并 → 用远端版本号重推 | **绝不静默覆盖**。合并把远端赢的那部分写回本地库并让内存里的 store 重读 —— 否则界面上还是合并前的旧值，而用户毫不知情 |
| FR-11.9 | **同步状态常驻可见**：登录的账号 + **上次推送成功**时间 + **上次拉取成功**时间（2026-09-03 分成两项，§0 变更 34）+ 待推送变更数。**2026-09-05（§0 变更 36）改了落法，四项一项没少：**摘要（四项里最坏的那一项）跟着应用外壳走、每一页都在，四项全文点开可见 —— 见 §12.3.1 | 静默失败的备份比没有备份更危险 —— 它给你虚假的安全感。**推通了不代表拉通了**：拉悄悄停掉的症状是「另一台设备上的东西一直不来」，而那和「我最近没在别的设备上练」长得一样，所以两个时刻必须分开显示。原文这里还有一项「token 剩余有效期」，随 PAT 一起作废 |
| FR-11.10 | 推送失败 → 显式报警横幅，且**离线时排队、恢复网络后自动重试** | 手机常在弱网下复习，失败必须是暂态而非丢失 |
| FR-11.19 | **（2026-09-03 新增，§0 变更 34）自动增量拉取**：启动、回前台、网络恢复时按文档版本号只拉变过的（`GET /v1/docs` 的 `version` 对本地 `syncVersions`），按 §2.4 合并写入本地；本地赢的部分**回推**；两次自动拉取之间至少 60s，且同时只有一次在跑 | **跨设备必须是「拿起手机就有」，不是「记得去按恢复」**。验收：桌面导入并对齐一课 → 手机回前台 → 那一课自己出现且带时间戳，一个按钮都不用点。常态下只有一次列表请求（版本号全都对得上时不拉任何全文）。**不跟随删除** —— 远端列表里没有的文档不动本地，删除只在本机发起 |

**手动导出（第二道保险，不同故障域）**

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-11.11 | 导出备份 JSON = **标注层全部，缓存层零字节**，文件名 `backup-YYYY-MM-DD.json` | 顶层带 `_warning` 版权提示（§3.1）；**按日期命名，永不覆盖同名文件** —— 覆盖式备份防不住「写坏数据后备份」 |
| FR-11.12 | 距上次手动导出超过 90 天 → 首页提醒。**2026-09-05（§0 变更 36）改成两档，且标注层为空时不提**：从未导出或 90–180 天是一行提醒，超过 180 天才升级成横幅 —— 见 §12.3.2 | 自动同步已是主力，手动只是防同步服务器本身出问题（机器没了、证书过期、账号登不上），故周期放长；不用弹窗打断。**分两档是因为旧的一档形状在全新安装的第一天就会亮**（`lastBackupAt` 只在导出时才写），而那时还没有任何东西可丢 —— 一条从第一天起就挂着的横幅只会教人忽略横幅 |

**恢复**

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-11.13 | 一键从服务器恢复到空设备：Google 登录 → 拉取全部文档 → 按 §2.4 合并写入本地 | 空浏览器 → 标注层齐全 → 课程显示「素材未下载」→ 补齐（FR-3.5）→ 复习照常。**不是「清空后覆盖」**：恢复到一台已有数据的设备上是正常用法 |
| FR-11.14 | 导入本地备份文件，按 §2.4 合并规则执行（**含设置**，2026-09-02 修正），展示合并摘要 | 导入前自动先导出一份当前状态（防呆）。**设置那一半以前是漏的**：备份文件里一直有 `settings`，而导入代码从不读它 —— 「换设备恢复」会把设置默默留在默认值上，见 §0 变更 28 |
| FR-11.15 | **每年提示一次恢复演练** | 工具在变，恢复路径会悄悄坏掉。见 §2.6.5 |

**其他**

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-11.16 | 启动时 `navigator.storage.persist()`；设置页显示持久化状态与 `estimate()` 用量 | — |
| FR-11.17 | 实现 `toShareablePackage(lesson)` 纯函数 + 单元测试（见 §3.1），V1 不暴露 UI 入口 | 测试必须断言导出结果不含任何正文片段 |
| ~~FR-11.18~~ | **设备配对二维码 —— 2026-09-02 随 PAT 一起删除**（`src/lib/qrPairing.ts`、`src/components/QrScanner.tsx` 与 `jsqr` / `qrcode` 两个依赖都已移除） | 它存在的唯一理由是「新设备上不用手敲一串 `github_pat_...`」。而那张二维码的内容**就是明文令牌**，谁扫到谁就有读写权 —— 当时为此写了限时隐藏和一整段警告。换成 Google 登录之后新设备本来就只需要点一下，这个形状连同它的安全提示一起没有了存在必要 |

### FR-12 设置

| 项 | 默认值 | 说明 |
|---|---|---|
| `newPerDay` | **10** | 每周 1 篇 ≈ 每天 3–4 个新词，设 30 会导致周一清空、之后空转 |
| `reviewPerDay` | **60** | 防爆闸，正常不会触顶 |
| `shadowingGapRatio` | 1.2 | 静默间隔 = 句子时长 × ratio |
| `shadowingRepeat` | 2 | 每句重复次数，可设为 0（无限，手动推进） |
| `playbackRate` | 1.0 | 0.7 / 0.85 / 1.0 / 1.2 |
| `dictationStrictCase` | true | 关掉后大小写错误不计错 |
| `autoAlignOnImport` | true | FR-15；「下载完就能直接练」是那个功能存在的理由，默认关掉等于没做 |
| `presetBand` | **4** | FR-17.4a；面板上的推荐起点（不是「已报名」）。前三档约三千词，C1 的人基本全认识 |
| `enrolledBands` | **`[]`** | FR-17.4；已报名的词频档，卡片按 `newPerDay` 每天惰性激活。默认空 —— 报名会一路发卡几个月，不该由默认值替用户决定 |
| `onlineDictFallback` | true | FR-16.5；内置词典查不到时联网查 de.wiktionary |
| `updatedAt` | — | 不是设置项，是**定序键**：设置整体同步时靠它比新旧（§0 变更 28）。只有 `useSettingsStore.update()` 写它 |

**这一整张表都跟着账号同步**（2026-09-02，§0 变更 28）。曾经的判断是「跟机器走、不同步」，
那是错的：真正跟机器走的只有「这台设备跑不动对齐模型」，而它记在黑匣子里（`align/journal.ts`，
localStorage），与 `autoAlignOnImport` 无关 —— 手机把那个开关同步成 true，两档都崩过之后照样自己停（FR-15.10）。

### FR-13 来源与自动导入（纯前端，无后端）

**分三层降级，L3 永不删除：**

| 层 | 方式 | 何时用 |
|---|---|---|
| **L1 全自动** | 选来源 → 看期次列表 → 选一期 → 音频和文稿自动到位 | 正常情况 |
| **L2 半自动** | 粘贴单个 DW 页面 URL（或直接粘 lesson id） | RSS 100 期窗口之外的存档期次 |
| **L3 手动** | 粘贴文本 + 选本地文件（即现有 FR-1.2 / FR-1.3） | DW 改版、教材音频、任何非 DW 素材 |

L3 是地板：DW 改版会打掉 L1/L2，Aspekte neu 这类教材根本没有 feed。**任何时候都不能让 L3 依赖 L1/L2 的代码路径。**

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-13.1 | 内置来源常量表：DW Alltagsdeutsch / DW Top-Thema / DW Langsam gesprochene Nachrichten（Sprachbar 的 RSS 已下线，变更 66 换掉），各带 RSS 地址与 adapter 标识 | 不做「用户自定义来源」UI（个人工具，加来源就是改一行常量） |
| FR-13.2 | 拉取 RSS（`fetch` + `DOMParser`），展示期次列表：标题、`itunes:duration`、`firstPublicationDate` | 列表缓存进 IndexedDB，离线可看，显示「抓取于 X」 |
| FR-13.3 | **排序与去重按 `firstPublicationDate`，不按 `pubDate`** | DW 在把 2019 年的旧期以当周日期重新推送（附录 A.2）。用 `pubDate` 排序会让「最新」全是老内容 |
| FR-13.4 | 选中一期 → 抓页面 HTML → 解析 `window.__APOLLO_STATE__` → 取 `Lesson:<id>` 的 `manuscript` / `teaser` / `knowledges`，取 `Audio:*` 的 `mp3Src` 与 `duration`。**没有 Lesson 时取 `Article:<id>`**（LGN，`a-<id>`）：文稿在 `text`，音频按它自己的 `audios` 顺序取第一条（慢速） | 见 §7.8；变更 66 |
| FR-13.5 | 下载 mp3 存入 IndexedDB，用 `Audio.duration` 预填 `audioDuration` | 有下载进度（`Content-Length` 已知，CDN 支持 `Accept-Ranges`）；可重试；**音频与文本任一失败不影响另一个** |
| FR-13.6 | `manuscript` 转纯文本后喂进现有流程，走 FR-2 切句 + FR-1.4 段落排除 | 自动导入不是独立分支，只替 FR-1.2 / FR-1.3 填数据。切句逻辑一行都不重写 |
| FR-13.7 | **（2026-09-02 改，§0 变更 24）自动导入一句都不排除。** 原规则「首个 `<strong>` 块 = 标题 + teaser，音频里不朗读 → 整块排除」**实测是错的**：播音员照着念了。相关代码（`teaserBlock` 判定、`excludeTeaserBlock`）已删除 | 排除的默认值只能站在「音频里真的没有」这一边：排除句不进对齐目标（FR-15.1），把念过的三句排掉等于在音频开头挖一个几十秒的空洞，逼 CTC 把那段声音塞给第一句正文。仍然需要排除的场景（手动粘贴 PDF 的文末 Glossar）走手工：「切句」页有**开头 N 句**与**文末 N 句**两个批量控件，前者带「恢复」以便一次撤掉旧课里已排掉的那几句 |
| FR-13.8 | 已导入期次按 **lesson id**（= RSS `<guid>` = URL 里的 `l-<id>`）标记「已导入」 | 不用 `sourceUrl` 匹配 —— RSS 的 link 带 `?maca=` 跟踪参数，不稳定 |
| FR-13.9 | 抓取失败 → 说明失败在哪一步 + 一键切到 L3，**保留已抓到的部分** | 不能只显示「导入失败」 |
| FR-13.10 | 串行请求、间隔 ≥ 1s、**不提供「批量导入全部期次」** | 见 §3.1.1 R-3 |
| FR-13.11 | 每个来源一个 adapter 模块，各配一份 `__APOLLO_STATE__` 快照回归测试 | DW 改版时**测试先红**，而不是某天用的时候才发现。快照受 R-5 约束，正文替换成自造句子，只保留结构 |
| FR-13.12 | **回填最近几期**（3 / 5 / 10）：串行导入列表里还没导过的期次，可停 | 冷启动用 —— 导进来之后每期的 Glossar 候选词就是一批**带真语料**的生词（德德释义 + 语境句 + 真人朗读），FR-17 的预置词库这三样都给不了。**与 R-3 的关系**：R-3 禁的是「一键导入全部 100 期」那个**形状**，不是「有循环」。这里满足它的方式是：有硬上限且**没有「全部」选项**、只从已拉到的 RSS 列表里取（不翻页/不猜 id/不爬归档）、速率约束复用 `politely()` 而不是自己重写、停在期与期之间不留半篇课程。速率上它与「手点十次导入」完全等价 |

### FR-14 Glossar 候选词（DW 素材专属）

DW 在 `manuscript` 里已经把它认定的生词内联标注好了（附录 A.3）：

```html
<span class="editable placeholder" data-id="43252624"
      data-title="Plattform, -en (f.)" data-type="GLOSSARY">Plattformen</span>
```

一个 span 同时给出四样东西：句中**表层形式**（`Plattformen`）、**词条 + 复数 + 性**（`Plattform, -en (f.)`）、指向释义的 `data-id`，以及精确的**字符位置**。对应的 `Knowledge:<id>.text` 是一句德语释义。

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-14.1 | 导入时解析 GLOSSARY span，生成**候选词**列表存进 `Lesson.glossary`（**标注层**，2026-09-02 从缓存层搬来，见 §0 变更 27），不自动建条目 | 这是 DW 选的词，不是「我不认识的词」—— 原始需求明确抱怨过现有 app 不能按后者挖空。候选是便利，不是代替判断 |
| FR-14.2 | 候选词在文本中以浅色下划线标出，点一下即接受为 `Blank` + `VocabEntry` | 一次点击完成 FR-7.3。**实现期补充**：接受同样受 §3.3 R1 约束 —— 所在句没有时间戳时拒绝并提示去打点，否则会造出一个听写不了、也生成不了音频卡的挖空 |
| FR-14.3 | 接受时自动填充 `surface` / `lemma` / `gender` / `plural` / `meaning` | 从 `data-title` 解析性和复数（格式 `名词, 复数词尾 (性.)`）；`meaning` 取 `Knowledge.text` 的纯文本 |
| FR-14.4 | 解析不出 `data-title` 格式时降级：只填 `surface`，其余留空 | 不能因为格式意外就丢掉整个候选 |
| FR-14.5 | 「全部接受」按钮，但**不是默认行为** | 有些期次 Glossar 有二十多条，全接受会把生词本灌满你本来就认识的词 |

**对 V2 的影响**：§7.7 原计划用 Claude API 做语境释义。在 DW 素材上，释义、性、复数 DW 已经给了，且是德德释义（比中文释义更适合 C1）。Claude API 的价值收窄到两处：① 非 DW 素材；② DW 没标注但你不认识的词。

---

### FR-15 自动打点（浏览器内 CTC 强制对齐）

打点是 §3.3 能力依赖链的入口：没有时间戳就没有跟读、没有听写、没有音频卡。而它是唯一必须逐句手工做的一步，一期 40+ 句，这是实际使用里最大的摩擦。

**做的是强制对齐（forced alignment），不是语音识别。** 文稿已经有了（FR-13 从 DW 抓的），所以问题不是「音频里说了什么」而是「已知的这段文字，每个字落在哪一帧」。后者是个动态规划问题，有确定解；前者是猜。这个区别决定了整套方案：不需要解码器、不需要语言模型，只需要一次声学模型前向 + 一次 Viterbi。

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-15.1 | 从「非排除句 + 音频」算出每句 `startTime`/`endTime`，写入 `timingSource: 'auto'` 与 `timingConfidence` | 排除句（FR-1.4：手工标出来的、音频里真的没念的段落，如粘贴 PDF 带进来的 Glossar）**不进对齐目标** —— 音频里没有它们，硬塞会把邻句边界一起拽歪。**反过来同样致命**：把念过的句子排掉会在音频里挖出一段无人认领的声音，见 §0 变更 24 |
| FR-15.2 | 同时产出**词级**时间戳，存进 `Sentence.words`（**标注层**，跟着备份与同步走；2026-09-02 从 `LessonCache.wordTimings` 搬过来，见 §0 变更 26） | 句内 offset 与 `Blank.ranges` 同坐标，听写时「只播这个挖空对应的词」是一次直接查找；通听的逐词高亮（FR-5.3）用的也是这一份。存在句子里而不是整课一个数组，是为了让合并/拆分/重新切句跟处理 `blanks` 用同一套办法 |
| FR-15.3 | 结果一律呈现为**待校对**，不呈现为完成；低置信句数摆在课程页头部（**2026-09-16 补**：这条提示有了退出条件 —— 逐句人耳确认完就回到静默，见 FR-15.19） | 说成完成会让人跳过校对。阈值是**相对本课中位数**的，不是绝对值 —— 见 §7.9 |
| FR-15.4 | **默认不覆盖人工标注**。`timingSource === 'manual'`，以及 FR-15 之前的老数据（有 `startTime` 但没有 `timingSource`），都跳过 | 打点是这个应用里最不可重建的劳动（§6）。漏掉老数据兼容那一条，一次自动打点就静默吃掉几十分钟的手工活 |
| FR-15.5 | 重跑（「重新对齐」）**不覆盖** `timingSource: 'manual'` 的句子；老数据里的手工时间戳按 FR-15.4 一并保护 | 手工打点界面已删（§0 变更 20），所以这条现在只作用于历史数据 —— 而那正是最不可重建的那部分 |
| FR-15.6 | 全程在 **Web Worker** 里跑，主线程只负责解码音频与显示分阶段进度 | 一课在 WASM 后端要几分钟。放主线程等于 PWA 冻住几分钟，连进度条都刷不出来 |
| FR-15.7 | 任何一次「音频到位」都立刻排队自动对齐（DW 导入、手动导入、补齐素材、重新绑定音频），按 `Settings.autoAlignOnImport`（默认开）；**不阻塞导入**，进度常驻在应用底部 | 导入是秒级的，对齐是分钟级的，串在一起会让导入按钮转十分钟、还把进度绑死在来源页上（切走就看不见）。失败不算导入失败 —— 与 FR-13.9「抓到哪算哪」同一个道理 |
| FR-15.8 | 权重取件顺序：**随包**（`npm run stage:align`，只有 Android 打包版跑）→ **自己的权重站**（同步服务器的 `/v1/align/weights/`，见 FR-15.21）。取到之后 transformers.js 存进 Cache API，之后离线可用 | 离线可用是这个功能的前提之一。**「退到 HF CDN」那一档 2026-09-21 起不存在了**：浏览器要的 4-bit 权重是我们自己量化的，HF 上只有 fp32 那一份（1204 MiB，浏览器跑不动）。ORT 的 wasm 走 Vite `?url`，构建产物自带，任何情况下都不联网 |
| FR-15.9 | **黑匣子**：每次对齐边跑边把「阶段 + 字节 + 后端 + 平台」同步写进 localStorage；下次启动看到没收尾的记录就报「上次被系统杀掉了」，并在设置页留最近几次 | 手机上真实的失败方式是**进程直接消失**：try/catch 抓不到、Banner 弹不出来、重开一切正常。不落盘就等于这个功能在手机上没有可诊断性 |
| FR-15.10 | **后端阶梯 + 自动降档**：`webgpu/q4` → `wasm/q4`（**同一份 230.2 MiB 的文件**，只换执行后端）；崩过的档位不再用，两档都崩过就停掉自动对齐并明说「去桌面对齐，句级时间戳会跟着备份回来」（**iOS 原生壳上这整套都不适用** —— 那里没有本机这条路，见 FR-15.14）。**「崩过」包括「Worker 被杀但主线程还活着」** —— 那一类 JS 有机会收尾，但必须记成 `crashed` 而不是 `error` | 崩溃救不回来，但可以不重复。没有「停掉」这一条的话，「导入后自动对齐」在跑不动的设备上就是一个每次导入都杀一次应用的循环。第 2 档原为 `wasm/int8`，实测**加载即被杀**（§0 变更 21），等于这个阶梯以前只有一档真的能用；而记成 `error` 的那个洞会让降档压根不触发，同一档被无限重试。变更 42 起两档共用一份 4-bit 权重：多一份 fp16 变体意味着多量化一次、多托管一份、多一条「这台机器到底加载了哪份」的排查路径，而它唯一的好处（WebGPU 上省一半显存）在桌面上不是问题 |
| FR-15.11 | **随包**权重按 **Range 分片**取（8MB/片），并关掉 transformers.js 的浏览器缓存回写；从权重站取的那一份则**照常**进 Cache API | 原来那条路上同时存在五份 187MB（见 `src/align/rangedFetch.ts` 顶部的事故分析），其中「把安装包里已有的文件再抄一份进 Cache API」纯属白费。两者的判据不同不是不一致：随包那份**磁盘上已经有了**，抄进缓存是纯浪费；权重站那份是网上下来的，不缓存就等于每次对齐重下 230MB，而「服务器挂了我自己算」正要靠这份缓存才成立 |
| FR-15.12 | 原生壳里每跑完一课就把模型放掉（连 Worker 一起终止）；桌面保留常驻 | 手机上对齐完的下一件事就是播这一课的音频，权重常驻只是在等下一次 jetsam。变更 42 之后 iOS 上已经没有本机这条路，这一条只剩 Android 壳还走得到 |
| FR-15.13 | **帧级 log-prob 是一个自足、可序列化的中间产物**（`src/align/emissionMatrix.ts` 的 `EmissionMatrix`）：`音频 → 矩阵` 与 `矩阵 + 文稿 → 时间戳` 之间只有这一道缝，两边各自可替换。定义这道缝的文件**不许 import `@huggingface/transformers`** | 这条流水线的两半是正交的：前一半要 300M 权重 + GPU（**全部的时间与内存都在这里**），且**压根不看文稿**（CTC 前向只吃波形）；后一半是纯 JS、只碰一个约 3MB 的矩阵，不要模型也不要 GPU。把矩阵定成契约的直接后果：前一半能整块换地方跑（原生插件 / 远端）而后一半一行都不用改，且远端那条路上**德语正文一个字都不出设备** —— §3.1 关于正文的约束因此完全不受影响，要认的只剩「音频经手」一条。那个 import 禁令不是洁癖：transformers.js 连着 onnxruntime-web 一共 500KB+，而主线程、单测、以及将来的线缆编解码器都只需要这里的类型 |
| FR-15.14 | ~~**iOS 上 emissions 由原生插件算**~~ **（2026-09-21 整条删除，§0 变更 42）** 原文：（`native-plugins/align-native`，Swift + ONNX Runtime）：走 `native/q4`，权重读 app bundle 里那份，桥上过的是 **mp3**（解码也在原生侧），矩阵 base64 回传给同一个 Worker 跑 viterbi。**这一档不在 FR-15.10 的阶梯上**，不参与降档，`blocked` 也不拦它 | iPhone 13 上 WebView 里两档**都被系统杀掉**，而 `q4f16` 已是最小变体 —— 降档在手机上救不了。原因不是设备内存不够：WKWebView 的 WebContent 进程 jetsam 线远低于原生进程，且 ORT-web 至少要「JS 堆一份 + wasm 堆一份」。桥上传 mp3 而不是波形是因为 Capacitor 的桥只能过 JSON：8 分钟波形 base64 之后 41MB 且会在 WebView 里同时存在两份，而同一课的 mp3 只有 7MB。不进阶梯是因为阶梯的语义是「这台设备的 WebView 里哪一档不会被杀」，混进去会让 `crashedSteps()` 把原生的失败算到第 0 档头上。**删掉它的理由**：它从建成到删除一次都没有真的在真机上跑完过一课（变更 33 已经把它从自动降成手动），而代价是确定的 —— 随包 418 MiB 权重、一个要联网 resolve binaryTarget 的 SwiftPM 依赖、以及 §7.12 里那条最费劲的符号链接。跑不动的是这台设备，不是这个功能：服务器那条（FR-15.17）留着 |
| FR-15.15 | **（2026-09-21 改写）iOS 原生壳上没有本机这条路**：有服务器（登录了）就照常自动对齐，没有就**不排队**，课程页把「这一课还没有时间戳」说成正常状态并指出两个出路（登录同步让服务器算／桌面算完同步过来）。判断是**设备本地的**，不碰 `autoAlignOnImport` | 手机本地那条路的故事：估 10–15 分钟一课，而 iOS 上没有 `UIBackgroundModes` 就意味着锁屏或切走 App 之后进程被挂起，带着 400MB 常驻被挂起又正是 jetsam 最先挑的那一个 —— 它十有八九跑不完，只是白热一台机器，最后在变更 42 被整条删掉。桌面上同一课 44 秒，而变更 26 之后句级 + 词级时间戳全进标注层并同步，所以「桌面算、手机练」一直是这个功能在手机上的正确形状；服务器那条（一两分钟，且这期间手机可以锁屏）是它的补充而不是替代。闸门不能做成关掉 `autoAlignOnImport`：那是同步项（变更 28），会传染到桌面 |
| FR-15.16 | **断点续算**：emissions 按块顺序算，每块算完把整份矩阵 +「已完成几块」原子地落进 Caches（`Checkpoint.swift`，先数据后 header）；下一次同一课、同一份音频就从那一块接着算，配一个指纹（采样点数、音频字节数、分块参数、权重文件名）来判断中间态还算不算数。**成功才扔断点，取消和崩溃都留着。**「停止」在块边界生效（`cancelEmissions`） | 十几分钟的活儿必须允许**分几次干**：切走 App、被系统杀、用户按停止，这三件事随时会打断它，没有断点的话每一件都等于十几分钟白烧，于是「在手机上对齐」变成一件运气好才能完成的事。指纹不能省：混用一份不匹配的中间态会得到「看起来正常但时间戳系统性错位」的结果 |
| FR-15.17 | **（2026-09-03 新增，§0 变更 35）服务器算 emissions**：`POST /v1/align/jobs`（上行 mp3）→ 轮询 → `GET .../result`（下行 `frames × 35` 个 float32）；Viterbi 与文稿留在设备上。串行队列、排队上限、结果取走即删 + TTL；整块可关（`ALIGN_ENABLED=false`）| **上行音频、下行矩阵，文稿一个字都不上去** —— 这是那道缝（FR-15.13）第三次被兑现。任务化而非一次长请求：手机握不住一两分钟的连接，而计算留在服务器上意味着**这期间手机可以锁屏、可以退出 App**。矩阵**不压**（不 float16、不 gzip）：两条路本来就因为权重不同而有几十毫秒的出入（变更 42），没有理由在传输上再叠一层**可以避免**的误差。对齐挂了不能拖下备份 —— 所以 `onnxruntime-node` 是 optionalDependency，装不上时三条路由回 503 + `code=align_off`，同步照常 |
| FR-15.18 | **（2026-09-21 改写）在哪儿算的判据只剩一条**：iOS 原生壳 → 服务器；其余（桌面浏览器）→ 本机。课程页上：手机只有「用服务器对齐」一个按钮（没登录时连它也不给，改成一段说明）；桌面主按钮是本机，旁边一个 ghost 的「送到服务器算」| 桌面一课 44 秒，没有理由上传 7MB；手机上已经没有本机这条路了。桌面那个 ghost 按钮留着有两个用处：验一下服务器算得对不对（两条路权重不同，这件事更值得能随手对照了），以及本机跑不动时的退路 |
| FR-15.19 | **（2026-09-16 新增，§0 变更 39）低置信句可以人耳确认**：通听里带 `?` 的行号点一下 = 「听过了，起点是对的」（`Sentence.timingChecked`，标注层），`?` 消失、该句退出待校对队列；点第二下撤销。全部确认完，课程页头部与那条提示一起回到静默。**重新对齐会把确认清掉**，`timingSource` 不变（仍是 `auto`） | FR-15.3 要求把低置信句数摆在头部，但没给它退出条件 —— 而低置信的常见原因（台标音乐、播音员交替、英语借词、被丢掉的数字）大多不影响句边界，所以「听完发现都对」是常态。一条听完也消不掉的提示只会教人忽略提示（§12.3 的反面）。不复用 `manual`：那个值会让重对跳过这一句（FR-15.5），而这里确认的是**那一版边界**，不是这个句子永远正确。确认不参与阈值计算，否则剩下几句的判定会跟着人的操作漂 |
| FR-15.20 | **（2026-09-21 新增，§0 变更 42）模型是德语原生的**：`oliverguhr/wav2vec2-large-xlsr-53-german-cv9`，35 token（`|` + a-z + ä/ö/ü/ß + `[UNK]`/`[PAD]` + 两个用不到的 `<s>`/`</s>`），**blank 是 `[PAD]`=32 而不是 0**，0 是词分隔符。目标序列里**词与词之间、句与句之间都要插一个 `|`**，它不属于任何词，折回时间戳时整个跳过 | 罗马化那套（MMS-FA）在德语上交两笔税：ä/ö/ü/ß 的区分被丢掉，以及词表里根本没有空格 —— 词间那几帧上模型把概率质量押在 `|` 上，而路径只能从 blank 或下一个字母上过，两者概率都低，于是边界抖动、整句 confidence 被拉低。两笔一起免掉的实测结果：同一课句级 confidence 中位数 **-1.1 上下 → -0.02**。`blankId` 因此必须是**参数**而不是常量（`forcedAlign`/`alignWindowed` 都显式收它）：给错等于把 blank 当成一个普通字母，路径全错而且不报错 |
| FR-15.21 | **（2026-09-21 新增）权重站**：同步服务器把 `${DATA_DIR}/models` 当只读静态目录对外提供（`GET /v1/align/weights/**`，支持 Range，扩展名白名单 `.json`/`.onnx`，**不要求登录**，靠 CORS 白名单护栏）。它与对齐是**分开的两件事**：`ALIGN_ENABLED=false` 之后这台服务器仍然要提供权重 | 浏览器要的 4-bit 权重是我们自己量化的（`scripts/quantize-align-model.py`），HF 上不存在；而前端托管在 Cloudflare，那边**单文件上限 25 MiB**，230MB 放不进去。于是唯一剩下的位置就是这台本来就要有的服务器。不要求登录不是偷懒：transformers.js 的取件走它自己的 fetch，加不上 `Authorization` 头 —— 而那里面只有一个 Apache-2.0 的模型文件，不是学习内容 |

**已知的精度损失，以及为什么接受**（2026-09-21 随换模型重写）：词表里**没有数字、没有标点、没有撇号**，所以这几类字符被整个丢掉：`zwischen 18 und 30` 变成 `zwischen|und`，音频里那两个数词（近 1 秒）没有对应 token，由 CTC blank 吸收。这对**句边界**几乎无损（误差是局部的），只有数字正好压在句首/句尾时才偏几十毫秒。展开德语数词（含 `1990` 读作 `neunzehnhundertneunzig` 这类年份读法）是独立一块活，收益只在词级精度上。换成德语原生模型之后 **ä/ö/ü/ß 那一笔损失没有了**（罗马化整个退休），撇号那一笔是新的：这个词表里没有 `'`，`geht's` 的撇号被丢掉 —— 所以断词规则改成**只认空白**，词内的撇号与连字符不断词（否则会在音频里根本不存在词边界的地方插一个 `|`）。

---

---

### FR-16 内置词典

随包带一本德语词典，解决三件事：标记生词时不用手填性和复数（FR-7.4）、去重能按词元而不是表层形式（FR-9.3）、以及给预置词库的卡片提供卡背（FR-17）。

**产物由 `npm run build:dict` 编译**（`scripts/build-dict.mjs`），来源与许可见 §7.11。实测规模：

| | 数量 | 体积 |
|---|---|---|
| 词条 `public/dict/w/` | 153,040 | 26.2 MiB |
| 词形→词元 `public/dict/f/` | 304,957 | 8.9 MiB |
| 预置牌组 `public/dict/deck/` | 17,313 | 0.5 MiB |

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-16.1 | 词典切成 **256 个分桶 JSON**，查一个词只取一个桶（约 105KB） | 不做「载入整本词典」。§7.10 记着手机端最硬的约束是内存 —— 一次 `JSON.parse` 26MB 正是对齐权重被 iOS 杀掉的那种形状 |
| FR-16.2 | 查词支持**词形还原**：`Plattformen` → `Plattform`，`gelaufen` → `laufen` | 分桶的哈希函数在构建脚本与运行时必须逐字一致，否则症状是「一部分词莫名查不到」。`src/dict/bucket.test.ts` 钉死了一批词的桶号并**逐字比对脚本源码** |
| FR-16.3 | 字段：词性、**性**、主格复数、**IPA**、德语释义、英译、中译 | 释义优先德语 —— FR-14 在 DW 的 Glossar 上已经得出过同一结论（德德释义比中文更适合 C1）。IPA 单独存一个字段：这个应用的痛点就是听觉识别，「这串音对应哪个词」是卡背上最该看见的一行 |
| FR-16.4 | 标记生词时自动填（FR-7.4），**DW Glossar 优先** | 只补 prefill 没给的字段 |
| FR-16.5 | 内置词典查不到时，可联网查 **de.wiktionary**（设置里可关，默认开） | 直连，无任何中转，符合 §3.1.1 R-1。实测 `de.wiktionary.org/w/api.php` 带 `origin=*` 回 `access-control-allow-origin: *`；**DWDS 的 API 回 200 但不带任何 CORS 头**，浏览器里用不了，而 R-1 禁止自建代理，所以那条路是死的、不是「以后再做」 |
| FR-16.6 | 设置页显示诊断：词条数、分桶数、当前是 web 还是原生壳、能不能查到一个测试词 | 与 FR-15 的对齐后端诊断同一个理由：**「词典在不在包里」只能由设备自己回答**。web 版与原生壳的行为不同（下一行），而用户看到的症状一样都是「查不到这个词」 |
| FR-16.7 | 设置页**原样显示数据来源与许可**，从 `meta.json` 读 | **这是义务不是装饰**：词典是 CC BY-SA，且随应用分发。写在构建脚本注释或 `meta.json` 里都不算履行署名 |
| FR-16.8 | 牌组里每个词**预存最多 6 个 IPA 最近邻**（在**全牌组 1.7 万词**里找，不按档分开找），供 FR-10.10 的辨形题当干扰项 | 放构建期而不是运行时：两两比较是 O(n²)（1.7 万词是 3 亿对），构建期靠首尾音分桶预筛降到 **12.5M 对**、几秒跑完，手机上不可能。**「档内找」是第一版的写法，实测被推翻**：第 4 档只有 30% 的词凑得到三个邻居、34% 一个都没有 —— 3000 词里没有足够多的最小音对。而那条限制本来就立不住：用户不知道某个选项属于哪一档，他只知道词义，所以「这个词我认识、所以不是答案」那条排除法在辨形题上用不上。放开之后 **81% 的词凑够三个**（16360/17313 有近邻）。牌组从 0.46 MiB 涨到 **1.42 MiB**，产物 35.0 → 36.5 MiB。**没有 IPA 的词退到按词形相似**（band-4 实测 9/3000 缺 IPA） |
| FR-16.9 | 牌组词**带 1–2 条例句**，从 de.wiktionary 正文的 `{{Beispiele}}` 段抓，供 FR-10.3 的卡背用 | WikDict **没有例句表**（它只有词条/词形/翻译三张），所以这一条是唯一需要第五个数据源的字段。只给**牌组词**（1.7 万，占词条总数 11%）抓，不给全部 15.3 万 —— 例句只在复习卡背上出现，而只有牌组词会成为预置卡。实测覆盖率 50/50 抽样全有，清洗后仍可用 **96%**。选句规则：**无 `<ref>` 引注优先、15–140 字符、取最短两条** —— 带引注的是书面文学引文，长且难，不适合当卡背 |

**web 版与原生壳在离线上不同，这是有意的**：§7.6 定的 `globPatterns` 不含 `json`，所以 Service Worker 不预缓存词典 —— 35MB 的预缓存不划算，而且它是缓存层数据、不该由应用壳的缓存策略管。于是 web 版靠浏览器 HTTP 缓存、断网时查不到没查过的词；原生壳里 `public/dict/` 在 App 包内，完全离线。FR-16.6 把当前是哪一种显示出来。

---

### FR-17 预置词库

「初期笔记不多时也能练单词」。词从内置词典的牌组来，声音从 Wiktionary 的自由许可录音或系统 TTS 来。

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-17.1 | 六个档，按**口语词频名次**切：1–500 / 501–1500 / 1501–3000 / 3001–6000 / 6001–10000 / 10000 名以后 | 按名次切不按频次值切：频次是齐夫分布，按值切第一档只会有几十个词 |
| FR-17.2 | **界面上必须写明「这是词频档，不是 CEFR 等级」** | 官方 Goethe Wortliste 只有 A1(约 650)/A2(约 1300)/B1(约 2400)，**B2/C1/C2 没有官方词表**；那三份是 © Goethe-Institut，随 App 分发属于再发布，还撞 EU 数据库权（§ 87a/b UrhG）—— 而 §3.1 R-5 自己立的标准比法律更严。所以不用那套标签。**把词频档标成 A1/B2 是编数据**，比不标更糟 |
| FR-17.3 | **整档报名**：选中一档 → 「把这一档全部加入学习计划」（如第 4 档 3000 词）。面板显示「已激活 N / 剩余 M」 | 原文那条「加 N 个词（10/25/50）」**已废除**（变更 29）：它把「我要学哪些词」和「今天学几个」压成了同一个动作，于是每学完十几个词就要回去点一次按钮 |
| FR-17.4 | 报名**不等于建卡**。卡片由复习页**每天惰性激活**：今天新卡不足 `newPerDay` 就按名次补齐，顺手取这十几个词的发音，并在后台预取下一批 | **这是唯一能撑到 3000 词的形状。** 直接为整档建卡要 60 次 MediaWiki 往返（发音 50 词一批）+ 100 MB 以上的录音落库，那条路走不通。而「哪些还没学」**不必落库** —— 它等于「档内词表减去生词本里已有的词」，是个纯派生量（复用 FR-17.5 的词元键）。所以整档报名只写一条设置（`enrolledBands`），**不新增 store、不动 `DB_VERSION`**。<br>与变更 28（设置也同步）合起来还有一个白拿的好处：**报名跟着设置同步**，在桌面上报名、手机上就会开始发卡，而「发到第几个」不需要同步 —— 它是从生词本推出来的，而生词本本来就同步 |
| FR-17.4a | 默认**不报名任何档**；面板把第 4 档（3001–6000）作为推荐起点 | 前三档合起来约三千词，C1 的人基本全认识；从第 1 档开始等于要点几千次「太简单」才挖到有用的地方。默认空而不是默认第 4 档：报名是**一次明确的选择**（它会一路发卡几个月），不该由默认值替用户做 |
| FR-17.5 | 去重按**词元键**，与 FR-9.3 同一套 | 课上标过 `Plattformen`、预置词库又给一张 `Plattform`，是这个功能最容易犯的错 |
| FR-17.6 | **加词时就把发音下下来**，不等复习时再取 | 加词是一次必然在线的主动操作，而复习按 §2.1 是在碎片时间、手机上、很可能没网时做的。发音存 `wordAudio`（缓存层，不进备份）。**否定结果也要存**，否则每次复习到这张卡都要再问一遍 |
| FR-17.7 | 卡片**正面只有声音，没有文字**；并标出音源（真人 / 合成）与「孤立词发音，练不到连读」 | 给了文字，这张卡考的就不再是听觉识别了，而那是这个应用存在的理由（看词选意思**也**值得练，但它是另一张卡：FR-21）。真人音与合成音都没有时才退成文字卡，且必须明说（FR-10.5） |

**发音选文件有两条不能省的规则**（都在 `src/dict/audio.ts`，各有测试）：

1. **滤方言。** 只收 `De-*`（德语维基词典标准录音）与 `LL-Q188 (deu)-*`（Lingua Libre 德语）。实测 `einhergehen` 的第一个音频是 `Bar-einhergehen.ogg` —— 巴伐利亚方言。拿它练听觉识别比没有声音更糟：声音是对的那个词，发音却不是标准德语。
2. **在「本设备放得动的」里面挑，而不是按偏好序硬挑。** 覆盖率最高的 `De-*` 是 **Ogg Vorbis**，而 Safari / WKWebView 对它的支持一直是缺的。按偏好序硬挑的话，iPhone 上几乎每个词都会判成「没有真人音」而退到合成音 —— 明明 Lingua Libre 的 wav 就在旁边、放得动。

**这个功能是冷启动填充物，不是长期主线。** 两条来源都是孤立词发音，练得到词形↔读音的对应，练不到连读 —— 而连读才是精听真正的难点。长期该靠课程里的真语料。

---

### FR-18 学习记录与分享图

「像别的学习类 app 那样，出一张图：一句德语鸡汤，下面是我的名字、今天的日期、
学了多久、连续几天、一共几天。」这是用户的原话（2026-09-15）。

这个功能**一半以上的工作量不在图上**：在它之前，整个应用没有任何地方知道
「你昨天学没学」—— FSRS 只留每张卡最后一次复习的时刻，课程只有 `createdAt`。
所以先有 FR-18.1 的记录，才谈得上 FR-18.4 的图。

**FR-18.1 计时口径（`src/study/tracker.ts` + `src/study/surface.ts`）**

三个条件**同时**成立才累加，每 5 秒跳一次表：

1. 人在练习界面上：通听 / 跟读 / 学词 / 听写 / 复习。切句、生词本、来源、素材、
   设置、记录页都不算 —— 那些是准备与管理，不是练。
2. 页面可见（`document.visibilityState`）。切到别的 App、锁屏，表立刻停。
3. 最近 60 秒内有过动静：点过屏幕或按过键，**或者音频正在播**。

第 3 条里的「音频正在播」不是补丁，它是主场景 —— 通听一课十分钟一次都不会碰屏幕。
反过来，「暂停着放在桌上去泡咖啡」两个条件都不满足，一秒都不记。

**口径宁可少算**，因为这个数字要发给别人看。按 tick 累加而不是记「进入/离开」的
时刻差，也是同一个理由：时刻差要在每一个中断点（切后台、锁屏、路由变化、
被系统杀掉）都正确收尾，漏一个就把几小时算进去；按 tick 累加最坏错 5 秒。

**FR-18.2 三个数（`src/study/stats.ts`，纯函数）**

| 数 | 定义 |
|---|---|
| 今天 | 今天（**本地日**）累计的秒数 |
| 连续 | 到今天为止连着「学过」的天数。**今天还没学够时不归零，数到昨天为止** |
| 累计 | 一共有多少天「学过」 |

「学过的一天」= 当天累计 ≥ **60 秒**（`MIN_STUDY_SECONDS`）。有门槛是因为
「打开看一眼到期张数」「听半句就锁屏」都会留下十几秒的记录，把它们算成一天
是在骗自己。连续天数在今天不归零：一天还没过完，而「早上打开发现连续记录已经是 0」
会让人以为记录坏了；真的断了是在第二天体现的。

**FR-18.3 数据与同步**

`StudyLog` 在**标注层**（§2.3 的标准：除音频与原始文稿外一切都备份 + 同步），
存在 `meta` 的 `studyLog` 键上，同步文档 id 是 `study`。

形状是 **日期 → 设备 → 秒数**，不是「日期 → 秒数」：

```
{ "2026-09-15": { "a3f1…": 900, "77c2…": 420 } }
```

理由是合并必须**幂等**（同一份远端会被反复拉取，409 之后还要再合一次）。
「按天求和」不幂等，会越加越多；「按天取 max」幂等，但会把手机上那 7 分钟吞掉。
分设备存之后两条性质同时成立：**逐格取 max 是幂等的，展示时把一天里各格相加，谁也没丢**。
设备 id（`meta.studyDevice`）**不备份也不同步** —— 恢复到新设备上就该是一个新名字，
否则两台设备会抢同一格。

**不回填历史。** 记录从 FR-18 落地那天（2026-09-15）开始攒。能用来回填的只有
课程 `createdAt`、生词 `createdAt` 和 FSRS 的 `last_review`，而 FSRS 只留**最后一次**
复习的时刻 —— 拿它凑出来的「学过的日子」必然偏少且不均匀，而连续天数无论如何
也只能从今天算起。一个不准的数字发出去比没有更糟。

**FR-18.4 分享图（`src/share/card.ts`）**

1080×1350（4:5），canvas 直接画，形状见 §12.8。内容自上而下：
应用名 → 德语句子 → 署名 → 中译 → 细线 → 名字与日期 → 今天 / 连续 / 累计。

- **句子**内置约 24 条（`src/share/quotes.ts`），只收两类：德语谚语，以及**作者去世满 70 年、
  在德国已进入公有领域**的引文（歌德、席勒、尼采、里尔克、埃布纳-埃申巴赫、洪堡、维特根斯坦）。
  凯斯特纳（1974）、布莱希特（1956）那几句流传更广的**故意没收**：还在版权期内。
  中译是自己的译法，不抄译本。
- **底图**六张 CC0 照片（`public/share/photos/`，约 1.2MB，随包），作者与出处在同目录的
  `CREDITS.md` —— CC0 不要求署名，但拿了别人的东西总该说得出它是谁的。
- **名字**取 `Settings.displayName`；没设过就用同步账号的名字；设成空串就是「别印名字」，
  那时日期从右边换到左边（否则那一行左边一片空、右边贴边一行小字，像排版坏了）。
- 句子与底图**按日期确定性地选**：同一天打开十次是同一句同一张，否则那张图就不是
  「今天的」而是「刚才那一下的」。两个按钮各自往后数一张。

**FR-18.5 交付**：原生壳写进 Cache 目录再拉系统分享面板（WKWebView 不实现 `a[download]`，
走浏览器那条路是**静默什么都不发生**）；浏览器优先 `navigator.share({ files })`，
不支持则 `a[download]` 下载。用户划掉分享面板不算失败。

**FR-18.7 应用开着就保持屏幕常亮（`src/platform/wakeLock.ts`，2026-09-22）**

应用挂载时申请 Screen Wake Lock，卸载时还回去。**范围是整个应用，不跟路由走。**

它是需求不是锦上添花：iOS 默认 30 秒到 2 分钟自动锁屏，而这个应用的主场景恰恰是**一句话都不碰屏幕**
（通听一课十分钟只有耳朵在动，跟读循环三遍之间也没有任何触摸）。练到一半黑屏、想看一眼原文
还要先解锁 —— 而在 iOS 上锁屏还会把 `<audio>` 一起停掉。

**第一版把范围定成「练习界面」（共用 FR-18.1 的 `isPracticeRoute`），当天就推翻了。**
用户的原话：「常亮改为整个 app 吧，只要我 app 处于打开状态，就常亮」。他是对的，
而且理由比省电重要 —— **边界本身就是一个 bug 面**：在生词本里读一段 AI 写回来的辨析、
在查词面板上看变形表、贴一篇译文，全都是「盯着屏幕但不碰屏幕」，而它们一个都不在那五个界面里。
一条「有时候亮有时候不亮」的规则，比不亮更让人不信任。

代价明确而且**自带上界**：页面一不可见系统就收回这把锁，所以「放在桌上忘了关」
最多亮到手机自己被按灭那一下为止。

**锁必须能被要回来。** 规范规定页面一不可见（切走、锁屏、来电）浏览器就**自动释放**这把锁，
而回到前台**不会**自己还给你。所以要监听 `visibilitychange`，回前台时若仍在练习界面就再申请一次 ——
少了这一步的表现是「切出去回来一次之后，常亮就永远失效了」，一个只在特定路径上复现的静默失效。

**不给开关**：页面一不可见就自动释放，代价有上界；而一个「要不要让屏幕在我用这个应用时
保持亮着」的开关，没有人有信息去回答它。真要关，把手机的自动锁屏调回来就是 ——
那本来就是系统给的那个开关。

**诊断行要报三件事，不是一件**（设置页 → 版本 → 「这台设备的边距与常亮」）：
`supported` / `wanted` / `held`，外加**最近一次申请的结果与时刻**。
因为「屏幕没保持亮」有三个完全不同的成因、三个完全不同的下一步：
没有这个 API（只能去原生侧动 `isIdleTimerDisabled`，要出新包）／申请被拒（省电模式、系统策略）／
拿到了但又被系统收走。只报「此刻有没有拿着」说不清「刚才那次到底成没成」，而那才是要诊断的。

**iOS 上现在有两道防线，不再只靠 Web API（变更 52，2026-09-23）**。起因是用户在
**非省电模式下**报了「申请被拒」——Web Wake Lock 是浏览器标准，WKWebView 认不认、
什么时候放行不由这个应用控制，而原来 `catch {}` 把异常整个吞了，连拒绝的是哪一种都
没留下。现在做了三件事：① 记下 `err.name`/`err.message`（不能用 `err instanceof Error`
判——真机抛的是 `DOMException`，不一定是 `Error` 子类，改成按「有没有 `name`/`message`
这两个字符串字段」认）；② 被拒之后等**下一次真实用户手势**（`pointerdown`）自动再申请
一次（`armGestureRetry`，一次被拒只挂一个监听器）——有的浏览器把 Wake Lock 的申请绑在
一次用户激活里，应用挂载那一刻不算数，光靠 `visibilitychange` 不够，那条路只在切后台
再回来时触发；③ **`SceneDelegate.sceneDidBecomeActive`/`sceneWillResignActive` 直接拨
`UIApplication.isIdleTimerDisabled`**，不经过 WKWebView 的 Wake Lock 实现，也就不受它
认不认的影响——这份工程有 `UIApplicationSceneManifest` + `SceneDelegate`，场景生命周期
起了之后 `AppDelegate.applicationDidBecomeActive` 根本不会被调用，所以必须挂在
`SceneDelegate` 上。两道防线并存、互不依赖：原生那边坏了不影响 JS 这边继续申请，
JS 这边被拒也不代表原生那边没接管。**低电量模式下两道都会被系统忽略**，那一档没有
代码能治，诊断行如实说。**这一条要等下一次真机验：屏幕放着五分钟不碰灭不灭。**

**FR-18.6 不做的**：不让用户自选照片（要处理任意尺寸、方向、EXIF 旋转，以及一张亮到
看不清白字的图）；不做模板与配色选项；不做周报月报；不把记录页放进导航（§12.1 的判据）。

### FR-19 逐句译文

「我会用 Claude 对文章进行翻译，这样便于我准确理解文章的意思。」（用户原话，2026-09-15）

**应用自己不翻译。** 它不调任何翻译接口，只做一件事：把在外面译好的一整篇中文
切回一句一句，挂到对应的句子上。动线是**复制走 → 在外面翻 → 粘回来**。

**FR-19.1 复制走（`translationRequest`）**：提示词 + 编号原文，一句一行。编号是
**界面上的显示号**（排除句不占号，也不出现 —— Glossar 那类段落不朗读，也没有翻译的必要）。
提示词里的三条要求**刻意不用数字编号**：它们会和课文的编号混在一起，既容易让对方数错，
也会让解析器把提示词本身当成前三句的译文。

**FR-19.2 粘回来（`parseTranslations`）**：**用编号当锚，不按行数猜。**

按行数对齐看起来更省事，但翻译工具多写一行说明、少一个空行，整篇就错位一句 ——
而**错位的译文比没有译文更糟**：跟读时你看到的是下一句的意思，还不知道自己被骗了。
编号解析失败是显性的（「48 句认到 46 句」），行数对齐失败是静默的。

| | 规则 |
|---|---|
| 宽容 | 行首的 `-`/`*`/`>`、`**加粗**`、`1.` `1、` `1)` `1：` 四种分隔符、前言与结语（落不到编号上，自然被忽略）、一条译文占好几行（续行接在上一条后面，空行结束这一条） |
| 严格 | **没有编号的行永远开不了一个新条目**；空条目不写入；同一个编号出现两次，后一次赢（补贴一段修正） |

**FR-19.3 写回（`applyTranslations`）**：**这次没给到的句子保留原有译文**（一篇太长分两次贴
是常事），要全清有单独的出口。解析出来但这一课里不存在的编号**报出来**，不静默丢弃 ——
那多半意味着贴错了课，或者贴的是重新切句之前的那一份。保存前有一屏**原文/译文对照预览**：
编号是锚，但锚本身贴错了这件事只有人眼能发现。

**FR-19.4 显示**：通听（展开文本后每句下面一行中文）与跟读（当前句下面一行中文）。
开关是**同一个**（`Settings.showTranslation`，跟着人走因而同步到所有设备），
**默认关** —— 和 FR-5.1 让文本默认折叠是同一个理由，而且对译文只会更强：
一眼看得到中文，耳朵就不必再努力了。没贴过译文的课上不出现这个开关。

**FR-19.5 数据**：`Sentence.translation`，标注层（跟着 `Lesson` 走，备份/同步/合并一行没改）。
结构编辑三处都要跟着动，漏一处就是静默错位：**合并**两段译文接起来；**拆分**整份留给左半句
（译文切不开，而留下的那份至多是「多说了后半句的意思」，不会指向别的句子）；
**重新切句**按文本认领搬过去，且「只有译文」的旧句也算值得抢救。
**绝不进 `ShareablePackage`**：译文是原文的衍生作品（§3.1）。

### FR-20 课程笔记

「我会自己写一些要点。」（用户原话，2026-09-15）

`Lesson.notes`，一块纯文本，**只做课程级**：句级那一层已经被挖空和生词本占满了，
再加一层句级笔记，过几周就想不起来某条东西当初记在哪一层；而「这篇在讲什么」
本来就是关于整篇的。

位置在**通听页底部**的折叠块（有内容时默认展开，摘要上带行数）——
写笔记和读笔记是同一件事的两半，分到两个地方去等于每次都要先找一遍。

**保存**：停手 800ms 自动存，另外在失焦与**卸载**时各存一次。切 tab 就是卸载，
而「切走一下回来发现刚写的没了」比任何多余的保存按钮都糟。空笔记不留字段。
和译文一样在标注层，一样不进 `ShareablePackage` —— 笔记里大概率抄着原句。

### FR-21 识词卡（读 → 义）

「复习的时候，我想加一个识词的练习，类似看到词之后选择正确的意思。」（用户原话，2026-09-21）

**这不是给现有卡换个题面，是给同一个词开第二张卡。** 直接起因是 FR-9.5 加进来的那批
课上的词：它们是读到的、不是听到的，而在此之前它们在复习里只有「听孤立词发音四选一」
这一条路 —— 对一个从纸上认识的词来说，那一关不是最该先过的。

§7.5 原来把这件事排除在外，措辞是错的（已同步修正）：看词选意思是**视觉识别**方向，
生产方向是「中文 → 德语」（要自己产出德语）。§7.5 自己的论据反过来同样成立 ——
听和看是两套独立表征，「看得懂但听不出来」和「听得出来但看到不确定什么意思」是两回事。

| 编号 | 需求 | 验收 |
|---|---|---|
| FR-21.1 | 一个 `VocabEntry` 挂**两张独立调度的卡**：**听卡**（现有 `fsrs`，FR-10）与**读卡**（新增 `fsrsRead`）。两张卡各有各的 FSRS 状态、各自算评分（`gradeFromAnswer()` 两边通用） | 这是 Anki 的 card type 模型：一个 note 生成多张卡，各自调度。**不做「同一张卡随机换题面」** —— FR-10.9 已经用过这条论据（「音都还没认住时问意思，等于一道题同时考两件事，答错了也说不清错在哪一环」），听和读混进同一条稳定度曲线是同一个错误，只是尺度更大：FSRS 攒出来的稳定度成了两种能力的混合值，而用户永远看不出自己是听不出来还是不认识 |
| FR-21.2 | 读卡**惰性开**：听卡进入 `Review` 状态之后，读卡才在复习页被激活，且**受 `newPerDay` 约束**（与听卡的新卡共用同一个额度） | `fsrsRead === undefined` 就是「还没开」—— 不需要额外字段、不需要新 store，与 FR-17.4 的每日惰性激活是同一个形状、同一段代码位置。不一开始就开两张：新词同一天考两遍是在背字形不是在学词；而且复习量一次翻倍会直接把队列冲垮（`reviewPerDay` 60 是按一个词一张卡定的） |
| FR-21.3 | **同日互斥**：同一个词的两张卡，同一天只出一张，**听卡优先**；被让开的那张顺延到下一天，**不算逾期** | 就是 Anki 的 sibling burying，它默认开着是有理由的：先做完听音辨义、再做看词辨义，第二张是白送 —— 答案几分钟前刚在眼前出现过，那一次答对喂给 FSRS 的是假信号。听卡优先是因为它是主线（§7.5 的痛点判断没变） |
| FR-21.4 | 读卡题型按卡龄渐进：`New` / `Learning` / `Relearning` → **看词形选释义**（`read-gloss`）；`Review` → **看释义选词形**（`read-form`）与**句中挖空**（`cloze`）**按 `reps` 奇偶交替** | 与 FR-10.9 同一条阶梯逻辑：先认出来，再想起来。交替而不是随机：随机会连着出五张同题型，而这两种题一共只有两种。也**不是「有句子就出 cloze、没句子退 read-form」** —— 那样 `read-form` 成了降级项，而它考的东西（**无语境地**想起词形）本身值得单独练，语境是拐杖。凑不到干扰项或没有句子时退回 `read-gloss`（choices.ts 规则⑥：题变简单，不抛异常） |
| FR-21.5 | 读卡正面是**文字**，不自动播音；卡面必须与听卡**一眼可分** | FR-10.2「正面只有声音、不给任何文字」是**听卡**的规则，不是全局规则（措辞已同步修正）。两种卡长得像的代价很具体：进卡先等一秒声音、等不到才反应过来这是读卡 —— 每天几十次。形状见 §12.13 |
| FR-21.6 | `cloze` 的句子**在开读卡时拷进词条**（`VocabEntry.examples`，最多 2 条），不在复习时现查词典 | 与 §2.3 那条「建卡时拷值」逐字同理：词典是缓存层、可重建，而**复习过的卡不能变** —— 换个词典版本例句就换了，那道 cloze 题也跟着换，FSRS 攒的那段历史对不上它。三个来源：课程卡用 `contextSentence`（100% 有）；预置卡用牌组例句（FR-16.9，抽样 96% 可用）；**`lookup` 卡在加词时顺手收下 de.wiktionary 的例句** —— FR-9.5 那一趟本来就抓了，只是没存，理由与 FR-17.6「加词时就把发音下下来」相同（加词必然在线，复习很可能没网） |
| FR-21.7 | `cloze` **与 `read-form`** 的干扰项取**同词性 + 词频名次接近**，**不用 IPA 近邻** | FR-16.8 预存的那 6 个近邻是为**辨音题**造的。`heulen` 填进 `teilen` 的空里一眼就假 —— 那道题不用读懂句子就能做对，与 FR-10.10 对辨形题的要求是同一条，只是方向相反：辨音题要音近，cloze 要**语义上讲得通、搭配上不对**。课程卡没有 `rank`，退到生词本里同词性的词（零请求）。**实现期补两条**：① 牌组文件里**没有词性字段**（`DictDeck.words` 只有 `w`/`r`/`d`），所以预置卡那条路只做得到「词频名次接近」，同词性那一半只在课程卡上生效 —— 要两者兼得得为每个干扰项查一次词典，那是每张卡三四次查词、最多四个 105KB 的桶；② 名次最近的那几个词**要再随机抽一次**再进选项，否则同一张卡每次出题的四个选项一模一样，几轮之后记住的是位置不是词 |
| FR-21.8 | 挖空遮的是**句中那个词形**（不是词元）；同一句里目标词的**别的变形也要一起遮**；空位**宽度固定**，不随词长变化 | 与 choices.ts 规则⑤（释义里的词头要遮掉）是同一类错误：例句里目标词出现两次，只遮一次等于把答案印在题面上。空位宽度不固定则是把词长泄出去 —— `___` 和 `_________` 之间，四个选项里能一眼排除掉两个 |
| FR-21.9 | 中译按 FR-19 的形状**在外面补齐**：导出缺中译的词条（编号 + 词形 + 德语释义）→ 在外面翻 → 按编号粘回来。补齐之前，**中文只出现在卡背**，题面一律德语 | 实测中译覆盖 **60%**、德语释义 **99.9%**（FR-10.9），所以中文四选一在 40% 的词上凑不齐三个干扰项 —— 而「有的词从不出中文题」用户看不出原因，只会觉得没规律。应用自己不翻译（与 FR-19 同一条立场）。解析复用 `parseTranslations`（**编号当锚，不按行数猜**），写回落在 `VocabEntry.meaningZh`，标注层。**编号是导出那一刻的序号，不是词条的稳定标识**（词条没有显示号那样的东西），所以顺序固定为 `createdAt` 升序 —— 期间新加的词排在末尾，不会挪动前面的编号；删过词就会错位，而**那一步由人眼挡住**：保存前有一屏「德语词 → 中文」对照预览，与 FR-19.3 同一条理由（锚贴错了这件事只有人眼发现得了）。形状见 §12.14 |
| FR-21.10 | `fsrsRead` 与 `examples` 都在**标注层**，跟着 `VocabEntry` 走 | `fsrsRead` 是复习历史，丢了等于那一半的记忆曲线归零。`examples` 看着像可重建（词典里就有），但理由与 FR-21.6 同一条 —— 拷进来之后它就是那张卡的一部分。合并按 §2.4 的 last-write-wins 走**整条**词条，不用动同步协议。**但那条规则的口径要补全**（实现期发现）：原来比的是 `fsrs.last_review`，只看听卡 —— 在桌面上只复习了读卡、手机上后来复习了听卡，合并时手机那条整条胜出，桌面那轮读卡的记录**静默消失**。改成取两张卡 `last_review` 的**较大值**（`mergeVocabEntries`，有测试）。剩下的局限是既有的、不是 FR-21 带来的：**两台设备在各自的「今天」各复习一张卡，整条覆盖仍会丢掉一边**（FR-21.3 的同日互斥只在单机上成立）。粒度要细到「按卡合并」才能根治，那要给每张卡各存一个时间戳并改写合并函数 —— 为一个一年遇不上几次的场景不值得 |
| FR-21.11 | 读卡与听卡共用「**没听清 / 不认识**」那个出口（FR-10.8）与答对的 600ms 一次性（FR-10.11） | 四选一的 25% 瞎猜命中率在读卡上一模一样，FR-10.8 那条理由一个字都不用改。600ms 那一闪在读卡上更该留：`read-form` 和 `cloze` 的选项就是光秃秃的词形，**冠词只能在那里出现** |

**不做的**：手打拼写（Quizlet / Memrise 升到最后那一档是**生产方向**，§7.5 排除）；
读卡上的发音按钮（那是听卡的事，两张卡各练一件事，给了按钮它们又变回一张）；
第三张「句中用」卡（cloze 与 `read-form` 考的是同一条记忆链的深浅，拆开只是把复习量 ×3）。

---

## 6. 数据模型（修订版）

模型按 §2.3 的两层切开。**标准（§0 变更 27）：缓存层只有音频和原始文稿，其余一切都在标注层。**
那两样的凭据是**标注层记着它们的下载地址**，不是「可重建」。

```ts
// ══════ 标注层：备份 + 同步 ══════

interface Lesson {
  id: string;
  title: string;                 // "Alltagsdeutsch: Der deutsche Wald"
  source:
    | { type: 'dw'; dwLessonId: string; sourceUrl: string }   // 可补齐（FR-3.5）
    | { type: 'manual'; audioFileName?: string;               // 不可补齐（FR-3.6）
        audioFiles?: string[] };                              // FR-1.8：几轨拼成一课时的文件清单（按拼接顺序）
  audioSrc?: string;             // 音频的原始下载地址（mp3 直链）。音频本身不备份，这一行备份
  audioDuration?: number;        // 秒；补齐或重绑定时校验时长
  audioBytes?: number;           // 音频字节数。**「DW 换没换过音频」的判据**（§0 变更 43）
  manuscriptHash?: string;       // plainText 的 hash；补齐后校验 DW 是否改过稿（FR-3.7）
  collection?: string;           // FR-1.9 课程列表按它分组（「Aspekte neu C1」）
  speakers?: string[];           // FR-1.7 切句时从行首剥掉的说话人标记；重切沿用同一份
  sentences: Sentence[];
  glossary?: GlossaryCandidate[];// FR-14 候选词。「还没接受哪些」是跨设备的待办
  notes?: string;                // FR-20 这一课的要点，一块纯文本。只做课程级
  createdAt: number;
  updatedAt: number;             // 跨设备合并用（§2.4）
}

interface Sentence {
  index: number;
  text: string;
  charStart: number;             // 在 LessonCache.plainText 中的 offset（FR-2.4）
  charEnd: number;
  startTime?: number;            // 秒，未标注为 undefined
  endTime?: number;              // 显式标记；否则按 FR-4.4 推断
  endTimeExplicit: boolean;      // 区分显式与推断
  timingSource?: 'auto' | 'manual';  // FR-15；缺失且有 startTime = FR-15 之前手打的
  timingConfidence?: number;     // 该句对齐路径的平均 log-prob（FR-15.3）
  words?: WordSpan[];            // FR-15.2 词级时间戳，句内 offset（变更 26 搬进标注层）
  translation?: string;          // FR-19 中文。人在外面译好、按编号粘回来的
  blanks: Blank[];
  markedDifficult: boolean;      // 跟读时跟不上的句子
  excluded: boolean;             // 非朗读内容，如 Glossar（FR-1.4）
  speaker?: string;              // FR-1.7 这段话的说话人标记，只挂在一段话的第一句上
}

interface WordSpan {
  charStart: number;             // 句内 offset，与 Blank.ranges 同坐标
  charEnd: number;
  start: number;                 // 秒
  end: number;
}

interface Blank {
  id: string;
  ranges: Array<{ start: number; end: number }>;  // 句内 offset；多区间支持 "hing ... ab"
  surface: string;               // 拼接后的表层形式："gelaufen" / "hing ab"
  lemma?: string;                // V2 填充
  vocabEntryId: string;          // 反向关联，听写结果回写 FSRS（FR-8.6）
}

interface VocabEntry {
  id: string;
  surface: string;
  lemma?: string;                    // FR-16 起由内置词典填
  gender?: 'm' | 'f' | 'n';
  plural?: string;
  meaning?: string;
  ipa?: string;                      // FR-16.3；痛点是听觉识别，这一行该在卡背上
  // 来源：课程 或 预置词库（FR-17）。下面三个字段**只有课程来的词条才有**。
  // 改成可选而不是给预置卡填 '' / -1，是为了让类型检查逼着每个读它们的地方
  // 显式处理「没有原句」—— 哨兵值只会把问题推到运行时，变成卡面上一句空白的原句。
  contextSentence?: string;          // 原句，只存本地，永不进 ShareablePackage
  lessonId?: string;
  sentenceIndex?: number;
  preset?: { band: number; rank: number };  // FR-17；band 是词频档，不是 CEFR
  lookup?: true;                     // FR-9.5：查词面板直接加的，没有课可去
  dwKnowledgeId?: string;
  hasTimestamp: boolean;
  suspended: boolean;
  // ══ 两张独立调度的卡（FR-21）══
  // fsrs 是**听卡**（FR-10），fsrsRead 是**读卡**（FR-21）。分成两个字段而不是
  // 一个数组：它们不是同质的 N 张卡，题型、开卡条件、降级出口都不一样，
  // 而数组会让每个读它的地方先问一句「第 0 个是哪种」。
  fsrs: FSRSCard;
  fsrsRead?: FSRSCard;               // undefined = 读卡还没开（FR-21.2），不需要额外的开关字段
  examples?: string[];               // FR-21.6：cloze 用的句子，开读卡时从词典拷进来，最多 2 条
  meaningZh?: string;                // FR-21.9：中译，靠「外面翻、粘回来」补齐；词典只覆盖 60%
  createdAt: number;
  updatedAt: number;
}

interface Settings {
  newPerDay: number;             // 10
  reviewPerDay: number;          // 60
  shadowingGapRatio: number;     // 1.2
  shadowingRepeat: number;       // 2
  playbackRate: number;          // 1.0
  dictationStrictCase: boolean;  // true
  presetBand: number;            // 4；面板上的推荐起点（FR-17.4a）
  showTranslation: boolean;      // FR-19.4 通听/跟读里显示中文。默认 false，先用耳朵
  enrolledBands: number[];       // []；已报名的词频档（FR-17.4），卡片每天惰性激活
  updatedAt?: number;            // 设置整体同步的定序键（§0 变更 28）；只有 useSettingsStore.update 写它
  lastBackupAt?: number;         // 备份提醒（FR-11.4）
  displayName?: string;          // FR-18.4 印在分享图上的名字。缺失 = 用同步账号的名字；空串 = 别印
}

// FR-18：学习记录。日期 → 设备 → 秒数（不是「日期 → 秒数」，理由见 FR-18.3）
interface StudyLog {
  days: Record<string, Record<string, number>>;  // 'YYYY-MM-DD'（本地日）→ deviceId → 秒
  updatedAt: number;
}

// ══════ 缓存层：本机持有，不同步，可随时丢弃 ══════
// 只剩音频和原始文稿两样，凭据是它们的地址记在标注层里（Lesson.audioSrc / source.sourceUrl）。
// 其余三个字段是关于本机那份文件的记账，不是内容。

interface LessonCache {
  lessonId: string;              // = Lesson.id
  manuscriptHtml?: string;       // DW 原始 HTML；手动导入时存粘贴的原文
  plainText?: string;            // 转换后的纯文本，Sentence.charStart/charEnd 的基准
  hasAudio: boolean;
  audioBytes: number;            // 占用统计（FR-3.8），读它不必载入 Blob
  fetchedAt: number;
}

interface GlossaryCandidate {
  dwKnowledgeId: string;
  sentenceIndex: number;
  ranges: Array<{ start: number; end: number }>;  // 句内 offset
  surface: string;               // "Plattformen"
  title: string;                 // "Plattform, -en (f.)"，原样保留以便降级（FR-14.4）
  lemma?: string;                // 解析出的 "Plattform"
  gender?: 'm' | 'f' | 'n';
  plural?: string;               // "-en"
  meaning?: string;              // Knowledge.text 的纯文本
}

// 可分享包（V1 只实现函数 + 测试，不暴露 UI）
interface ShareablePackage {
  formatVersion: 1;
  sourceUrl?: string;
  title?: string;                // 仅内容标识，如 "Alltagsdeutsch, 2025-11-03"
  timings: Array<{ index: number; start: number; end: number }>;
  blanks: Array<{
    sentenceIndex: number;
    ranges: Array<{ start: number; end: number }>;
    lemma?: string;
  }>;
  // 绝不含：plainText / Sentence.text / Blank.surface / contextSentence
}
```

**IndexedDB object stores**：

| store | 层 | 内容 |
|---|---|---|
| `lessons` | 标注 | `Lesson`（含 `sentences`、`blanks`） |
| `vocab` | 标注 | `VocabEntry` |
| `settings` / `meta` | 标注 | 设置（整体同步，§0 变更 28）、学习记录（`studyLog`，FR-18）、同步会话与版本号、上次备份时间等。**会话令牌、版本号与 `studyDevice` 不进备份**：前两个分别是凭据和「这台设备记的远端版本」，第三个是「这台机器在学习记录里占哪一格」—— 恢复到新设备上就该换一个 |
| `lessonCache` | 缓存 | `LessonCache`（原始文稿 + 体积元数据）。Glossar 候选词与词级时间戳**已搬进标注层**，见 §0 变更 26/27 |
| `audioBlobs` | 缓存 | `lessonId → Blob`，**单独一个 store** |
| `wordAudio` | 缓存 | 词元键 → 发音（FR-17.6）。**DB_VERSION 2 新增**。不并进 `audioBlobs`：那个 store 的键是 `lessonId`，混在一起会让 FR-3.8 的「按课清缓存」把发音一起删掉 |

音频单独开 store，是为了让 FR-3.8 的占用列表和缓存命中判断能只读元数据，不必把 6–10 MB 的 Blob 载进内存。

**FR-15 带来的字段**（实现期）：

| 字段 | 层 | 为什么在这层 |
|---|---|---|
| `Sentence.timingSource: 'auto' \| 'manual'` | 标注 | 「机器给的」和「我确认过的」必须能分开，否则待校对列表不可信。它和 `endTimeExplicit` 是**两个不同维度**：自动对齐的终点是显式的（最后一个音素的止帧），但一点也不等于确认过。缺失值不是「未知」而是「FR-15 之前的老数据」，按人工处理（FR-15.4） |
| `Sentence.timingConfidence: number` | 标注 | 该句对齐路径的平均 log-prob。跟着时间戳走，所以跟时间戳同层；人工改过就清掉 |
| `Sentence.words: WordSpan[]` | 标注 | **原来在缓存层（`LessonCache.wordTimings`），2026-09-02 搬到标注层**，见 §0 变更 26。「丢了能不能重建」这条标准要**按设备问**：桌面上有音频+文稿就能重算，手机上跑不动对齐模型，重算不出来。放缓存层等于「只有对齐过的那台机器能逐词高亮」，而它是用户手里的东西 —— 用户的东西一律要备份。一课约 800 条、约 30KB；同步是按课分片的（`lesson:<id>`），只有改过的那一课会推 |
| `Settings.autoAlignOnImport: boolean` | 标注 | 与其他设置同层，跟着账号同步（§0 变更 28）。**它不是「这台设备能不能跑对齐」的开关** —— 那件事记在黑匣子里（localStorage），所以把它同步过去不会让手机反复去撞一个跑不动的模型 |
| `VocabEntry.preset` / `ipa` / 词典填的 `gender`/`plural`/`meaning` | 标注 | **看着像可重建，其实不是。** 词典本身是缓存层（可由 `build:dict` 重建），但值一旦被拷进词条就归标注层：词典换个版本释义可能就变了，而**复习过的卡不能变** —— 卡背改了，FSRS 攒下来的那段历史就对不上它了。所以建卡时**拷值**，不在复习时现查 |
| `public/dict/**`（词典产物） | **缓存** | 完全可由 `npm run build:dict` 从公开数据重建 —— 就是「丢了能重建」那条标准。所以既不进备份也不进 `ShareablePackage`。它入 git 是另一件事（省掉 CI 那 1GB 下载），与分层无关 |
| `wordAudio` | **缓存** | 同上，能从 Wiktionary 重新取 |

因此**句级时间戳进备份、词级不进**。这正好让「桌面预处理、手机只学习」成立：手机换设备打开，句子照样能播；词级时间戳缺了就退回句级播放，需要时本机再算一遍。

**关于一处刻意的冗余**：`Sentence.text` 和 `VocabEntry.contextSentence` 严格说可以由 `LessonCache.plainText` + offset 推导出来，放进标注层是重复存储。这是**故意的** —— 它让标注层在 DW 撤稿、改稿或你清空缓存之后**依然完整可读**：句子还在，生词的语境还在，复习照常，只是没有音频。为了这个，多存几百 KB 完全值得。反过来，`plainText` 本身不进标注层，因为它能从这些句子拼回来。

---

## 7. 技术决策

### 7.1 句子切分

`Intl.Segmenter('de', {granularity:'sentence'})` 初切（Chrome 87+ / Safari 14.1+ / Firefox 125+），随后按序应用合并规则：

| 规则 | 条件 | 说明 |
|---|---|---|
| **R-lower** | 下一片以**小写字母**开头 → 合并 | 最强的启发式：德语所有名词大写、句首必大写，小写开头几乎一定是误切 |
| **R-abbr** | 本片以已知缩写结尾 → 合并 | 见下表 |
| **R-ordinal** | 本片以 `\d+\.` 结尾 → 合并 | `am 3. Oktober`、`im 19. Jahrhundert`（这类靠大小写无法判断，因为 Jahrhundert 本就大写） |
| **R-initial** | 本片以单个大写字母 + `.` 结尾 → 合并 | `A. Merkel` |
| **R-quote** | 引号或括号未闭合 → 合并 | — |

缩写表（可在设置中扩充）：
`z. B.` `u. a.` `d. h.` `bzw.` `usw.` `evtl.` `ggf.` `ca.` `vgl.` `bspw.` `i. d. R.` `z. T.` `v. a.` `o. Ä.` `u. Ä.` `Dr.` `Prof.` `Nr.` `Abb.` `S.` `Jh.` `Mio.` `Mrd.` `St.` `Str.`
（同时匹配带空格与不带空格的变体：`z. B.` 与 `z.B.`）

**无论规则多好，FR-2.3 的手工修正都是必须的。** 规则只是把需要手工修的句子从 30 句降到 3 句。

### 7.2 时间戳

DW 只给文本和音频，不给对应关系，映射必须自己造。V1 用手动打点：工程量接近零，且符合实际使用——一期里真正需要反复听的可能就五六句，全部标注多数用不上。

数据模型已留好 `startTime` / `endTime`，V2 接自动对齐（aeneas / Montreal Forced Aligner / whisper.cpp-WASM）时不改模型，只加一个写入器。

### 7.3 音频播放

- **全局单例 `<audio>` 元素**（iOS 手势链约束，§3.2）。
- 句子播放 = 设 `currentTime = start` + `play()`，检测越过 `end` 后 `pause()`。`timeupdate` 触发频率约 4Hz，精度不够，用 `requestAnimationFrame` 轮询 `currentTime`。
- 跟读循环用**状态机**实现，不用嵌套 `setTimeout`：
  `IDLE → PLAYING → GAP → (repeat--) → PLAYING | NEXT`
  变速、跳句、暂停都作为状态机事件处理，避免定时器泄漏。

### 7.4 听写校验分级

先做归一化：`trim` + 折叠连续空白。然后：

| 结果 | 判定 | 处理 |
|---|---|---|
| **正确** | 完全一致（含大小写、含变音符） | FSRS `Good` |
| **转写等价** | `ae/oe/ue/ss` ↔ `ä/ö/ü/ß`，**以及去掉变音符的写法**（`a` ↔ `ä`） | 不计错，提示「注意变音符」，FSRS `Good`（照顾没有德语键盘的场景） |
| **仅大小写错** | 忽略大小写后一致 | FSRS `Hard`，提示「注意大写」（`dictationStrictCase=false` 时按正确处理） |
| **错误** | 其他 | FSRS `Again`，展示字符级 diff |

注：`ß/ss` 在瑞士德语中是合法变体，但 DW 是标准德语，故归入「转写等价」而非「正确」——给提示但不惩罚。

**实现期修订（2026-08-31）**：上表原本只写 `ae/oe/ue/ss ↔ ä/ö/ü/ß`，但 §10 验收清单要求把 `Hauser`（对 `Häuser`）判为「转写等价」—— 而 `Hauser` 既不是 `Häuser` 也不是 `Haeuser`，它是**去掉变音符**的写法。两者取并集：`ä` 同时接受 `ae` 和 `a`。理由与规则本身的理由是同一个 ——「照顾没有德语键盘的场景」，而没有德语键盘的人打出来的正是 `Hauser`。代价是 `schon`/`schön` 这类真实对立也会被放过，但它仍然给「注意变音符」提示，只是不惩罚；相比之下判成全错、丢进 `Again`，惩罚要重得多。

### 7.5 FSRS 与卡片方向

用 `ts-fsrs`。卡片方向是**识别**方向，不做**生产**方向（「中文 → 德语」，要自己产出德语）。

识别方向有两个，各是一张独立调度的卡：**听觉**（FR-10，主线）与**视觉**（FR-21）。

理由：最初的痛点是听觉识别 —— 看到 `Zuversicht` 认得，听到 /ˈtsuːfɐˌzɪçt/ 反应不过来。
这条判断没变，听卡仍是主线（同日互斥时它优先，FR-21.3）。但**同一条论据反过来也成立**：
听和看是两套独立表征，「看得懂但听不出来」和「听得出来但看到不确定什么意思」是两回事 ——
FR-9.5 之后生词本里进了一批**课上的词**，它们是读到的，视觉那一头反而是空的。

**原文这一节把「看词选意思」归成了生产方向的卡，那是错的**（FR-10.2 里有同样的措辞，
一并改了）：它不要求产出任何德语，是纯识别。生产方向仍然不做。

### 7.6 技术栈

| 层 | 选择 | 理由 |
|---|---|---|
| 构建 | Vite | — |
| 框架 | React + TypeScript | — |
| PWA | `vite-plugin-pwa` | manifest + Service Worker；只缓存应用壳，不缓存学习数据。更新走 `registerType: 'autoUpdate'` + 自己注册（`src/platform/pwa.ts`）：新壳激活时页面自动刷新，并每小时/回前台/刚联网各探测一次（变更 30） |
| 存储 | IndexedDB via `idb` | localStorage 存不下音频 |
| SRS | `ts-fsrs` | 成熟实现，调度质量明显优于 SM-2 |
| 状态 | Zustand | 单用户小应用，不需要 Redux |
| 样式 | Tailwind | 个人工具，出界面快 |
| 测试 | Vitest | 至少覆盖：切句规则、听写校验、`toShareablePackage`、合并逻辑、各来源 extractor |
| 抓取 | 浏览器原生 `fetch` + `DOMParser` | 无后端、无第三方库。RSS 是 XML，`DOMParser` 直接解析；页面只需要内嵌的 JSON（§7.8） |

### 7.7 V2 预留（不在 V1 实现，但不挡路）

- **语境释义**：优先走 Claude API（把「词 + 整句」一起发过去，返回该语境下的释义、支配介词），省掉 spaCy 那层 Python 基础设施。PWA 直连需要 `anthropic-dangerous-direct-browser-access` header 或一个极薄的代理。
  ~~词形还原~~ 已由 FR-16 的词形索引解决（30 万条词形→词元），**但可分动词仍然不行** ——
  `hing ... ab` → `abhängen` 要句法分析，查表查不出来，这一半仍留给 V2。
- ~~Wiktionary 离线词典（Kaikki.org 解析后 JSON dump）~~ **已在 FR-16 落地**，
  但换成了 WikDict 而不是 Kaikki：Kaikki 的 de-extract 是未裁剪的解析结果（GB 级、一条词条几十个字段），
  而 WikDict 已经把 DBnary 归一化成「词条 / 词形 / 翻译」三张表，要的六样东西直接是列 ——
  少写一层解析器，也少一层解析出错的地方。署名照旧是义务（FR-16.7）。
- ~~TTS~~ 已在 FR-17.6 作为发音的兜底落地（`speechSynthesis`）。
- 自动强制对齐、自动挖空判定、无文稿素材转写

### 7.8 DW adapter（纯前端）

没有后端。整条链路就是浏览器里的三次 `fetch`：

```
浏览器 PWA
   │
   ├─① fetch(RSS)        → DOMParser 解析 XML   → 期次列表
   │
   ├─② fetch(页面 HTML)  → 正则截出 window.__APOLLO_STATE__
   │                      → JSON.parse           → manuscript / knowledges / mp3Src
   │
   └─③ fetch(mp3Src)     → Blob                 → IndexedDB
```

三个端点全部返回 `Access-Control-Allow-Origin: *`（附录 A.1），因此无需代理、无需扩展、无需本地进程。

**解析 `__APOLLO_STATE__`**：它是 `window.__APOLLO_STATE__={...}` 形式的内联 JS，不是 `<script type="application/json">`，所以取不到现成的 JSON。用**字符串感知的大括号配平**从 `={` 扫到匹配的 `}`（要正确处理 `\"` 转义和字符串内的花括号），再 `JSON.parse`。不要用贪婪正则。

需要的实体（键名即 `<类型>:<id>`，id 与 RSS `<guid>` 一致）：

| 实体 | 字段 | 用途 |
|---|---|---|
| `Lesson:<id>` | `manuscript` | 正文 HTML（只含 `<p> <strong> <br> <span>`） |
| | `teaser` | 导语原文。**不再用于判定非朗读块**（FR-13.7 已改，见 §0 变更 24）—— 它和标题一样会被念出来 |
| | `name`、`namedUrl`、`firstPublicationDate` | 标题、`sourceUrl`、真实首发日期 |
| | `knowledges` | `__ref` 数组，指向 Glossar 条目 |
| `Audio:<id>` | `mp3Src`、`duration` | 音频直链与秒数（预填 `audioDuration`） |
| `Knowledge:<id>` | `name`、`text` | 词条（含性和复数）与德语释义（FR-14） |

**manuscript → 纯文本，必须保留 offset 映射**（关键实现约束）：

正文里的 GLOSSARY span 携带 FR-14 需要的位置信息，而 FR-2 切句、`Sentence.charStart`、`Blank.ranges` 全都基于纯文本 offset。所以 HTML→文本的转换**不能**是 `innerText` 一把梭，必须在遍历 DOM 时同步产出「纯文本」和「每个 GLOSSARY span 在纯文本中的 `[start, end)`」。这一步做错，FR-14 的候选词会标到错误的位置上，而且是**静默错位**，很难发现。为它单写一组单元测试。

转换规则：`<br />` → `\n`；`</p>` → `\n\n`；HTML 实体（`&bdquo;` `&ldquo;` `&ndash;` `&rsquo;` 等）要解码成真正的字符 —— 德语引号 `„…"` 会进入 §7.1 的 R-quote 规则，解码错了切句就跟着错。

**已否决的替代方案**：

| 方案 | 否决理由 |
|---|---|
| 本地 Node helper 进程 | 原方案。CORS 实测不存在，helper 解决的是一个不存在的问题；还会带来两个 origin、两份 IndexedDB、以及一个要记得启动的进程 |
| CORS 代理（公共或自建） | 把服务器放进内容通路，正是 §3.1.1 R-1 的唯一红线 |
| 浏览器扩展 | 为一个每周用一次的功能维护扩展的打包与权限流程，不值 |
| Tauri / Electron | 已在 §2 定为 PWA；且现在也没有它能解决而 PWA 不能的问题。**注**：这条是针对「抓 DW 内容」这个用途下的，FR-15 之后打包成原生应用有了新的理由（随包带 200MB 权重、从第一次使用起离线），见 Q9 |
| 抓 DW 的 GraphQL 端点而非页面内嵌 state | 端点和 query 结构未公开，比内嵌 state 更容易变；内嵌 state 是页面渲染必需的，DW 不会轻易去掉 |

### 7.9 自动打点：模型、算法与规模（FR-15）

**模型：德语原生的 wav2vec2 CTC**（`oliverguhr/wav2vec2-large-xlsr-53-german-cv9`，Apache-2.0，
Common Voice 9 上 WER 9.48% / CER 1.92%（贪心，不带 LM），35 token 的词表、
`inputs_to_logits_ratio = 320`，16kHz 下 **0.02 秒/帧**）。

**上一个模型是 MMS-FA**（`onnx-community/mms-300m-1130-forced-aligner-ONNX`，31 token 的**罗马化**字符词表），
2026-09-21 换掉（§0 变更 42）。三个理由，按重要性排：① 它是罗马化的 —— 德语进去之前 ä→a、ö→o、ü→u、ß→ss，
而那几个音恰恰是德语里最要紧的区别；② 它的词表里**没有空格**，词边界完全不被建模；
③ 它是 CC-BY-NC-4.0（非商用）。换完的实测在本节末尾那段阈值标定里。

**两处权重，不是同一份**（这是主动放弃的一致性，见 §0 变更 42）：浏览器跑自己量化的 4-bit
（230.2 MiB，`scripts/quantize-align-model.py`，MatMulNBits，WebGPU 与 wasm 两个 EP 通吃），
服务器跑未量化的 fp32（1204 MiB）。所以同一课换个地方重对，边界会有几十毫秒级的出入。

**为什么是 CTC 强制对齐，而不是 Whisper 的词级时间戳**：手机上必须能跑，而 CTC 是**单次前向、无自回归解码**；Whisper 要把 6 分钟音频逐 token 解出来，在手机 WebView 里是几分钟级。而且词级时间戳这条需求下，CTC 对齐一遍就出字符级→词级→句级三层且互相一致；Whisper 那条路还得拿假设文本和稿件再做一次模糊匹配，多一层会漂的环节。

**模型是可插拔的**（`src/align/config.ts` 的 `AlignModelConfig`），而 2026-09-21 真的换了一次 ——
**那次换模型验证了这个设计**：动的是 `modelId`、`vocabSize`/`blankId`、以及 `vocab.ts` 那张表和字符映射；
对齐算法（viterbi / windowed / target）一行没改。唯一需要提前想到、而当初没有的一处是 **`blankId` 必须是参数**：
新模型的 blank 是 32（`[PAD]`）而不是 0（0 是词分隔符 `|`），而 `forcedAlign` 原来给它一个默认值 0 ——
默认值在这里是个陷阱，给错不报错，只是路径全错。现在它没有默认值。

**两个必须处理的规模问题**（都不是优化，不做就直接崩）：

| 问题 | 数字 | 做法 |
|---|---|---|
| 自注意力是 O(n²) | 6分16秒 = 18800 帧，一把喂进去注意力矩阵 3.5 亿项 | 切 20 秒一块推理。**块间重叠 2 秒，每块只采用中间那段**，两侧各丢掉一半重叠 —— 块边界的帧缺少上下文，后验明显变差，不这么做每 20 秒就有一处系统性边界误差，而句子边界恰好密集出现在那里 |
| Viterbi trellis 是 frames × (2T+1) | 实测 18826 帧 × 3988 token → **1.5 亿格**，回溯指针哪怕 1 字节也是 143MB（9:56 那一课 387MB） | 按锚点向前滑窗，每窗独立对齐，trellis 压到约 1100 万格。窗内 token 数靠速率估，**故意低估**（×0.75）再额外丢掉窗尾 15% 不提交 —— 估错的两个方向不对称：喂少了由 blank 吸收、完全无害；喂多了会把没念到的 token 硬塞进窗尾，产生一串挤在一起的假边界。**窗口 120 秒**，不是最初的 30 秒：30 秒比 Alltagsdeutsch 的长句（实测 437/475/625 字符）还短，速率估计误差与一整句同量级，于是漂移逐窗累积（见变更 22） |

拼接后的 log-prob 只有 18800 × 35 个 float32 ≈ 2.6MB，比音频本身小得多，所以「先全部算完再对齐」是划算的，不需要流式。

**CTC 的那条关键约束**：状态序列展开成 `[blank, t0, blank, t1, …, blank]`，「跳过一个 blank」的转移**仅当前后两个 token 不同字**时才允许。少了这个条件，所有含双写字母的德语词（`alle`/`immer`/`kommen`，满篇都是）边界都会错。

**待校对阈值用相对基准**，且**标定过两次** —— 第一次标在了一个 bug 上。

最初按感觉写的绝对阈值 -0.6 会把 37/38 句全标黄，等于没标。改成相对基准（低于本课中位数 0.8）
是对的方向；但当时量到的分布「中位数 -1.11 / 最差 -3.50」里那个 -3.50 **是滑窗漂移的症状,
不是素材的性质**（窗口 30 秒太短，见变更 22）。也就是说余量 0.8 是拿坏对齐的分布标出来的。
它的症状在另一课上暴露：31/45 句全被标黄 —— 那一课中位数已经掉到 -2.99，触发下面那条下限。

窗口修好之后重标（三课）：中位数 -0.88 / -1.08 / -1.17，最差 -2.09 / -1.77 / -1.59。
分布整体收窄（跨度从 2.4+ 降到 1.1~2.1，这正是对齐变好的表现），于是 0.8 这个加法余量
够不着尾巴：三课分别报 4/0/0 句，后两课等于宣布「一句都不用看」，正是 §3.3 拒绝的假装降到 0。
**余量改成 0.5**，三课报 9/4/0 —— 而 8:05 那一课的分布真的没有离群值，报 0 是诚实的。
排序本身修完之后仍然准：各课最差的几句里就有本节点名的那几类（„Great Ocean Road"、
"zwischen 18 und 30"、"Mitte der 1950er-Jahre"）。坏的从来不是排序，是阈值的尺度。

用相对基准而不是绝对值，是因为绝对值随模型和语言漂：换模型后整条分布会整体移动，
绝对阈值当场失效。另有一条 -2.5 的绝对下限兜底，防止「整课都差」时一句不报 ——
修完之后它正常情况下不再触发（最差只到 -2.09），纯粹是兜底。

**第三次标定：2026-09-21 换成德语原生模型之后**（§0 变更 42）。「绝对阈值会漂」这件事这次真的发生了：
同一课（6:16、45 句、webgpu/q4）的分布从「中位数 -1.08、跨度 1~2」变成
**中位数 -0.02 / p25 -0.03 / p75 -0.01 / 最差 -1.59** —— 贴着 0，只有几个真正的离群值。
罗马化丢掉的那些区分和缺失的词分隔符，原来一直在给每一个 token 交税。
**余量 0.5 一行没改**（相对基准的价值就在这一刻兑现），在新分布上报 **3 句**，
而那三句全是**采访同期声**（-1.59 „Australien, weil ich…"、-0.63、-0.54 都是受访者的自然口语）。
也就是说排序仍然指着真正难的地方，只是「难」的定义从「模型看不懂的字符」变成了「真的不好念」。
3 句在「四到九句」那个量级的下沿，但它是诚实的：p75 到中位数只差 0.01，这一课确实没有别的可看。

**线程与解码的分工**：**Web Audio API 在 Worker 里不存在**（`AudioContext` / `OfflineAudioContext` 只在主线程暴露），而推理必须在 Worker 里。所以主线程 `decodeToMono16k()` 解出波形，把 `ArrayBuffer` **transfer** 进 Worker（6 分钟音频是 24MB float32，没必要拷）。相应地，解码函数单独一个模块（`src/align/decode.ts`）、配置与运行时设置也拆开（`config.ts` / `runtime.ts`）—— 否则主线程 import 一下就把 500KB+ 的 transformers.js + onnxruntime-web 拽进首屏包（实测主包会从 497KB 涨到 1013KB）。

**ORT 的 wasm 必须自托管**：transformers.js **默认从 cdn.jsdelivr.net 拉 onnxruntime-web 的 wasm**，不覆盖 `env.backends.onnx.wasm.wasmPaths` 的话断网时模型压根起不来。做法是 Vite 的 `?url` 导入（`onnxruntime-web/ort-wasm-simd-threaded[.asyncify].{wasm,mjs}`，Safari 走非 asyncify 那份），构建时自动进 `dist/assets/`。曾经改为复制进 `public/ort/`，结果 dist 里出现**两份**同样的 23MB wasm —— Vite 分析 onnxruntime-web 时还会自己打一份。

**§7.6「不配任何 runtimeCaching」不必为此破例**：transformers.js 自己就把模型权重和 ORT wasm 都存进 Cache API（`env.useBrowserCache` / `useWasmCache` 默认 true），离线由它保证。

**一个部署陷阱**：`wrangler.jsonc` 的 `not_found_handling` 是 `single-page-application`，缺失路径会返回 **200 + index.html**。所以「本地有没有权重」不能只看 `res.ok`；打开 `allowLocalModels` 却没有权重时，transformers.js 会拿 HTML 去 `JSON.parse` 然后报「配置解析失败」，和真正的原因完全对不上号。判据曾经是 `HEAD` + 验 content-type，接原生壳（§7.10）时改成了 **GET 那份 1KB 的 `config.json` 并真的 parse 一遍** —— 原生壳里 dist 不是被 HTTP 服务器托管的，而是 iOS 的 `capacitor://` scheme handler / Android 的 `WebViewAssetLoader` 在读本地文件，它们对 HEAD 和响应头的支持都不保证。而这里一旦误判成「没有本地权重」，随包带的 200MB 就白带了。parse 这个判据不依赖任何响应头，三种托管方式下都成立。

### 7.10 原生壳（Capacitor，iOS + Android）

Q9 的手机那一半的落地。**壳里跑的是同一份 web 产物，业务代码没有为它分叉**：全项目只有一个文件知道「自己可能不在浏览器里」（`src/platform/native.ts`），加一处原生代码（`AppDelegate.swift` 的音频会话）。

> **2026-09-21（§0 变更 42）：iOS 包里不再有对齐权重。** 下面这一整节里所有关于
> 「随包带 418~490 MiB 权重」「砍掉用不到的那份」「装机 553.6MB」的段落都是**历史记录**，
> 保留是因为那些数字是实测出来的、而且解释了当初为什么要套壳。现状是：
> iOS 包**实测 95.2MB**（0.5.0 真机装机，2026-09-21；剩下的大头是 40MB 词库和应用本身。
> 对照：0.2.x 是 553.6MB —— 少了 458MB，83%。IPA 那一侧是 410.2 MiB → 32.7 MiB），
> `release-ios.yml` 里不再有 `stage:align`，
> 手机上的对齐由服务器算。**Android 那条没动**，它仍然随包带权重（现在是 230.2 MiB 一份）。

**为什么值得套壳**（纯 web 版给不了的三件事）：

| | 纯 web / 主屏幕 PWA | 原生壳 |
|---|---|---|
| ~~200MB 对齐权重~~ | 首次使用联网下一次，之后靠 Cache API | ~~**随包带**，装完第一次用就离线。这是 Q9 决定做原生壳的原始理由~~ —— **2026-09-21 作废**：iOS 上本地对齐整条删掉（变更 42），这一条只剩 Android 还成立。套壳剩下的两个理由（下面两行）仍然成立 |
| iOS 上的数据寿命 | Safari 的「7 天无交互清 storage」策略，靠「必须添加到主屏幕」豁免（README §一那条警告） | App 的 WebView 数据只在卸载时消失，**不依赖用户记得装主屏幕** |
| 静音开关 | `<audio>` 跟着侧边拨杆走，一拨等于整个应用坏掉，症状还是「点了没反应」（§3.2 当时留的方案是「必要时换 Web Audio API」） | `AVAudioSession` 设 `.playback` 一行解决，不用重写全局单例 `<audio>` 那套 |

**为什么是 Capacitor 而不是 Tauri v2**：Tauri 的优势是「一套覆盖桌面 + 手机」，但桌面这一半这里根本不急（桌面就是浏览器，工位上开着就行），而手机这一半 Capacitor 的成熟度和文档量明显更高，同账号下的 funny 项目也已经在用、CI 的形状可以直接抄。真要做桌面壳时再单独上 Electron 或 Tauri，与这里不冲突 —— 三条路里跑的都是同一份 `dist/`。

**六条必须知道的实现约束**（每条都写进了对应文件的注释）：

| # | 约束 | 为什么 |
|---|---|---|
| 1 | **`base` 保持 `'/'`，不要改成 `'./'`** | KICKOFF 里那句「打包要配 `base: './'`」是针对 **Electron 的 `file://`** 的。Capacitor 不走 `file://`：iOS 从 `capacitor://localhost/` 提供 webDir、Android 从 `https://localhost/`，两边都有真正的 URL 根，`?url` 产出的 `/assets/…` 原样就对 |
| 2 | **原生构建必须去掉 Service Worker**（`vite build --mode native`） | 它在原生壳里没用途（dist 已经在包里，本来 100% 离线），且有害：`registerType: 'autoUpdate'` 是为「刷新页面拿新版」设计的，而原生壳没有「刷新」—— 一旦某次装出来的 SW 缓存了旧壳的 index.html，下次 `cap sync` 换掉 dist 也照样吃旧的，而用户没有任何界面能清它 <br>**「去掉」的实现是 `disable: native`，不是把插件从 plugins 数组里删掉**（变更 30 起）：注册改成 import 虚拟模块 `virtual:pwa-register` 之后，插件不在数组里就没人解析那个 id，原生构建直接报「找不到模块」。`disable` 下插件仍然解析它、但给的是空实现，同时不生成 sw.js、不注入 manifest 与注册脚本 —— 实测原生产物的主 chunk 内容哈希与改动前逐字节一致（`index-bIZTSlaa.js`），那段代码被完整摇掉了 |
| 3 | **不启用 `CapacitorHttp`** | 它 patch `window.fetch` 走原生栈以绕过 CORS。一是不需要（附录 A.1 实测 DW 的 RSS/页面/mp3 三者全部 `Access-Control-Allow-Origin: *`），二是有害：mp3 是几 MB 二进制，原生桥要 base64 过一遍 JSON，内存与耗时都翻番且拿不到流式响应 |
| 4 | **`a[download]` 在 WKWebView 里不存在** | 点下去什么都不会发生 —— 不报错、不下载，最难查的那种静默失败。手动导出改走 Filesystem 写进 App 的 Documents 目录 + 系统分享面板（`src/lib/download.ts`），Info.plist 的 `UIFileSharingEnabled` 让「文件」App 直接看到那个文件夹 |
| 5 | **顶部安全区要加在吸顶导航自己身上**，不是 body | `sticky` 是相对视口定位的，加在 body 的 padding 上导航照样滑到状态栏底下。写成普通 CSS（`.app-nav`）而不是 Tailwind 的 `pt-[…]`：它要盖掉同一个元素上的 `py-2`，而 Tailwind v4 的工具类在 `@layer utilities` 里，未分层的规则才稳赢。Android 走另一条路 —— 让状态栏**不覆盖** WebView，因为 Capacitor 的 Android 壳不把系统 inset 喂给 `env(safe-area-inset-top)` |
| 6 | **Android 硬件返回键必须接管** | 默认行为是**任何一次按下都直接退出应用**，在跟读页误触一下等于把整个循环关掉。hash 路由的每次跳转都进了 history，所以 `canGoBack` 时 `history.back()`、到根了再按才 `exitApp()` |

**速度仍然是手机端的硬约束，体积不再是 —— 而内存是比这两者都硬的一道。**
套壳不改变运行时：手机里仍然是 WebView 里的 WASM/WebGPU。原生壳的本地服务器不发
COOP/COEP 头（线上静态托管也没发），ORT 拿不到 `SharedArrayBuffer`、跑不了多线程，
所以退到 WASM 那一档会比桌面慢一个量级，一课按十分钟量级估。

此处原文写「iOS 的 WKWebView 没有 WebGPU，所以 `pickDevice()` 一定退到 `wasm`」——
**这条是错的**：iPhone 上黑匣子记下来的第一次尝试就是 `webgpu/q4f16`（见变更 21），
WKWebView 有 WebGPU。

但真机上真正发生的事比慢得多严重：**iPhone 13 上两档都在「加载对齐模型」这一步被系统杀掉。**
而 `q4f16`（187.6 MiB）已经是这个模型**最小的一个变体**，所以降档在手机上救不了 ——
WebView 的 WebContent 进程 jetsam 线远低于原生进程，而 ORT-web 至少要 JS 堆一份 +
wasm 堆/GPU 一份。手机端要真的跑得动，只有两条路：换一个小一个数量级的模型，
或者把 emissions 那一半整块挪出 WebView（原生插件 / 远端，见 `src/align/emissionMatrix.ts`）。
在那之前，手机端的正确形状就是 FR-15.10 兜底说的那句话：去桌面对齐，句级时间戳跟着备份回来。

**2026-09-03：走了第二条路（§0 变更 31、FR-15.14）。** `native-plugins/align-native` 把
emissions 整块搬进原生进程（Swift + ONNX Runtime 的 SPM 分发，读包里那份 `model_q4.onnx`），
viterbi 仍在 WebView 的 Worker 里 —— 那道缝（FR-15.13）是变更 21 时切的，这次证明了它切在对的地方：
**换掉一半，另一半一行没动。**

**2026-09-03 晚：第三条路也走了（§0 变更 35、FR-15.17/15.18）。** emissions 现在还能在
**自己那台 VPS** 上算（`server/src/align/`：ffmpeg 解码 + onnxruntime-node 跑同一份 `model_q4.onnx`），
客户端只多一个 provider（`src/align/remoteEmissions.ts`）。那道缝因此被兑现了三次
（wasm → 原生 → 远端），而 viterbi 及其下游**一行都没动过**。

于是手机端的形状变了一次：**默认送到服务器算**（一两分钟，而且这期间手机可以锁屏、
可以退出 App —— 计算不在这台设备上），原生插件那条降级成「服务器不可用时的手动退路」。
上面那两个「只有真机能回答」的问题因此都降了级：第 1 个已经在服务器上有了答案
（`onnxruntime-node` 1.29 的 CPU EP 认 `MatMulNBits`，加载 0.6 秒、20 秒一块推理 3.4 秒），
虽然 iOS 那份 ORT 分发仍要真机自己回答；第 2 个（速度 + 切后台被挂起）在主路径上不再是拦路的东西。

两件还没有答案的事，只有真机能回答：
1. **ORT 的 iOS 官方分发里 `MatMulNBits`（com.microsoft 域的 4-bit 算子）在不在。**
   `model_q4.onnx` 全靠它（146 个节点）。不在的话报错会是「算子找不到」，
   补救是换 `bnb4`（212.2 MiB，走 DequantizeLinear + MatMul 这条标准路）。
2. **速度。** CPU EP、大核一半的线程数；桌面 wasm 那档是约 1.4× 实时，手机不会更快。
   一课 8 分钟按分钟量级估，而 iOS 会在应用切后台后挂起进程 —— 「对齐时别切走」这件事
   目前没有任何机制保证（后台任务断言只给约 30 秒，救不了分钟级的活）。

**跑通之后马上能省 196MB**：原生只用 `q4`，`q4f16` 在 iOS 包里就是纯死重
（`release-ios.yml` 里少下一份即可）。**但顺序不能反** —— 先砍权重再验插件，
插件一旦不工作，那台设备连回退到 WebView 的路都没有了。

**首次发布实测（2026-09-01，run 33515038917）**。一次通过，5m45s，`UPLOAD SUCCEEDED`。
落地的数字与原估差得多，记在这里免得再传抄：**对齐权重共 490.2 MiB**
（当时是 `onnx/model_q4f16.onnx` 187.6 + `onnx/model_int8.onnx` 302.6，`stage-align-assets.mjs`
两份都带 —— web 版必须两份都有），**IPA 381MB**，**iPhone 上装机 553.6MB**。
（第 2 档后来换成 `model_q4.onnx` 230.3 MiB，见变更 21 —— 权重共 **417.9 MiB**，
装机应降到约 481MB，下一次出包时回写实测值。）
超过 App Store 蜂窝下载的 200MB 门槛，装的时候要 Wi-Fi。

**0.2.0 出包实测（2026-09-02，run 33661964392）**：6m51s，`UPLOAD SUCCEEDED`，
**版本 0.2.0 构建号 4，IPA 400MB**（0.1.0 是 381MB —— 权重降了 72.3 MiB，
内置词典加了 35MB，净 +19MB）。装机体积还没量。这是**第一个带同步的包**。

**0.2.1 出包实测（2026-09-03，run 33750498587）**：**版本 0.2.1 构建号 7，IPA 408MB**，
`UPLOAD SUCCEEDED`。这是**第一个带原生对齐插件的包**（变更 31 / FR-15.14）。
+8MB 全是 ONNX Runtime 那个 xcframework —— 权重一份没多（原生和 WebGPU 那档
共用包里同样的两份），所以砍掉 `q4f16` 那 196MB 之后装机应该落到 210MB 量级。

**同一个 tag 打了两次包，第一次 archive 失败（exit 65）**，唯一一条错误是
`AVAudioConverterOutputStatus has no member 'inputRanOut'` —— 那个 case 叫
**`inputRanDry`**。值得记的是这次失败**能有多干净**：包图全对（SwiftPM 解析到
onnxruntime **1.24.2**，模块名 `OnnxRuntimeBindings`），另外两个 Swift 文件
一条错都没有 —— Swift 是整个模块一起 type-check 的，所以「只报了这一条」
等于「其余都过了」。

**`ios-v0.2.1` 这个 tag 指向的是编不过的那个提交（`239c306`）**，真正被构建的是
`b032b40`，走的是 **手动 Run**（`workflow_dispatch` 取 `package.json` 的版本号）。
没有重打 tag：force-push 一个 `ios-v*` tag 会**再触发一次出包**，等于往 TestFlight
多推一个同版本的重复构建。**下次遇到编译失败，正确的顺序是先修好、再打 tag。**

**还没有答案的仍然是那个真正的问题**：编得过不等于跑得动。`model_q4.onnx` 靠
`MatMulNBits`（com.microsoft 域，146 个节点），ORT 的 iOS 官方分发里有没有这个
算子的 CPU 内核，只有真机能回答；没有的话报错是「算子找不到」，补救是换 `bnb4`。

**2026-09-03 真机第一次跑（0.2.1）：不是卡死，是静默 + 时长（§0 变更 33、FR-15.15/16）。**
症状是「自动对齐一开始就卡住，好几分钟没有进展」。查完这条路没有卡死的 bug ——
原本只有一种进度事件（每块算完报一个比例），而它前面排着四件在手机上要几十秒到几分钟的事：
9MB base64 过桥、原生解码、加载 241MB 权重、第一块推理。于是最需要耐心的那几分钟
恰好是唯一没有反馈的几分钟，而它和这一节上面那次 jetsam 在界面上长得一模一样。

顺带答了上一段那道题一半：**没有出现「算子找不到」那条错误横条**，所以 ORT 的 iOS 分发里
`MatMulNBits` 的 CPU 内核大概是有的。但这只是「没报错」，真正的凭据是块数往前走 ——
那要等下一个包（进度条现在会把它说出来）。

补完进度之后真正的账才算清楚。一课（8 分钟音频）是 **27 块**（480 秒 / 18 秒步长，算出来的），
**估**每块 20–30 秒、合计 **10–15 分钟** —— 依据是桌面 `wasm/q4` 实测 1.4× 实时，
加上这个模型每块约 700 GFLOP 的量级。**真机上还没有跑完过一课，这个数是估的**，
进度条现在会把每块的秒数摆出来，拿到之后回来换成实测值。全程 3 线程满载、常驻约 400MB。
而 Info.plist 里没有 `UIBackgroundModes`，锁屏或切走 App 之后进程被挂起，
iOS 默认 30 秒到 2 分钟就自动锁屏 —— 也就是说这个任务的时长**超过了手机愿意给的时长**。
结论是把手机端从「导入后自动跑」改成「桌面算、手机练」，手动那一次跑得起来靠两件事：
防锁屏（`isIdleTimerDisabled`）和断点续算（`Checkpoint.swift`）。

一个待验的数：`setIntraOpNumThreads(activeProcessorCount / 2)` 那行注释说这是「大核数量」,
**在 iPhone 上不对** —— A15 是 2 大 + 4 小，`6 / 2 = 3` 会把一个小核算进去，
而 ORT 的 intra-op 是等最慢那个线程的。没有盲改：现在进度条报块数，
真机上读出每块的秒数之后再动它。


**这一次撞了一道只有装上第三方插件才会出现的坑，第一次 archive 直接失败**（exit 65）：
`xcodebuild` 命令行上的构建设置会套到**每一个** target，包括 SPM 依赖的包 target，
而那些包不支持描述文件 —— `Alamofire_Alamofire does not support provisioning profiles`，
一个包一条（GoogleSignIn / AppAuth / Facebook SDK / Alamofire / GoogleUtilities /
Promises / GTM* 全都报）。**0.1.x 没撞上是因为当时 SPM 图里只有 Capacitor 自己的包**；
Google 登录插件（`@capgo/capacitor-social-login`）才把这一大片带进来。
修法是把真正的设置写进 App target 的 Release 配置，值用一个 Xcode 不认作签名设置的
自定义变量（pbxproj 里 `PROVISIONING_PROFILE_SPECIFIER = "$(DEUTSCH_PROFILE_NAME)"`，
workflow 只喂 `DEUTSCH_PROFILE_NAME`）—— 包 target 照样收到它但不解释它。
副作用正好是想要的：本机用 Xcode 打开时变量是空的，`CODE_SIGN_STYLE = Automatic` 接手，
开发机不需要装分发描述文件。
**这条的形状值得记住：命令行上的构建设置没有「只给某个 target」这回事**，
所以凡是只对 App target 成立的设置，都要走「pbxproj 里引用一个自定义变量」这条路。

**「上传成功」到「手机上能装」之间还有两道**，原文只写了第一道：

1. ASC 处理十几分钟 → `processingState` 变 `VALID`。
2. **出口合规**：`ios/App/App/Info.plist` 里的 `ITSAppUsesNonExemptEncryption=false`
   让这一道自动过；这一项没写的话，每个 build 都要去 ASC 手动答一遍加密问卷才能测。
3. **必须有测试组**：ASC → TestFlight → Internal Testing 建组并把自己加进去。
   **一个组都没有时 build 不出现在任何人的 TestFlight App 里** ——
   `processingState=VALID` + `internalBuildState=READY_FOR_BETA_TESTING` 都满足了也一样。
   原文写「处理完之后内部测试员自动可见」，是错的。

**唯一值得做的体积优化：砍掉用不到的那份权重。**（**2026-09-21 被变更 42 整个盖过去了**：
砍的不是「用不到的那份」，是**两份一起砍** —— 手机上根本不再有本机对齐。下面这一段是历史。）
481MB 里 418MB 是权重，而
`pickPlan()` 两条路只会走一条 —— 有 WebGPU 走 `q4f16`，退 WASM 走第 2 档。
web 版必须两份都带（用户设备什么都有），**原生壳只在一种 WebView 里跑，另一份是纯死重**。
为此在**设置页加了「对齐后端」诊断**（显示 `device / dtype` 与每份权重在不在包里，
用 `Range: bytes=0-0` 探大小而不真的下载），让设备自己回答，再改 `release-ios.yml`。

设备已经回答了，而答案让这个优化的方向反了过来：iOS 侧走的是 `webgpu/q4f16`，
所以该砍的是第 2 档那份（230.3 MiB），装机降到约 250MB —— 不是原文说的砍 `q4f16`。
**但两档在 iPhone 13 上都被杀过**，所以在上面那段说的两条路（换小模型 / 挪出 WebView）
之一落地之前，砍权重是在给一个跑不起来的功能省体积，优先级低于让它跑起来。

**图标与启动图自己画**（`scripts/generate-icons.mjs`，`npm run icons`）。官方的 `@capacitor/assets` 依赖 sharp，而 sharp 要跑安装脚本编原生模块 —— 为画几十个 PNG 引入一条需要编译的依赖链，比自己多写两百行糟得多；这与 §7.6「不为一次性构建产物装依赖」是同一条判断。也没有让图像模型出图：**要的不是一张图，是三十多张互不相同的图**（iOS 那张要无 alpha、Android 自适应前景要抠掉底、maskable 要多留被裁的余量、11 张启动图各有比例），图像模型只能给一张 1024，剩下全靠降采样，而降采样出的 48px 一定比直接在 48px 上算覆盖率的糊；何况这枚图标里有字母，而变音符恰好是图像模型最容易画错的东西。

**图案**：一枚圆润的半透明对话气泡，里面是斜体的 Ä，一条渐强上扬的波从气泡背后穿过。三个元素各说一件事：气泡 = 语言/说话（语言类 App 的通用视觉词），Ä = 德语（带变音符的字母是任何人都认得的「外语」记号），波 = 听。这一版是三次返工的结果，两次教训值得记住：

1. **只画波形说不出「学语言」**。第一版是「两点变音符 + 一条波」，看上去只是个音频 App —— 播客、录音机、播放器的图标全长那样。变音符那两点是给认识德语正字法的人看的暗号，普通人接不到。补上气泡才把「语言」这层说出来。
2. **两点 + 一条弯线 = 一张脸**。等幅正弦只有 1.5 个周期时，中间那道下凹的弧配上头顶两点，整枚图标会被读成两只眼睛加一张嘴。SVG 草图上看不出来，渲染成 512 才躲不开。现在的两点是 Ä 自己的变音符（在气泡**里**），一旦把它们挪到气泡外面，脸的读法立刻回来。

**「灵动」只有两个旋钮**，都在打破对称，而不是加元素（48px 装不下第四层）：Ä 是斜体（把查询点做水平错切，横杠仍然水平）；波是渐强的（振幅左小右大、中线一路抬高，右端甩出气泡）—— 等幅正弦是节拍器，渐强才像一句话被说出来。

**「通透」靠图层顺序**（底 → 波 → 气泡 → Ä）：气泡是 76% 的半透明，波在气泡里透出浅浅一截、探出气泡后是实色，一眼能看出气泡有厚度。这个数值跟底色是一对：底是 `sky-100` 时 0.62 的气泡会融进底色，而底浅到 `sky-50` 那档时 0.62 就够 —— 调 `TILE` 必须回头看 `BUBBLE_ALPHA`。但 **iOS 的 AppIcon 不能带 alpha 通道** —— App Store 校验看的是通道在不在，而不是有没有透明像素，所以那一张必须写成 PNG color type 2，且透明只能是**画出来的**（半透明色叠在浅底上），不能是真的镂空。这个坑 Xcode 本地构建完全不报错，只在第一次上传时炸。

**画法是 SDF + 覆盖率**（覆盖率 = 0.5 − 有符号距离 / 像素边长）：一个像素只算一次距离，比超采样又快又准。旧版画的全是矩形，有没有锯齿看不出来，而圆点、圆角、斜笔必须抗锯齿。几何全部写在 1024 的设计稿坐标里（脚本顶部那坨常量），摆放时按**墨迹外接框的长边**贴合 —— 不能按宽度，否则换成高瘦的图案会竖向溢出留白区。因为贴合的是紧贴墨迹的外接框而不是整张设计稿，各档 `padding` 比直觉上的小：常规圆角 0.08 / 无裁切方形 0.10 / maskable 与圆形 0.18 / Android 自适应 0.28（这一档是按「中间 66/108 可见」反解出来的下限 0.268，不能再小）。

**换成浅色图标要连带改四处**，漏一处就会出现调子打架：`index.html` 的 `theme-color`、vite manifest 的 `theme_color`/`background_color`、Android 自适应图标的底色 `values/ic_launcher_background.xml`（必须等于脚本里的 `TILE`，否则浅蓝图案压在白底上没有边界）、以及启动图 —— 启动图现在用 `shape: 'none'` 画（只画图案、不画图标的方框），否则白底上会浮出一个和底色差一点点的方块边界。

**App 名字按系统语言分设**（iOS：`{zh-Hans,de,en}.lproj/InfoPlist.strings`；Android：`values/` 兜底德语名 + `values-zh/` 中文名）。非中文系统一律显示德语名 `Hörtraining`，两个平台的回落链在这一点上是一致的。三个坑：
1. **`capacitor.config.ts` 里的 `appName` 只在 `npx cap add` 时用过一次**（CLI 的 `editProjectSettingsIOS/Android` 只被 `add` 调用），改它对已有工程毫无作用 —— 名字的真实来源是 `Info.plist` 和 `res/values*/strings.xml`。
2. **iOS 有两个名字键。** `CFBundleDisplayName` 是主屏幕图标下面那个，模板已经填对；`CFBundleName` 模板给的是 `$(PRODUCT_NAME)` = target 名 **App**，它会原样显示在设置 → 通用 → iPhone 储存空间 / 电池用量 / 通知设置里。「看到的名字和设的不一样」就是这条。改它不影响构建产物 —— 二进制名走的是 `CFBundleExecutable`。超过 15 字符会被系统截。
3. **`.lproj` 必须挂进 pbxproj**（`knownRegions` 加语言 + 一个 `InfoPlist.strings` 变体组 + 进 Resources 构建阶段），光把文件放在磁盘上不会进包。副作用：一旦有 `.lproj`，App Store 就会把这些语言列为「支持的语言」。`cap sync` 不会覆盖以上任何一处，但重新 `cap add ios` 会把工程重建，届时要一起补回来。

PWA 那侧做不到同样的事：manifest 的 `name`/`short_name` 是单值，规范里没有本地化，唯一办法是按 `Accept-Language` 返回不同的 manifest —— 为一个自用工具不值得，所以网页版固定中文名。

**发布走 CI，本机不需要 Mac**（`.github/workflows/release-ios.yml`，推 tag `ios-v*`）。形状抄 funny 的同名 workflow，但三处必须不一样：Capacitor 8 的 iOS 模板走 **SPM 而非 CocoaPods**，所以是 `-project` 不是 `-workspace`、也不跑 `pod install`；这份 pbxproj 没有 `VERSIONING_SYSTEM = apple-generic`，所以**不能用 `agvtool`**，版本号改在 xcodebuild 命令行上覆盖；~~多一步 `npm run stage:align`~~（**2026-09-21 删掉，变更 42**：iOS 包不再带权重，`cap sync ios` 也不再往 `Package.swift` 里写那个要联网 resolve 的 binaryTarget）。Capacitor 模板不带共享 scheme（Xcode 自动生成的那份在 `xcuserdata/` 里、被 gitignore 了），所以 `App.xcscheme` 手工写了一份入库。Android 那条（`release-android.yml`，tag `android-v*`）只出一个签名 APK 作为构建产物，**不上 Play Store**：单 APK 100MB 的限制带着 490MiB 权重必然超，而这是个人自用工具，侧载就行。


### 7.11 词典数据管线（FR-16 / FR-17）

**四个来源，全部许可可再发布**（署名落在 FR-16.7 那一屏）：

| 文件 | 给的东西 | 许可 | 体积 |
|---|---|---|---|
| `de.sqlite3`（WikDict） | 性、IPA、复数、**词形→词元** | CC BY-SA | **1056 MiB** |
| `de-en.sqlite3` | **德语释义**（`sense` 列）、英译 | CC BY-SA | 25 MiB |
| `de-zh.sqlite3` | 中译（较薄，2.7 万条） | CC BY-SA | 5 MiB |
| `de_50k.txt`（hermitdave/FrequencyWords） | 口语词频 | **MIT** | 0.6 MiB |
| de.wiktionary `action=query` 取的 wikitext | **例句**（`{{Beispiele}}` 段，FR-16.9） | CC BY-SA 4.0 | 不落盘（缓存在 `.cache/dict/wikitext/`） |

**为什么用口语语料的词频**：`de_50k.txt` 是 OpenSubtitles 语料的统计，对一个**精听**训练器比新闻语料更对路。代价是影视对白的味道会带进来（人名、粗话），所以牌组里加了「每一条德语释义都像名字就排除」这条过滤。Leipzig 的词频表（CC BY 4.0）本来更权威，但它站点挂了 Anubis 人机验证 —— 构建脚本过不去，而绕验证不做。

**为什么源文件不入库、产物入库**：与 `public/models/` 的处理相反，判据是**体积**而不是原则（那 418MB 权重排除掉也是因为大）。产物 35MB 入库，换来的是 **CI 完全不必下那 1056 MiB**。源文件缓存在 `.cache/dict/`（`.gitignore` 排除）。

**例句为什么走 API 而不是 dump**：dewiktionary 的 `pages-articles` dump 是 1 GB 量级，而例句只需要 **1.7 万个牌组词**——`action=query&prop=revisions` 一次能问 50 个标题，347 次往返就够，速率约束复用 R-3 那个 `politely()`，原始 wikitext 缓存在 `.cache/dict/wikitext/`（重跑免费）。为一份只用到 11% 的数据去下 1 GB 是本末倒置。**署名单列一行**（FR-16.7 那一屏）：例句取的是 de.wiktionary **正文**，与 WikDict 那条「经 DBnary 的结构化数据」法律地位不同。

**`node:sqlite` 是内置的**（Node 22+），所以这条管线**一个依赖都不装** —— 与 §7.6「不为一次性构建产物装依赖」是同一条判断（`scripts/generate-icons.mjs` 拒绝 sharp 也是它）。

**六个把结果整片弄错的坑，每一个都实测踩过**：

| # | 坑 | 症状 |
|---|---|---|
| 1 | `entry` 表 102.8 万行里只有 **24.9 万**是德语维基词典自己的条目，其余是英/法/瑞典/库尔德等维基词典关于德语词的条目（`eng/_deu__Zuversicht__Noun__1`） | 那些条目的 `gender` 基本是 null，不按 `lexentry LIKE 'deu/%'` 过滤就会把性覆盖成空 —— 而 FR-7.4 说名词不带性等于没记 |
| 2 | 判功能词**必须看 `lexentry` 中段的德语词性名**，不能看 `part_of_speech` 列 | 代词、连词、冠词的 `part_of_speech` 全是 **null**（`ich` = `Substantiv/noun` + `Personalpronomen/null`）。只看那一列的话，`das Ich`（自我，确实是合法名词）会把代词 `ich` 的词频整份继承过来 —— band-1 曾经长成 `Ich Sie Du Sein Haben Gehen Gut` |
| 3 | `form.number` 的取值是 **'Singular' / 'Plural'（首字母大写）** | 按小写比会一个复数都找不到，**且不报错** |
| 4 | 复数**只能对名词取** | 不限词性的话 `nachhaltig` 会得到 `nachhaltigere` —— 那是比较级，形容词压根没有「复数」这一栏 |
| 5 | 词形索引存的必须是**归一化词元键**，不是显示形式 | `Laufen`（名词）与 `laufen`（动词）归一化后同键，合并成一条词条时显示形式只能留一个（取到的是名词那个）—— 而索引里存的是动词的 `laufen`，于是输出阶段那句「词元得确实在 w/ 里」判假，`gelaufen` / `dachte` / `gesagt` 这类最常用的动词变形**全部被静默丢掉** |
| 6 | 有歧义的词形**不能计频次** | 以前把整份计数加到每个候选词元头上，于是 `abgewogen` 的频次同时记给 `abwägen` 和 `abwiegen` —— 这种记法会把生僻的同形异义词一路抬到词频表最前面（band-1 里出现过 `Wa` 和古语 `fahen`）。少算一点频次远好于排名被污染 |

**一条启发式要如实记着**：源数据里同一个名词可能有两个并列的主格复数（`Mädchen` 同时有 `Mädchen` 和口语的 `Mädchens`，两行 `rank` 都是 2，**没有任何字段能分出主次**）。显示时取最短的那个 —— Wiktionary 列出的 `-s` 变体一般是口语附加形。这是启发式，不是数据里的事实。


---

### 7.12 iOS 原生壳的热更新（变更 41，2026-09-20）

**问题**：`webDir: 'dist'` 把整份前端打进 IPA，冻结在出包那一刻；Service Worker 又在原生
构建里被特意关掉（§7.10 第 2 条 / 变更 30）。于是 `push main` 只更新 d.gamestao.com，
手机上一动不动 —— ios-v0.3.0（2026-09-03）之后的变更 36~40 在 iPhone 上一个都没有。

**做法**：`@capgo/capacitor-updater`，**手动模式**。判断全在 `src/platform/nativeUpdate.ts`，
原生侧只负责下载与切换。

| 环节 | 落法 | 为什么不是另一种 |
|---|---|---|
| 查更新 | 启动时 GET `/ota/manifest.json`（`cache: 'no-store'`） | 插件自带的 autoUpdate 要 **POST** 到端点，而 wrangler.jsonc 是纯静态部署、没有 Worker 脚本（§3.1.1 R-1 的字面落地）。GET 一份静态 JSON 之后，判断变成可单测的纯函数 |
| 判断 | `decideUpdate(manifest, ctx)` | 见下面两条 |
| 过桥取数 | `askBridge()`：**每个数各问各的、各自 4 秒超时**，问不出来回 `undefined` | 桥调用失败的方式不只是 reject —— 插件没注册、原生侧不回调，那个 Promise 就**永远不 settle**。原来这里是 `await Promise.all([current(), getInfo()])`，**没有超时**：2026-09-22 用户的 iPhone 因此卡死在这一行，manifest 拉到了却走不到 `download()`，连着五次冷启动纹丝不动，**永久脱离热更**（变更 50）。用 `Promise.all` 把两个数绑在一起也是错的 —— 一个挂住会把另一个本来问得出来的也拖死 |
| 未知怎么算 | **问不出来的数一律不否决更新** | 两种坏法代价不对等：门槛判错 = 「点了没反应」，下一版就能修回来；门槛卡死 = 设备彻底脱离热更，而**修复本身也是 JS、只能靠热更下发**，死锁只能再发一次 TestFlight 才解得开。所以只有「确实问出来了且确实不够」才拦。跑着哪一版问不出来时靠 `localStorage['ota.queuedBuildId']`（**不过桥**）判断「这一版已经下过」，免得每次冷启动白下 10MB |
| 版本比较 | **buildId（commit 短 sha）相等判断** | 不比 semver 大小：只有 main 一条线，而比大小会让**回滚**（版本号往回走）被拒绝，恰好在最需要更新时不生效 |
| 门槛 | `minNative`（手改，在 `scripts/build-ota.mjs` 里） | 热更换不了原生代码。用到新 Swift 方法的 JS 推给旧壳 = 「点了没反应」，比崩溃难查（崩溃有 `notifyAppReady` 回滚兜着） |
| 生效 | `next()`：后台下好，**下次冷启动**生效 | `set()` 立刻重载整个 WebView，会清掉只活在 React state 里的听写答案 —— 变更 30 那条「不在打字的当口刷」在这里加倍成立。界面上**不给**「现在就更新」的按钮 |
| 安全网 | `notifyAppReady()` 挂在启动时序的 `onAlive` 那一档（`src/app/boot.ts`），超时 20 秒回滚 | 放模块顶层证明不了什么：连库都打不开的构建照样能执行到 import。但也**不能无条件等到表全读完**——`Promise.allSettled` 接得住 reject，接不住「永不 settle」（IndexedDB 的 `blocked`），那样一张卡住的表就会让这个 bundle 被判死并回滚。所以 `onAlive` 最多等 8 秒（变更 50） |
| 装新 IPA | `resetWhenUpdate: true`，丢掉所有热更 bundle | 新壳可能带了新的原生方法，而留着的旧 JS 不知道它们存在 |

#### 诊断：插件到底注册没注册（变更 52，2026-09-23）

变更 50 修的是「桥调用挂住」，没查出**为什么**。真机上后来确认 `App.getInfo()` 好、
只有 `CapacitorUpdater.current()` 哑 —— 而两者走的是同一条桥调度队列
（`CapacitorBridge.dispatchQueue`，单条串行），队列真堵了不可能让 `getInfo()` 先回来。
读 `@capacitor/ios` 源码确认了更像的成因：`CapacitorBridge.handleJSCall` 里
`guard let plugin = plugins[call.pluginId] ?? load() else { ...; return }` ——
插件类没注册上，桥直接丢掉这次调用，JS 那边的 Promise **没有人会 resolve/reject 它**。

`probePlugins()`（`src/platform/nativeUpdate.ts`）同步读 `window.Capacitor.PluginHeaders`——
那是 `JSExport.exportJS` 在插件**注册成功**时一次性注入的静态数据，跟调用挂不挂没有关系。
据此能把「类没链进二进制 / 没被发现」和「调用本身超时」这两种成因分开，而它们的下一步
完全不同：前者要动 Swift（见下）、后者纯粹是等或重试，JS 什么都做不了。

`checkNativeUpdate()` 现在**不管走到哪一步、怎么失败，都往 `CheckLogEntry` 里写一笔**
（`readLastCheckLog()`，不过桥，存 localStorage）：`plugin-import` / `fetch` / `current` /
`getInfo` / `decide` / `download` / `next` 每一步的结果，加上那一刻的 `probePlugins()` 快照。
变更 50 那次死锁的教训是「重启多少次都一样，且什么都不剩」——诊断的价值就是把「什么都不剩」
变成「剩一份能看的记录」。顺带补了两个还没盖到的自锁点：`fetch` 本身没有超时（手写
`setTimeout` + `AbortController`，不用 `AbortSignal.timeout()`——那是 Safari 16.4 才有，
部署目标是 iOS 15，老设备上会直接同步抛 `TypeError`）；`notifyNativeAppReady()` 自己
过桥也没超时，不调用的后果和 `checkNativeUpdate()` 卡死一样重——20 秒后插件判定
这个 bundle 起不来并回滚。

**Swift 那半截是一次有依据的赌注，不是确诊**：`ios/App/CapApp-SPM/Sources/CapApp-SPM/CapApp-SPM.swift`
在变更 52 之前只有 `isCapacitorApp = true` 一行，没有任何代码真正用到 `CapacitorUpdaterPlugin` /
`SocialLoginPlugin`——它们进不进最终二进制全靠 `NSClassFromString` 在运行时动态查找，
**没有编译期保证**说 SwiftPM 静态库的链接器一定会留下"看起来没人用"的符号。现在文件里
显式引用这两个类，让链接器没法丢。代价是零（这两个类本来就是既有依赖，见 `Package.swift`），
猜错了也不会更坏。`.github/workflows/release-ios.yml` 的 `cap sync ios` 之后新增一道校验，
挡的是最便宜就能查的那一档错——`packageClassList` / `Package.swift` 里漏列了这个插件。
**这一条要等下一次出包装机、看 `probePlugins()` 的名单里有没有 `CapacitorUpdater` 才算数。**

#### 真机验出：插件确实注册了，卡的是 `download()`/`next()`（变更 53，2026-09-23）

装上 `ios-v0.6.2` 之后真机诊断确认：`probePlugins()` 的名单里**有** `CapacitorUpdater`，
`current` 也在它的方法列表里——上面那段「类没链进二进制」的假设被这份数据**推翻**了。
但点「现在就查一次更新」按钮**连着几分钟没有反应**。查下来是这份诊断本身留的一个缺口：
`current()`/`getInfo()` 套了 `askBridgeVerbose`，**唯独 `download()`/`next()` 这两步还是
原来的裸 `await`**——真机上大概率是 `current()` 4 秒超时后 `decideUpdate` 落进
`download` 分支，然后卡死在这个没有任何保护的调用上，和变更 50 那次死锁**一模一样的
坏法，只是换了个调用**。

修法：`download()` 套 `askBridgeVerbose` 给 45 秒（manifest 的包统共几 MB 到十几 MB，
§7.12「构建与发布」写着 33 个文件 10.2 MiB，45 秒对任何正常网络都够；真等到 45 秒还没
回，十有八九是桥没回话不是网速慢——插件自己 Swift 里 `downloadTimeout` 是 600 秒，那是
给「文件真的很大」留的余量，不是这里要防的坏法）；`next()` 给 10 秒（只是登记一个 id，
正常是毫秒级）。**任一步失败都不能假装成功**：`next()` 超时或报错时，包可能已经下好了，
但不能把 `queuedBuildId`/`pending` 当成「登记成功」写下去——那样设置页会显示「已下好，
下次打开生效」，而原生侧其实什么都没登记，是在撒谎。

**这次没有只推 OTA**：还不确定「热更这条通路本身通不通」的时候，把修复推给 OTA 就是在
赌「OTA 是好的」，而那正是要验证的东西。所以直接出了 `ios-v0.6.4`，把修好的版本焊进
builtin bundle，不依赖任何桥调用就能生效。

#### 桥自检与诊断上报（变更 57，2026-09-23）

装上 `ios-v0.6.4` 之后症状没变：「一直显示查询中，版本号问不出来，原生桥没有回话」。
到这一步为止，所有的诊断都只能回答「哪一步超时了」，回答不了**为什么**。所以这一版
把能问的一次问全，并且给报告开一条出得来的路。

**① 冷启动窗口：4 秒可能根本不够。** `CapacitorUpdater.current()` 的 Swift 实现是纯
同步的——拿到 `BundleInfo` 立刻 `call.resolve()`，**它不可能慢**。唯一能让它慢的是它
前面那一步：Capacitor 的插件是**懒加载**的，第一次桥调用才 `loadPlugin()` → 跑插件的
`load()`，而 Capgo 的 `load()` 在主线程上做了一整套磁盘活（`cleanupObsoleteVersions`、
默认频道状态文件、`autoReset`）、发一次统计到 capgo 的服务器、最后还要 `initialLoad()`
去切 WebView 的 serverBasePath。所以「`current:timeout`」很可能不是桥哑了，而是
**我们在插件正初始化的当口就放弃等待了**。

改法：**这个会话里桥还一次都没回过话之前，每个调用至少给 25 秒**
（`COLD_BRIDGE_TIMEOUT_MS`）；只要回过一次话（resolve 或 reject 都算——原生侧真的动了），
就切回 4 秒。**但显示用的那条路（`runningBuild()`）不走这条放宽**：设置页那两行版本号
是人盯着等的，「4 秒级，不能久到让人以为这一页坏了」是它从一开始就有的约定。干活的那条
路等久一点没人看见，显示的那条路等久一点就是坏掉的观感——两者不该互相迁就。

**② 步骤记录带上耗时。** `current:ok(37ms)` / `current:timeout(25003ms)`。没有这个数，
「4 秒就放弃」和「等满 25 秒还没回」在日志里长得一模一样，而这两种的下一步完全不同。

**③ 逐方法自检（`runUpdaterSelfTest`）。** 把 `CapacitorUpdater` 的七个只读方法
（`getPluginVersion` / `getBuiltinVersion` / `getDeviceId` / `isAutoUpdateEnabled` /
`current` / `getNextBundle` / `list`）**串行**调一遍，各自计时、各自超时，再加一个对照组
`App.getInfo()`。串行不并行是刻意的：并行会让「第一次调用触发 `load()`」这件事被后面的
调用分摊掉，而分辨故障靠的正是那个形状——

| 自检长什么样 | 是哪种故障 | 下一步 |
|---|---|---|
| 全部 timeout，连 `App.getInfo` 也是 | 整条桥坏了 | 发新包 |
| 更新器全 timeout、`App.getInfo` 好 | 插件实例造不出来 | 换插件或换方案 |
| **头一个特别慢、后面都快** | 插件 `load()` 慢，我们等得不够 | 放宽超时（即 ①） |
| 个别方法 timeout | 那一条路自己的问题 | 只修那一条 |

`getPluginVersion` 放第一个是刻意的：它是插件里最便宜的调用（回一个常量字符串），
**它的耗时就是 `load()` 本身的耗时**，不掺别的。

**④ 把 console 抄一份（`src/platform/consoleTap.ts`）。** Capgo 插件的**原生**日志是通过
`webView.evaluateJavaScript("console.info(…)")` 打进 JS console 的（插件源码
`Logger.swift`）。也就是说：在 JS 侧抄下 console，等于**把 Xcode 控制台搬进了应用本身**
——而这个项目的开发机是 Windows，从来没有过 Xcode 控制台可看。`load()` 里那几十行
`logger.info`（「init for device …」「version native …」「appId …」）正是回答「插件初始化
到哪一步」的唯一证据。装在 `main.tsx` 的最顶上（模块级，登录弹窗那条分支也装），环形缓冲
最近 500 行、单行截断到 600 字符，**抄这件事本身永远不能吃掉一条日志**（原函数先调，
记录整个包在 try 里）。

**⑤ 两条出口，互不依赖。** 「发到服务器」`POST /v1/diag`（要登录，见下）；
「复制诊断」走剪贴板、剪贴板被 WKWebView 拒掉就把全文摊在一个 textarea 里让人自己选。
**诊断真正用得上的时候，坏的可能正是登录**——所以复制那条路必须平行存在，而不是发送
失败之后的兜底。

**服务端 `POST /v1/diag`**（`server/src/diag.ts`）：要会话令牌（一个能往公网服务器盘上
写文件的匿名接口不值得开），上限 2MB，每用户一个子目录、最多留 30 份、超了删最旧。
**存文件不进 sqlite**：这些东西是一次性的，形状还会跟着下一次诊断需要什么而变
（报告自带 `schema`），塞进有 schema 的库里要迁移要清理，而一个目录 + `ls -lt` 就够了。
`GET /v1/diag` 列表、`GET /v1/diag/:id` 取回，都只看得到自己那些。

**⑥ 记录改成「每一步立刻落盘」，而不是最后一起写。** 这一条是用户在同一次对话里补的
一句反馈逼出来的：**「没有『上次查更新』这个信息，也没有步骤的信息」**。那份记录原来只在
`checkNativeUpdate()` 的 `finally` 里写一次——对「跑完了但结果不对」是够的，对
**「根本跑不完」**却完全无效：函数不返回，`finally` 就不执行，一个字都不留。于是**最该被
记下来的那种故障，恰恰是唯一不会留下记录的那种**。现在进门先写一笔「开始了」（这句话之前
不许有任何 `await`），之后每一步 append 并立刻覆盖写，`outcome` 在被换掉之前一直是
「（开始了，但没有走到结束——卡在下面最后那一步上）」。**日志停在哪一步，就是卡在哪一步。**
`nativePlatform()` 那一句也记一步（`platform:ios`）——它只是一次动态 import、不过桥，
但它照样是个 `await`，而这条路上「某个 await 永不 settle」已经发生过三次了。

**⑦ 「一笔记录都没有」本身要说出来——立场在这里反转了。** 变更 52 当初定的是「从没查过
就不画，别留一个空壳」。真机反馈把它推翻了：那时候这一块不画，于是**这条最强的线索在界面上
长得和「一切正常、只是还没查过」一模一样**。现在没有记录时画一行 `warn`，明说「连『开始了』
那一笔都没有——它连第一行都没跑到，或者这台设备写不了 localStorage」。

**⑧ 「现在就查一次更新」有一道总闸（150 秒）。** 里面每一步都有超时，但
**「每一步都有超时」不等于「整个函数一定会返回」**——真机上按钮就是一直转着不停，而那
恰恰说明有个 `await` 没被任何一道超时盖到。这一层不负责找出是哪一个，只负责让按钮一定会
放开，并且**明说它没有回来**：「按钮转个不停」本身是要报告的事实，不是一个可以让人一直
等下去的状态。

**这一整条都必须出新包才到得了手机**：热更本身就是坏的那个，改的又全是 JS。
已发 `ios-v0.6.5`；服务端那一半从外面验过（未登录 `POST /v1/diag` 回 401 而不是
404/503，即路由在、sink 也真的装上了）。**剩下的只有真机那一段**：看「上次查更新」
停在哪一步、跑一次自检看它是哪种形状。

#### 诊断给出了答案：卡在取插件那个 `import()`（变更 59，2026-09-23）

`ios-v0.6.5` 的真机报告（服务器上 `1790159360683-vmjl2f.json`）把这条查了四天的故障
钉死在一行上。三条互相独立的证据：

| 报告里的数 | 说明了什么 |
|---|---|
| `probePlugins()` 名单里有 `CapacitorUpdater`，56 个方法齐全 | 类链进去了、注册成功了 |
| 自检：`getPluginVersion` 4ms、`current` 0ms、`list` 1ms、`App.getInfo` 2ms | 桥、插件实例、每一个方法**全是好的**——上面那张「四种形状」表里一种都不是 |
| 「上次查更新」的步骤 = `['platform:ios']`，结论仍是「开始了，但没有走到结束」 | `checkNativeUpdate()` 走到**取插件**那一步就再也没回来 |

第三条还有一个独立的印证：同一次启动里 **`notifyAppReady()` 根本没到过原生侧** ——
原生日志里没有插件那句 `Current bundle loaded successfully. [notifyAppReady was called]`，
只有回滚检查自己的定时器打的 `Built-in bundle is active. We skip the check`。而
`notifyNativeAppReady()` 在平台判断之后的下一步，也正是取插件。**两条互不相干的路
停在同一行**：`await import('@capgo/capacitor-updater')`。

旁证在抄下来的原生日志里：那次启动打过一条 `Semaphore wait timed out after 20000ms`
——插件 `load()` armed 的信号量等满 20 秒才放开，而这段时间它正在主线程做磁盘活并
`setServerBasePath()` 切 WebView 的 web 根。**那个 4KB 的 chunk 是同一个 WebView 去取的**：
根在切、handler 在忙，这次请求就悬在那儿，既不成功也不失败，而 `import()` 对
「请求永远不回」的表现就是 promise 永远不 settle。

**这是这条路上当时唯一一个没有死线的 `await`。** 变更 50/52/53 一路把每个**过桥调用**
都套上了超时，而漏掉的是「过桥之前先把插件拿到手」这一步——[[诊断块不能自己消失]]那条
教训（补超时要顺着调用形状全仓库搜一遍）第四次在同一个地方生效，只是这次的形状不是
「过桥」而是「取东西」。

**① 热更这条路从此不取任何新 chunk。** 插件包的 JS 层只是一层壳：
`registerPlugin('CapacitorUpdater')` 造一个代理，代理的每个方法就是
`Capacitor.nativePromise('CapacitorUpdater', 方法名, 参数)`（`@capacitor/core` 的
`createPluginMethod`）。而 `registerPlugin` 在 `@capacitor/core` 里，**core 那个 chunk
在走到这里之前必然已经加载完了**——`nativePlatform()` 用的就是它，日志里那句
`platform:ios` 就是证据。所以 `acquireUpdater()` 首选用 core 造代理，插件包那个动态
import 降级成退路。构建产物里对 capgo chunk 的 `import()` 从 2 处降到 1 处。
跳过的是插件包 JS 层的副作用（它给 history 打的补丁，服务于 `keep_url_path_after_reload`）
——本项目是 hash 路由、也没开那个选项，用不到。类型因此手写（`UpdaterPlugin`）：
`import type` 不留运行时代码，但会把「这个包必须存在」写进构建，而这里的目的正是
让热更不再依赖它。

**② 两条路各自有死线**（`withDeadline`，8 秒）。「换一条不会挂的路」和「这条路万一
也挂了怎么办」是两件事——core 已经在内存里、`registerPlugin` 是同步的，但
「理论上不会挂」在这条路上已经被现实打脸三次。拿不到就记
`plugin:core-timeout,import-failed` 并跳过这一轮，**绝不挂住调用方**。

**③ `notifyAppReady()` 会重试，并且留记录。** 它原来只有**一次机会，失手即止，
而且一个字都不留**。那次侥幸没事，因为跑的是随包 bundle（`isBuiltin()` 直接跳过回滚
检查）；但热更一旦真的装上一个 bundle，同一个失手就是：新版起来了 → 没人说「我起来了」
→ 下次启动原生侧判定它起不来 → **回滚**，症状正是「下下来了、永远不生效」。
现在隔 3/10/30 秒重试三次（**必须跨过那个二十秒的窗口**，只隔一两秒地重试等于在同一堵
墙上撞三下），结果写进 `AppReadyLog` 并在设置页显示。**只重试「没人回话」这一种**：
原生侧真的 reject（旧壳里没有这个插件）意味着它收到了也执行了，再试四次只是把同一句
错话再听四遍。

**④ 诊断本身不再依赖 localStorage 写得进去。** 这一版之前，「记录停在第一步」有两种
完全不同的成因能长成一模一样的样子：真的卡在那一步，或者**后面几步写不进存储**
（`writeCheckLog` 的 catch 是哑的）。分不清这两种，这份诊断在最关键的问题上就不可信。
现在记录在内存里各存一份（本次会话的真相，不经过任何存储），localStorage 只负责跨会话，
写失败的原因单独记下来并上报（报告的 `storageWriteError`，`schema` 升到 2）。

**这一版同样必须出新包**：修复是纯 JS，而送 JS 过去的那条路正是坏的那条。
`ios-v0.6.6` 已出包上传 ASC（构建号 17），线上 manifest 是 `0.6.6+be58199` ——
**和 IPA 里那份 builtin bundle 是同一份代码**，所以装上之后第一次热更下的就是它自己，
正好把整条链走一遍。**这和变更 53 的 `ios-v0.6.3` 恰好相反**——那次是「没动 Swift 就不该出包」，
这次是「没动 Swift 也必须出包」，判据不是「改了什么」，而是**「OTA 这条路现在能不能
把它送到」**。

#### 走过了取插件，停在取 manifest：CORS（变更 60，2026-09-23）

`ios-v0.6.6` 的真机报告（`1790164398739-qwgelj.json`）证明变更 59 修对了：步骤变成
`platform:ios → plugin:cached → fetch:threw:Load failed`，`notifyAppReady` 也是
`ok(4ms, via core)`，原生日志里第一次出现 `[notifyAppReady was called]`。**链条第一次走到
网络那一步，随即停在网络那一步。**

`Load failed` 是 WebKit 对「请求没拿到可用响应」的统称，而这里的成因很直接：壳里页面的
origin 是 `capacitor://localhost`，取 `https://d.gamestao.com/ota/manifest.json` 是**跨域**
请求，而 Cloudflare 的静态资源响应里**没有任何 `Access-Control-Allow-Origin`**
（`curl.exe -sI -H "Origin: capacitor://localhost"` 当场复现）。网页版同源，对此毫无感觉；
这条路从变更 41 接上热更起就是断的，只是此前每次都卡死在更前面的几步，**从来没走到过这里**。

修法是 `public/_headers`（Workers static assets 读 `dist/_headers`）给 `/ota/*` 加
`Access-Control-Allow-Origin: *` —— manifest 本来就是公开文件、请求不带凭据，列 origin 没有意义。
zip 由插件的 `download()` 走原生 URLSession 下载，不受 CORS 管。`_headers` 不进热更包
（`build-ota.mjs` 的 `EXCLUDE`）。`deploy.yml` 在 `wrangler deploy` 之后多一步线上
检查：带 `Origin: capacitor://localhost` 取 manifest，响应里没有放行头就红。

**不必出包**：改的是服务器的响应头，手机上 0.6.6 那份代码原样就能拿到 manifest。
下一个要看的判据是步骤走到 `download` / `next`，以及下一次冷启动之后 `current()` 的
`bundle.id` 不再是 `builtin`。

#### 走通了：同一版只下一次、旧包要清（变更 61，2026-09-23）

变更 60 上线后的两份真机报告是热更**第一次端到端走通**：按钮那一趟
`fetch:ok → decide:download → download:ok(770ms) → next:ok`；冷启动之后 `current()` 是
`vUUWJo6hmG / 0.6.6+4f04891 / success`，原生日志 `[notifyAppReady was called]` 落在新包上，没有回滚。

同一份报告露出一个缺口：**同一版下了两次**。启动自动查（12:25:28）刚下完、登记好，9 秒后
按钮那一趟又下了一遍 —— `decideUpdate` 只和「跑着的那版」比，不看「已下好待生效」的那个；
而代码里**一个包都不删**，多出来那份永远 pending，每次热更再留一个约 10MB 的旧包。三处修法：

1. **判断时看 `getNextBundle()`**（`UpdateContext.nextVersion`，`null` = 问出来了没有、`undefined`
   = 问不出来）：登记的正是 manifest 那一版就跳过「已下好，等下次冷启动生效」。它是原生侧的
   真相（回滚会如实反映），所以和 `queuedBuildId` 那条「桥好时记号不参与判断」的约定不冲突。
   设置页的 `pendingUpdateVersion()` 也认它 —— 原来只有这一趟自己下了才有。
2. **单飞**：`checkNativeUpdate()` 同一时间只跑一趟，后来的调用拿同一个 promise。**锁 3 分钟过期**
   （每一步都有死线，最坏约两分钟）：万一哪一步又漏了死线，单飞锁不能变成「这个会话里再也查不了」
   —— [[别留会把自己锁死的路]]。
3. **每趟结束清理**：`bundlesToDelete()` 删掉不是内置、不是跑着的、不是下次用的、不在下载中的包，
   一趟最多 5 个，每个删都过 `askBridgeVerbose`。**跑着的和下次用的两个都问出来了才清**（问不出来
   的那个可能正是要留的）；`next` 没登记上的那一趟不清。清理在结论落地**之后**、单飞锁放开**之前**
   —— 放在锁外的话，下一趟刚下完、还没 `next` 的那个包会被当成没人要的删掉。步骤记
   `nextBundle:ok(..)`、`cleanup:none` / `cleanup:deleted-N/M` / `cleanup:delete-rejected(id)`。

纯 JS，走热更下发即可。

**上线后的真机复验更正了一处判断**（`1790167173811-xtherv` / `1790167209248-0tkmvb`）：「代码里一个包都不删、
多出来那份永远 pending」**不对** —— 插件在冷启动 `load()` 里自己会清旧包（`cleanupObsoleteVersions`）。
旧代码那一趟把 `be01a38` 在 7 秒内下了三次，加上之前的两个，手机上一度有五个包；冷启动切到新包之后，
我们的清理还没跑 `list` 就只剩一个，于是新代码第一趟记的是 `cleanup:none`。所以真正的损失是**重复下载的流量**
（每次 10MB），由 ①② 修掉；③ 只是兜底「本会话内、还没重启时多出来的」那种，留着无害。
新代码那一趟：`nextBundle:ok(1ms) → decide:skip（已经是最新）→ cleanup:none`，行为符合预期。

#### 真正的障碍：`/models/` 与 `/dict/`

热更把 web 根整个切到 `Library/NoCloud/ionic_built_snapshots/<id>/`，而 `/models/`
（418MB 权重，`align/config.ts` 的 `LOCAL_MODEL_PATH`）与 `/dict/`（40MB 词库，
`dict/lookup.ts` 的 `DICT_BASE`）都是**相对站点根的绝对路径** —— 根一换就全 404。

> **2026-09-21（变更 42）：`/models/` 这一半没有了。** iOS 包里不再有权重，
> `linkBundledAssets()` 现在只链 `/dict/` 一条。下面这套取舍留着，因为词库还在走它，
> 而且「为什么不是 `convertFileSrc`」那一条的理由（Range）仍然是这个项目里最贵的一课。

| 路 | 为什么没选 |
|---|---|
| 塞进热更包 | 每次更新 458MB |
| 改走 `Capacitor.convertFileSrc()` | 赌 `_capacitor_file_` handler 支持 **Range**，而随包权重正是靠 Range 分块读的（§7.9 / `rangedFetch.ts`）—— 赌输就是 187MB 整份进内存、被系统杀掉 |
| **符号链接**（选这条） | web 代码**一行不动**，路径还是 `/models/`，取数走的还是同一个 `WebViewAssetHandler`，Range 行为逐字节一致 |

链接由 `AppDelegate.linkBundledAssets()` 在 `didFinishLaunching` 里补齐，**每次启动都重建**：
app 包路径里那个 UUID 每次安装都会变（`/var/containers/Bundle/Application/<UUID>/App.app`），
上一版留下的链接全是断链 —— 而 `fileExists` 跟随链接、对断链回 false，
「没有才建」会走进 `createSymbolicLink` 再以 EEXIST 失败，悄悄留下一个永远坏着的 `/models/`。
判断用 `attributesOfItem`（走 `lstat`，不跟随链接）。

**时序是对的**：插件下载新 bundle 时这个函数早跑完了，但新 bundle 要到**下次启动**才激活
（`next()` 不是 `set()`），而下次启动会再跑一遍这里。

#### 构建与发布

```
npm run build:ota     # = build:native + scripts/build-ota.mjs → ota-staging/
```

- **打的必须是 native 构建**（无 SW）。把带 SW 的 web 构建热更下去，等于给一个没有地址栏、
  没有开发者工具的壳装上一个清不掉的缓存层，**发出去没有回头路**（那份会自己缓存自己）。
  脚本里有一道 `sw.js` / `registerSW.js` 的硬校验挡着。
- 包里**排除** `models/`、`dict/`，**保留** `share/`（FR-18 的六张底图，1.3MB，会跟着代码改）。
  实测 **33 个文件 / 10.2 MiB**。
- 产物落 `ota-staging/`（不是 `dist/ota/`）：dist 要先后被两次构建占用，而 `vite build` 会清空 outDir。
- deploy 流水线：**打包排在 web 构建之前**（否则 web 产物被 native 产物覆盖，线上站当场失去 SW），
  **搬运排在之后**（web 构建刚把 dist 清空重建过）。两步的 `env` 必须与 web 构建**一模一样** ——
  漏了 `VITE_SYNC_API_BASE` 就是把「同步整块关掉」的构建推到手机上，而它不报错。

#### 这条路更新不了什么

原生代码一律不行：`align-native` 那个 Swift ONNX 插件、`AppDelegate` 的音频会话与符号链接、
Capacitor 插件本身、Info.plist、图标与启动图。它们照旧走 `ios-v*` tag → TestFlight。

**Android 不做**（这次）：`WebViewAssetLoader` 有 canonical path 检查，很可能拒绝指向 app 目录外的
符号链接，要另想寻址方式。Android 照旧发包。

## 8. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 浏览器驱逐 IndexedDB | **缓存层：无所谓**（重新下载即可）<br>**标注层：致命** | 分层（§2.3）把风险面从 700 MB 缩小到几百 KB。`persist()` + iOS 必须装主屏幕 + 自动同步（FR-11.6/11.7）—— 现在保护的只是那个小文件。**原生壳（§7.10）里「必须装主屏幕」这条自动满足**：App 的 WebView 数据只在卸载时消失 |
| 换手机 / 设备丢失，几年的 FSRS 状态蒸发 | **致命，且不可重建** | 自动备份异地存放（§2.6.3）；`vocab.json` 每次复习结束就推，不可重建的数据不过夜 |
| 备份静默失败，用完全无察觉 | **致命** —— 比没有备份更糟，因为它给虚假的安全感 | 同步状态常驻可见（FR-11.9）；失败显式报警；离线排队重试（FR-11.10）；会话失效立刻切回「未登录」而不是继续假装在同步 |
| 数据被写坏后，坏数据覆盖掉好备份 | 高 | 服务端每个文档保留最近 30 版（`revisions` 表，取代原方案的 git 历史）；手动导出按日期命名不覆盖（FR-11.11） |
| 同步服务器没了 / Google 账号登不上 | 中 | 保留手动导出作为不同故障域的第二道保险（FR-11.11）。服务器侧的数据就是一个 SQLite 文件，`scp` 一份下来即可 |
| **对齐把同步一起拖下水**（2026-09-03 新增，变更 35；**2026-09-21 随 fp32 重新评估**）| 中 —— 一次 OOM 就是「备份也停了」| 四道：① `mem_limit` 256m → 2g → **4g**（变更 42 换成 fp32，1204 MiB 的权重加载时 protobuf 缓冲与张量同时在，峰值比 q4 那 584MB 高一档）；② **闲置 10 分钟释放会话**（`ALIGN_IDLE_MS`）—— 这条是新加的，它让「常驻 2.1 GiB」变成「一周里有几分钟 2.1 GiB」，也是这台机器还跑着别人东西时最要紧的一道（**2026-09-21 实测：2.145 GiB → 56.27 MiB**）；③ 串行队列 + 排队上限，永远只有一份权重在内存里；④ `onnxruntime-node` 是 optionalDependency、`ALIGN_ENABLED` 可整块关 —— 对齐坏掉时同步照常，路由只回 503。**2026-09-21 实测**：真跑完一课之后 `docker stats` 是 **2.145 GiB / 4 GiB（53.6%）** —— 比原来估的 1.3GB 高，但离上限还有一半；**加载那一刻的峰值没盯着看**，所以 4g 这个上限先不动。哪天嫌它占得多就把 `ALIGN_MODEL_DTYPE` 改回 `q4`（那时两条路又回到同一份权重）。`/v1/healthz` 把对齐的状态单列一项，因为它会**单独**坏掉（权重下不下来、ORT 装不上），而那从同步一侧完全看不出来 |
| **服务器算的和本机算的不是同一份时间戳**（2026-09-03 新增，变更 35）| 中 —— 它不会报错，只会让同一课在不同设备上边界微妙地不同 | 三条路（浏览器 / Swift / 服务器）用**同一个 dtype（q4）**、同一套分块与帧号规则，且矩阵在线缆上**不做任何有损压缩**（不 float16、不 gzip）。分块那套数学在两份 TS 实现里各有一份单测，期望值是同一组（`server/src/align/frames.test.ts` 顶部写了它充当契约）；真正的验收是拿 `probe.ts` 打出的指纹和桌面上同一课对比 |
| 恢复路径长期没人走，早已悄悄坏掉 | 中 | 验收必须完整演练一次；每年提示重演（FR-11.11、§2.6.5） |
| 存储配额被音频撑爆 | 中 | 缓存可清空且无损（R-缓存-3）。配额告警时引导清除最旧的 N 课缓存，标注层不受影响 |
| DW 改稿导致补齐后 offset 全错 | 中 | `manuscriptHash` 校验（FR-3.7）。这是**静默数据损坏**类风险，必须在补齐时就拦住，不能等到跟读发现对不上 |
| 切句错误导致整篇索引错乱 | 高 | 强制手工修正 UI（FR-2.3）+ R-lower 启发式 |
| Manuskript 含 Glossar，与音频不对应 | 高 | 段落排除（FR-1.4） |
| 手机上没有音频，复习退化成看文本 | 高 | 音频缺失态显式化（FR-3.3）+ 手动搬运流程写进 README |
| 两端同时复习，FSRS 状态分叉 | 中 | 约定单向搬运（§2.3）+ last-write-wins 合并 + 合并摘要 |
| V1 无 lemma，同词跨课重复建卡 | 中 | surface 匹配提示合并（FR-9.3），接受局限，V2 补批量归并 |
| 标注全部句子太累导致弃用 | 中 | 稀疏打点是**设计**不是妥协；UI 明示「已标注 6 / 112」不制造焦虑 |
| iOS 静音开关导致「没声音」被误判为 bug | 低 | **原生壳里已消除**：`AppDelegate` 把 `AVAudioSession` 设成 `.playback`（§7.10）。纯 web / 主屏幕 PWA 上仍然存在，靠 README 提示 |
| 原生壳的版本更新走 App Store，坏版本回滚慢 | 中 | **2026-09-20 起 iOS 走热更（§7.12）**：前端改动 push main 之后手机自己拿，回滚也只是再 deploy 一次（buildId 相等判断，方向无所谓），崩了还有 `notifyAppReady` 超时自动退回上一个 bundle。**换不了原生代码**——AppDelegate、Info.plist、图标仍然只能出包，那部分照旧 `ios-v*` tag → TestFlight，重发一个新 tag 就是一次修复。Android 暂时全靠发包。线上 web 版（d.gamestao.com）始终是同一份代码的可用退路，且数据在同步服务器上而不在包里 |
| DW 改版打掉自动抓取 | 中 | 三层降级（FR-13）+ `__APOLLO_STATE__` 快照回归测试让改版先让测试红；**L3 手动路径永不删除** |
| DW 收紧 CORS 头，纯前端方案失效 | 中 | 这是整个 FR-13 的单点依赖。真发生了就退回本地 helper 方案（§7.8 已记录其设计），或退 L3。**因此 adapter 层必须与 UI 解耦**：换成 helper 时只改数据来源，不动导入流程 |
| GLOSSARY span 的 offset 映射静默错位 | 中 | HTML→文本转换单独写测试（§7.8）；候选词在 UI 上高亮显示，错位肉眼即可见 |
| 自动导入被误解为「工具在分发内容」 | 中 | §3.1.1 的 R-1～R-5 写进 README；实现上就是浏览器直连，代码里根本没有服务器 |
| 「一键导入全部 100 期」的诱惑 | 低 | 明确不做（FR-13.10）。既是 R-3 的礼貌约束，也是防止一次性灌进 100 期后一篇都不学 |

---

## 9. 建议实现顺序

V1 一次做完，但按下面顺序写，每步都能跑：

1. ✅ 数据层：IndexedDB 两层 schema + `persist()` + 导出/导入合并（先把命根子做稳）——**已完成**（2026-08-31）。~~含 QR 配对二维码（FR-11.18，实现中加的，原规格没有）~~ —— 二维码 2026-09-02 随 PAT 一起删了，见 §0 变更 25
1b. ✅ **自动备份 + 恢复（FR-11）** —— 紧跟数据层，在积累任何真实数据之前就跑通。等攒了半年再补备份，那半年是裸奔的——**逻辑已完成、测试通过，但整条链路仍未端到端跑通**：2026-09-02 从 GitHub + PAT 换成自建后端 + Google 登录（§0 变更 25），现在卡在 DNS / Google 客户端 ID / Caddy 三项配置上，步骤见 `deploy/README.md`；前端已部署上线 https://d.gamestao.com（CI/CD，见 §2.5），后端在 `sync.gamestao.com`
2. ✅ 导入 + 切句 + 手工修正（②）—— `lesson/segment.ts`、`lesson/sentences.ts`、`lesson/resegment.ts`
3. ✅ 音频存取 + 全局播放器单例（③）—— `audio/player.ts`，全程一个 `<audio>`
4. ✅ 时间戳标注（④）—— `lesson/timing.ts` + `TimestampsTab`
5. ✅ **跟读模式**（⑥）—— `audio/shadowing.ts` 状态机（不是嵌套 setTimeout）
6. ✅ 生词标记 + 挖空（⑤）—— `lesson/tokens.ts`，支持 `hing ... ab` 的多区间 Blank
7. ✅ 听写（⑦）—— `dictation/check.ts` 四级判定 + LCS 字符 diff
8. ✅ 生词本 + FSRS 复习（⑧）—— `srs/fsrs.ts`（ts-fsrs 只在这一个文件里出现）+ `srs/queue.ts`
9. ✅ PWA 打包 + 手机端复习界面适配 + 跨设备同步 —— `vite-plugin-pwa`（只缓存应用壳）、
   safe-area / 44px 触控目标、FR-11.13 一键恢复（`sync/restore.ts`；2026-09-02 之前是
   `github/restore.ts`，见 §0 变更 25）
10. ✅ DW adapter + 自动导入（FR-13 / §7.8）—— 已用真实页面跑通，见附录 A.6
11. ✅ Glossar 候选词（FR-14）—— 14 条候选全部落位正确，见附录 A.6

**全部 11 步于 2026-08-31 完成。** 之后又加了两块 §9 原本没有的：
**FR-15 自动打点**（§7.9）与 **§7.10 原生壳（Capacitor，iOS + Android）**。
~~尚未做的是真实 PAT 的 GitHub 联调（附录 B.2 仍未定）~~ —— **这个窟窿是 2026-09-02
把整条 GitHub 路换掉（§0 变更 25）之后消失的，不是补上的**；它现在的等价物是
`deploy/README.md` 里那份验收清单（真实 Google 登录 + 跨设备恢复演练），同样还没跑。
§10 里依赖备份链路的那几条验收项，
以及原生壳的**真机验证** —— 两套工程与 CI 都写完了，但没有 Mac 的机器上跑不了 xcodebuild，
iOS 那条流水线**已于 2026-09-01 首次跑通**（`ios-v0.1.0`，run 33515038917，
5m45s 一次通过，`UPLOAD SUCCEEDED`，build 1.0(1) 已进 TestFlight 并装到真机），
实测数字与「上传成功之后还有两道」都回写进 §7.10。
Android 那条仍未跑过。

**第 12 步（2026-09-02 追加）：内置词典 + 预置词库（FR-16 / FR-17）。**
排在全部 11 步之后，因为它解决的是「还没有笔记时怎么练」—— 一个只在**开头**存在的问题，
而前 11 步决定的是工具长期能不能用。做的时候顺手修掉了 FR-9.3 记着的那条已知局限
（去重从 surface 升到词元），那是词形索引的免费副产品。

**第 13 步（2026-09-02 追加）：复习形式改成听音四选一（变更 29）。**
紧跟第 12 步，因为第 12 步刚把「有卡可练」解决掉，而卡的**形状**是错的 ——
预置词库发出来的卡进的是一个 Anki 形状的队列（显示答案 + 四档自评），
于是「每天十个词」在体感上是「看十次答案、自评十次」。
顺序上例句（FR-16.9）必须排在卡面之前：卡背要用它，而它是构建期产物，
写完卡面再回头补例句意味着卡面要先上线一版**答错了没有例句**的形态。

理由（同原规格 §8）：界面优先级 **跟读 > 听写 > 复习**。跟读每次都会用，复习要攒够词才有意义。

第 10 步排在后面，是因为它只替 FR-1.2 / FR-1.3 填数据 —— 技术上第 2 步之后随时能做，但它省的是每篇 5 分钟，而前 9 步决定的是工具到底能不能用。**先把地板铺好，再铺地毯。**

不过在探明 CORS 之后（附录 A），第 10 步的成本从「写一个 Node 服务 + 处理两个 origin」掉到「三个 `fetch` + 一次 `JSON.parse`」，大约半天。如果第 2 步做完时手痒，提前做掉也不会亏 —— 它不改动任何已有代码路径。

第 11 步依赖第 6 步（挖空）已经存在，否则候选词无处可去。

---

## 10. 验收清单：跑通一篇 Alltagsdeutsch

> **进度（2026-08-31）**：下面标 ✅ 的是已经实际跑过的；标 🧪 的只有单元测试覆盖，还没在真实素材上手动走过；
> 标 ❌ 的全部卡在同一件事上 —— **备份链路还没端到端跑通过**。
> （2026-09-02：这件事换了形状 —— GitHub + PAT 那条路整条删掉换成了自建后端 + Google 登录，
> 见 §0 变更 25。现在卡的是 DNS、Google 客户端 ID、Caddy 那三项配置，步骤在 `deploy/README.md`。）
> 这一节没有一条是「代码写完了就算过」，请照着单子亲手走一遍。

- [x] ✅ 导入成功 —— 用真实期次 `45334084` 走的是**自动导入**（FR-13，比清单里的手工路径更强）；手工路径也已跑通（自造文本 + 粘贴）
- [x] ✅ 文末 Glossar 不出现在练习中 —— DW 正文里本来就没有 Glossar 段落（附录 A.3）。**自动导入现在一句都不排除**：曾经自动排掉的开头 3 句（标题+导语）实测在音频里被念了出来，见 §0 变更 24
- [x] ✅ 卡片正面自动播放句子音频；队列到期/未到期与下次到期时间都正确
- [x] ✅ 听写判级：`gelegenheitsjob` → 判为「仅大小写错」，FSRS 记 `Hard`
- [x] 🧪 `toShareablePackage` 单元测试通过（断言导出物不含任何正文）
- [ ] FR-6.8 真麦克风跟读一轮：录到的是自己的声音而不是外放的原句（回声消除生效），回放音量正常；iPhone（壳 ≥ 0.7.0）上开麦之后原句仍然走外放、不被切到听筒
- [ ] 🧪 跟读循环、静默间隔、0.85 倍速 —— 状态机有 10 个单元测试（含跳句取消定时器、迟到回调丢弃），但还没在真实音频上连着跟读一遍
- [ ] 🧪 不连续搭配 `hing ... ab` 的多区间挖空 —— 有单元测试，真实素材上还没标过
- [ ] 自动切句后，手工修正不超过 5 处即可得到正确句子列表 —— 42 句的切分结果肉眼看过，没有逐句数修正次数
- [ ] 通听一遍，中途关闭浏览器再打开，**无任何网络请求**即可继续播放（缓存命中，R-缓存-1）
- [ ] 标注 6 句想练的句子，其中有一句用微调修正了起点
- [ ] 标记 10 个生词
- [ ] ❌ 以下全部依赖同步链路接通（`deploy/README.md` §0 那三项配置）：Google 登录 / 非白名单账号被 403 挡下 / 复习后服务器上的 `vocab` 涨一个版本 / 断网排队后自动推送 / **恢复演练** / 手机上只靠 PAT 恢复

原始清单如下，逐条对照着走：

- [ ] 从 DW 下载一期 mp3 + 复制 Manuskript，导入成功
- [ ] 自动切句后，手工修正不超过 5 处即可得到正确句子列表
- [ ] 文末 Glossar 被成功排除，不出现在任何练习中
- [ ] 通听一遍，中途关闭浏览器再打开，**无任何网络请求**即可继续播放（缓存命中，R-缓存-1）
- [ ] 标注 6 句想练的句子，其中有一句用微调修正了起点
- [ ] 跟读这 6 句：循环、静默间隔、0.85 倍速都正常；标记其中 2 句为困难；切换到「只练困难句」正常
- [ ] 标记 10 个生词，其中至少 1 个是不连续搭配（如 `hing ... ab`）
- [ ] 听写：故意打 `Hauser` → 判为「转写等价」；故意打 `zuversicht` → 判为「仅大小写错」；故意打错一个词 → 判错并显示 diff
- [ ] 错词自动进生词本，带原句
- [ ] 第二天打开，SRS 队列有卡；卡片正面自动播放句子音频
- [ ] 导出备份 JSON：含 `_warning` 字段，**不含任何音频数据**，体积在几十 KB 量级
- [ ] 点「用 Google 登录」→ 显示已登录 + 邮箱，**全程不需要手抄任何字符串**（原文这里是「粘贴 PAT → 一键建仓」，随 §0 变更 25 改写）
- [ ] 用**不在白名单**的 Google 账号登一次 → 明确的 403 提示（不是白屏、不是「登录成功但同步不动」）
- [ ] 复习一轮 → 服务器上 `vocab` 文档版本号 +1，FSRS 状态已更新（`GET /v1/docs/vocab`）
- [ ] 断网复习 → 同步进入排队且有提示 → 恢复网络后自动推送成功
- [ ] **恢复演练（不做完不算验收通过）**：换一个浏览器 profile，只用 Google 登录 → 一键恢复 → 标注、生词、FSRS 状态全部齐全 → 课程显示「素材未下载」→ 补齐 → 完成一轮带音频的复习
- [ ] 手机上「添加到主屏幕」（或装原生壳），同样只靠 Google 登录恢复（**不传任何文件**），完成一轮带音频的复习
- [ ] 桌面上清除这一课的缓存 → 标注、生词、FSRS 状态全部完好 → 重新补齐后时间戳依然对得上（`manuscriptHash` 校验通过）
- [ ] `toShareablePackage` 的单元测试通过（导出物不含任何正文）
- [ ] **标记一个变形词**（如 `Plattformen`）→ 性、复数、释义、IPA 自动填上，词头显示为 `Plattform`（FR-16.2 / FR-16.4）
- [ ] **在两课里标同一个词的不同变形**（`laufen` / `gelaufen`）→ 第二次提示「已有该词条」（FR-9.3 修订）
- [ ] **报名第 4 档**（3000 词）→ 面板显示「已激活 0 / 剩余 3000」，**几秒内完成**（写一条设置，不建 3000 张卡）（FR-17.3）
- [ ] 进复习页 → 今天自动激活 `newPerDay` 张新卡，发音随之取好；面板的「已激活」数字跟着涨（FR-17.4）
- [ ] **复习一张新卡** → 正面只有播放键、没有文字；四个选项是**德语词形**；选错 → 正确项变绿并展开卡背（性/复数/IPA/德语释义/例句）（FR-10.2 / 10.8 / 10.9）
- [ ] **复习一张已进入 Review 的卡** → 四个选项换成**德语释义**，且每条都短到能读；播放键下方亮出**那个词**，题干写「这句里有这个词，选出它的意思」（FR-10.9 / 10.10 / §12.19）
- [ ] **答对一张名词卡** → 顶部闪出带冠词的「✓ der …」→ 自动进下一张，**全程没有任何「几天后复习」的选项**（FR-10.4 / 10.11）
- [ ] **辨形题的四个选项确实音近**（如 `Vorhang / Vorgang / Vorhand / Anhang`），不是随机四个不相干的词（FR-10.10 / FR-16.8）
- [ ] **断网复习预置卡** → 声音照样有（发音在激活时就下好了，FR-17.6）；例句也在（随包，FR-16.9）
- [ ] 设置页显示词典条数与**数据来源署名**（FR-16.6 / FR-16.7 —— CC BY-SA 的义务）

**界面（§12，2026-09-05 新增）**

- [x] 桌面与手机两种宽度、浅色与深色两种主题下走完一遍：课程 / 复习 / 生词本 / 一课的四个 tab / 来源 / 素材 / 设置
- [x] **对齐进行中打开一课** → 进度折叠成音频条上沿那条线，播放键**没有被挡住**（§12.2）
- [x] `grep -E '(bg|text|border|divide)-(neutral|amber|sky|emerald|rose|red)-[0-9]+' src` 在 tsx 里**零命中**（注释除外）
- [x] 界面上搜不到 `FR-`、`§`、`L1/L2/L3` 这类内部代号
- [ ] **iPhone 真机**：底部标签栏与音频条各自让出 home indicator（`env(safe-area-inset-bottom)`）；顶栏让出刘海；深色下状态栏是深的（`theme-color` 的 `media` 分支）

**学习记录与分享图（FR-18 / §12.8，2026-09-15 新增）**

- [x] 在复习页待 40 秒（只点一下）→ `meta.studyLog` 里本机那一格出现 45 秒；**离开复习页后再待 20 秒，数字不动**（浏览器实测）
- [x] 记录页三个数、最近 14 天柱子、卡片预览在桌面/手机 × 浅色/深色四种组合下都正常（浏览器实测）
- [x] 点「保存 / 分享」→ 桌面浏览器落到下载，导出物是 1080×1350 的 PNG（浏览器实测）
- [ ] **锁屏/切后台验证**：手机上在跟读页锁屏五分钟，回来后今天的时长**没有**涨五分钟
- [ ] **跨设备**：桌面练 10 分钟、手机练 5 分钟 → 同步之后两台机器都显示今天 15 分钟（逐格取 max 相加）
- [ ] **iPhone 真机**：「保存 / 分享」拉起系统分享面板，存进相册的图是完整的 1080×1350
- [ ] 连续天数跨零点后自增（等一天）

**译文与笔记（FR-19 / FR-20 / §12.9，2026-09-15 新增）**

- [x] 「⋯ → 译文」复制走的文本是「提示词 + 编号原文」，排除句不占号也不出现（浏览器实测）
- [x] 粘一份**带前言、结语、加粗编号、缺一条、多一条**的译文 → 认到几条、缺哪一句、
      多出来哪个编号三样都在界面上说清楚；保存后覆盖率正确（浏览器实测）
- [x] 一份**没有编号**的译文 → 一条都不认，并说明为什么不按行数猜（单元测试 + 界面）
- [x] 通听展开文本 → 「显示中文」开关出现，每句下面一行中文，没译文的那句留空（桌面/手机 × 浅色/深色）
- [x] 跟读当前句下面一行中文，和通听共用一个开关（浏览器实测）
- [x] 笔记写几行 → **立刻切 tab 再回来**，内容还在（卸载时那次保存）；摘要显示行数（浏览器实测）
- [x] 合并 / 拆分 / 重新切句之后译文的去向符合 FR-19.5（单元测试）
- [x] `toShareablePackage` 里既没有 `translation` 也没有 `notes`（单元测试）

**自动化测试（变更 44，2026-09-21）**

> 这一段和上面几段不同：它**不是让人照着走的单子**，是「哪些事已经不用人再走一遍了」的清单。
> 每加一条，上面就少一件要靠记性的事。

- [x] 四条都绿：`npm run typecheck`（**含 e2e/**，新增 `tsconfig.e2e.json` —— Playwright 自己只转译、不查类型）、`npm run test:run`（**847**）、`npm --prefix server test`（**120**）、`npm run test:e2e`（**40**）
- [x] 三个 job 都是 CI 门禁 —— 任一红就不发（push `main` 就是上线）
- [x] E2E **跑构建产物**（`vite build` + `vite preview`），不是 dev server —— 它守的是「类型过了、单测过了、构建也过了，但打开是白屏」
- [x] E2E 里的数据一律**从界面种**（导入一份备份 JSON），没有一处 `page.evaluate` 直接写 IndexedDB
- [x] 刷新页面之后课程还在（真实 IndexedDB 持久化）
- [x] 选一个真 WAV 进去，时长被浏览器解出来并显示成 `0:02`
- [x] 导出备份：`a[download]` 真的落了一个 `backup-YYYY-MM-DD.json`，内容里**没有** `plainText` / `manuscriptHtml`
- [x] 导入备份：先自动落一份 `before-import-*`（FR-11.14），确认之前不进库，确认之后**不用刷新就看得到**
- [x] 复习评分真的落盘：答一张之后刷新，导航角标从 2 变 1
- [x] **跟读录音回放（FR-6.8，变更 63，2026-09-26）**：`shadowing.test.ts` +12 条 —— 录音一直录到 `finishTake()` 才回放、回放放完才进下一句、保底上限 = max(20 秒, 句长 × 5) 且到点当作读完、连点两次/上限与按钮撞车只回放一次、不在录音时 `finishTake` 无效、重复 2 遍两遍都录、`echo` 关着行为不变、麦克风起不来退回固定间隔、没录到不回放、交出录音前跳句不放迟到的录音、录音中跳句上一句的「读完了」作废、回放中停止/重播；假录音机的 `stop`/`play` **带真实延迟**
- [x] **录音器本身与 iOS 门槛（FR-6.8，`echo.test.ts` +24 条）**：交出的 Blob 带录音器的 mimeType、要了回声消除、跟读期间只申请一次麦克风、没 open 就 start 会抛（状态机据此退回固定间隔）、`halt()` 之后才到的 stop 事件交出 null、`close()` 真的停音轨、回放被掐断后到的 ended 不推进、**ended 永远不来时时长 + 1 秒保底**且与 ended 撞车只推进一次；`echoSupported()`：iOS 壳 ≥ 0.7.0 才开、**旧壳和问不出版本都回 false**（调了就是闪退）、Android 不问原生侧直接 false、缺 MediaRecorder/mediaDevices 回 false；`describeMicError` 四种错误的文案。**五处关键判断各自删掉一次，都有用例变红**（iOS 门槛、作废检查、保底、掐断检查、回声消除）
- [x] **E2E 用 Chromium 假麦克风跑真链路（`e2e/shadowing.spec.ts` +4 条）**：getUserMedia → MediaRecorder 真吐出 webm/opus → decodeAudioData 真解得开 → 录了 2 秒就真放了 > 1.2 秒才进下一句（解码坏掉时这条会红，已反证过）、第二句用 Enter 收尾；停止后麦克风音轨全部 ended；麦克风被拒时出提示、退回倒计时、没有「读完了」按钮、照样进下一句；关掉开关后一次都不申请麦克风。`timedLessonBackup` 多了 `sentenceSeconds` / `settings` 两个可选参数（1.5 秒一句，否则每条用例要干等 10 秒原句）
- [x] **导航角标与复习页口径一致（变更 62，2026-09-24）**：角标套 `newPerDay`/`reviewPerDay` 上限、算法与 `buildReviewQueue` 同一份，不再是不设上限的原始到期数——19 张到期新卡、`newPerDay=10` 时角标是 10，不是 19（`AppNav.test.tsx`）
- [x] **识词卡（FR-21，变更 45）纯函数层**：队列按卡展开、同日互斥、开卡门槛、三种题型的降级链、挖空遮不干净就不出题、合并取两张卡的较新者（`srs/queue` `srs/choices` `srs/questionSource` `srs/glossZh` `backup/merge`）
- [x] **评分落在哪一张卡上**（`ReviewPage.test.tsx`）：读卡答完 `fsrsRead.reps` 涨了而 `fsrs` **一个字都没动**。这是整个 FR-21 最容易静默写错的地方 —— 写反了不报错、界面上也看不出来，几周后表现为「那个词的听力题再也不来了」
- [x] **开读卡时把句子拷进卡里**（`useVocabStore.read.test.ts`）：课程卡用原句且**不查词典**、没有原句的去词典取两条、加词时已收下的那份**不被覆盖**、词典查不到或抛异常照样开卡。这条链断了的症状同样是静默的 —— cloze 组不出来就降级成 read-form，而那是一道完全正常的题
- [x] **听卡的题干跟题型走（§12.19，变更 65）**：辨形题说「你听到的那个词」且**正面没有词形**、辨义题说「选出它的意思」且词形**恰好亮一处、不在选项里**、孤立词发音的辨义卡同样亮出来、任何一种都不再出现「挖掉」
- [x] **读卡的正面**：题干说清考什么、**没有播放键**、放弃出口只写「不认识」、cloze 题面里定宽的空在而原词不在
- [x] **备份带得走读卡那一半**：`fsrsRead` / `examples` / `meaningZh` 经过真的 `JSON.stringify` 往返还在；**早于 FR-21 的老备份**导入不崩，恢复出来就是「读卡还没开」（没有额外的开关字段，所以不需要迁移）
- [x] **补中译那一块**（`ZhPanel.test.tsx`）：一个词都不缺时**整块不出现**、预览按编号给出「德语词 → 中文」对照、保存落库、认不出编号时不给保存按钮
- [x] **当天做完就清零（FR-10.13，变更 47）**：新卡四档没有一档排在当天、**重学**那条路（毕业卡被 Again）同样至少隔一天且不进 `Relearning`、存量 `Learning` 卡一评就毕业、今天评过的卡哪怕 `due` 已过也不进队列、**昨天**评过的照常回来、真评一次之后重建队列为空且 `nextDueAt` 说得出口（`srs/fsrs` `srs/queue`）。两道闸**分开测**：`busy` 那道挂在 FR-21.3 名下、最小间隔那道挂在 FR-10.13 名下，它们会分开坏而且坏起来都像在做好事
- [x] **卡背缺中文时自动问 AI（FR-10.14，变更 55）**：答错且两样中文都没有 → 先「AI 解释中…」再把答案显示出来**并落进 `entry.note`**；**四条「不该问」**分开钉 —— 有 `meaningZh` 不问、有 `note` 不问、**答对不问**（那条路不露卡背）、缓存命中不问；问不到时卡背其余部分照常在上面且「重试」真的能再问一次；没配 AI 时**一个字都不多说**；**翻页之后才回来的答案仍然落到那个词条上、不串到当前这张卡**（不做取消是有意的，加上取消的症状是钱照花、答案没了）；同一张卡 revealed 期间重渲染只问一次；问的是**词元**不是卡上那个变位形式；课程卡带的语境是**原句**而不是词典例句；读卡的卡背同样会补；缓存读坏了照样问得到（`ReviewPage.test.tsx`，+15 条）。**E2E 另有一条**（`e2e/review.spec.ts`）：真构建产物 + 没登录时，卡背上 AI 那一块三种形态一个都不出现 —— 组件测试里 `aiAvailable()` 是 mock 的，它真实的返回值只有这里问得到
（识词卡相关本次共 +86 条）
- [x] **E2E 里读卡真的打得开**（+3 条）：题面是文字、**卡上没有播放键**、挖空句里定宽的空在而原词不在、下一张是 `read-gloss` 且题面就是那个词、评分落在读卡上而**听卡的下次时间没被动过**
- [x] 补中译（FR-21.9）与 FR-19 的解析器接得上：整段带前言、`**2.**`、`- 3)` 三种写法一起贴回来，编号照样对得上
- [x] 窄屏（Pixel 7）：底部标签栏三个活动、热区 ≥ 44px、详情页收起、`--bottom-inset` 等于标签栏实测高度、六个页面都不横向滚动
- [x] 六个页面走一遍，**控制台零报错**
- [x] **桥自检与诊断上报（变更 57，2026-09-23）**：七个只读方法**串行**各调一遍 + 对照组 `App.getInfo`、
      只有某一个方法哑住时其余照常回答（分得清「全哑」和「个别坏」）、原生侧报错记 `rejected` 并带上它说的那句话、
      浏览器里整块空操作；**桥还没回过话时 4 秒不算超时**（验证过：把 `COLD_BRIDGE_TIMEOUT_MS` 调回 4000，这条用例就红）、
      **回过一次话之后窗口缩回 4 秒**、**显示用的 `runningBuild()` 不走放宽**（「4 秒级」那条旧用例就是判据）、
      步骤记录带耗时；`consoleTap` 抄得到且**原函数照常被调用**、参数序列化不了不抛、装两遍不记两份、环形缓冲留最近的；
      `runningBuild` 吃到不认识的形状不抛（验证过：去掉可选链这条用例就红）；
      服务端收件箱存/读/列表、每用户隔离、超上限删最旧、id 与 userId 都不能跳出目录；
      路由要登录、2MB 上限、坏 JSON 不落盘、没开收件箱回 503；设置页三个按钮在、自检结果带耗时显示、
      发送失败说清原因、**剪贴板被拒时把全文摊开而不假装成功**；
      **走不完也留得下记录**（日志停在哪一步就是卡在哪一步）、一笔都没有时要说出来、
      查更新整个卡住时按钮也一定会放开并明说「它没有回来」（前端 +26、server +12）
- [x] **取插件那一步（变更 59，2026-09-23）**：首选 core 造代理（步骤记 `plugin:core`）、
      core 塌了退回插件包那条路照样走完、**两条都拿不到时是「这次不查了」而不是挂住**（真机症状的判据）；
      `withDeadline` 对永不 settle 的东西也会在时限后回 `null`、抛出来的当「走不通」不甩给调用方；
      `notifyAppReady` 没人回话会重试三次并最终 settle、**原生侧真报错就不重试**、结果落进诊断；
      **存储写不进去时这一趟的记录照样完整**并把写失败的原因说出来（此前这两种成因在文件里长得一模一样）；
      设置页多两行：「我起来了」成没成/第几次、localStorage 写失败过（前端 +12）
- [x] **manifest 的 CORS（变更 60，2026-09-23）**：`deploy.yml` 发布后带 `Origin: capacitor://localhost`
      取线上 `/ota/manifest.json`，响应里没有 `Access-Control-Allow-Origin` 就红——网页版同源，这条只有手机壳会踩
      **推之前也守着**（前端 +7）：`otaHeaders.test.ts` 解析 `public/_headers`，`/ota/manifest.json` 必须拿到 `*`、
      不能同时声明允许凭据、规则不把整站放开；`nativeUpdate.test.ts` 补真机那种**立刻失败**的形状
      （`fetch:threw:Load failed`，后面几步一步不走）、非 2xx 跳过，以及**取 manifest 保持简单请求**
      （绝对 URL、不带凭据、不加请求头——`*` 只对简单请求成立，改成带凭据或加头都会让手机重新停在这一步）。
      两条都做过「改坏就红」的反证
- [x] **同一版只下一次 + 清理旧包（变更 61，2026-09-23）**：已下好待生效（`getNextBundle`）就不再下；
      两个调用同时进来只下一次、结果相同；**前一趟挂住 3 分钟后新调用不再等它**；已是最新时清掉多出来的 pending；
      下了新版时删旧「下次用」、留刚下的；跑着的或下次用的问不出来、`next` 失败时都不清；删失败只记一笔不改结论（前端 +14）
- [ ] **仍然只能靠人**：iPhone 真机的安全区与 WKWebView 行为、真实对齐（要下权重）、真实 DW 抓取、真实 Google 登录与恢复演练

**热更新（§7.12 / §12.12，2026-09-20 新增）**

- [x] `decideUpdate` 的五条判据各有用例：内置必更新 / 版本相同不动 / 壳太旧不下 / **回滚要跟着走** / manifest 残缺当没看见（单元测试）
- [x] `npm run build:ota` 的包里 **index.html 在根、没有 models/ 与 dict/、没有 sw.js、share/ 七张都在**（实测 33 个文件 10.2 MiB）
- [x] 拿 web 构建（带 sw.js）跑打包脚本 → **构建期报错退出**，不产出包
- [x] `npx cap sync ios` 之后 `CapacitorUpdaterPlugin` 出现在 `packageClassList` 里（实测）
- [x] 设置页「版本」小节：网页版那半在浏览器里走过桌面/手机 × 浅色/深色；原生那半（两个版本号、「随包那份」、待生效提示、静默档）由组件测试覆盖
- [x] **发布链路实测（2026-09-21，`38ae39a`）**：deploy 跑完之后从**公网**拉 `/ota/manifest.json`（`buildId` 就是那次的 commit），按里面的 url 下 zip，**sha256 与 manifest 逐位相符**
- [x] `ios-v0.4.0` 出包并上传成功（构建号 9，IPA 430MB，5m50s）—— 第一个带热更客户端的壳
- [ ] **iPhone 真机（0.4.0 壳装上之后）**：deploy 一次 → 杀进程重开 → 设置页「版本」里前端那行变成新的 buildId
- [ ] **iPhone 真机**：热更生效之后**对齐与查词仍然可用**（这一条验的是符号链接；断了的症状是对齐退回「从 HF 下载 200MB」、查词查不到）
- [ ] **iPhone 真机**：装一个新 IPA（0.4.1）→ 热更 bundle 被丢掉、回到随包那份，且 `/models/` 没有变成断链
- [ ] **回滚演练**：把 main 回退一个 commit 再 deploy → 手机下次启动退回旧版
- [x] `ios-v0.6.5` 的真机诊断**指认出了卡住的那一行**（取插件的 `import()`）：插件注册成功 + 七个方法全是毫秒级 + 步骤停在 `platform:ios` + 原生日志里没有 `notifyAppReady was called`
- [ ] **iPhone 真机（0.6.6 壳装上之后）**：「上次查更新」走完整条（`plugin:core → fetch:ok → current:ok → getInfo:ok → decide:… → download:ok → next:ok`），且「我起来了」那一行是 `ok`

**查词（FR-9.5 ~ FR-9.10 / §12.11，2026-09-20 新增）**

- [x] 查一个**变形词**（`abgewogen`）→ 词头显示 `abwägen`，并说出「你查的是 abgewogen」；
      卡上有 `Präteritum: wog ab/wägte ab, Partizip II: abgewogen, abgewägt`、例句、近义词、词源（浏览器实测）
- [x] 查一个名词（`Zuversicht`）→ `die` + 复数 + IPA + 德语释义 + 中译 + 英译 + 例句；
      同一句例句**不会**因为引号差异出现两遍（浏览器实测 + 单元测试）
- [x] 点「念一遍」→ 真人录音响，卡上写明「孤立词发音，练不到连读」（浏览器实测）
- [x] 查一个不存在的词 → 说清是「都没有」，给维基词典搜索链接，且仍可原样收下（浏览器实测）
- [x] 「加入生词本」→ 列表里立刻多一条，来源写「查词添加（孤立词发音）」，不是「（课程已删除）」（浏览器实测）
- [x] 再查同一个词的**另一个变形** → 提示「生词本里已经有 abwägen 了」，不给新建（浏览器实测）
- [x] 桌面/手机 × 浅色/深色四种组合下版面正常；`grep` 两条判据仍然零命中（浏览器实测）
- [x] 把「联网查」关掉再查 → 照样出结果（性/复数/IPA/释义/中译/例句都在），来源那一行写
      「内置词典 · 联网查已在设置里关掉」（浏览器实测）。**真断网**那一支（写「de.wiktionary 没回应（离线？）」）还没走过
- [x] 查词加进来的词**复习一轮**：卡面是孤立词发音那一档（真人录音 + 「练不到连读」），
      卡背写「查词时加进来的词，不来自这里的任何一课」，**没有**「去这一课重新对齐」那个出口（浏览器实测）
- [ ] 跨设备：桌面上查词加进来的词，手机上同步之后在生词本里

**低置信句的确认（FR-15.19 / §12.10，2026-09-16 新增）**

- [x] 通听展开文本 → 带 `?` 的行号点一下：`?` 消失，头部「N 句置信度偏低」与那条提示同步减一（浏览器实测）
- [x] 全部确认完 → 头部只剩「已对齐 N / M 句」，提示与通听那条 `Hint` 都不见了；**刷新后仍然静默**（浏览器实测）
- [x] 再点一下同一个行号 → `?` 回来，头部计数跟着加回去（浏览器实测）
- [x] 确认几句**不改变**剩下几句的低置信判定（单元测试：阈值算在全部自动对齐句上）
- [x] 「重新对齐」之后确认全部作废，那几句重新带 `?`（单元测试）
- [x] 重新切句（改一个错字）之后确认跟着搬，不会重新变成待校对（单元测试）
- [x] 手机宽度 + 深色：行号的 44px 热区**没有**把句子第一行推下去；折行与译文都对齐在编号之后（浏览器实测）
- [ ] **剪贴板主路径**：真浏览器里点「复制原文与提示词」真的复制成功（自动化环境里被拒，
      走的是「自己选中复制」那条退路 —— 那一条已验）
- [ ] **跨设备**：桌面贴译文 → 手机上同一课的通听里看得到中文

**iOS 去掉本地对齐 + 换德语原生模型（变更 42，2026-09-21 新增）**

- [x] `python scripts/quantize-align-model.py`：1204.0 MiB → **230.2 MiB**，5 秒（本机实测）
- [x] `npm run stage:align` 从 `.cache/align/` 抄权重、从 `scripts/align-model/` 抄两个 json（本机实测）
- [x] 桌面浏览器真实一课（6:16、45 句、`webgpu/q4`）：**44 秒跑完 45/45 句**，
      句级 confidence 中位数 **-0.02** / 最差 -1.59，3 句待校对且全是采访同期声；
      时间戳单调无重叠、最小间隔 0.04 秒、字符速率中位数 14.6/秒、末句止于 366.15s（音频 376.5s）（浏览器实测）
- [x] `wasm/q4` 那一档也能加载并推理（同一份权重换执行后端，浏览器实测）
- [x] 词级时间戳落在正确的 `charStart/charEnd` 上（`Die|Schulzeit|ist|beendet` 逐词对得上，浏览器实测）
- [x] 权重站的安全边界：目录穿越（含 `%2e%2e`）、扩展名白名单、Range/416/404（服务端单测 14 条）
- [x] **真的从权重站下一次权重**（2026-09-21 线上实测）：在 d.gamestao.com 上导入同一课 →
      进度条显示「加载对齐模型 **230.2 MB**」（那个数只有权重站有，HF 上那份是 1204 MiB）→
      **75 秒跑完 45/45 句**（本机权重已在时是 44 秒，差的正是这一次下载）；
      confidence 分布与本机**逐位一致**（最差 -1.59 / p25 -0.03 / 中位数 -0.02 / 末句止于 366.15s）
- [x] **权重站的响应头**：`Content-Range: bytes 0-0/241409901`（与本地文件字节数一致）、
      `Accept-Ranges: bytes`、`Cache-Control: …immutable`；两个 json 200；
      目录穿越（`..%2F..%2F.env`）与 `.env` 都回 404（线上实测）
- [x] **服务器那一半上线**（2026-09-21）：compose 送达（容器 `HostConfig.Memory` = 4294967296）、
      `model_q4.onnx` 241409901 + fp32 1262494460 + 两个 json 就位、容器 healthy、
      CI 与 deploy 两个 job 全绿（`45b8bfb`）
- [x] **服务器真的算一课**（2026-09-21）：他自己在桌面上送了一课上去；`probe.ts` 复测
      —— 加载权重 **2.4 秒**、满块（20 秒音频）**6.7 秒**、实时倍率 **2.81×**，
      即 **fp32 比 q4 慢约 1.5 倍**（一课 6:16 从约 1.5 分钟变成约 2 分钟）。
      数字与 q4 时代的对照表在 `deploy/README.md` §4b
- [x] **闲置释放**（2026-09-21 实测，一分钟一次探出来的两行）：
      `09:31:58 mem=2.145GiB status=ready` → `09:33:00 mem=56.27MiB status=idle` ——
      **2.145 GiB → 56.27 MiB**，会话真的放掉了。也就是说这台容器平时只占几十 MB，
      只有真在算的那几分钟才吃到 2 GB
- [x] **装机体积**（2026-09-21 真机实测）：**95.2MB**，对照 0.2.x 的 553.6MB —— 少了 458MB（83%）。
      估的是「135MB 量级」，实际更小
- [x] **出包**（2026-09-21，`ios-v0.5.0`，构建号 10，run 35583768816，6m04s）：
      `cap sync ios` 与 archive / export / 上传 ASC 全绿 —— 那一步是「插件删干净了没有」的试金石
      （它按 package.json 重写 `Package.swift`）。**IPA 从 410.2 MiB 降到 32.7 MiB**
      （0.4.0 那 410 MiB 里几乎全是压不动的 4-bit 权重；剩下的 40MB 词库是 JSON，压得很狠）。
      **装机体积另算**，要装上去在「设置 → 通用 → iPhone 储存空间」里看
**桌面对齐完的时间戳真的到手机（变更 43，2026-09-21 新增）**

- [x] 去抖窗口一开始就落库标脏，drain 时照常推；同一课连标多次只写一条（单元测试）
- [x] 推成功后标脏项消失；推失败则升级成要报警的那一种，「待推送 N 项」只数后者（单元测试）
- [x] 升级后第一次同步把本地每一课补推一遍，第二次不再推（单元测试）
- [x] 字节数一致 → 补齐素材**不触发**重对；字节数不同 → 触发（单元测试）
- [x] 老课程没有 `audioBytes` 时比的是「解码值 vs 解码值」而不是「解码值 vs 页面整数秒」（单元测试）
- [ ] **真链路**：桌面上导入并对齐一课 → **对齐一结束就关掉标签页**（不等那 30 秒）→
      重新打开桌面（这一下应该把它补推出去）→ 手机上拉到这一课 → 打开它自动补齐素材 →
      头部直接显示「已对齐 M / M 句」，**底部没有对齐进度条**，横幅说的是
      「时间戳是同步来的，不用重对」
- [ ] **老课程那一支**：随便挑一门变更 43 之前对齐过的课，在手机上补齐素材 → 同样不重对

- [ ] **iPhone 真机剩下的三条**（0.5.0 已装上，体积见上）：课程页上**没有**「在这台手机上算」，
      没登录时那段说明出现；登录后导入一课能自动送服务器算完（期间可锁屏）；
      **热更仍然生效** —— `/dict/` 那条符号链接现在是 `linkBundledAssets` 唯一还在链的东西，
      断了不报错、只是查不到词

**改名 + 五件手机上的实事（变更 46，2026-09-22）**

*自动化测试（不用人再走一遍）*

- [x] `aiNotes`：两个口子（`!meaning` / `askAi`）都进队列、有 `note` 的不再进、
      又标一次照旧进、按 `createdAt` 升序、写回时清掉 `askAi`、一次贴一半保留其余、
      落空的编号报出来、多行解释整块收下（22 条单测）。**变更 48（2026-09-22）起整个模块删除**——
      问 AI 改成服务器实时调用，不再有待办队列，测试覆盖挪到 `server/src/ai.test.ts` 与
      `src/ai/explain.test.ts`
- [x] `safeArea`：iPhone 13 / 灵动岛 / SE / iPad 各查到该查的值；env 报得出数时兜底**零效果**；
      web 与 Android 一律不补；探针量完就摘掉（12 条单测）
- [x] `wakeLock`：挂载申请 / 卸载归还、已拿着锁时重复调不再申请、**申请还在路上应用就关了要立刻还掉**、
      页面不可见时不申请、**切出去回来要把锁要回来**、第一次失败后还有第二次机会、
      卸载后不再响应、不支持时是空操作；`wakeLockState` 四档（支持 / 拿到 / 被拒 / 还没试过）
      且**锁还回去之后仍说得出上一次的结果**（13 条单测）
- [x] `sfx`：三个文件一次取完、幂等、一个取不到另外两个照常、没有 AudioContext 时静默、
      点一下要 resume（iOS 手势链）、过统一衰减再接 destination（5 条单测）
- [x] **E2E**：标记一个词 → 那一块出现 → 粘回解释 → 保存 → **刷新之后解释还在**（真 IndexedDB）；
      查不到的词照样收得下并自己排进待办（那个按钮曾经是死的）。**变更 48 起这两条改写**——
      剪贴板面板没了，e2e 现在守的是「没登录时优雅降级」（见变更 48 的自动化测试小节）

*补测试（2026-09-22 第二轮，906 → 949）*

> 这 43 条里**有 7 条不是这一轮的** —— 同一天另一个会话做的变更 47（FR-10.13）
> 落在两次提交之间，它那 7 条也计在这个总数里。这一轮自己补的是 **36 条**。

盘的仍然是缺口不是数量。这一轮补的四处，共同点是**坏了不报错**：

- [x] `toggleAskAi` 抽成纯函数并测（5 条）：**取消标记要删字段而不是置 `false`**。
      `VocabEntry` 整条 last-write-wins 同步，留一个 `false` 会和「从来没标记过」
      在数据上不一样，于是两台设备互相覆盖一次，换不来任何信息 —— 而这种事
      没人会追到那一行 `onClick` 上来。**`askAi` 字段与这个函数在变更 48 一并删除**——
      问 AI 不再是一次性标记，随时可点、随时可重问，没有需要「取消」的状态
- [x] `AiNotesPanel`（13 条，与 `ZhPanel.test.tsx` 对齐）：0 个待办时整块不出现、
      两个口子的说明行分开说、预览按编号对上、保存写 `note` 清 `askAi`、
      只贴一半时另一半留着、落空的编号报出来、**一个编号都对不上时不给保存按钮**。
      **变更 48 起这个组件删除**，生词本每行直接一个「问 AI」按钮
- [x] `renderShareCard`（7 条，假 2D 上下文记账）：**名字真的画上去了**、留空就不印、
      有名字时日期靠右没名字时靠左、名字与日期同基线、句子长短不影响底部三个数字、
      应用名就是现在这个名字。写它的直接理由是用户报过「分享图上没有名字」——
      而那个功能本来就有；它坏掉的话没有任何人会发现，直到又有人以为它没做
- [x] `runningBuild`（6 条）：**桥永远不回调时也要 settle**。这一条守的正是那个
      藏了一年的坏法（见上面「当天改过两次的两处」）
- [x] `ReviewPage` 的音效（5 条）：进页面就预取、点一下先「嗒」再判定音、
      答错响另一声、「不认识」同样两声、**设置关掉之后一声都不响也不预取**

**顺带修掉三条会随机变绿的用例**。会随机变绿的门禁比没有门禁更糟，所以三条都是
先复现、再改成确定性写法，而不是加重试：

1. 新写的 wakeLock 重试用例撞上还没复位的 `requesting`；
2. 一条音效用例被上一条的 120ms 判定定时器污染；
3. **服务端那条「改过一个字符的令牌不认」**（`server/src/session.test.ts`，
   不是这次新写的，一直在）。它把 token 的最后两个字符**对调** —— 那两个字符
   碰巧相同时（base64url 六十四个字母，概率约 1/64）token 一个字节都没变，
   校验理所当然地通过，用例红。**CI 上真的红过一次**（本机连跑三遍全绿）。
   改成改签名段的第一个字符，并加一行 `expect(tampered).not.toBe(token)` 防呆。
   顺带记下另一个坑：**不能改最后一个字符** —— HS256 的签名 32 字节 = 256 位，
   base64url 编成 43 个字符携带 258 位，末字符有 2 位是填充位、解码时被丢掉，
   只动到填充位的话签名解出来一模一样，照样通过。

*浏览器里实走过（2026-09-22，桌面 + 390px + 深色）*

- [x] 标题、PWA manifest、分享图顶部那行都是「努力学德语」
- [x] **分享图上的名字**：记录页输入「王涛」→ 预览里那一行立刻出现（用户报的第 2 条，功能本来就在）
- [x] 查一个两边都查不到的复合词 → 「加入生词本」**真的加进去了**（这一下之前什么都不会发生）
- [x] 复习页答一题：`tap` 与判定音**各响一次**（在真浏览器里数的 `AudioBufferSourceNode.start` 调用）；
      三个 wav 都是 200
- [x] 设置里的音效开关；对照预览在 375px 下两行排，长复合词不再压住中文

*当天改过两次的两处（都记在这里，免得以后当成没验过）*

- [x] **「版本」这一块会把自己整块抹掉** —— 从 0.4.0 起就在，而在 iPhone 上**一次都没出现过**。
      第一行是 `if (build === undefined) return null;`，而 `build` 来自一个过原生桥的调用；
      桥不回调时那个 Promise **永远不 settle**，于是整块消失。症状是「设置页翻到底，
      对齐后端下面什么都没有」，不报错、不留日志。修法两条：`runningBuild()` 加 4 秒超时；
      这一块**无论如何都渲染**，问不出来就如实写「问不出来」。补了 3 条专守这个的用例
- [x] **常亮的范围从「练习界面」改成「整个应用」**（用户当场推翻，理由见 FR-18.7）

*上线与出包（2026-09-22）*

- [x] `85d7cfc` 推 main → CI 绿 → deploy 全绿；线上 `<title>` 是「努力学德语」，
      `/sfx/{tap,right,wrong}.wav` 三个都 200（tap 3862B，与本地逐字节一致），
      OTA manifest `0.6.0+85d7cfc`、`minNative` 仍是 0.4.0（这一版没用到新的原生能力）
- [x] `ios-v0.6.0` 上传 App Store Connect 成功（构建号 11，run 35700196035，4m29s）；
      **IPA 33.0 MiB**（0.5.0 是 32.7 MiB），导出的文件名是 **`努力学德语.ipa`** ——
      那个文件名本身就证明 `CFBundleDisplayName` 改到了产物里
- [x] **`.gitignore` 的 `*.wav` 差点让音效静默丢掉**：本机 `public/` 里有文件，
      于是五条验证全绿，而 CI 构建出来的包里 `/sfx/` 会全是 404，症状只是「点一下没声音」。
      已开一条窄口子 `!public/sfx/*.wav`

**问 AI 改成服务器实时调用（变更 48，2026-09-22）**

*自动化测试（不用人再走一遍）*

- [x] `server/src/ai.ts`（`createAiExplainer`，5 条）：提示词里正确拼进词/原句/已有释义、
      没给的两项不强塞、多段 `text` block 拼接且跳过非 `text` block、上游非 2xx 抛错但
      **不透传响应体**（可能带账单细节）、返回体没有文本时抛错而不是把空字符串存进 `note`
- [x] `server/src/app.ts` 的 `/v1/ai/explain`（5 条）：`deps.ai` 缺席时 503 `code=ai_off`、
      和别的 `/v1` 接口共用同一道登录中间件、缺 `word` 时 400 且不去问模型、
      `context`/`existing` 有给就传没给就是 `undefined`、上游失败 502 `code=ai_failed`
- [x] `server/src/config.ts` 的 `ai` 字段（3 条）：没配 key 时是 `undefined`（不是空字符串，
      这是 `app.ts` 判断整块开不开的依据）、配了 key 带着走、模型名可换
- [x] `src/ai/explain.ts`（7 条）：没登录时 `aiAvailable` 返回 false 且不发请求、
      登录过返回 true、服务器答过 503（`ai_off`）之后再问直接 false 不再发请求、
      `resetAiAvailability` 之后恢复、`explainWithAi` 没登录直接抛错不发请求、
      成功时 token 与请求体都对、502 时把服务器给的原因原样抛出来
- [x] **E2E**（`review.spec.ts`，2 条）：生词本页没登录时点「问 AI」给出「尚未登录」而不是
      静默失败；查不到的词自动问一次 AI，没登录时说「AI 服务暂时不可用」，
      加进生词本之后那句提示也如实说「解释暂时没要到」。**这两条顺带抓出一个真 bug**：
      查词面板的 AI 兜底状态最初用 `Pending<string> | 'unavailable'` 表示，而
      `'unavailable'` 本身是字符串——`typeof x === 'string'` 分不出哨兵值和真解释，
      e2e 第一次跑时发现新卡的 `note` 字面量存成了 `"unavailable"`。改成判别联合类型
      `'loading' | 'unavailable' | { note: string }` 后不会再犯

*没验的（要真机）*

- [x] **iPhone 13 顶部不再压住状态栏**（2026-09-22 真机，`ios-v0.6.0`）。
      **这一条同时定死了 env() 就是 0**：0.5.0 → 0.6.0 之间唯一动过版面的是那个 `max()`，
      而 `--safe-top-min` 只在量到 0 时才写 —— env() 要是好的，算出来的内边距与 0.5.0
      逐像素相同，就该还压着。仍未定的是「为什么是 0」，见 §12.15
- [ ] **那一行诊断本身还没被人读到过**（他在 0.6.0 上没找到那一块；那个包出自 `85d7cfc`，
      诊断当时还只挂在原生分支里、且在设置页最后一块的折叠块内）。热更到 `2d4e3d1`
      之后再看一眼，把 `上 Npx` 那个数字记下来 —— 它是将来判断「Capacitor 修没修好」的基线
- [x] **热更死锁的根子不是「插件没链进二进制」——2026-09-23 真机验完，假设被推翻**：
      `ios-v0.6.2` 装机后「插件注册」那一块明确显示 `CapacitorUpdater` 在 12 个插件
      的名单里，`current` 也在它的方法列表里。也就是说 `CapApp-SPM.swift` 那处显式
      引用**到底是不是必需的仍然不知道**（没有「改之前」的对照数据），但可以确定
      「类没注册」不是这次故障的成因。真正卡住的是 `download()`/`next()` 两个没加
      超时保护的裸调用（变更 53），已修复并出了 `ios-v0.6.4`——**这一条要等下次真机
      点「现在就查一次更新」，看 `readLastCheckLog()` 里 `download`/`next` 那两步
      最终是 `ok` 还是别的，才算真正验完**
- [ ] **屏幕常亮真的亮着**：手机上打开应用放着不动两分钟，**随便哪一页**，屏幕不灭；
      切出去再回来仍然不灭（后半句是最容易静默失效的那半 —— 锁被自动释放之后要自己要回来）。
      读不准的话看诊断行：它会说「锁拿着」／「被拒了」／「这台 WebView 没有这个 API」
- [x] **2026-09-23 真机复验，通过了**：装上 `ios-v0.6.2` 之后诊断行报「锁拿着，这个应用
      开着的时候屏幕不会自己灭」——Web Wake Lock 这次直接申请成功，不用等到
      `SceneDelegate` 那道原生兜底顶上。上一轮（2026-09-22）「被拒且非省电模式」的
      症状没有复现；三处改动（记原因 / 手势重试 / 原生接管）具体是哪一处起的作用
      还说不清（申请一次就成功，没有触发到「记原因」和「手势重试」那两段代码），
      但结果达到了。**仍未单独验的半句**：屏幕放着五分钟不碰、以及切出去再回来
      仍然不灭——上面「屏幕常亮真的亮着」那条的后半句
- [ ] **⚠️ 2026-09-22 真机上验证到「网页上正常、手机上没声音」，根子已经找到并修了**：
      `playSfx` 原来的写法是 `resume()` 不等就直接 `start()`——suspended 的上下文里排
      的音，Chrome 桌面上 resume 之后会照样响，但 WKWebView 不认这一套，静默丢在原地
      （模块本来的设计就是失败不报错，所以症状只是「点了没声音」，和 FR-18.7 那次一样
      难自己想到原因）。改成等 `resume()` 真 resolve 了再 `start()`（变更 51），
      [src/audio/sfx.ts](src/audio/sfx.ts)。
      **这一条还没在真机上重新确认过**——下一次热更到手机之后点一下选项再看
      还响不响；**德语句子还在播时答题，两个声音要同时在**（不能互相掐掉）也要顺带再看一眼
- [ ] 应用名在主屏幕图标下 / 设置 → 通用 → iPhone 储存空间里都是「努力学德语」
      （前者读 `CFBundleDisplayName`，后者读 `CFBundleName`，两处分别本地化）
- [ ] 跨设备：桌面上问出来的 `note` 同步到手机（它在标注层，跟着 `VocabEntry` 走）
- [ ] **「问 AI」真的登录 + 服务器配了 `ANTHROPIC_API_KEY` 之后走一遍**（变更 48）：
      生词本点「问 AI」、查词面板两个词典都查不到时自动问，两条路都要在真实网络下验一遍——
      e2e 只验了没登录时的优雅降级（`review.spec.ts`），没有一条用例真的打到 Anthropic API

**查到的词也有 AI 入口 + 答案缓存跨设备同步（变更 49，2026-09-22 深夜）**

*自动化测试（不用人再走一遍）*

- [x] `src/ai/cache.ts`（`cache.test.ts`，12 条）：`normalizeAiCacheKey` 大小写/前后空格
      不影响命中；`mergeAiCaches` 逐词取 ts 更新的那条（不是「远端总赢」也不是「本地总赢」）、
      ts 相等时保留本地、只有一边有的词照单全收；`aiCacheNeedsPush` 三种情形；
      `rememberAiNote`/`getCachedAiNote` 落库往返、大小写/空格不影响查到、重新问覆盖旧答案、
      形状不对的旧数据按空缓存处理不炸
- [x] `src/ai/explain.ts`（新增 2 条）：`explainWithAi` 成功之后答案真的进了缓存、
      失败时不写缓存
- [x] `src/sync/restore.ts`（`restore.test.ts` 扩到五种文档）：`aiCache` 恢复到空设备、
      每次都记版本号、逐词取 ts 更新的合并规则、没带来新东西时不写库、
      远端形状不对（没有 `entries`）时整块忽略不炸也不清空本地
- [x] **E2E**（`review.spec.ts`，1 条）：查到的词（`Zuversicht`，内置词典真有的词，
      离线可查）在 `ResultCard` 里也有「问 AI」按钮，不自动问，点了之后没登录说
      「AI 服务暂时不可用」——守住的是「查到的词以前完全没有这个入口」这个真问题
- [ ] **`src/sync/trigger.ts` / `src/sync/pull.ts` 的 `aiCache` 推拉没有专门的集成测试**——
      与 `study` 那份数据同一个处境：合并算法本身在 `cache.ts`/`log.ts` 里测了，
      但 `pushAiCache`/`pullAiCache` 的推拉外壳（409 合并、增量判断）没有专门覆盖。
      项目里 `study` 这份数据一直是这个覆盖水平，不算新缺口，如果哪天 `aiCache` 出了
      「跨设备不同步」的真 bug，先补的应该是这一层

*没验的（要真机 / 真登录）*

- [ ] **答案真的跨设备同步**：桌面上问一个词（`ResultCard` 或生词本行），
      手机上查同一个词能不能直接看到缓存的答案，不用再点一次——这是本条改动的
      核心诉求，e2e 建不出真登录会话，验不到
- [ ] **`explainWithAi` 每次都写缓存，`scheduleAiCacheSync` 的 5 秒去抖有没有生效**：
      连着问好几个词，网络面板里应该看到攒起来的一次 `PUT /v1/docs/aiCache`，
      不是每问一个词就推一次

**更新器自己会把设备锁死（变更 50，2026-09-22 深夜）**

*自动化测试（不用人再走一遍）*

- [x] `decideUpdate` 的「未知不许否决更新」（`nativeUpdate.test.ts`，5 条）：壳版本问不出来时
      **不卡** `minNative`（哪怕门槛写成 `9.9.9`）、跑着哪一版问不出来时照样下、
      但同一版已经下过（`queuedBuildId` 对得上）就不重下、记号对不上（又发了新版）继续下、
      桥好的时候 `currentVersion` 才是权威（记号不参与判断）
- [x] **`checkNativeUpdate`（6 条，这条路以前一条用例都没有）**：正常情况下载并 `next()` 登记；
      **两个过桥调用都永不回调时照样把包下下来**（这条就是那个把手机锁死的 bug 的回归用例——
      修之前 `download()` 一次都不会被调用）；桥哑着时下过一次就不再重下；桥哑着但服务器
      又发了新版时记号对不上、照样下；manifest 拉不到时安静跳过不抛给调用方；浏览器里一次桥都不过
- [x] `runningBuild` 改成「各问各的」之后（改 2 条 + 新增 1 条）：一个数问不出来时另一个照常报、
      桥永不回调时也要 settle、**两个都哑了也要回一个对象**（「问不出来」本身就是要显示的答案）
- [x] `VersionSection`（新增 3 条）：两个版本号都问不出来时如实写「问不出来」并给出
      「原生桥没回话」那句警告、只有一个问得出来时把问得出来的照常报、
      桥哑着时仍报出「上次下好等着生效的那一版」（那个记号不过桥，是唯一能证明更新器还活着的东西）

- [x] **启动时序（`src/app/boot.ts` + `boot.test.ts`，7 条）**：补这一组的时候发现启动链上
      **有同一个坑**——`App.tsx` 原来是 `Promise.allSettled(五张表).then(关启动图 /
      notifyAppReady / 查更新 / 同步)`，而注释写的是「allSettled 而不是 all：某张表读挂了
      也得关」——想到的是 reject，**没想到「永不 settle」`allSettled` 一样接不住**
      （IndexedDB 的 `blocked` 就会这样：不成功也不失败）。一张表卡住 = 启动图不关 +
      `notifyAppReady` 不调（插件 20 秒后判定这个 bundle 起不来而**回滚**）+ 连更新都不查
      一次，也就是「设备退回旧版并且再也更新不动」。<br>改法：时序拆成两档。`onAlive`
      （关启动图 / 取消回滚倒计时 / 查更新）**最多等 8 秒**——一张读不完的表不该把整个
      bundle 判死；`onStoresReady`（同步）**不设超时**，因为它有一条真实的顺序约束
      （拉取会写库并让 store 重读，初次 `load()` 晚于它就会把界面退回旧值），而它坏掉的
      方式只是「显示旧数据」，比回滚轻得多。用例覆盖：顺利时两档各一次、某张表 reject
      照常走、**永不 settle 时 onAlive 照样到**（核心回归）、`onStoresReady` 不被超时提前
      触发、超时说过之后表读完不再说第二遍、读得快时不等满超时、默认超时明显小于 20 秒
- [x] `notifyNativeAppReady`（3 条，以前一条都没有）：iOS 上真的通知原生侧、浏览器里是
      空操作、插件没装时安静跳过（抛出去的话 `onAlive` 里排在它后面的「查更新」就不跑了）

*真机结果（2026-09-22 晚，装上 `ios-v0.6.1` 之后）*

- [x] **手机恢复可用**：问 AI 在 iPhone 上正常工作了。但这一条**只证明了新包的内置 bundle
      是好的**（`ios-v0.6.1` 出自 `2d72cb1`，那里面本来就带着变更 49 的问 AI），
      **不证明热更本身恢复了** —— 那要等下一次 push 之后手机自己拿到才算数
- [x] **「版本」块终于在真机上显示出来了**，而且**当场指出了是哪一个调用坏的**：
      显示「应用壳 0.6.1」（`App.getInfo()` **是好的**）+「前端 问不出来」
      （`CapacitorUpdater.current()` **才是哑的那个**）。这正是变更 46 那条「诊断块不能
      自己消失」换来的回报：以前整块不显示，什么都查不出来；现在一眼看出范围在 capgo
      那个插件上，而不是整座桥。<br>**也验证了这次改动的设计是对的**：如果只给
      `Promise.all` 套一个超时、失败就 `return null`，现在照样更新不了；正因为改成了
      「各问各的 + 未知不否决」，`nativeVersion` 问得出来（0.6.1 ≥ `minNative` 0.4.0 过门槛）、
      `currentVersion` 问不出来就走 `queuedBuildId` 那条路，更新照样能往下走
- [ ] **热更真的恢复**（唯一还没验的那条，判据现成）：启动链那个修复（`4afe900`）当时
      没跟着 `ios-v0.6.1` 一起出包，所以它**只能靠热更上手机** —— push 之后手机自己拿到
      变更 51 的界面/行为，就是热更活着的证据；拿不到就是还有第二层问题
- [ ] **`current()` 为什么哑**：范围已经缩到 `@capgo/capacitor-updater` 这一个插件
      （同一座桥上的 `@capacitor/app` 是好的，所以不是「桥整个不通」，也不是 `cap sync`
      没跑）。**已知的副作用**：`current()` 一直问不出来的话，「跑着哪一版」这个数永远缺席，
      更新判断只能靠 `queuedBuildId` 那个本地记号 —— 而记号的弱点是**万一某个 bundle
      下好了却回滚了，记号仍然说「下过了」，于是那一版不会重下**。自愈条件是下一次部署
      （buildId 一变就重新下），所以不是死局，但真要查 `current()` 就从这里查起

**热更诊断到根 + 常亮改走原生（变更 52，2026-09-23）**

*自动化测试（不用人再走一遍）*

- [x] `probePlugins`（`nativeUpdate.test.ts`，5 条）：浏览器里没有 `window.Capacitor`、
      桥在但还没注册任何插件、**类没链进二进制的那种失败**（别的插件都注册了，唯独
      `CapacitorUpdater` 不在名单里）、注册成功时报出方法名、`PluginHeaders` 格式不认识
      时不炸
- [x] `checkNativeUpdate` 的诊断日志（新增 5 条）：正常跑完时每一步按顺序落地、桥哑掉的
      具体原因（`timeout`）进日志而不是整条记录消失、`fetch` 本身悬着也要超时也要落一笔、
      记录里带着那一刻的插件注册情况、从没查过时 `readLastCheckLog()` 是 `null` 不是抛错
- [x] `notifyNativeAppReady`（新增 1 条）：桥永远不回调也要 settle，不能把 `App.tsx` 里
      排在它后面的「查更新」一起拖死
- [x] `wakeLock.ts`（新增 5 条）：被拒时记下 `err.name`/`err.message`（不用
      `instanceof Error`——`DOMException` 不一定是 `Error` 子类）、成功之后错误信息不残留
      上一次被拒的内容、`pointerdown` 之后自动再申请一次、一次被拒只挂一个监听器、
      应用已卸载时 `pointerdown` 不会凭空申请
- [x] `VersionSection`（新增 7 条）：iOS 上才有「现在就查一次更新」按钮、web 上没有、
      点一下会查一次且查完刷新版本号与日志（按钮短暂显示「查询中」）、
      `CapacitorUpdater` 没注册上时明说「不是超时，是没链进这次构建」、
      注册成功时报出方法名、上次查更新的记录摆出来、从没查过时不留一个空壳
- [x] 前端单测从 959 增到 982，server（133）/e2e（43）不受影响，五条验证全绿
- [x] **用户问「有测试可以加吗」，又补了 9 条（982→991）**：`probePlugins` 吃到畸形原生
      数据不炸（数组里混非对象元素、`CapacitorUpdater` 的 `methods` 字段形状不对）；
      `checkNativeUpdate` 日志把 `rejected`（原生侧真报错）与 `threw`（过桥调用同步抛）
      跟 `timeout` 分开，三态各有各的下一步；`readLastCheckLog` 面对损坏的 JSON 回
      `null` 不抛错；`wakeLock` 被拒的值不是 `Error`/`DOMException`（比如裸字符串）
      时不炸；`resetWakeLockForTests` 真摘掉了手势重试监听器（断言
      `addEventListener`/`removeEventListener` 拿到同一个函数引用，不是间接数调用
      次数——那条路会被 `requesting` 那道闸掩盖掉，测不出问题）。**顺带抓到一个真
      缺口**：`VersionSection.runCheckNow` 里 `await checkNativeUpdate()` 没有
      `catch`，mock 成 reject 当场证实会产生未处理的 rejection——生产实现自己不会
      reject，但按钮不该依赖这个前提，补了 `.catch(() => null)`

*真机结果（2026-09-23，装上 `ios-v0.6.2` 之后）*

- [x] **`probePlugins()` 的名单里有 `CapacitorUpdater`**——「类没链进二进制」这个假设
      被推翻，见变更 53
- [x] **Web Wake Lock 直接申请成功**——「锁拿着」，不必等原生兜底顶上
- [ ] `isIdleTimerDisabled` 接管之后屏幕放着五分钟不碰真的不灭（这次没触发到需要
      它接管的场景，Web API 直接就成功了，仍未单独验证这道防线本身）
- [x] **「现在就查一次更新」点一下，看出了新问题**：按钮卡在「查询中…」连着几分钟不动——
      这不是「看不懂卡在哪一步」，是**诊断日志压根没写出来**，因为 `checkNativeUpdate()`
      本身卡死在没加超时保护的 `download()` 调用上，见变更 53

**热更死锁到根、常亮改走原生 + download()/next() 超时（变更 53，2026-09-23）**

*自动化测试（不用人再走一遍）*

- [x] `checkNativeUpdate` 的 `download()`/`next()` 超时（`nativeUpdate.test.ts`，
      新增 3 条）：`download()` 永不回调也要 settle、`download()` 真的报错
      （reject）不用等超时立刻反映、`next()` 永不回调时不能假装登记成功了
      （`queuedBuildId` 不落地）。前端 991 → 994，server（133）/e2e（43）不受影响
- [x] 这一轮验证撞上了同一份共享检出里**另一个并行会话在途改动
      `src/pages/ReviewPage.tsx`**——`git checkout --` 误清掉过它一次（已用
      `git worktree` 隔离重新验证，未影响最终提交，见 [[never-git-add-all]]）

*没验的（要真机，判据现成）*

- [ ] `readLastCheckLog()` 里 `download`/`next` 两步最终是 `ok` 还是别的——这才是这次
      死锁真正的根因是否已经解开的判据；`ios-v0.6.4` 已出包，等下次真机点一下
      「现在就查一次更新」看结果
- [ ] `isIdleTimerDisabled` 接管之后屏幕放五分钟不碰真的不灭（上一条真机结果里 Web
      Wake Lock 直接成功了，没触发到这道防线，仍需单独验）

**挖空题的音频放不出来 + 三处静默失败（变更 54，2026-09-23）**

*自动化测试（不用人再走一遍）*

- [x] **课程听卡的自动播放只播一次，不会自己把自己掐掉**（`ReviewPage.test.tsx`）：
      根因是那个 effect 把 `resolveRange()` 每次渲染新建的对象当依赖，一变就重跑，
      清理函数又把刚起播的那句 `pause()` 掉——句子播几毫秒就被自己掐断，症状正是
      用户报的「挖空的题，音频根本无法正常播放」。修法是 `useMemo` 钉住 `range`；
      用真的还原过一次 bug（临时去掉 `useMemo`）确认这条用例会红，再改回来确认绿
- [x] **FR-10.5 补的第四种静默失败**：`hasMaterial` 说有、`audioBlobs` 里这一次
      真取不到 → 「音频文件找不到了」；取到了但 `audioPlayer.load()` 解不了码 →
      「音频文件解码失败」，两条都不再是永久变灰的播放键
- [x] **读卡卡背「念一遍」三处修复**：没有任何音源（无真人音、无系统嗓音）时按钮
      不出现，不是给一个按下去没反应的键；课程卡（有原句）念的是原句而不是孤立词
      形（用户当场选定的形状）；`speak()`/`audioPlayer.load()` 的失败不再静默吞掉
- [x] 前端 999 / server 133 / e2e 43，五条验证全绿，无 server/e2e 改动

**生词本里 AI 解释默认折叠（变更 56，2026-09-23）**

*自动化测试（不用人再走一遍）*

- [x] `VocabPage.test.tsx`（新文件，3 条）：有 `note` 时默认收起（`<details>` 没有
      `open` 属性）、点开摘要之后 `<details>` 变成 `open` 且正文可见、没有 `note`
      时整块折叠都不出现。三个子面板（`DictLookup`/`PresetPanel`/`ZhPanel`）全部
      stub 掉，避免它们的网络/IndexedDB 依赖拖进这个文件
- [x] 前端 1014 → 1017，server 133 / e2e 44 不受影响，五条验证全绿

**通听的当前词在手机上看不见（变更 58，2026-09-23）**

*自动化测试（不用人再走一遍）*

- [x] `ListenTab.test.tsx`（新文件，9 条；这一页此前零测试）：当前词的底色**不等于**
      当前行的底色（回归判据，还原成旧写法验证过这条会红）、高亮跟着播放位置走且同一
      时刻只有一个词是当前词、播放位置落在句间空档时一个词都不标、点一个词就从那个词
      开始播、整句没有时间戳时点它不播、低置信句行号带 `?` 且点一下真的把 `timingChecked`
      打上（FR-15.19）、三条提示各自的出现条件（含「一切正常就一条都不出现」）、
      没贴过译文的课不摆「显示中文」按钮（FR-19.4）
- [x] `e2e/listen.spec.ts`（新文件，3 条）：**播放时当前词的底色算出来跟当前行不是同一个
      颜色** —— 这条才是这次 bug 的对口判据，组件测试断言得了 class 名、断言不了颜色；
      点一个词真的跳到那一句；没有时间戳的课播着也一个词都不亮（FR-5.2 不做伪同步）。
      还原成旧写法验证过第一条会红（报的是 `rgb(251, 241, 224)`，`--color-warn-soft` 的色值）
- [x] `fixtures.ts` 新增 `timedLessonBackup(lessonId)`：音频（缓存层）与词级时间戳
      （标注层）分属两层，只能「先导入带 WAV 的一课拿 id、再用备份把时间戳盖上去」；
      走「课程页选本地音频」那条会顺手 `enqueueAlign()`，而 E2E 里没有对齐服务器
- [x] 构建产物里确实生成了 `.bg-warn` 与 `.text-surface`（`grep dist/assets/*.css`）；
      §12.5 那条颜色 grep 判据仍然零命中
- [x] 浅色/深色 × 桌面/手机四种组合下新旧两种写法并排看过（浏览器实测）：
      旧写法两行完全一样，新写法一眼可见
- [x] 前端 1043 → 1052（本次共 +9 条；仓库当日总数是 1064，另外 12 条来自并行进行的变更 59）、e2e 44 → 47、server 145 不变，五条验证全绿

*还要人验的*

- [ ] **手机上真的看得见了**（推上线后 iPhone 上打开一课通听、展开文本、播放）。
      如果那时连**整行**的浅黄底色都不跟着句子走，那就不是这一条修的问题 ——
      那说明手机上播放位置压根没推进，要另查

**教材导入（变更 64，2026-09-26）**

*自动化测试（不用人再走一遍）*

- [x] `pdfText.test.ts`（12 条）：折行合并、连字符三种情况、页码/页眉/页边音轨号、说话人符号开段、
      标题与 `Person N` 自成一行、`Wort:` 折行不劈开、整理前后的切句数对比
- [x] `speakers.test.ts`（13 条）：候选与默认勾选（符号总勾、名字 ≥2 次才勾、带数字的不算）、只认清单里的、
      切句后 offset 仍是 plainText 里的绝对位置、**不给清单时结果与以前完全一样**
- [x] `bookImport.test.ts`（13 条）：按标题切段 + 所在章、占位行不进正文、`Person 1~N` 默认 N 轨、自然排序、按顺序分轨
- [x] `concat.test.ts`（17 条）：手工拼的 MPEG1 Layer III 帧 —— 去 ID3v2/ID3v1/Info/Xing、VBR 与规格不一致拒绝按字节拼、
      夹着垃圾字节放弃、**同样几个文件拼两次字节一样**、单文件原样返回、WAV 头
- [x] `bindAudio.test.ts`（12 条）：**同一份音频绑回来不排对齐**（这次那个 bug）、换了才排、没时间戳总要排、
      拼过的课按清单顺序拼、少一轨不绑并报缺、整组认领
- [x] `useLessonStore.test.ts` +10：手动导入记 `audioBytes`、`audioFiles`/`collection`/`speakers` 落库、
      重切沿用说话人清单（换了清单时间戳会全丢）、`audioChanged` 四档判据且是写回之前比的
- [x] `LessonsPage.test.ts`（5 条）：分组、组内自然排序、补音频的结果摘要
- [x] `e2e/book.spec.ts`（3 条）：整本导入（勾一章、一题两轨、三个 WAV 乱序选入按名排、**真的拼成 4 秒**、
      说话人从正文拿掉并在句首标出）；**换设备一次选齐整组音频后提示「不用重对」**；拼过的课少选一轨报缺
- [x] 真实文稿（Aspekte neu C1 Lehrbuch Transkript，官网公开下载）在浏览器里解析：10 章 · 37 题，
      ●×193 ○×182 △×47，K1 Modul 2 Aufgabe 2a 默认 8 轨（= 书上的 Track 1.2-1.9）；手机宽度无横向溢出
- [x] 前端 1125 → 1207、e2e 51 → 54、server 146 不变，五条验证全绿
- [x] 随后「有测试可以加吗」又补 +47 / e2e +1（前端 1207 → 1254、e2e 54 → 55），**抓到两个真缺口**：
      ① 标题只看行首 —— 正文里一行恰好从 `Kapitel 3 zeigt, …` / `Track 5 hat mir …` 开始就会开出一个假章节、
      把那句话劈开；改成「整行就是一个标题」（不含逗号句号、题目标题以 `Aufgabe N` 收尾，去掉了 `Text N`）
      + 像标题但下一行小写开头时当作折行（`isHeading` / `isChapterHeading`，真实 C1 文稿仍是 37 题）；
      ② 单课导入保存失败时错误落在音频那块、标题是「这个音频文件读不出来」—— 改成单独一条「没保存上」
- [x] 新增 `BookImportPage.test.tsx`（12 条：章全选、轨数下限、文件不够/多了、同名课提示、认不出标题、
      按钮何时能点、保存参数、有音频才排对齐、中途失败）、`ImportPage.test.tsx`（8 条）、
      `LessonsPage.test.tsx` 补组件测试 9 条（默认折叠、展开状态记在本机、localStorage 抛错或压根没有、
      认领摘要）；`e2e/book.spec.ts` +1：单文件课在课程页绑回同一份音频后**底部不冒出对齐进度条**。
      反向验证过：把 `shouldRealign` 改回恒为 true，两条 e2e 都红
- [ ] 已知小缺口：课程页上绑音频成功之后，「素材未下载」横幅整个消失，写在横幅里的那句
      「时间戳是同步来的，不用重对」也跟着看不见（以前就是这样，不是这次引入的）

*还要人验的*

- [ ] **真的导一章**：allango 下载的音频文件名能不能按自然顺序排对（文件名里没有轨号就要手动对）、
      是不是 CBR（是的话拼出来的文件名是 `.mp3`，否则是体积大得多的 `.wav`）、对齐结果怎么样
- [ ] 从 PDF 阅读器里**真的复制**出来的文字和脚本抽出来的是不是同一个样子（阅读器可能按栏重排、丢掉符号）
- [ ] `Person 1` 这类小标题音频里念不念：不念就在切句页排除掉
- [ ] 手机上：桌面导入 → 手机回前台课程自己出现（带时间戳）→ 组头「一次选齐音频」→ 提示不用重对

---

## 11. 待决问题

| # | 问题 | 建议 |
|---|---|---|
| Q1 | 手机上是否真的要能跟读？如果只复习，音频搬运可以省掉整个流程 | 先按「能跟读」做（音频照搬），实际用两周后再看要不要砍 |
| ~~Q2~~ | ~~一年后音频累计约 700MB，是否需要「归档旧课程」~~ | **已解决**：数据分层（§2.3）后这就是 FR-3.8 的「清除缓存」，对 DW 课程无损，不需要单独的归档概念 |
| Q3 | 是否需要「一个生词对应多个语境句」 | V1 一对一；FR-9.3 的合并操作会丢弃被合并方的 `contextSentence`。若实际很需要，模型改成 `contexts: Array<{ lessonId, sentenceIndex, sentence }>` |
| Q4 | 手机端是否也做听写（输入德语变音符很痛苦） | 已加 ä ö ü ß 快捷按钮（FR-8.7）；先做，不好用再砍 |
| ~~Q5~~ | ~~Manuskript 能不能抓到~~ | **已验证：能。** 在 `window.__APOLLO_STATE__` 的 `Lesson:<id>.manuscript` 字段里，服务端渲染，约 7.8k 字符。见附录 A.3 |
| ~~Q6~~ | ~~DW 是否返回宽松 CORS 头~~ | **已验证：RSS、页面、mp3 三者全部 `Access-Control-Allow-Origin: *`。** 本地 helper 整个方案作废。见附录 A.1 |
| Q7 | 存档期次（400+ 期）RSS 只给 100 条窗口，老期次怎么进来 | **L2 已实现**（来源页粘 URL 或裸 lesson id，见 `parseLessonId`）。存档列表页 `/de/alltagsdeutsch/s-9214` 是否在 `__APOLLO_STATE__` 里给出完整列表**仍未验证** —— 若给了，L1 就能覆盖全部存档，是个 10 分钟的增量 |
| Q8 | DW 的 Glossar 释义是德语的，对 C1 是优点还是负担 | 先按德德释义用（FR-14.3）。真觉得吃力，V2 的 Claude API 可以在此基础上补中文，而不是从头查词 |
| Q9 | **打包成原生应用**（用户已决定要做） | **手机这一半已做，见 §7.10**：Capacitor，iOS + Android 两套工程入库、CI 出包、iOS 自动上传 App Store Connect，200MB 权重随包带。选 Capacitor 而不是 Tauri v2 的理由、以及六条实现约束都在 §7.10。**桌面那一半仍未做也不急** —— 桌面就是浏览器，工位上开着 https://d.gamestao.com 就行，真要做再上 Electron，与 §7.10 不冲突（三条路跑的都是同一份 `dist/`）。原文里「要给 vite 配 `base: './'`」这条**只对 Electron 的 `file://` 成立，对 Capacitor 是错的**，见 §7.10 约束 1 |
| Q10 | MMS-FA 是 **CC-BY-NC-4.0（非商用）**。自用与免费分发没问题，商用不行 | 已把模型做成可插拔（§7.9），换成宽松许可的德语 CTC 模型是改配置 + 一次性 ONNX 转换。真要商用再换，不必现在做 |

---

## 12. 界面规范

**为什么有这一节。** 这份文档到 §11 为止有一千六百行，把**功能**定得很死（FR-1 到 FR-17），
而**形状**一次没定过。结果每个页面各自发明了一套，而且只能借用文档里唯一存在的词汇 ——
FR 编号。于是这些字面进了产品界面：

> 存储（§2.2 / FR-11.16）· 对齐后端（FR-15 诊断）· 账号与同步（FR-11.1 ~ FR-11.3）·
> 同步状态（FR-11.9：常驻可见）· 手动导出 / 导入（FR-11.11 / FR-11.14，第二道保险）·
> 词典（FR-16 / FR-17）· L1 全自动 / L2 半自动 / L3 手动（地板）

「界面一团混乱」不是审美判断，这就是它的机械原因。**以后界面上的争议查这一节，不查 FR 编号。**

改造前的实测（2026-09-05，`src/**/*.tsx`）：166 处字号里 147 处是 `text-sm`/`text-xs`（88%）；
75 处圆角在 `rounded`(4px) 与 `rounded-lg`(8px) 之间随机二选一；颜色用到 neutral 五档
加 amber / sky / emerald / rose / red，其中 rose 与 red 都在表示「错误」；`dark:` 出现 **0 次**。

### 12.1 导航：三个活动 + 一个抽屉

| | 内容 | 位置 |
|---|---|---|
| 活动 | **课程**（听）· **复习**（练）· **生词本**（词） | 桌面在顶栏，手机在**底部标签栏** |
| 管道 | 来源 · 素材 · 设置 | 顶栏右侧的「⋯」抽屉 |

判据一句话：**这是不是一件我今天要做的事。** 不是的不占导航位 —— 你不会「今天去做一下素材」。

- 手机放底部是**拇指可达**。同一个理由让 FR-10.7 把复习的评分按钮钉在底部，导航没有理由例外。
  旧导航是六项横排、手机上要横向滚动，于是 §2.1 写着「手机是复习工位」而复习是滚动条里的第 4 个小字。
- 导航项是 `<a>`，拿不到 `index.css` 里那条 `@media (pointer: coarse)` 的 44px 规则，
  所以它们在标记里显式写 `min-h-11`。旧导航的热区只有约 30px。
- **底部标签栏在课程页与导入页隐藏**：那两页是「推进去」的详情页（顶栏给一个返回），
  而课程页的底部归音频条 —— 见 §12.2，两者不并存。
- 「今天到期几张」是导航上的**角标**（两处导航都有），不是首页上一条会被读一次就再也不读的横幅。

### 12.2 底部单浮层契约

**同一时刻底部最多两层：基座 + 对齐进度。** 基座 = 底部标签栏 **或** 音频条（按路由互斥）。

优先级 **音频条 > 对齐进度**。有音频条在场时，对齐进度默认折叠成音频条上沿的一条 2px
进度线加一个圆点（整条 20px，点哪儿都能展开）；展开后是完整一行，且落在基座**之上**。

实现：每一层挂载时把**实测高度**报进 `src/components/bottomLayer.ts`，那里算出
`--base-bar-h`（对齐条据此上移）与 `--bottom-inset`（页面内容据此留白，见 `.app-content`）。

这一条修的是一个真 bug，不只是版面问题：改造前 `AlignBar` 是 `fixed bottom-0 z-30`、
`AudioBar` 是 `sticky bottom-0 z-10`，**自动对齐一跑，进度条就正正压住播放键**；
`AlignBar` 上那个 `align-bar` class 本来是留给避让偏移的，而 `index.css` 里从来没有对应规则；
页面容器上的 `pb-24` 是个孤立的魔法数，它既不知道当前有哪几层，也不知道安全区有多高。

**高度不能只靠 `ResizeObserver`。** 这一层的高度变化绝大多数是 React 自己引起的
（折叠↔展开差一倍，跟读页给音频条多挂一排按钮），所以每次渲染之后都量一遍；
RO 只负责不是 React 引起的那些（字体加载完、转屏、安全区换边）。
依据：在一个不渲染的浏览器面板里 RO 一个回调都不投递，连初次那一发都没有 ——
那是特例，但它说明 RO 的投递时机不由我们决定，而底部留白错了就是内容被浮层压住。

### 12.3 状态语言分三档

| 档 | 什么时候 | 长什么样 | 组件 |
|---|---|---|---|
| **静默** | 一切正常 | 什么都不画 | —— |
| **一行** | 需要知道，但不挡路 | 单行、**无底色无边框**，只有一个 6px 圆点带颜色 | `Note` |
| **拦路** | 这一页现在做不了 | 有底色；`title` **必填**；`action` 强烈建议给 | `Banner` |

两条硬规则：

1. **界面上只留「怎么办」。** 设计理由（故障域、jetsam、UIBackgroundModes、亮屏时长、
   CEFR 词表的版权状况）留在代码注释和这份文档里。改造前它们是整宽横幅上的三到五行散文，
   而那些横幅出现在课程页头部与首页 —— 也就是每天都要读一遍的地方。
2. **`Banner` 的 `title` 是必填的**，这是故意的类型约束：它逼作者把事情在一行里说完。
   `Banner tone="info"` 整个删掉了 —— 「顺便说一声」属于 `Note`。

要在但不该占主路径的东西（诊断、许可署名、参数解释）进 `Disclosure`（原生 `<details>`）。

#### 12.3.1 FR-11.9 的落法：头部状态芯片

同步状态**仍然常驻可见**，但形状换了：从「首页一行五个字段」换成**跟着应用外壳走的一个芯片**。

旧形状有两个相反方向的问题：① 它**只在首页** —— 而 FR-11.9 要防的「同步静默停摆」，
在你连着三天只开复习页的那三天里最危险，恰好是它一次都不出现的三天；
② 它在首页**一切正常时也照占一行五个字段** —— 天天看同一行绿字的结果是不再读它。

新形状：芯片在每一页都在（比原来更「常驻」），一行只说**四项里最坏的那一项**
（严重度顺序：登录没了 → 推送有积压 → 拉取停摆 → 一切正常）；
点开是 FR-11.9 要求的四项全文（账号 / 上次推送 / 上次拉取 / 待推送数），一项没少。
设置页另有一份**绝对时刻**（精确到分钟），那是跟服务器对账时要的，用途不同。

「最坏那一项」而不是「拼一行摘要」是关键：拼摘要会把「待推送 3 项」和「上次拉取 12 天前」
并排放，而人只会读第一段。

#### 12.3.2 FR-11.12 的落法：两档升级

手动导出提醒分两档，**且有东西可丢才说**（标注层为空时不提）：

| 距上次导出 | 表现 |
|---|---|
| < 90 天 | 静默 |
| 从未导出，或 90–180 天 | 一行（`Note`）+「去导出」 |
| > 180 天 | 拦路横幅（`Banner`）+「去导出」 |

旧形状有个具体的毛病：`lastBackupAt` 只在导出时才写，所以
**全新安装第一次打开就会看到「距上次手动导出已超过 90 天」**，而那时你还没有任何东西可丢。
一条从第一天起就挂着、内容还是三行取舍论证的横幅，教会人的只有「忽略横幅」。

「从未导出」停在提醒那一档、不升级成横幅：没有安装日期可比，而刚导完第一课的人
和攒了三年的人在这个字段上长得一样。

### 12.4 排版四级

| 级 | 尺寸 | 用在哪 | 工具类 |
|---|---|---|---|
| 德语正文 | 19px / 1.75 | 通听、跟读、学词、听写里的**句子**，以及辨形题的选项 | `text-de` |
| 界面正文 | 15px / 1.5 | 按钮、标签、列表项、`body` 默认 | `text-ui` |
| 说明 | 13px / 1.5 | `Hint`、`Note`、编号、单位、元信息 | `text-note` |
| 标题 | 22px | 页面标题与区块标题 | `text-title` |
| （加一个）词 | 28px | 复习卡背那**一个**词 | `text-word` |

**德语是主角，所以它有自己的一级，而且是正文里最大的一级。** 改造前 88% 的字号是
`text-sm`/`text-xs`，德语句子和表单说明文字同一号。

数字与时间码一律 `.tnum`（`font-variant-numeric: tabular-nums`）—— 时间码每秒都在变，
比例数字会让整行左右抖。

手机上**不画页面标题**：顶栏已经说了你在哪儿（`hidden sm:block`）。

### 12.5 颜色令牌与深色

能用的颜色只有十个名字，全在 `src/index.css` 的 `@theme` 里：

- 中性六档 `surface`（页面底）· `raised`（卡片与浮层）· `sunken`（输入框与进度槽）·
  `line` / `line-strong` · `ink` / `muted` / `faint`
- 强调 `accent` / `accent-ink` / `accent-soft`
- 语义三档 `ok` / `warn` / `danger`，各带一个 `-soft` 底色

**刻意不设 `info` 色**：改造前 sky 既是主按钮又是 info 横幅，一种颜色担两个含义等于都没有。
信息性强调一律用 `accent`。

**执行手段是把 Tailwind 自带的调色板清空**（`--color-*: initial`）。于是 `bg-neutral-200`
这类写法不再生成任何 CSS，一处漏改会在界面上露出无色元素，而不是悄悄多出第十一种灰。
这条让「grep 不到原始色号」成为一个可信的验收标准。

圆角**两档**：控件 `rounded-ctl`(6px)、容器 `rounded-box`(12px)。别的一律不用。

**深色只重定义令牌**，不写任何 `dark:` 变体 —— 组件因此对主题无感。
另外两件必须一起做的事：`:root { color-scheme }` 跟着换（否则原生 checkbox、range、
number 步进器、select 下拉在深色下全是亮白的）；`index.html` 里两条带 `media` 的
`theme-color`（浅色仍是图标底色 `#e0f2fe`，深色是 `#121211` = 深色的 `--color-surface`）。

理由不是好看：**晚上躺着练听力是 §2.1 写的主场景**，而在这之前整个应用是 `#fff` 满屏，
`dark:` 出现 0 次。

### 12.6 一课的动线

**落地页是「通听」，不是「切句」。** tab 条只有每天走的那四个，顺序就是真实动线：

> 通听 → 跟读 → 学词 → 听写

`sentences`（切句）**不在 tab 条里**，收进课程页头部的「⋯」。它是一次性的准备工作，
而旧形状把五个 tab 平级排着、第一个是切句 —— 打开一课的默认状态因此是**编辑模式**。
`#/lesson/<id>/sentences` 这个深链接照旧有效；从导入流程跳进来时**仍然直接落在切句上**
（那是唯一「切句就是下一步」的时刻）。

tab 名旁边那个点是**前提状态**，不是「做完了」：

| tab | 前提 | 点是 accent 色的条件 |
|---|---|---|
| 通听 | 时间戳 | 至少一句有时间戳 |
| 跟读 | 时间戳 | 同上 |
| 学词 | 只要正文 | 恒亮 |
| 听写 | 时间戳 **且** 挖空 | 至少一句两样都有 |

「做完了」在这里没有意义（通听永远可以再听一遍），而「点进去能不能用」在点进去之前
就该看得出来。缺前提时点进去仍会得到一句话说清缺什么。

### 12.7 不做的

- 不引组件库、不引图标库（一共三个图标，手写路径）
- 不做动画系统
- 界面上不出现 FR 编号、`§` 章节号、`L1/L2/L3` 这类内部代号（`来源`页三层降级的用户面
  名字是 **订阅列表 / 按地址或 id 导入 / 完全手动**，降级关系写在代码注释里）
### 12.8 记录页与分享图的形状（FR-18，2026-09-15）

**记录页在「⋯」抽屉里，不占导航位。** 照 §12.1 那句判据：看记录不是一件今天要做的事，
它是做完之后回头看的东西。把它放上去要挤掉三个活动之一，而那三个才是每天真的会点开的。

页面只有两块：**数字**（今天 / 连续 / 累计 + 最近 14 天的柱子）和**那张图的真实预览**
（同一个 canvas、同一份渲染代码，所见即所得）。中间不设「模板」「配色」这类东西 ——
换一句、换张图两个按钮就是全部可调项，再加一个「图上的名字」输入框。

**分享图不跟界面主题走。** 它永远是照片 + 白字，深色浅色下长得一样 —— 发出去的图
在谁那儿都该是同一张。这也是它用 canvas 直接画、而不是截 DOM 的图的原因之一：
另外两个原因是 html2canvas 要几百 KB 依赖并复刻一遍 CSS 排版，而 SVG `foreignObject`
在 Safari 上导出常年出问题。

图上的排版规则（`src/share/card.ts`）：

- **从底往上排**：数字行 → 名字与日期 → 细线，位置全部钉死；引文块从细线往上长。
  于是句子长了是往上占照片，底部三个数字纹丝不动。
- **句子是主角**，58px 衬线；放不下就降一号（52 / 46 / 42），到最小还超行就让它超 ——
  宁可挤，不截断：`Wer immer strebend sich bemüht…` 被截掉后半句就没有意义了。
- **折行要两套规则**：德语按空格断，中文按字断。中译那一行按空格断的话整句是一个「词」。
- **压暗层下重上轻**，外加所有文字一层淡阴影 —— 六张底图里最亮的那张（白垩崖）是
  一片白与浅绿，不压的话白字直接消失。

### 12.9 译文与笔记的形状（FR-19 / FR-20，2026-09-15）

**译文页在课程页的「⋯」里，不进 tab 条。** 照 §12.6 的判据：tab 条上只有每天走的
那四个，而贴译文和切句同类 —— 一次性的准备工作，贴一次就跟着这一课同步到别处。
进去和切句一样有一条明确的返回。

**译文是一层，不是一个活动。** 所以它在通听与跟读上是一个**开关**，不是第五个 tab：
通听展开文本后每句下面一行中文，跟读当前句下面一行中文，两处共用一个开关（「今天想不想
看中文」不该在两页上各答一次），默认关。**没贴过译文的课上这个开关不出现** ——
一个永远没反应的按钮只会让人以为功能坏了。

排版上**德语仍然是主角**：德语 `text-de`(19px)，中文降一级到 `text-ui`(15px) 且用 `muted`，
缩进对齐在编号之后。中文比德语大或一样重，眼睛会先读中文，这个应用就白做了。

**笔记在通听页底部，是一个折叠块**（有内容默认展开，摘要上带行数）：写和读是同一件事的
两半，分到两个地方去等于每次都要先找一遍。它不占导航位也不进「⋯」—— 通听是落地页，
也正是「弄懂这篇在讲什么」的那一步。

**对照预览是保存前的最后一道人眼关卡。** 编号是锚，但「锚本身贴错了」（贴了另一课、
或者贴的是重新切句之前的那一份）机器分不出来 —— 所以保存前把原文和译文并排列全，
没认到编号的那几句用 warn 色标出来。它不是装饰：这一条挡住的正是整篇错位一句那种坏法。

### 12.10 低置信句确认的形状（FR-15.19，2026-09-16）

**确认在通听里，不另开一个校对页。** 判据是「确认要在听的那一刻做」：这件事的全部内容是
「刚才那一句起点对不对」，而回答它的人正在通听里跟着播放读文本。另开一页意味着同一段音频
要在两个地方各听一遍。

**热区就是行号本身。** 带 `?` 的行号是个按钮：点一下 = 听着对，`?` 消掉；点第二下撤销。
不加「确认」二字的按钮，也不加勾选框 —— 那一行已经有 19px 的德语正文占满，
再挂一个控件就会把每一行都变成一个表单行，而 58 行里只有 6 行需要它。

**确认过的行号和从没被标出来的行号长得一模一样。** 照 §12.3「一切正常就静默」：
消掉 `?` 之后换上一个永久的勾，等于把一种常驻标记换成另一种。可撤销这件事靠热区还在，
不靠画出来 —— 点错一下的代价是再点一下，而它只发生在 `?` 出现过的那几行。

**「点一下行号」这句话必须写在界面上**（通听展开后的一条 `Hint`，带还剩几句）：
手机上没有 hover，`title` 在那里等于不存在，而这是整个界面上唯一一个看不出来的操作。

**每一行是 flex 两列（行号 / 文本），不是内联的编号 + 文本。** 直接原因是触屏：
`index.css` 那条 `@media (pointer: coarse)` 给所有 `button` 44px 最小高度，
内联的按钮会把它所在的**行盒**撑高，于是句子的第一行被推下去半行；作为独立的 flex 项
它只影响整行高度。`items-baseline` 让 13px 的行号仍然坐在德语正文的基线上。
连带收益是**悬挂缩进**：折行之后的文本、以及下面那行译文，都对齐在编号之后
（以前折行回到最左边，译文靠一个写死的 `pl-6` 对齐）。

### 12.11 查词的形状（FR-9.5，2026-09-20）

**查词在生词本里，不是第四个活动。** §12.1 的判据是「这是不是我今天要做的事」——
查词是，但它的下一步几乎总是「这个词要不要进复习」，而那件事就在这一页上。
单开一页会把「课上碰到一个词 → 它进了我的复习队列」这条动线劈成两页。
位置在页顶：报名预置词库一年点两次，查词一节课点五次。

**结果是一张卡，不是两份结果。** 内置词典和 de.wiktionary 各有各的缺口，
但用户问的是一个词，不是两个数据源。**来源写在卡底一行**（`内置词典 + de.wiktionary`），
因为离线时缺的正是例句、同义词和词源那几段 —— 不说清会让人以为这个词就只有这么点解释。
在线那一趟还没回来时那一行写着「正在向 de.wiktionary 补例句…」，关掉了就写关掉了。

**卡上有三个出口，主次分明**：`加入生词本`（主按钮）· `念一遍` · `在维基词典打开`。
第三个不是装饰：解析器永远会漏东西（变格表、构词、译文），而那一页就在一个链接之外。

**查不到也是一种结果，不是错误。** 不用 `Banner`（§12.3：拦路那一档是「这一页现在做不了」，
而查不到一个词之后这一页照常能用），用一张说明卡：说清是「都没有」还是「现在没网」，
给一个去维基词典搜的链接，并且**照样可以原样收下这个词** —— 多词搭配和生僻复合词
两边都查不到，而那恰恰是最值得记的一类。

**输入框旁边挂 ä ö ü ß 四个键**（不给大写：查词不区分大小写）。
手机上的中英键盘打不出变音字母，而这是德语查词的主路径之一 —— 与 FR-8.7 同一条理由。

**不做的**：查词历史（查完的下一步是加或不加，不是回头翻）、整句翻译（那是 FR-19，
而且应用自己不翻译）、把查词结果缓存进标注层（词典是缓存层，加进生词本的那一份才是标注层）。


---

## 附：素材来源

| 来源 | 等级 | 说明 |
|---|---|---|
| DW Alltagsdeutsch | C1/C2 | `learngerman.dw.com/de/alltagsdeutsch/s-9214`。400+ 期存档。**实测：RSS 给最近 100 期，但其中多为存档旧期以当周日期重新推送**（附录 A.2），单期时长 6–10 分钟。音频 + Manuskript + Glossar 全部可程序化获取 |
| DW Sprachbar | C1/C2 | `dw.com/de/deutsch-lernen/sprachbar/s-9011`。专讲词场和惯用法 |
| DW Langsam gesprochene Nachrichten | B2 | `learngerman.dw.com/de/langsam-gesprochene-nachrichten/s-60040332`。每天更新，每条 100–200 词，带全文。词汇偏时事，覆盖面窄 |
| Aspekte neu C1 | C1 | 教材音频，Klett 官网部分免费提供 |
| RSS | — | `rss.dw.com/xml/DKpodcast_alltagsdeutsch_de` |

**V2 无文稿素材**（需先做转写）：`Alles gesagt?`、`Hotel Matze`、`Fest & Flauschig`（纯 Umgangssprache）、`Zeit Verbrechen`（真实案件叙事，与 Schirach 同语域）、ARD/ZDF Mediathek（带德语字幕）。

---

### 12.12 版本的形状（§7.12，2026-09-20）

设置页最后一块（诊断区，`VersionSection`）。**存在的唯一理由是让热更不是黑箱** ——
接了热更之后「我手机上到底是哪一版」不再能从 App Store 的版本号推出来：壳的版本和跑着的
前端版本从此是两个数，而且大多数时候不一样。

| 在哪 | 显示什么 |
|---|---|
| 浏览器 | 一句话：会自己更新，不用做任何事。**不摆版本号** —— web 上没有「壳」这个概念，摆出来只会让人以为要去哪里升级 |
| 原生壳 | 两行事实：`应用壳 0.4.0 —— 换它要过 App Store` / `前端 0.4.0+a1b2c3d —— 这一份会自己更新`。没热更过时前端那行写「随包那份」，**不写 `builtin`**（那是插件的内部值，摆到界面上等于让人去搜一个搜不到的词） |
| 下好了待生效 | 升到一行提示（`Note`，§12.3 的「一行」档）：`新版 … 已下好，下次打开这个应用时生效` |

两条：

1. **不给「立刻换上这一版」的按钮。** 那是整个 WebView 重载，会清掉只活在
   React state 里的听写答案，而这一页恰恰可以从练习中途进来。
2. **平时是静默的**（§12.3 第一档）：没有待生效的更新时，这一块只有两行灰字，不画任何状态。

**「现在就查一次更新」按钮（变更 52，2026-09-23）——iOS 上才有，摆在 `Section` 的
`aside`**。这个按钮和上面那条不矛盾：**查一次**和**换一次**是两件事，`checkNativeUpdate()`
只是后台一次网络请求 + 判断，不碰当前跑着的这份前端。加它的理由是变更 50 那次死锁——
没有 Mac、看不到 Xcode 控制台时，「push 了 main，手机到底拿到没有」原来要等下次冷启动
才能看到结果，现在能在设置页当场把这个回路闭上。

**诊断区新增两块**（`Disclosure`，跟安全区/常亮同一处）：

| 块 | 内容 | 为什么 |
|---|---|---|
| 插件注册 | `probePlugins()` 的名单 + `CapacitorUpdater` 在不在里面 + 它的方法名 | 分开「类没链进二进制」（要发新包）和「调用本身超时」（可能下一版自己就好）这两种成因——见 §7.12「诊断」那段 |
| 上次查更新 | `readLastCheckLog()`：多久前、走到了哪一步、结论。**一笔都没有时画一行 `warn` 明说**（变更 57） | `checkNativeUpdate()` 原来失败就是 `catch { return null }`，重启一次什么都不剩；现在无论成功、跳过还是异常**、以及根本没跑完**都落得下（逐步落盘，见 §7.12 ⑥）。「从没查过就不画空壳」那条旧约定在变更 57 被真机反馈推翻——不画的话，最强的线索长得和「一切正常」一样 |

前一块只在 iOS 壳上画（`probe.platform === 'ios'`，
安全区那次实测顺带定下来的平台）。

**诊断区再新增一组三个按钮（变更 57，2026-09-23）——同样只在 iOS 壳上画**：

| 按钮 | 做什么 | 形状上的约定 |
|---|---|---|
| 跑一次桥自检 | `runUpdaterSelfTest()`，结果按 `方法 → 结果（耗时）` 一行行摆出来 | **耗时必须显示**，它本身就是答案（见 §7.12 那张表）。有任何一个 `timeout` 时整块升到 `warn` |
| 发到服务器 | 收一份报告 `POST /v1/diag`，成功后把 id 说出来 | 没自检过就先自己跑一遍——一份没有自检的报告基本说明不了问题 |
| 复制诊断 | 同一份报告的**纯文本**形式进剪贴板 | 不是 `JSON.stringify`：这条路的终点是聊天窗口，要的是人扫一眼就看懂的东西。剪贴板被拒就把全文摊开，**不假装复制成功** |

按钮上方常驻一句话：自检可能要一两分钟。不说的话会以为是又卡住了——而「又卡住了」
正是这一块要查的东西。

### 12.13 识词卡的形状（FR-21，2026-09-21）

**读卡和听卡必须一眼分得出来，而且不能靠「有没有声音」去分。**
听卡进去就自动播一次，读卡什么都不播 —— 如果两张卡长得一样，用户每次进读卡都会
先愣一秒等声音。所以卡顶给一行**题干**，用人话说清这一关考什么：
`听这个词` / `这个词是什么意思` / `哪个词是这个意思` / `哪个词填得进这个空`。
不用图标不用色块：这一行同时是题目本身，§12.3 的第一档（平时静默）在这里不适用。

**列数按题型分**（延续 FR-10.7 的判据 —— 看选项文本有多长，不看它属于哪张卡）：

| 题型 | 选项内容 | 窄屏 | 宽屏 |
|---|---|---|---|
| `read-gloss` | 四条德语释义（截到 80 字符） | 单列 | 2×2 |
| `read-form` | 四个词形 | 2×2 | 2×2 |
| `cloze` | 四个词形 | 2×2 | 2×2 |

**挖空句在题面上方，不在选项里。** 句子用**大一号的字**（§12.4 的第二级），
空位是固定宽度的下划线 —— 宽度跟着词长走的话，四个选项里一眼能排掉两个（FR-21.8）。
句子超过两行就截断并给一个「展开」：cloze 考的是那个空，不是耐心。

**`read-gloss` 的释义选项要遮词头**，与听音辨义题同一条（choices.ts 规则⑤），
而且**四个一起遮** —— 只遮正确项的话，带「…」的那个就是答案。

**「没听清 / 不认识」那个出口在读卡上只写「不认识」**：读卡从头到尾没有声音，
一个说不通的选项会让人犹豫一下自己是不是点错了地方。出口本身两张卡都有（FR-21.11）。

**答错展开的卡背两张卡是同一块**（FR-10.3）：词 + 性/复数 + IPA + 完整释义 + 例句/原句。
读卡的卡背上**多一个「念一遍」按钮** —— 这是这张卡唯一出现声音的地方，
而「我认得这个词但从没听过它」正是它该被补上的时候。卡面上不给（FR-21「不做的」那一条）。

**「念一遍」念的是什么，按卡的来源分（变更 53，2026-09-23 定形）**：课程卡（有原句、
`audioStatus === 'ok'`）念**原句**，不念孤立词形 —— 那句本来就在本机、也正是挖空题面
用的那句，念它才练得到连读；预置卡/查词卡没有原句，仍然退孤立词。**没有任何音源时
按钮不出现**，不给一个按下去没反应的键（原来的实现无条件渲染这个按钮，点了没反应，
是变更 53 顺手一起修的三处静默失败之一）。

**记录页（FR-18）按卡分别计数**：听 / 读两条曲线，不合成一条。
合成一条就看不出「最近一周只在练读」这种事，而那恰恰是同日互斥会造成的正常漂移。

**不做的**：手打拼写框（生产方向，§7.5）；读卡上的自动发音；
在卡面上标「这是第几张卡」（用户要知道的是这一关考什么，不是数据模型）。

### 12.14 补中译的形状（FR-21.9，2026-09-21）

**位置在生词本页底部的折叠块**，与 FR-20 把课程笔记放在通听页底部同构：
低频（一年点几次）、但和这一页的主业是同一件事。不做成第四个活动，
也不放页顶 —— 页顶是查词，那是一节课点五次的东西（§12.11）。

**没有缺中译的词时整块不出现。** 与「没贴过译文的课上不出现译文开关」（FR-19.4）
同一条：一个永远显示「0 个词待补」的块，只是在每次打开生词本时提醒你它没用。

**动线和译文那边逐字相同**：`复制走` → 在外面翻 → `粘回来`，中间隔着一个外部工具。
标题上带数量（`补中译（37 个词还没有中文）`），因为这个数决定了要不要现在做。

**保存前必须过一屏对照预览**：左边德语词、右边粘回来的中文，一行一个。
编号是导出那一刻的序号而不是词条的稳定标识（FR-21.9），所以「整篇错位一格」
是这里唯一真正危险的失败 —— 而它在预览里一眼就看得见（`Vorhang` 对着「窗帘」
还是对着「过程」）。解析不出编号的那些**照旧显性报出来**（「37 个词认到 35 个」）。

**不做的**：自动翻译（应用自己不翻译，FR-19 同一条立场）；
把中译搬进题面（词典的中译只覆盖 60%，四选一凑不齐干扰项 —— FR-21.9）；
一次只导一部分（分批是**贴回来**那一侧的事，`applyTranslations` 本来就允许只贴一半）。

### 12.15 顶部安全区的兜底（变更 46，2026-09-22）

**症状**：iPhone 13 的原生壳上，吸顶导航和系统状态栏（时间、信号、电量）压在一起。

**而规则是对的**：`index.html` 里 `viewport-fit=cover` 在，`index.css` 里
`.app-nav { padding-top: calc(0.5rem + env(safe-area-inset-top)) }` 也在。
两条都在却还压着，只剩一个可能 —— **`env(safe-area-inset-top)` 在那台设备上解析成了 0**。

**成因没有验出来，所以没有按嫌疑去改。** 最像的是 `capacitor.config.ts` 的
`contentInset: 'never'`（它把 WKWebView 的 `contentInsetAdjustmentBehavior` 设成 `.never`，
而 env() 取自同一套「未被遮挡区域」的计算）。但那只是嫌疑，而另一个候选值 `'always'`
的失败方式更糟：系统再插一层 inset，和 CSS 的 padding 叠起来，导航下面空出一条 ——
那是一个**「看着像没坏」的坏法**，比现在这个明显的重叠更难被发现。

**`env()` 确实是 0 —— 这一半 2026-09-22 已经定了，而且不是靠那行诊断定的。**
他装上 `ios-v0.6.0` 之后报「布局已经对了」，而 0.5.0 → 0.6.0 之间**唯一动过版面的
就是上面那个 `max()`**。`--safe-top-min` 只在「量到 env() 是 0」时才会被写进去，
所以假如 env() 本来是好的，`max()` 会原样选中它，算出来的内边距与 0.5.0 逐像素相同，
顶部就该还压着状态栏。它没压 → env() 当时报的就是 0，顶上那 47px 是查表补的。
（**仍未定的是「为什么是 0」**。但那一条已经不急：兜底在 env() 正常的设备上零效果，
哪天 WebKit / Capacitor 那边修好了，这段代码会自己退场。）

**做的是一件不需要知道成因的事**（`src/platform/safeArea.ts`）：

| 步 | 做什么 |
|---|---|
| 量 | 启动时插一个 `padding: env(safe-area-inset-*)` 的探针，读 `getComputedStyle` |
| 补 | **只在原生 iOS 上**，量到 0 且屏幕尺寸像刘海机时，把该有的值写进 `--safe-top-min` / `--safe-bottom-min` |
| 让 | CSS 写成 `max(env(…), var(--safe-top-min))` —— env() 正常时兜底**零效果**，不是多出一条白边 |
| 说 | 实测值进设置页的诊断块（`Disclosure`，§12.3 第一档） |

用 `padding` 而不是 `height` 去量：`height: env(…)` 在值为 0 时元素会被折叠，
而 `paddingTop` 无论如何都返回一个像素字符串。

补多少**按屏幕的 CSS 尺寸查表**（390×844 → 47，393×852 → 59，…）：env() 坏掉之后，
WebView 里没有第二个地方能问到真实的安全区高度，而屏幕尺寸和机型一一对应。
表里没有的退到「长宽比 ≥ 2 就算刘海机」加一个保守值 —— 全面屏 iPhone 全部 ≥ 2.16，
SE（375×667）是 1.78，两边都留了足够余量。**iPad 与 Android 一律不补**：
在一台本来就没有刘海的设备上凭空加 47px，是实打实的破坏。

**那一行诊断不是装饰，它是这一节的退出条件。** 设置 → 版本 → 「这台设备的边距与常亮」：
写着「env 报 47」就说明 env 是好的、问题在别处；写着「env 报 0，已兜底到 47」就说明
嫌疑成立，那时才轮到去动 `contentInset`。

两条**第一版写错了、当天就改掉**的：

1. **它只挂在原生那一支上**，理由写的是「浏览器上这两条没有争议」。那句话是错的，
   而且错得很实际：藏进原生分支意味着**只有装了新包的人才看得到它**，
   而这一块存在的全部理由就是**在装包之前先问出一个数**。现在两个分支都画。
2. **行首必须写平台**。同一个 `env() = 0`，在桌面浏览器和 iPhone 的普通 Safari 标签页里
   都是**正确**的（web 内容本来就不在刘海底下），只有在 `ios` 原生壳里才是故障 ——
   不写平台，就是让人拿一个 0 去猜三种含义。所以 `SafeAreaProbe` 带上 `platform`，
   而且那一行的后半句按平台分开说。

诊断块**自己也会量**：`App.tsx` 启动时那一次排在 `nativePlatform()`（异步动态 import）之后，
它失败或哪天被挪了位置，这一块就只会显示「还没量」—— 那时它就废了。没有就现量一次。

### 12.16 问 AI 补解释的形状（FR-9.11 / FR-9.12，2026-09-22 起服务器实时调用；当晚变更 49 补上查到的词那一支）

**四处入口，一次调用**：生词本每一行一个「问 AI」按钮；查词面板里查到的词也是一个
「问 AI」按钮（`ResultCard` 里的 `AiBlock`，变更 49 新增——之前完全没有）；两个词典
都查不到的词，查词面板**自动**问一次（`NotFound` 复用同一个 `AiBlock`）。三处都是
同一个客户端函数（`src/ai/explain.ts` 的 `explainWithAi`），区别只在有没有 `context`
（原句）/ `existing`（词典已给的释义）。**查到的词不自动问**：内容已经有了，自动问
一次只会让「查一个查到过的词」每次都白花一次调用——这条界线是变更 49 才有的，
之前只有一支，无所谓自动还是按钮。

**答案缓存，跨设备同步，命中就不再问（变更 49）**：`explainWithAi` 每次成功都会把
`{词, 答案}` 写进一份独立的 `AiCache`（`src/ai/cache.ts`，归一化词 → `{note, ts}`，
第五种同步文档，见 `sync/docs.ts` 的 `AI_CACHE_DOC_ID`）。查词面板每次提交都先查
这份缓存：命中就直接显示，不管这个词查到没查到、不管缓存是从哪个入口问出来的
（生词本行上问的、还是查词面板问的，共享同一份缓存）。没命中，查到的词等按钮、
查不到的词照旧自动问一次。缓存独立于 `VocabEntry`——查一个词不一定会把它收进
生词本，缓存必须不依赖「有没有收进生词本」才能满足「以后再查这个词直接显示」。
合并规则是逐词取 `ts` 更新的那条（不是学习记录那种逐格求和/取 max）：一个词只需要
一份解释，「重新问」就是想让新答案覆盖旧的。

**第四处入口是复习卡背，而且它不是按钮（FR-10.14，变更 55）**：答错展开卡背时，
如果这个词**一个中文字都没有**（`meaningZh` 和 `note` 都空），就自动走一次
「先查缓存 → 没有才问」，答案写回 `entry.note`。这里**不给按钮**：答错当下人的
注意力在「这个词到底是什么」上，多一个要点的按钮等于多一道摩擦，而这一档的成本
被两件事夹住 —— 有任何一种中文就不问，答对根本不露卡背。**卡背不等它**：词形、
IPA、德语释义、例句立刻就在上面，中文回来了再填进来；请求也不随翻页取消（答案
已经付过钱了，落库比丢掉划算）。**没配 AI 时这一块完全不出现** —— 与三处按钮式
入口不同，那里「AI 服务暂时不可用」是对一次点击的回答，而卡背上没人点过任何东西，
每答错一次就唠叨一行是纯噪音（§12.3「一切正常就静默」）；真的发出去又失败了才给
那一行 + 「重试」。

**没有待办列表、没有编号、没有对照预览** —— 这三样都是「批量剪贴板」那个形状特有的
（§12.14 的「补中译」仍然是那个形状，因为翻译还是「应用自己不翻译，外面做」，
但问 AI 已经不是了）。一次点击就是一次完整的请求，不需要攒一批。

**生词本行上的按钮永远可点，不是一次性标记**：查到的词点一次是「问」，
再点一次是「重新问、覆盖旧的 `note`」——按钮文案跟着 `note` 是否已有内容换
（「问 AI」/「重新问 AI」），中间态显示「解释中…」并禁用，失败就在那一行下面
用 `Hint` 说清楚，不吞掉。

**查词面板里，两个词典都尘埃落定地没有这个词，才触发**——不能在 de.wiktionary
还没回来的时候就问，那会把「还没查完」误判成「查不到」。问的结果显示在
「没查到」卡片里（缩进、`whitespace-pre-line` 保留换行），点「加入生词本」时
跟着 `createFromLookup` 一起收下，不用户再去生词本页点第二次。

**生词本列表里，`note` 默认折叠（2026-09-23，变更 56）**：AI 的解释常常有好几段，
不折叠就把整行「词形 / 释义 / 原句 / 状态」这些扫词条时真正要看的东西挤到屏幕外。
复用 §12.14「补中译」同一个 `Disclosure` 组件，摘要固定写「AI 解释」、默认收起
（与 `Disclosure` 在 FR-20 笔记那边默认展开相反——这里内容本来就会盖住一整块列表，
折叠块是用来收纳的，不是给「有内容就该露出来」那条默认让路）。

**失败一律呈现成「AI 服务暂时不可用」**，不区分没登录 / 服务器没配 key /
上游限流 / 网络错误——这几种原因用户能做的事一样：过会儿再试，或者去看设置页的
登录状态。区分它们换不来任何有用的下一步动作。

**不做的**：批量/待办队列（一次点击的延迟可以接受，攒批不再有意义）；
限流或用量上限（先观察实际用量，见变更 48 讨论）；
把 `note` 搬进复习的题面（它可能是一整段辨析，题面放不下，卡背才是它的位置）；
给「问」分等级（「有点不懂」和「完全不懂」下一步是同一个动作）。

### 12.17 逐词高亮的形状（FR-5.3，2026-09-23 修正）

通听展开文本之后，三样东西同时在说「读到哪儿了」，它们的强度必须**逐级递增**：

| 层 | 样子 | 为什么是这一档 |
|---|---|---|
| 刚读完的那一行 | 左边一道 `warn/40` 竖线，文字转 `ink` | 句间空档（台标音乐、换播音员）里不让高亮整个消失 |
| 当前行 | `bg-warn-soft` 整行底色 + 文字 `ink` | 一眼扫到「在第几句」，但不能抢词 |
| **当前词** | **`bg-warn` 实底 + `text-surface` 字**（反白）+ `font-medium` | 这是这一页真正的主角 |

**当前词不许用 `-soft` 那一档**，这是一条硬规则：词级高亮**永远**发生在当前行上，
而当前行的底色就是 `bg-warn-soft` —— 同色叠同色等于没画，剩下的只有一档字重，
而字重能不能看出来**取决于设备的字体渲染**（变更 58：Windows 上看得见、iPhone 上看不见）。
反白的两个令牌都跟着深色翻转：浅色是深棕底白字，深色是亮金底黑字，两边都不靠字重说话。

回归判据写在 `ListenTab.test.tsx` 里，而且是**两个类之间的关系**（当前词的底色 ≠ 当前行的
底色），不是「等于某个类名」—— 换配色照样过，退回同色就红。

### 12.18 教材导入的形状（FR-1.6 ~ FR-1.9，2026-09-26）

**两个入口，不合并。** 单课导入（`#/import`）照旧是「一课一稿一音频」，只多了三样：
「归到哪一组」输入框、「整理 PDF 断行」按钮、说话人勾选（有候选才出现）；音频可以多选（按文件名顺序拼）。
整本导入是单独一页（`#/import-book`），入口在课程列表顶上（「导入教材」）和单课导入页顶上一行小字。
不合并是因为两者的主语不同：单课页的主语是「这一课」，整本页的主语是「这本书里的哪几题」。

整本导入页从上到下四块，顺序就是动作顺序：

1. **分组**：教材名，必填（课程列表按它分组），给已有组名做候选
2. **文稿**：一个 textarea，失焦才解析（FR-1.2 的 20000 字符约束）；区块右上角是「N 章 · M 题」；
   下面是说话人勾选。认不出任何标题时是一条拦路 `Banner`，出口是「用单课导入」
3. **选题与音频**：按章分块，章名那一行是整章的勾选框；每题一行 = 勾选框 + 题目标题 + 句数，
   勾上之后行尾出现 `− N 轨 +`；选了音频之后每行下面一行小字列出分到的文件名（分不到时明说「可以之后再补」）。
   选音频的按钮在区块标题右侧（`aside`），因为它作用于整个区块、不属于任何一题。
   轨数总和与文件数对不上时一条 `Note`（不拦路 —— 先导文字、之后补音频是合法的）
4. **导入 N 课** 主按钮，进行中显示「正在导入 k/N…」；完成回课程列表

**课程列表里的组**：没分组的课在最上面照旧平铺；每组一个 `<details>` 块，**默认折叠**，
摘要行 = 组名 + 「N 课 · 已对齐 M」+（缺音频时）一个 warn `Chip`。展开后先是「一次选齐音频」那条 `Note`（只在有课缺音频时），
再是和平铺时一模一样的课程行。

**说话人标记长什么样**：句首一个 `text-note` 的 `faint` 色小标，符号原样、名字原样；只在一段话的第一句上出现。
它在通听、切句、跟读、学词四处出现，不在复习卡上出现（卡面只有一句，谁说的不重要）。

### 12.19 听卡题干与辨义题的目标词（FR-10.9，2026-09-26）

**题干跟题型走，不写死。** 听卡正面那一行小字就是题目本身（与 §12.13 读卡的题干同一个理由）：

| 题型 | 题干 | 卡面上的词形 |
|---|---|---|
| `form` 辨形（新卡 / 重学） | `听这一句，选出你听到的那个词` | **不给**（FR-10.2：考的正是听不听得出） |
| `gloss` 辨义（Review） | `这句里有这个词，选出它的意思` | **给**：播放键下方一行，`text-word` 加粗，裸词形不带冠词 |

**不写「挖掉」「挖空」。** 听卡的句子是整句播的，那是读卡 `cloze` 的词。
以前这里写死一句「选出挖掉的那个词」，两种题型都在说错话，辨义题上尤其让人以为选项放错了。

**为什么辨义题要亮出词形**：一整句话里没说考哪个词，只能靠「四条释义各属于谁、谁在这句里」去排除，
那道题同时考了两件事。它已经过了辨形那一关（FR-10.9 的阶梯），再藏词形没有训练价值。
显示的是 `entry.surface`（句子里出现的那个形状，方便在音频里对上），不带冠词 —— 性留给答对的 600ms 和卡背（FR-10.11）。
释义选项照旧遮词头（choices.ts 规则⑤）：题面上有词形，选项里再写一遍就等于标出了答案。

**孤立词发音的卡（预置 / 查词）同样亮出**，只是不改那一行音源说明；
没有任何音源时下面已经把词形亮出来了（FR-10.5），不重复显示。

---

## 附录 A：DW 接口实测结果

> 探测时间 **2026-08-30**。样本：Alltagsdeutsch「„Work and Travel" – Geld verdienen und Reisen im Ausland」，lesson id `45334084`。
> 这是 FR-13 / FR-14 / §7.8 的全部事实依据。DW 改版后此附录即失效，需重跑 A.5。

### A.1 CORS（决定性发现）

| 端点 | 结果 |
|---|---|
| `rss.dw.com/xml/DKpodcast_alltagsdeutsch_de` | `Access-Control-Allow-Origin: *` |
| `learngerman.dw.com/de/<slug>/l-<id>` | `Access-Control-Allow-Origin: *` |
| `radiodownloaddw-a.akamaihd.net/...mp3` | `Access-Control-Allow-Origin: *`，另有 `Accept-Ranges: bytes`、`Content-Length` |

**浏览器可以直连三者**，所以不需要后端、代理或本地进程。这是整个 FR-13 得以纯前端实现的唯一原因，也是它的单点依赖（见 §8）。

### A.2 RSS feed

100 个 `<item>`。单条包含：

```xml
<item>
 <guid isPermaLink="false">45334084</guid>
 <pubDate>Tue, 25 Aug 2026 07:36:00 GMT</pubDate>
 <title>„Work and Travel" – Geld verdienen und Reisen im Ausland</title>
 <link>https://learngerman.dw.com/de/work-and-travel-.../l-45334084?maca=de-DKpodcast_alltagsdeutsch_de-2283-xml-mrss</link>
 <description>...两句德语摘要，此处按 §3.1.1 R-5 省略...</description>
 <itunes:duration>06:16</itunes:duration>
 <enclosure url="https://radiodownloaddw-a.akamaihd.net/Events/podcasts/de/2283_.../6CB36D94_2-podcast-2283-45334084.mp3"
            type="audio/mpeg" length="7117421"/>
 <itunes:keywords>..., Alltagsdeutsch, C1</itunes:keywords>
</item>
```

要点：

- `<guid>` = lesson id = `<link>` 里的 `l-<id>` = `__APOLLO_STATE__` 的 `Lesson:<id>`。**用它做主键**（FR-13.8）。
- `<description>` **只是两句摘要，不含 Manuskript**。文稿必须从页面取。
- `<link>` 带 `?maca=` 跟踪参数，不要拿它当稳定标识。
- **`pubDate` 不是首发日期。** 本例 `pubDate` 是 2026-08-25，而页面里的 `firstPublicationDate` 是 **2019-04-02** —— DW 在把存档旧期以当周日期重新推送。这印证了 §7 素材表里「更新已放缓，靠吃存档」的判断，也是 FR-13.3 存在的原因。
- RSS 的 `<enclosure>` URL 与页面里的 `mp3Src` 路径不同（`/podcasts/de/2283_...` vs `/dira/mp3/deutschkurse/...`），但指向同一个文件 id（`6CB36D94_2`）。两个都能下，用哪个都行。

### A.3 页面：`window.__APOLLO_STATE__`

页面 79KB，**服务端渲染**，内联一个约 60KB 的 Apollo GraphQL cache。（页面 HTML 里出现的两处 `Manuskript` 字样只是界面 i18n 文案，不是正文 —— 按关键词找会误判。）

实体键：`Lesson:45334084`、`Audio:78400094`、`Knowledge:*`（本期 13 条）、`Image:*`、`Navigation:*`、`Article:*`。

`Lesson` 关键字段实测值：

| 字段 | 值 |
|---|---|
| `manuscript` | 7824 字符 HTML，**只用到 `<p> <strong> <br /> <span>` 四种标签** |
| `teaser` | 214 字符，与 `manuscript` 开头 `<strong>` 块内容重合 |
| `name` / `namedUrl` / `canonicalUrl` | 标题与稳定 URL |
| `firstPublicationDate` | `2019-04-02T13:00:47.203Z`（真实首发） |
| `knowledges` | 13 个 `__ref`，指向 Glossar 条目 |
| `dkLearningLevel` | `32`（推测为 C1，未验证映射表） |

`Audio` 关键字段：`mp3Src`（直链）、`duration`（**376 秒**，可预填 `audioDuration`）、`formattedDuration`（`06:16`）。

`manuscript` 结构（实测）：

- 开头是一个 `<strong>` 块 = 标题 + teaser。~~音频里不朗读 → FR-13.7 排除。~~
  **这一条是错的（2026-09-02 实测推翻，见 §0 变更 24）**：这一期的音频开头，播音员把标题和这两句导语一字不差地念了出来。
  自动导入因此不再排除任何句子。
- 结尾**没有** Glossar 段落，正文到最后一句就结束。Glossar 是独立的 `Knowledge` 实体，不在正文里。这比预期好 —— 对 DW 自动导入而言，FR-1.4「批量排除文末 N 句」基本用不上（手动粘贴 PDF 时仍然需要）。
- 生词在正文中**内联标注**：

```html
<span class="editable placeholder" data-fromselection="true"
      data-id="43252624" data-title="Plattform, -en (f.)"
      data-type="GLOSSARY" title="Glosar">Plattformen</span>
```

对应实体：

```json
"Knowledge:43252624": {
  "knowledgeType": "GLOSSARY",
  "name": "Plattform, -en (f.)",
  "text": "<p>...德语释义...</p>"
}
```

即：句中表层形式、词条、复数词尾、性、德语释义、精确字符位置，一次全给。这是 FR-14 的全部依据。

其他实测样本（说明 `data-title` 的格式变化，FR-14.4 要能降级）：

- `Gelegenheitsjob, -s (m.)` — 标准名词，性 + 复数词尾
- `surfen` — 动词，无性无复数
- `Down Under (aus dem Englischen)` — 带来源说明，无性
- `Great Ocean Road (f., nur Singular, aus dem Englischen)` — 性 + 「仅单数」+ 来源

### A.4 尚未验证

- 存档列表页 `/de/alltagsdeutsch/s-9214` 是否也在 `__APOLLO_STATE__` 里给出完整的 400+ 期列表（Q7）
- ~~Sprachbar 与 Langsam gesprochene Nachrichten 的页面是否同构~~ —— **不同构**（2026-09-26 实测，变更 66）：LGN 是 Article；Sprachbar 的 RSS 已下线
- `dkLearningLevel` 数值到 CEFR 等级的映射

### A.6 实现期复验（2026-08-31）

用真实的 Alltagsdeutsch 期次 `45334084`（即附录 A 的样本）跑通了完整链路，附录 A 的全部结论**仍然成立**：

| 检查项 | 结果 |
|---|---|
| RSS 直连（CORS） | ✅ 100 个 item，无代理 |
| 页面 `__APOLLO_STATE__` 大括号配平扫描 | ✅ 解析成功，取到 `manuscript` / `teaser` / `knowledges` / `Audio.mp3Src` |
| mp3 直连下载 | ✅ 5.7 MB 入 IndexedDB，`<audio>` 可播，时长 6:16 与 `Audio.duration`（376s）一致 |
| `firstPublicationDate` | ✅ 2019-04-02，与 `pubDate`（2026-08-25）确实不同 —— FR-13.3 有存在必要 |
| FR-13.7 teaser 块判定 | ⛔ **判定本身没问题（首个 `<strong>` 块确实含 `teaser`，开头 3 句被自动排除），但「该不该排」是错的** —— 2026-09-02 听音频发现这 3 句被念了出来。整个机制已删除，见 §0 变更 24 |
| **FR-14 候选词 offset 映射** | ✅ 14 条候选**全部**落在正确句子上，句内 offset 切出来正是 `surface` 本身 |
| `data-title` 解析与降级 | ✅ `Agentur, -en (f.)` → 性+复数；`Down Under (Englisch)` → 只有词条；`Great Ocean Road (f., Englisch)` → 性但无复数 |
| 切句 | 45 句（当时把开头 3 句当 teaser 排掉了，故记作 42 句 —— 见上一行）。`z. B.`、`Dr.`、序数日期均未误断 |

一处**新发现的行为**（不是 bug，是 §7.1 R-quote 规则的直接后果，记在这里免得下次当成 bug 查）：
跨句的整段引语会被合并成**一个**句子。本期里最长的一段 437 字符，因为德语开引号 `„` 直到段末才闭合。
这对切句语义是对的（引语确实是一个整体），但对跟读来说太长 —— 需要时用 FR-2.3 手工拆开即可。
若将来嫌烦，可考虑「引号内超过 N 个句末标点就允许在内部断开」，但那会让规则变复杂，暂不做。

### A.5 重新验证命令（PowerShell）

> 注意用 `curl.exe` 而不是 `curl` —— 后者在 Windows PowerShell 里是 `Invoke-WebRequest` 的别名，参数不兼容。

```powershell
curl.exe -sI -H "Origin: https://example.app" https://rss.dw.com/xml/DKpodcast_alltagsdeutsch_de | Select-String "access-control"
```

```powershell
curl.exe -sL https://rss.dw.com/xml/DKpodcast_alltagsdeutsch_de | Select-String -Pattern "<enclosure|<guid|<itunes:duration" | Select-Object -First 6
```

```powershell
curl.exe -sL "https://learngerman.dw.com/de/work-and-travel-geld-verdienen-und-reisen-im-ausland/l-45334084" | Select-String -Pattern "__APOLLO_STATE__" -SimpleMatch | Select-Object -First 1
```

---

## 附录 B：GitHub API 实测结果（历史，2026-09-02 起不再适用）

> 这一整附录记录的是**已被换掉的那套备份方案**（§0 变更 25）。留着是因为里面的实测结论
> 本身没有过时（`api.github.com` 的 CORS 行为、OAuth 端点的限制），哪天要重新评估
> GitHub 那条路时不必再测一遍。**现行方案的接口文档在 `server/README.md`。**

> 探测时间 **2026-08-30**。这是 §2.6.3 与 FR-11 的事实依据。

### B.1 CORS：API 开放，OAuth 端点封闭

| 端点 | 预检 (OPTIONS) | `Access-Control-Allow-Origin` | 浏览器可用 |
|---|---|---|---|
| `api.github.com/*` | — | `*`（**连 401 响应都带**） | ✅ |
| `github.com/login/device/code` | **404** | 无 | ❌ |
| `github.com/login/oauth/access_token` | **404** | 无 | ❌ |

**这张表决定了 FR-11 的形状**：token 拿到之后所有操作都能在浏览器里做，但**获取 token 的过程不能**。

三条 OAuth 路径全部堵死：

1. **授权码流程** —— 需要 `client_secret` 换 token，秘密不能放在浏览器里
2. **PKCE**（SPA 的标准解法）—— GitHub 至今不支持
3. **Device Flow** —— 本是为「存不住秘密的设备」设计、只需 `client_id`，但两个端点都在 `github.com` 而非 `api.github.com` 上，**预检 404、无任何 CORS 头，浏览器根本发不出请求**

因此 V1 采用 PAT，并把「粘贴之后的一切」全部自动化（FR-11.2 ~ 11.4）。

### B.2 待实现时确认

- token 过期时间的**具体响应头名称**（FR-11.5 依赖它做续期提醒）
- fine-grained PAT 的最长有效期上限，以及是否允许「永不过期」
- `POST /user/repos` 创建的仓库默认分支名（拼接 contents API 路径时要用）

### B.3 重新验证命令（PowerShell）

```powershell
curl.exe -s -o NUL -D - -H "Origin: https://example.app" https://api.github.com/user | Select-String "access-control-allow-origin"
```

```powershell
curl.exe -s -o NUL -D - -X OPTIONS -H "Origin: https://example.app" -H "Access-Control-Request-Method: POST" https://github.com/login/device/code | Select-String "HTTP/|access-control"
```
