# 新会话开工提示

> 复制下面整段，粘贴到新会话即可开工。

---

项目：德语精听训练器（个人自用工具，工作目录 D:\deutsch）

**先读 SPEC.md，再读 README.md。** 需求文档已定稿，包含完整的功能需求、数据模型、技术决策、风险表和验收清单。
附录 A 是 DW 接口的实测结果（A.6 是实现完成后用真实期次做的复验），附录 B 是 GitHub API 的实测结果——
这些是真实探测出来的事实，不要重新假设，也不用重新验证（除非怀疑对方改版了）。

## 现状（2026-09-21，iOS 去掉本地对齐 + 换德语原生模型）

**用户的原话**：「ios 版本上，把那个语言对齐的功能去掉吧。大部分时候根本跑不起来，
还占用了 200 多 mb 的体积。桌面浏览器和服务器的功能保留。然后看看现在没有体积限制后，
服务器版本能否采用更准确更优的方案。」两件事一起做了（SPEC §0 变更 42 + 新增 FR-15.20/21）。

**① iOS 上的本地对齐整条删掉。** 删的是 `native-plugins/align-native`（Swift + ONNX 插件）、
`src/align/nativeEmissions.ts`、发布流水线里的 `stage:align` 两步、`AppDelegate` 里 `models`
那条符号链接。理由：它从建成（变更 31）到改成手动（变更 33）**一次都没有真的跑完过一课** ——
一课十几分钟满载、必须一直亮屏，而 iOS 三十秒就锁屏、锁屏即挂起。代价却是确定的：
随包 418 MiB 权重（装机 553.6MB）、一个要联网 resolve binaryTarget 的 SwiftPM 依赖。
**「送到服务器算」留着**，手机上时间戳现在只有两个来源：服务器算 / 桌面算完同步过来。
**iOS 装机体积 553.6MB → 95.2MB（真机实测，少 458MB / 83%）**；IPA 那一侧 410.2 MiB → 32.7 MiB。Android 那条**没动**（它仍然随包带权重）。

**② 模型换成德语原生的。** `oliverguhr/wav2vec2-large-xlsr-53-german-cv9`（Apache-2.0，
CV9 上 WER 9.48%）。上一个 MMS-FA 是多语言**罗马化**对齐器，德语进去之前 ä→a、ö→o、ü→u、ß→ss，
而且词表里**没有空格** —— 词边界完全不被建模。新词表 35 个 token，自带 ä/ö/ü/ß 和词分隔符 `|`，
**blank 是 32（`[PAD]`）不是 0**（0 是 `|`），所以 `blankId` 从常量变成了
`forcedAlign`/`alignWindowed` 的显式参数（给错不报错，只是路径全错）。
`vocab.ts` 整个重写：罗马化 → 直接映射，词与词之间、句与句之间插 `|`，折回时间戳时整个跳过。

**实测（本机浏览器，同一课 6:16、45 句、webgpu/q4）**：44 秒跑完 45/45 句，
句级 confidence **中位数 -0.02 / p25 -0.03 / 最差 -1.59**（换之前是「中位数 -1.08、跨度 1~2」），
3 句待校对且**全是采访同期声**（真的不好念），不再是「模型看不懂的字符」。
时间戳单调无重叠、字符速率中位数 14.6/秒、末句止于 366.15s（音频 376.5s）。
`apply.ts` 的 `REVIEW_MARGIN` **一行没改**就还准 —— 相对基准的价值在这一刻兑现了。

**③ 权重从哪儿来变了。** 服务器跑 **fp32**（1204 MiB，自己从 HF 取）+ **闲置 10 分钟释放**
（`ALIGN_IDLE_MS`，那 1.3GB 没理由 7×24 占着），`mem_limit` 2g → 4g。
浏览器跑**自己量化的 4-bit**（230.2 MiB，`python scripts/quantize-align-model.py`，5 秒）——
**HF 上没有这份文件**，而 Cloudflare 单文件上限 25 MiB 放不下，所以那台同步服务器多了一个身份：
**权重站**（`GET /v1/align/weights/**`，`server/src/align/weights.ts`，支持 Range、不要求登录、
靠 CORS 白名单护栏）。它和对齐是分开的两件事：`ALIGN_ENABLED=false` 之后权重站仍要工作。
**代价写在明处**：两条路不再用同一份权重，同一课换个地方重对会有几十毫秒级的出入。

**一个坑，记下来免得再踩**：`preprocessor_config.json` 里的
`processor_class: Wav2Vec2ProcessorWithLM` 必须删掉（改过的那份入库在 `scripts/align-model/`），
否则 transformers.js 会连着去加载一个这个仓库根本没有的 tokenizer，
报错是 `Unexpected token '<', "<!doctype "... is not valid JSON` —— 和「权重没放好」长得一模一样。

**验到哪一步了**：typecheck + **595 个前端测试**（server 那套 66 个，新增 14 条权重站的）
+ build 全绿；桌面浏览器上真的对了一课（数字在上面）。**服务器那一半一次都没跑过** ——
要先把 compose、两个 json 和那份 q4 送上去，见 `deploy/README.md` §4b（已按新的改写）。
iOS 那一半也还没出包（要一个 0.5.0 的壳才验得到「包变小了」和「课程页上没有那个按钮」）。

## 现状（2026-09-20，热更）

**iOS 原生壳有热更新了（2026-09-20，SPEC §0 变更 41 + 新增 §7.12 + §12.12）。**
用户的原话是「为何网页上的版本更新之后，ios 的版本没有更新呢？我需要重新发布 app 吗？
我希望更新尽量走热更新」。**前半句的答案是「设计如此」，而代价已经攒了两周半**：
`webDir: 'dist'` 把前端冻在出包那一刻，SW 又在原生构建里被特意关掉（变更 30），
所以 ios-v0.3.0（2026-09-03）之后的**变更 36~40 在 iPhone 上一个都没有** ——
界面重做、学习记录与分享图、译文与笔记、人耳确认、查词。SPEC 风险表里那条
「不做 OTA 热更」因此作废，已改写。

- `@capgo/capacitor-updater`，**手动模式**：启动时 GET `/ota/manifest.json`，
  判断在 `src/platform/nativeUpdate.ts` 的 `decideUpdate`（纯函数，10 个测试）。
  不走插件自带的 autoUpdate —— 那要 POST 到端点，而 wrangler.jsonc 是纯静态部署、
  没有 Worker 脚本。
- **后台下载，下次冷启动生效**（`next()` 不是 `set()`）。界面上不给「现在就更新」的按钮：
  立刻换 bundle 是整个 WebView 重载，会清掉只活在 React state 里的听写答案。
- **版本用 commit 短 sha 相等判断**，不比 semver 大小 —— 比大小会让回滚被客户端拒绝。
  `minNative` 门槛挡住「用到新 Swift 方法的 JS 推给旧壳」。
- **最费劲的一处是 `/models/` 与 `/dict/`**：热更把 web 根整个切走，那两个绝对路径会全 404。
  选的是**符号链接**（`AppDelegate.linkBundledAssets()`，每次启动重建），
  因为它让 web 代码一行不动、Range 行为逐字节一致 —— 而随包权重正是靠 Range 分块读的。
  三条路的取舍见 §7.12 那张表。
- `npm run build:ota` → `ota-staging/`；deploy 流水线**打包在 web 构建之前、搬运在之后**。
  包里排除 models/dict，保留 share/：**33 个文件 / 10.2 MiB**。
- 设置页最后多了一块「版本」（§12.12）：壳的版本和前端的版本分开说，
  下好了待生效时多一行提示。接了热更之后这两个数不再是同一个。

**验到哪一步了**：typecheck + **589 个测试** + build 全绿；`npx cap sync ios` 确认插件进了
`packageClassList`；热更包内容逐项验过（index.html 在根、无 models/dict/sw.js、share 七张都在）；
设置页在浏览器里走过桌面/手机 × 浅色/深色。**Swift 那半截和整条真实链路一次都没跑过** ——
它要先出一个 0.4.0 的壳才验得了，见下一步 0g。

**Android 这次不做**：`WebViewAssetLoader` 有 canonical path 检查，很可能拒绝指向 app 目录外的
符号链接，要另想寻址方式。Android 照旧发包。

**已上线并发布（2026-09-21）**：`38ae39a` 推 main，CI 绿 → deploy 全绿（含新加的
「build ota bundle」「stage ota bundle into dist」两步）。**发布链路已从公网实测**：
拉 `/ota/manifest.json` 拿到 `buildId: 38ae39a`，按里面的 url 下 zip，sha256 逐位相符。
`ios-v0.4.0` 出包上传成功（构建号 9，IPA 430MB，5m50s，run 35568197225）——
**第一个带热更客户端的壳**，顺带把积压的变更 36~40 一起带上手机。
**从 0.4.0 之后**纯前端改动才是自动的。

**一个已知的小代价**：`buildId` 取 commit 短 sha，所以**纯文档提交也会产出一个新 buildId**，
手机会为此多下一次 10MB。不值得现在修（每次 10MB、一天最多几次），真嫌烦的话
把 `buildId` 换成 zip 内容的哈希即可 —— `decideUpdate` 是相等判断，换了也不用改。

---

## 现状（2026-09-20）

**生词本里能查词了（2026-09-20，SPEC §0 变更 40 + 新增 FR-9.5 ~ FR-9.10 + §12.11）。**
用户的原话是「生词本里，加一个查词的功能，我现在的德语课很多时候要查生词。
谷歌翻译的解释很多时候不太详细」。**这是一个此前没有入口的来源** —— 课上的词
不来自这里的任何一课，也不在预置词库的名次里；而内置词典（FR-16）在这个应用里一直
只在幕后跑（标记生词时 prefill、复习卡背），没有任何地方能让人主动查一个词。

- `src/dict/online.ts` 重写成**一次解析、两个视图**：`parseOnlineEntry` 给查词面板
  （多出**例句 / 同义词 / 反义词 / 词源 / 变形**），`parseExtract` 仍旧给 prefill 那条路。
  变形那一项（`Präteritum: wog ab/wägte ab, Partizip II: abgewogen, abgewägt`）是
  **内置词典完全没有的一类**，而 C1 上它往往比释义更要紧。
- `src/dict/view.ts`（纯函数 + 7 个测试）：两个来源合成一个答案 —— 义项整份取内置那边
  （它带中译、字段整齐），例句/同义词/词源这些**记录级**的东西才合并，逐义项的 IPA 与变形
  **按词性**对上去。例句去重要**去掉引号**：构建脚本洗过 `„…“`、在线这份没洗。
- `src/pages/vocab/DictLookup.tsx`：生词本页顶部的一块。输入框 + ä ö ü ß 四个键 +
  一张结果卡（词头带冠词 / IPA / 念一遍 / 各义项 / 例句 / 近义反义 / 词源 / 来源行 /
  加入生词本）。**在线那一趟拿词头去问**，不拿用户输入的词形。
- **「加入生词本」建的是没有课的词条**（`VocabEntry.lookup`，`createFromLookup`）。
  `cardAudioStatus` 的 `preset-word` 因此改名 **`word-only`**（`isWordCard`）：
  预置卡与查词卡共用「孤立词发音」那一档 —— 少了这个标记，这张卡在复习页会被送去
  「这一课重新对齐」，而那一课从来不存在。
- 设置里那个开关从「内置词典查不到时联网查」改成「**联网查**」：它现在管两处。

**验到哪一步了**：typecheck + **578 个测试** + build 全绿；浏览器实测走完整条动线
（查变形词 `abgewogen` → 词头 `abwägen` + 变形 + 例句 + 近义 + 词源、查名词 `Zuversicht`、
念一遍出真人录音、查不到的词、加入生词本 → 列表里写「查词添加」、再查另一个变形 → 提示已有、
**复习一轮**卡面卡背都对）× 桌面/手机 × 浅色/深色。§10 新加的那一段里
剩**真断网**、**跨设备**两条，见那里。

---

## 现状（2026-09-16）

**低置信句可以人耳确认（2026-09-16，SPEC §0 变更 39 + 新增 FR-15.19 + §12.10）。**
用户的原话是「我能理解句子编号的？，我希望我可以自己在听完之后可以对其进行确认，然后消除？
因为大部分时候都是正确的」。**这是 FR-15.3 的一个缺口**：那一条要求把低置信句数摆在课程页
头部，却没给这条提示任何**退出条件** —— 而低置信的常见原因（台标音乐、播音员交替、
英语借词、被丢掉的数字）大多不影响句边界，所以「听完发现都对」是常态，头部那行字却一个字
都不会变。一条听完也消不掉的提示，教会人的只有忽略提示（§12.3 的反面）。

- `Sentence.timingChecked`（标注层，跟着 `Lesson` 走 —— 备份、同步、合并**一行没改**）；
  `flaggedByConfidence()`（阈值判定，含已确认）与 `reviewQueue()`（只报未确认的）分开，
  `toggleTimingChecked()` 纯函数落库在组件里。
- 通听里带 `?` 的行号变成按钮：点一下确认、点第二下撤销；展开文本时多一条写明这件事的
  `Hint`（手机上没有 hover，`title` 等于不存在）。
- **不复用 `timingSource: 'manual'`**：那个值会让重新对齐跳过这一句（FR-15.5），
  而确认的是「机器算的**这一版**边界我听过」—— 所以 `applyTimings` 写新时间戳时清掉它，
  重切句（`resegment`）则跟着时间戳一起搬。
- **确认不参与阈值计算**，否则剩下几句的判定会跟着人的操作漂。
- 确认过的行号与从没被标出来的行号**长得一模一样**（不画勾）：消掉 `?` 换上一个永久的勾
  等于没消；可撤销靠热区还在，不靠画出来。
- 顺带把通听每一行改成 **flex 两列**（行号 / 文本）：触屏上 `button` 那条 44px 最小热区
  会撑高内联行盒、把句子第一行推下去半行。连带收益是**悬挂缩进** ——
  折行的文本与译文都对齐在编号之后（译文那个写死的 `pl-6` 也因此去掉了）。

**验到哪一步了**：typecheck + **560 个测试** + build 全绿；浏览器实测走完整条
（确认 → 计数减一 → 全部确认后头部与提示都静默 → 刷新仍静默 → 再点一下撤销）×
桌面/手机 × 浅色/深色，含「44px 热区没把句子第一行推下去」那一条。§10 新加的七条里
只有真机那一类没做（这个功能没有手机独有的路径，所以没单列）。

## 现状（2026-09-15）

**译文与笔记（2026-09-15，SPEC §0 变更 38 + 新增 FR-19 / FR-20 + §12.9）。** 用户的原话是
「我会用 Claude 对文章进行翻译」「我会自己写一些要点」。**应用自己不翻译** ——
它只负责把在外面译好的一整篇中文切回一句一句。

- `src/lesson/translation.ts`（纯函数 + 16 个测试）：复制走的提示词与编号原文、
  解析（**用编号当锚，不按行数猜**）、写回、覆盖率。没有编号的行**永远开不了新条目**：
  宁可少认几句让人看见「48 句认到 46 句」，也不能靠位置猜 —— 错位的译文比没有译文更糟。
- 课程页「⋯」里新增**译文**页（复制 → 粘回 → **保存前一屏原文/译文对照**）；
  通听与跟读上一个共用的「显示中文」开关（`Settings.showTranslation`，默认关，
  没贴过译文的课上不出现）。
- `Sentence.translation` / `Lesson.notes` 进标注层，跟着 `Lesson` 走 ——
  备份、同步、合并**一行没改**。结构编辑三处都补了（合并接起来、拆分留给左半句、
  重切按文本搬），漏一处就是静默错位。两者都**不进 `ShareablePackage`**。
- **笔记**是通听页底部的折叠块（有内容默认展开，摘要带行数），停手 800ms / 失焦 / 卸载各存一次
  —— 切 tab 就是卸载，而「切走一下回来发现刚写的没了」比多一个保存按钮糟得多。

**验到哪一步了**：typecheck + **553 个测试** + build 全绿；浏览器实测走完整条动线
（复制出的文本、贴一份带前言/加粗/缺一条/多一条的译文、对照预览、保存、通听与跟读里的中文、
笔记切走再回来）× 桌面/手机 × 浅色/深色。**剩两条**：剪贴板主路径（自动化环境写不了剪贴板，
验的是「自己选中复制」那条退路）和跨设备，见 §10 新加的那一段。

**顺手修了一个埋着的地雷**：`Disclosure` 的展开状态原来直接钉在 prop 上，
而它现在要出现在**每帧重渲染**的通听页里 —— 那样用户折叠之后下一帧就会被强行弹开。


**学习记录与分享图（2026-09-15，SPEC §0 变更 37 + 新增 FR-18 + §12.8）。** 用户要的是
「一张精美图片，上面一句德语鸡汤，下面我的名字、日期、学了多久、连续几天、一共几天」。
做之前才发现**这个应用从来不知道你昨天学没学** —— FSRS 只留每张卡最后一次复习的时刻，
课程只有 `createdAt`。所以一半以上的工作量在**先把学习行为记下来**：

- `src/study/`：计时器（练习界面 + 页面可见 + 最近一分钟有动静或音频在放，三条同时成立
  才每 5 秒跳一次表）、逐日记录（`meta.studyLog`，形状是**日期 → 设备 → 秒数**，
  合并逐格取 max —— 这样反复同步既不会翻倍也不会吞掉另一台设备的时长）、派生统计。
- `src/share/`：24 条公有领域德语谚语/引文（带自译中文）、6 张 CC0 底图
  （`public/share/photos/`，1.2MB，`CREDITS.md` 记着作者与出处）、canvas 画的 1080×1350 卡片、
  保存/分享（原生走 Filesystem + Share，浏览器走 `navigator.share` 或下载）。
- 「⋯」抽屉里新增**记录**页；`study` 进了同步文档与备份文件（可选字段 `studyLog`）。

**记录从 2026-09-15 起攒，不回填** —— 旧数据里推不出「连续几天」，硬凑必然是假的。

**已推已上线**（`0af0f05` → CI 34953413989 绿 → deploy 34953507189 绿，2026-09-15）。
**同一次 push 顺带把 2026-09-07 就合进 `main`、却一直没发出去的界面重做也送上线了** ——
下面那一段「现状（2026-09-05，界面重做）」里「线上仍是旧界面」那句从此作废。

**验到哪一步了**：typecheck + **537 个测试** + build 全绿；浏览器实测三件事 ——
复习页待 40 秒本机那一格记 45 秒、离开复习页再待 20 秒数字不动、「保存 / 分享」
导出 1080×1350 的 PNG；桌面/手机 × 浅色/深色四种组合看过。
**剩手机真机那几条**（锁屏五分钟不涨、系统分享面板、跨设备相加），见 §10 新加的那一段。

**通听的「跟随播放」修了一处（2026-09-15，SPEC FR-5.3 已改）。** 起因是一张截图 +
「文本要自动滚动」—— 而自动滚动从变更 24 起就有，`follow` 的默认值也一直是 `true`。
真正的毛病是 `onWheel` / `onTouchMove` 直接 `setFollow(false)`：展开文本后随手滚一下
（几乎必然发生）跟随就被**永久**关掉，于是用户看到的永远是「不跟随」三个字和一段不动的文本，
看起来像是这个功能根本没做。改成**手动滚动只暂停 5 秒**（`RESUME_MS`，停手后清掉
`scrolled.current` 再滚回当前句），彻底关掉仍然只有那个按钮能做。同时当前句的落点
从框高的 1/2 改成 **1/3** —— 下方要留出还没读到的几句，上方一两句就够。
`src/pages/lesson/ListenTab.tsx` 一个文件；typecheck + 537 个测试绿。

---

## 现状（2026-09-05，界面重做）

**界面整体重做（2026-09-05，SPEC §0 变更 36 + 新增 §12「界面规范」）—— 2026-09-07 已合进 `main`
（合并提交 `3b4149c`，分支 `ui/redesign-section-12` 已删）。** 合并后 typecheck + 478 个测试 + build
复跑全绿。**2026-09-15 已随 FR-18 那次 push 一起上线**（CI + deploy 都绿）。

产出的主体是 **SPEC.md §12**：这份文档到 §11 为止一千六百行把功能定得很死（FR-1~17），
形状一次没定过 —— 于是每个页面各自发明一套，且只能借用文档里唯一存在的词汇，
`存储（§2.2 / FR-11.16）`、`L1 全自动` 这类字面因此直接进了产品界面。改代码只是把 §12 落一遍：
导航六个平级 tab → 三个活动 + ⋯ 抽屉（手机在底部）；新增底部单浮层契约
（`src/components/bottomLayer.ts`）；状态语言三档 静默 / `Note` / `Banner`；排版四级 +
颜色令牌 + 深色（`dark:` 在改造前的 `src` 里出现 0 次）；一课的落地页从「切句」改成「通听」。
顺手修掉三个真 bug：对齐一跑进度条就压住播放键、全新安装第一天就亮「距上次导出超过 90 天」、
导航项是 `<a>` 拿不到 44px 规则（手机上热区只有约 30px）。

**验到哪一步了**：typecheck + 478 个测试 + build 全绿，桌面与手机 × 浅色与深色在浏览器里走过一遍，
§10「界面」那五条验收打勾四条。**剩 iPhone 真机的安全区那一条**（底部标签栏与音频条各自让出
home indicator、顶栏让出刘海、深色下状态栏是深的）—— 见「下一步建议」第 0c 条。

**§9 实现顺序的 11 步已全部完成**，`main` 上可跑、可构建、**478 个前端测试通过**
（`server/` 那套 52 个另算，见下面同步那一节；其中 `src/pages/ReviewPage.test.tsx` 是第一个**组件**测试 ——
复习页的时序行为只有把纯函数装起来才测得到）。

已经用真实的 Alltagsdeutsch 期次 `45334084` 端到端跑通：RSS → 页面 `__APOLLO_STATE__` → mp3 下载 →
切句（45 句；当时开头 3 句被当 teaser 自动排掉了，现在不排了，见变更 24）→ 14 条 Glossar 候选词**全部落位正确** → 打点 → 接受候选词 →
听写判级 → FSRS 复习。详见 SPEC.md 附录 A.6。

**FR-15 自动对齐已完成**（在 §9 的 11 步之外新增，见 SPEC.md §0 第 17 条、FR-15、§7.9）：
浏览器内跑 CTC 强制对齐（MMS-FA + 自己实现的 Viterbi），从「已切好的句子 + 音频」直接算出
句级与词级时间戳。已在同一期真实素材上验过：45 秒写入 38 句 + 744 个词级时间戳，
标出 5 句待校对，词级边界抽查合理。代码在 `src/align/`。

**iOS 的自动对齐已经改走原生插件**（2026-09-03，SPEC §0 变更 31 / FR-15.14）。
`native-plugins/align-native`（Swift + ONNX Runtime 的 SPM 分发，读包里那份 `model_q4.onnx`）
把 emissions 整块搬出 WebView，**viterbi 与下游一行没动** —— 那道缝（`emissionMatrix.ts`）
是变更 21 时切的，这次证明了它切在对的地方。**包已出并上传：0.2.1 构建号 7，IPA 408MB**
（run 33750498587；+8MB 全是 ORT 的 xcframework，权重一份没多）。
**但真机一次都没跑过** —— Swift 那半截在这台 Windows 机器上编不了，
落地标准是「在 iPhone 13 上真的跑完一课」，typecheck 绿不算。见「下一步建议」第 0 条。

**FR-16 内置词典 + FR-17 预置词库 + FR-13.12 回填最近几期已完成**（2026-09-02，见 SPEC.md
§0 第 23 条、FR-16、FR-17、§7.11）。解决的是「笔记还不多时怎么练」：

- **词典随包带**，`npm run build:dict` 从 WikDict + 一份口语词频表编译出 15.3 万词条 +
  30 万条词形索引 + 1.7 万预置词，产物 35 MB、切 256 个桶（查一个词只解析约 105KB）。
  **产物入库、1GB 的源文件不入库** —— 换来 CI 完全不必碰它。
- 顺手修掉了 FR-9.3 记着的那条已知局限：去重从 surface 升级到**词元**，
  `laufen` 与 `gelaufen` 现在归到同一个键。这是词形索引的免费副产品。
- **预置卡的正面只有声音、没有文字**（FR-17.7）。给了文字它就变成「看词回忆意思」，
  而这个应用存在的理由是听觉识别。声音优先用 Wiktionary 的真人录音（加词时就预取，
  所以断网也能复习），没有的退 TTS，卡面标出是哪一种。
- **档位是口语词频名次，不是 CEFR 等级**，界面上也这么写。原因见 FR-17.2：
  官方 Goethe 词表只有 A1/A2/B1 且有版权，B2/C1/C2 压根没有官方表。
- **FR-13.12 回填**是另一半：预置词库的孤立词发音练不到连读，旧刊的 Glossar 才是
  带真语料的那一批。R-3 没有被绕过 —— 它禁的是「一键导入全部 100 期」那个形状，
  实现里有硬上限、没有「全部」、只取已拉到的 RSS 列表、复用 `politely()`。

**复习形式改成「听音四选一」，评分改成系统自动算**（2026-09-02，SPEC §0 变更 29，
FR-10.2/10.3/10.4 重写 + 新增 FR-10.8–10.11、FR-16.8/16.9、FR-17.3/17.4 重写）。

上一批（变更 23）落地时复习页是照 FR-10 原文做的 **Anki 形状**（显示答案 + 四档自评）——
而「百词斩形式」这个结论那次**只在对话里说过、没写进 SPEC**，所以实现时按原文做了。
这次先改文档再改代码。改掉的三件事：

- **正面只有声音**，进页面自动播一次、点大圆按钮重播，不给任何文字（连课程卡的挖空原句也不给：
  给了文字就能靠语法猜出来，那就不是听力题了）。2×2 选项 + 一个「没听清 / 不认识」。
  答对 → 顶部闪 600ms 的 `✓ der Vorhang` → 自动下一张；答错才亮正确项和完整卡背。
- **评分不问用户**（`src/srs/grade.ts`）：选错/不认识 → Again，答对 ≥4s → Hard，
  <4s → Good，<1.5s 且这张卡历史无错 → Easy。「忘了/勉强/记得/太简单」连同间隔预览一起删了。
  **Easy 那个「历史无错」的条件不能去掉** —— 四选一有 25% 瞎猜命中率，只看用时的话
  乱点正好点对会被判成 Easy，一次幸运能把不会的卡推到一个月以后。
- **预置词库从「加 10/25/50 个词」改成「报名一整档」**，卡片由复习页按 `newPerDay` 每天惰性激活。
  报名只写一条设置（`enrolledBands`），**不新增 store、不动 DB_VERSION** ——
  「哪些还没学」= 档内词表减去生词本已有词，是纯派生量。整档 3000 个词一次建卡要 60 次
  MediaWiki 往返和 100MB 以上录音，那条路走不通。**与变更 28（设置也同步）合起来白拿一个好处**：
  报名跟着设置同步，桌面上报名、手机上就开始发卡，而「发到第几个」不需要同步 ——
  它从生词本推出来，而生词本本来就同步。

配套的两处词典改动（都在 `npm run build:dict` 里，**产物已重新生成入库，35.0 → 38.6 MiB**）：

- **FR-16.9 例句**：WikDict 没有例句表，所以这是这条管线第一次需要**第五个数据源** ——
  de.wiktionary 正文的 `{{Beispiele}}` 段，走 API 按 50 个标题一批、347 批抓完，
  原始 wikitext 缓存在 `.cache/dict/beispiele.json`（缓存清洗前的原文，调规则不必重抓）。
  实测 **16499/17313（95%）** 有可用例句。选句规则：无 `<ref>` 引注优先、15–140 字符、取最短两条。
- **FR-16.8 干扰项**：每个牌组词预存最多 6 个 IPA 最近邻。**第一版按「档内找」，实测被推翻**：
  第 4 档只有 30% 的词凑得到三个邻居 —— 3000 词里没有足够多的最小音对。放开到全牌组
  1.7 万词之后 **81% 够三个**（`heilen → heulen / teilen / weilen / eilen`）。
  那条限制本来就立不住：用户不知道选项属于哪一档，辨形题问的是「你听到的是哪个词」。

**验到哪一步了**：447 个前端测试 + typecheck + `npm run build` 全过，**并且在桌面浏览器上
按 430px 宽点过一轮真流程** —— 报名第 4 档（3000 词）只建 10 张卡、10 个全拿到真人录音、
面板显示「已激活 10 / 剩余 2990」；四个选项是 `vorab / Vorrang / Vorhang / Vorgang`
这类真音近词；答对闪「✓ der Vorhang 下次 6 分钟后」并自动跳；答错（`liegen` / `wiegen` 那对）
亮出正确项 + IPA + 释义 + 两条例句。**剩下的是 iPhone 真机**（见「下一步建议」第 0 条）。

那一轮还顺手改掉两处只有真尺寸下才看得见的问题（提交 `fix: 复习页选项列数按题型分…`）：
430px 下四个选项本来是单列（`sm:grid-cols-2` 要 640px 才生效），现在辨形题恒 2×2、
辨义题窄屏单列；卡面改成撑满剩余高度并居中，否则播放键和底部选项之间空掉半屏。

**这批与同步那批是同一天并行的两摊活**，合并时撞了三处：变更表编号（我这条改成 29）、
`ReviewPage.tsx` 里的 `backupVocabNow` → `syncVocabNow`、以及 `Settings` 接口两边各加了字段。
**在 worktree 里做界面改动验不了 UI**：`preview_start` 的 cwd 钉在 `D:\deutsch`，
在那里起出来的 dev server 服务的是共享检出的代码（判据：去取一个只存在于 worktree 的文件，
回来的是 SPA fallback 的 HTML）。所以那阶段的替代手段是组件测试（`ReviewPage.test.tsx` 就是为此写的），
真界面等合并回 `main` 之后才验得了。

**词典/预置词库那三样都只在浏览器里验过**（真人录音抽 10 个词 10/10 命中、178KB；查词/词形还原/
署名那一屏都看过），**没有在 iPhone 上验过**。iOS 上有一处已知会退化：覆盖率最高的
`De-*.ogg` 是 Ogg Vorbis，WKWebView 放不了 —— 代码会挑 Lingua Libre 的 wav，
挑不到就退 TTS。真机上真实的真人音比例只能等设备回答。

**FR-13.7 那条自动排除被实测推翻，通听改成逐词高亮（2026-09-02，SPEC §0 变更 24）**：

- **DW 的标题和导语在音频里是被念出来的**（Alltagsdeutsch 45334084，一字不差），
  而附录 A.3 一直写着「音频里不朗读」，于是导入时开头 3 句被整块排掉。
  这不是「少练三句」的问题：排除句不进对齐目标（`target.ts` 的第一条硬规则），
  开头几十秒真实存在的声音因此无处安放，只能被挤给第一句正文。
  **自动导入现在一句都不排除**，`teaserBlock` 判定与 `excludeTeaserBlock` 已删除。
  「切句」页多了「开头 N 句 → 恢复 / 排除」这个批量控件 —— **旧课里已经排掉的那几句，
  点一次「恢复」再点页头「重新对齐」就回来了**（这是这个控件存在的主要理由）。
- **通听改成逐词高亮**（`src/lesson/karaoke.ts` + 重写的 `ListenTab.tsx`）：
  当前句浅底、正在读的那个词加深底，当前行自动滚到视野中间（滚一下滚轮就停止跟随），
  **点任意一个词从那里开始播**。数据是 FR-15 一直在算、却只有听写在用的那份词级时间戳
  （一课约 800 条），零额外代价。
  两个坑记着：① 渲染单位必须用 `tokens.ts` 的 tokenize（与挖空同坐标），
  不能用词级时间戳自己那套罗马化词 —— 否则 `Work-and-Travel` 碎成三块各自闪、
  数字和引号整个消失；② FR-5.2 的「不做伪同步」没有让步：只有**两侧都有真时间戳**时
  才把中间被罗马化丢掉的数字补上（`bridged`），一句词级时间戳全缺时宁可只做句级高亮。

**对齐的 emissions 挪到了服务器上（2026-09-03，SPEC §0 变更 35、FR-15.17/15.18）**：

起因是一句「wnet 的 VPS 根本没人用，资源放着也是浪费」。那台机器（4 vCPU EPYC、
7.9G 里当时剩 5.2G、盘剩 31G）正好补上手机端最后一个缺口：本地十几分钟且必须一直亮屏
（变更 33 判定「跑不完」），服务器一两分钟，**而且这期间手机可以锁屏、可以退出 App**。

- **只加了一个 provider 就完事** —— 那道缝（FR-15.13）在变更 21 切的，这是它第三次被兑现
  （wasm → 原生 → 远端）。viterbi、applyTimings、落库、同步**一行没动**。
- **上行 mp3、下行 3MB 矩阵，文稿不上去**。CTC 前向只吃波形，所以文本没有理由离开设备；
  §3.1.1 里那句「没有任何一段音频或文稿离开过设备」**已经按实改掉了**（它现在只对文稿成立）。
- **任务 + 轮询，不是一次长请求**：`POST /v1/align/jobs`（上传）→ 轮询 → `GET .../result`。
  一课一两分钟，而手机握不住这么久的连接 —— 同步请求会把变更 33 那面墙原样搬到服务器上。
- **手机上的自动对齐因此重新打开**（`useAlignStore` 的闸门现在问「有没有服务器」而不是
  「是不是手机」）；课程页给两个显式按钮，本地那条降级成 ghost + 写明代价。
- **一律不压矩阵**（不 float16、不 gzip），服务器的 dtype 也必须与原生那一档相同（都是 q4）——
  同一课在哪台机器上算都要得到同一份时间戳。分块那套数学在服务端是**第三份拷贝**
  （`server/src/align/frames.ts`），它的单测期望值与前端那份是同一组，充当两份之间的契约。

**本机实测到的两个数**（Windows、3 线程、真权重）：`onnxruntime-node` 1.29 的 CPU EP
**认 `MatMulNBits`**（241MB 权重加载 0.6 秒），20 秒一块推理 **3.4 秒**、峰值 RSS **584MB**。
所以 compose 的 `mem_limit` 从 256m 提到了 **2g** —— 不提的话第一次对齐会 OOM 掉容器，
**连同步一起带走**。

**客户端那条链路在浏览器里端到端验过**：拿一个假对齐服务器（40 行 Node）喂真协议 ——
上传 5.73MiB → 轮询两轮 → 下发 2.22MiB 矩阵 → Worker 跑 viterbi → 「已对齐 2/2 句」，3 秒。

**服务端那一半已经在那台 VPS 上跑过了**（2026-09-03，0.3.0 部署之后，用 ffmpeg 现场生成的
25 秒正弦波当输入 —— 探针要的三个数与音频内容无关）：

| | 实测 |
|---|---|
| ffmpeg | 7.1.5（镜像里有），解码 0.1~0.2 秒 |
| `MatMulNBits` | **认**（没有「算子找不到」）—— iOS 那份 ORT 分发仍要真机自己回答 |
| 权重加载 | 首次 **60.7 秒**（含从 HF 下 241MB）；之后 **0.6 秒**（落在 `/data/models`，镜像重建不碰它）|
| 一块 20 秒音频 | **4.1~4.5 秒** → 一课 27 块约 **2 分钟**，实时倍率 4.5× |
| 容器内存 | 空载 104MiB / 2GiB |
| 可复现性 | 同一份输入两次跑出**逐位相同**的指纹（前 5 个 log-prob 与全局均值都一样）|

**部署时手工做了两步**（CI 只搬 src/package.json/lock/Dockerfile，compose 与 .env 不动）：
`scp server/docker-compose.yml`（`mem_limit` 2g + `cpus` 3.0，已确认容器上是
`mem=2147483648 nanocpus=3000000000`），权重则让服务器自己从 HF 取了（那 60 秒）。

**还没验的只剩一件**：真手机（登录状态）导入一课 → 自动送到服务器 → 锁屏 → 回来看时间戳。
那一步要真机 + 真账号，只有他能做。

**同步补上了「拉」那一半（2026-09-03，SPEC §0 变更 34、FR-11.19 + FR-3.5a）—— 在此之前跨设备只自动了一半**：

起因是一句提问：「现在有了后端，跨设备是自动同步的吗？我在电脑上把文本和音频同步了，
但是手机上还得自己跑一遍。」查下来答案是**只有推是自动的**：拉从头到尾只有设置页那个
「从服务器恢复」按钮（FR-11.13），而它是照「恢复到空设备」设计的，没人会为了看今天新导的一课去按它。

**真正的代价不是多点一次按钮，是它把人推上一条错路**：手机上看不到那一课，
最顺手的下一步是在手机上重新导入 —— `importFromDw` 走 `generateId()`，于是同一期变成两门课，
第二门标注全空，还要在手机上重跑十几分钟对齐（正是变更 33 刚判定「跑不完」的那件事）。

做了三件事：

- **`src/sync/pull.ts`：自动增量拉取**。启动、回前台、网络恢复各拉一次（`syncNow()` = 先推排队的再拉），
  按 `GET /v1/docs` 列表里的 `version` 对本地 `syncVersions`，只拉变过的文档 ——
  **常态下这条路只有一次列表请求**。60s 节流（onResume 会连着来几次）+ 单飞。
  合并走 §2.4 同一套规则，**本地赢的部分会回推**（否则手机上刚复习完的 FSRS 状态
  会被一次启动拉取悄悄按回去）。**刻意不跟随删除** —— 判断错一次就是静默删标注。
- **FR-3.5a：打开一课就自动补齐素材**（DW 来源）。缺的只有音频和原文这两样按设计不同步的东西，
  让人读一句「去『来源』页补齐」再自己找过去，就是把纯机械的步骤留给人做。
  同一课**本次会话只自动试一次**（模块级 Set，失败不重试；切一次 tab 就是一次 6~10MB）。
  连带修掉一个陈旧读：`useLessonAudio` 原来只在挂载时读一次 Blob，素材是在这一页补齐的话
  播放条会一直停在「素材未下载」—— `hasAudio` 现在进依赖数组。
- **去掉「补齐之后无条件重新对齐」**（`hasTimings()`）。那行代码的理由是「补齐等于刚下载完，
  时间戳可能作废」，而同步落地之后常态恰恰相反：标注层里那份时间戳正是桌面刚算出来的。
  重算一遍在 iOS 原生壳上被变更 33 的闸门挡住（看起来「没反应」），
  **在 web 和 Android 手机上会真跑十几分钟再把同样的值盖回去**。
  会让时间戳作废的只有音频变了，而那件事有 `audioDuration` 可比。

状态条与设置页因此把「上次推送」和「上次拉取」分成两行（FR-11.9 修订）：推通了不代表拉通了，
而拉悄悄停掉的症状是「另一台设备上的东西一直不来」，和「我最近没在别的设备上练」长得一样。

**浏览器里验过一遍**（造一门缺素材的 DW 课程 → 打开 → 横幅「正在补齐素材…」→ 5 秒后
音频 5.7MB 到位、播放条自己变成可播 6:16、**没有**重跑对齐）。拉那条路的真机/真账号验证
仍然压在「真的用 Google 登录跑一遍」后面，见下面同步那一节。

**分层标准换了（2026-09-02，SPEC §0 变更 27）—— 这条会影响以后每一个新字段的归属**：

> 缓存层**只装音频和原始文稿**，其余一切都备份且同步。那两样的凭据不是「可重建」，
> 而是**标注层记着它们的下载地址**（`Lesson.audioSrc` = mp3 直链，`source.sourceUrl` = 文稿页面）。
> 新字段该放哪层，先答一个事实问题：**它有地址吗？** 答不出就放标注层。

据此这一批搬了两样：`LessonCache.glossary` → `Lesson.glossary`（候选词「还没接受哪些」是跨设备的待办），
以及新增 `Lesson.audioSrc`。FR-3.5 补齐时优先用页面上新抓到的直链（CDN 地址会变），
页面里没有就退到记着的那个（页面也可能改版下架）。
**候选词这一份写了迁移**（`src/db/migrate.ts`，启动读库时跑一次，幂等，搬完排一次同步）——
词级时间戳那次没写是因为「重新对齐」能拿回来，而候选词只有「重抓页面」一条路，
且它带着待办，不该静默消失。手工重切那条路上也要搬（`carryGlossary`）。
**设置也同步了**（2026-09-02，SPEC §0 变更 28）：新增 `settings` 文档 + `Settings.updatedAt` +
`mergeSettings`（整体 last-write-wins）+ 5s 去抖 + 恢复时一并拉回。原来「跟机器走」那个理由站不住 ——
真正跟机器走的是「这台设备跑不动对齐」，而它记在黑匣子（localStorage）里、与 `autoAlignOnImport` 无关，
所以手机把那个开关同步成 true 也照样自己停（FR-15.10）。
**顺带补了一个缺口**：备份文件里一直带着 `settings`，而导入这条路从来不读它 ——
「换设备恢复」以前会把设置默默留在默认值上。

**词级时间戳搬进了标注层（2026-09-02，SPEC §0 变更 26）**：
`LessonCache.wordTimings` → `Sentence.words`（新类型 `WordSpan`）。
起因是用户那句「所有用户的信息都是需要备份的」—— 原来的分层判断用的是
「有音频+文稿就能重算」，但那句话漏了「**在哪台设备上**」：手机跑不动对齐模型，
在那台设备上它压根重建不出来，于是「桌面预处理、手机学习」只兑现了一半。
备份/同步/合并/导出**一行没改**（它跟着 `Lesson` 走，同步又是按课分片的）。
存进句子而不是整课一个数组，是因为它和 `blanks` 同为句内 offset ——
合并、拆分、重新切句三处都要跟着平移/分家/搬迁，**漏一处就是静默错位**（三处都补了）。
老库里那份 `wordTimings` 不迁移：缓存层可以随时丢，而开头三句不再排除之后
每一课本来就要重新对齐一次，重跑就写满了。

**手工标注已整节删除（2026-09-01，SPEC §0 变更 20）**：「标注」tab 连同 `Enter` 打点、
±0.1s 微调、清除时间戳一起去掉了。时间戳只有一个来源 —— 音频一到位（DW 导入、手动导入、
补齐素材、重新绑定音频）就排队自动对齐，**不阻塞导入**，进度常驻在应用底部那条
`AlignBar`（`src/components/AlignBar.tsx` + `src/state/useAlignStore.ts`）。
课程页头部的 `AlignStatus` 显示「已对齐 N / M 句 + 低置信句数」并给「重新对齐」。
FR-4 只剩 FR-4.4（区间推断规则）。老数据里手打过的点不会被重跑覆盖。

**同一批做的还有 FR-15.9~15.12（都是为了让手机上跑得完）**：
`src/align/journal.ts` 黑匣子（每阶段同步写 localStorage，进程被系统杀掉后下次启动能自报现场）、
后端阶梯 + 崩溃降档（`webgpu/q4f16` → `wasm/q4`，两档都崩过就停掉自动对齐）、
`src/align/rangedFetch.ts` 按 8MB 分片取随包权重并关掉 transformers.js 的缓存回写。
起因是一次真机事故，见下面「iOS 上那次崩溃」。

**FR-15 的两个 bug（2026-09-02 实测查出来并修掉，SPEC §0 变更 21/22）**：

- **第 2 档以前是必死的。** `wasm/int8`（302.6 MiB）在 WASM 上**加载即让进程被系统杀掉**，
  桌面 32GB 的 Chrome 也一样，JS 侧一行报错都没有 —— 也就是说「没有 WebGPU 的浏览器」
  以前根本跑不了自动对齐，不是文档写的「慢一个量级」。换成 `wasm/q4`（230.3 MiB）后跑得通
  （25 秒音频 36 秒，约 1.4× 实时）。随包权重 490.2 → **417.9 MiB**。
  连带补上降档的漏洞：Worker 被杀而主线程还活着时以前记成 `error`、不进 `crashed` 计数，
  于是 `nextPlanStep()` 压根不降档，同一档被**无限重试**。现在那一类记 `crashed`。
- **滑窗漂移。** 一课自动对齐后 31/45 句被标低置信，查下来不是阈值问题：同一份 emissions，
  滑窗跑出中位数 −2.99、整段对齐跑出 −1.17。逐窗漂移 −1052 → +684 帧，15 窗里有 3 窗喂进去的
  token 装不进窗口，最后把 904 个字符挤进 19.7 秒（45~53 字符/秒，物理上念不出来）。
  根因是窗口只有 30 秒，**比 Alltagsdeutsch 的长句（实测 437/475/625 字符）还短**，
  而窗内 token 数只能靠全局平均速率估。改成 **120 秒**，三课实测与整段对齐在「最差单句」上
  完全持平，代价只有回溯缓冲 0.7MB → 11MB（整段要 143~387MB）。

**同时切出一道缝**（FR-15.13，`src/align/emissionMatrix.ts`）：帧级 log-prob 矩阵现在是一个
自足、可序列化、不带任何运行时依赖的中间产物。`音频 → 矩阵` 那一半要 300M 权重 + GPU
（全部的时间和内存都在那儿）且**压根不看文稿**；`矩阵 + 文稿 → 时间戳` 那一半是纯 JS、几秒。
所以前一半可以整块换地方跑（原生插件 / 远端）而后一半一行不改，且远端那条路上
**德语正文一个字都不出设备**。Worker 现在收两种入口（`input: 'audio' | 'emissions'`）。

**§7.10 原生壳已接入**（Capacitor 8，iOS + Android 两套工程入库）：
壳里跑同一份 `npm run build:native` 产物，业务代码只多了一个 `src/platform/native.ts`
加一处原生代码（`AppDelegate.swift` 把 `AVAudioSession` 设成 `.playback`，顺手修掉了
「iOS 静音开关让 `<audio>` 没声音」那条老坑）。对齐权重随包带（实测 490.2 MiB）。
出包走 CI：推 `ios-v*` tag → 出 IPA 并自动上传 App Store Connect；推 `android-v*` tag → 出签名 APK。
（权重后来降到 417.9 MiB，见上面 FR-15 那两个 bug。）

**iOS 流水线已跑通并装机（2026-09-01）**：`ios-v0.1.0` → run 33515038917，5m45s 一次通过，
`UPLOAD SUCCEEDED`，build 1.0(1) 进 TestFlight 装到 iPhone。实测 IPA 381MB、装机 553.6MB。
签名材料全套是这次重签的（旧的丢了，见下面的教训），台账在本机 `D:\cloud\ios\`
（Google Drive 同步）：`apple-account.md` 是账号级值，`new-app-checklist.md` 是无 Mac 的完整流程，
`apps/deutsch.md` 是这个 App 的专属值与状态。**要动 iOS 发布先读那个目录。**
**Android 那条仍未跑过**（tag `android-v*` 一次都没推）。

**iOS 上那次崩溃（2026-09-01，已修但未在真机复验）**：TestFlight 版导入一课后自动对齐，
进度走到「加载对齐模型 181 MB / 187.6 MB」，**整个应用直接消失**（jetsam，JS 侧抓不到）。
根因是峰值内存：同一份 187.6 MiB 权重同时存在五份 —— Capacitor 的 iOS scheme handler 对非媒体
扩展名走 `Data(contentsOf:)` 整份读进原生进程、WebKit 再搬一份、transformers.js 拼一份
`Uint8Array`、**它还会把这 187MB 再抄一份进 Cache API**（一份已在安装包里的文件）、
最后 ORT 拷进 WASM/GPU。第四份正好发生在进度条走到最后那一刻。
修法见 `src/align/rangedFetch.ts` 顶部那段事故分析。
**顺带纠正一条文档断言**：WKWebView **是有 WebGPU 的**（真机诊断页显示 `webgpu / q4f16`），
SPEC §7.10 与旧 README 里「iOS 一定是单线程 WASM + int8」是猜的，猜错了。

**2026-09-02 的坏消息：那次修复不足以让 iPhone 跑得动。** 他的 iPhone 13 上黑匣子记下来的是
**两档都在「加载对齐模型」这一步被系统杀掉**（第一次 `webgpu/q4f16`、降档后 `wasm/int8`，
各约 1 秒）。而 `q4f16`（187.6 MiB）已经是这个模型**最小的一个变体**
（其余 212 / 230 / 302 / 602 / 1204 MiB），**所以降档在手机上救不了** ——
WebView 的 WebContent 进程 jetsam 线远低于原生进程，而 ORT-web 至少要 JS 堆一份 + wasm 堆/GPU 一份。
应用现在的兜底行为是对的（两档都崩过就停掉自动对齐，并说「去桌面对齐，句级时间戳会跟着备份回来」）。
真要在手机上跑，只有两条路，**那道缝（FR-15.13）就是为它们准备的**：
① 换一个小一个数量级的模型（HF 上没有现成的小体积德语 CTC ONNX，得自己 `optimum-cli` 导，
例如从 `facebook/wav2vec2-base-10k-voxpopuli-ft-de` 出发，95M 参数 q4f16 约 60 MiB）；
② 把 emissions 那一半挪出 WebView（Capacitor 插件走 onnxruntime-objc / CoreML，
原生能 `mmap` 那份 .onnx，峰值从「约 3 倍模型体积」掉到「约 1 倍」）。
**2026-09-03：走了第 ② 条**（`native-plugins/align-native`，SPEC §0 变更 31 / FR-15.14），
0.2.1 构建号 7 已上 TestFlight，**真机未验**；第 ① 条仍然摆着，见「下一步建议」第 0 / 4 条。
**服务器那条已经评估过并否掉了** —— 不是因为违反 R-1（R-1 管获取通路，mp3 仍是浏览器直连抓的），
而是 CC-BY-NC + Cloudflare Workers 跑不了（得上 VPS，那笔永久部署依赖）。
要做的话正确切法是「只上行 mp3、只下行矩阵」。
**2026-09-02 补充：这条否决理由的一半已经没了** —— 同步后端就在那台 VPS 上，
「永久部署依赖」这笔账已经付过。剩下的理由还站得住，但换了性质：现在的问题是
**上行 mp3 就等于让服务器经手音频**，那是 R-1 真正在管的东西（SPEC §3.1.1 里
「同步后端与 R-1 的关系」那段专门划了这条线：存你自己的东西可以，代你去拿内容不行；
而上行 mp3 落在两者中间，要单独论证）。加上 7.9GB 内存里 mssql 已经占了一大块，
一个 CTC 模型常驻不进去。

**一条教训**：GitHub Actions Secret 只写不可读，`funny` 那套签名数值就是这么丢的
（流水线好使、TestFlight 上过线，但值找不回来，只能整套重签）。
**流程有文档 ≠ 数值有存档**，两件事要分开保管。

**大窟窿一（2026-09-02 换了形状，不是补上了）：备份链路从来没端到端跑通过。**

原来的窟窿是「GitHub 备份没用真实 PAT 联调过」。用户 2026-09-02 说「GitHub 那套感觉还是麻烦」，
于是整条路换成**自建后端 + Google 登录**：`src/github/` 九个文件 + 配对二维码 + `jsqr`/`qrcode`
两个依赖全部删除，换成 `server/`（Node + Hono + SQLite，跑在 `wnet-server` 那台 VPS 上）
和 `src/sync/`。合并规则、离线队列、状态常驻可见、手动导出四样一字未改（SPEC §0 变更 25）。

**已经跑通的**：容器在 VPS 上起来了（`~/deutsch-sync`，`docker compose ps` 可见），
`/v1/healthz` 从 Caddy 容器里能通，拿一张伪造 ID token 去登录会被 Google 的 JWKS 挡成 401
（也就是说出网验签这条路是活的）。服务端 27 个测试、前端 21 个同步测试全绿。

**当天下午全部接通了**：DNS（灰云 A 记录）、Google 的 Web + iOS 两个客户端、白名单
`tao.wang.go@gmail.com`、Caddy 的 site block，证书已从正式 Let's Encrypt 签下。
`https://sync.gamestao.com/v1/healthz` 通、无令牌访问回 401、CORS 只放行 `d.gamestao.com`。
**前后端也都进了 CI**：push main → CI 绿 → `deploy` 发 Cloudflare、`deploy-server` 发 VPS，
两个 job 首次跑通（10s / 44s），线上主包里能 grep 到注入的客户端 ID。

**唯一还没做的是「真的用 Google 登录跑一遍」以及 §10 那串验收（含恢复演练）** ——
那需要在弹窗里输密码，只能由用户做。步骤、排错、验收清单全在 **`deploy/README.md`**。
**2026-09-03 起这一关又多担一件事**：FR-11.19 的自动增量拉取（变更 34）只在浏览器里
用假响应测过（`src/sync/pull.test.ts` 11 个），**真链路一次没走过** ——
验收的样子很具体：桌面导入并对齐一课 → 手机回前台 → 那一课自己出现且带时间戳，一个按钮都不用点。

Android 那个 OAuth 客户端**还没建**：`android-v*` 流水线一次没推过、签名 keystore
还不存在（本机也没 JDK 算 SHA-1）。等真要出 Android 包时再建，那个 ID 不进代码。

**应用壳自动更新（2026-09-02，SPEC §0 变更 30）**：起因是他说「浏览器刷新后没有谷歌登录」。
**线上两边都是最新的** —— CI 绿、`deploy` 与 `deploy-server` 两个 job 都成功、
Variables 三个构建期变量都在、线上主包里 grep 得到 Web 客户端 ID 和「用 Google 登录」、
`sync.gamestao.com/v1/healthz` 回 `{"ok":true,"users":0,"docs":0}`。
在干净会话的浏览器里打开 `#/settings`，登录按钮正常显示。**问题在客户端缓存**：
§7.6 的 `navigateFallback: '/index.html'` 让导航请求由 Service Worker 从预缓存直出，
所以 deploy 之后那一次刷新拿到的**必然**是旧 SW 手里的旧 index.html，要刷第二次才吃到新壳。
**这个症状与「没部署成功」长得一模一样**，这次是把 CI、Variables、线上产物全查一遍才排除掉的。
修法见 `src/platform/pwa.ts`：注册从插件注入的 `registerSW.js` 换成虚拟模块 `virtual:pwa-register`
（`autoUpdate` 下它监听新 SW 的 `activated`+`isUpdate` 自己 reload），另加每小时 / 回前台 / 刚联网三处探测，
以及唯一自己写的那点逻辑「不在打字的当口刷」。原生构建同时从「不加载这个插件」改成 `disable: native`。
**在 `npm run preview` 上实测过两条路**：空闲时来更新 → 3 秒内自己 reload（`navigation.type === "reload"`、
`sessionStorage` 标记存活，证明是同一个标签页）；光标在输入框里时 → 新 SW 已 activated 但 3.5 秒后不刷，
`blur()` 之后立刻刷且保留 hash 路由。原生产物主 chunk 哈希与改动前完全一致，那段代码被整块摇掉。
**注意一个一次性的坎**：这个修复本身要靠一次部署才上线，而浏览器里缓存的是**没有**它的那一版 ——
所以还得手动刷最后一次（或 Ctrl+Shift+R）拿到带自动更新的构建，**从那之后**才是自动的。
`.claude/launch.json` 因此加了个 `preview` 配置：`vite dev` 下 `devOptions.enabled: false`，
SW 压根不装，这类行为只能在 preview 上验。

**iOS 0.2.0 已上传 TestFlight（2026-09-02，run 33661964392）**：6m51s，`UPLOAD SUCCEEDED`，
构建号 4，IPA 400MB。距 `ios-v0.1.1` 隔 22 个提交、五摊活（新图标与按系统语言分设的 App 名、
内置词典 + 预置词库、四选一复习、同步换自建后端 + Google 登录、逐词高亮与分层标准）。
**这是第一个带同步的 iOS 包**，也是第一个带新图标和本地化 App 名的包。
**第一次 archive 挂了**（exit 65）：命令行上的 `PROVISIONING_PROFILE_SPECIFIER` 会套到每一个
target，包括 Google 登录插件带进来的那一大片 SPM 包，而它们不支持描述文件。修法与形状写在
SPEC §7.10 与 `.github/workflows/release-ios.yml` 的 archive 步注释里。
**重跑走的是 `workflow_dispatch`（`--ref main`）而不是新 tag** —— `ios-v0.2.0` 原本打在了
修复之前的提交上。那个 tag 已删并重打到真正被构建的提交 `83e67b2`（2026-09-03），
所以**这个 tag 名下没有 run**，查构建记录要去 run 33661964392。
顺带一条：**推 `ios-v*` 会触发一次完整出包**，重打旧 tag 时要么先
`gh workflow disable release-ios.yml`，要么推完立刻 `gh run cancel` ——
否则白跑 7 分钟并在 TestFlight 里多一个内容相同的构建。
上传成功之后还有两道不在 CI 里：ASC 处理十几分钟转 `VALID`（出口合规自动过），
以及内部测试组必须存在（0.1.0 那次已建，这次不用再建）。

**web 版 Google 登录进不去，是两个叠在一起的 bug（2026-09-03，已修未部署）**：他截图里是弹窗中的
`错误 400: redirect_uri_mismatch`。第一层：`@capgo/capacitor-social-login` 的 **web 实现不是 One Tap**，
只有「弹窗 + 完整 OAuth 重定向」一条路（`window.open` → `accounts.google.com/o/oauth2/v2/auth`
带 `response_type=token id_token`），`redirect_uri` 默认取 `origin + pathname`，**末尾带一条斜杠**；
而 `deploy/README.md` ② 当初让他登记的是裸域名，还附了一句「GIS 用不到重定向，填上无害」—— 那句话是错的。
Google 对这个值逐字符比对。第二层要修好第一层才会露出来：弹窗跳回的是本站首页，
那个窗口会**再启动一遍整个 App**，而「postMessage 回主窗口 + `window.close()`」挂在插件模块 import 的副作用上，
我们又是懒加载插件 —— 弹窗里没人 import 它，主窗口就一直等到 5 分钟超时（界面停在「登录中…」）。
修法：`src/sync/session.ts` 的 `oauthRedirectUrl()` 把地址写死成裸 `location.origin`（跟部署路径与 hash 路由脱钩，
只剩一个值要登记），`src/main.tsx` 先用 `isOAuthPopupReturn()` 认出弹窗、只 import 插件不启动 App。
论证写进 SPEC 的 FR-11.3 下面那段引文与 `deploy/README.md` ②。
**测法值得复用**：不用真账号也能验 OAuth 配置 —— 把 `window.open` 换成假的截下前端拼的授权 URL，
再把那个 URL 直接喂给浏览器，`redirect_uri` 校验发生在输密码**之前**。实测三行就是全部结论：
`http://localhost:5173` → 正常登录页，`https://d.gamestao.com` → 正常登录页，
`https://d.gamestao.com/`（带斜杠）→ `错误 400: redirect_uri_mismatch`。他已把两条裸地址登记进控制台。
**代码还没部署**，所以线上仍是带斜杠的旧包，在 d.gamestao.com 上点还是同样的 400。
另外这次用的是 `.env.local` 里的 Web 客户端 ID；线上是仓库 Variables 里的 `GOOGLE_WEB_CLIENT_ID`，
两者若不是同一个，结论要重验。

## 下一步建议（按价值排序）

0h. **变更 42 剩的三步**（按顺序做，前两步不需要 Mac）。
   **⚠️ 这三步都排在 `push main` 之前** —— 推 main 会同时发前端和后端，而权重还没上去时
   线上桌面对齐会报「取不到对齐权重」，旧的 `mem_limit: 2g` 又会让新后端第一次对齐被 OOM
   连着把同步带走一次。所以：先放权重 + 改 compose，再推。
   ~~① 服务器那一半上线~~ **✅ 2026-09-21 做完了**：compose（4g）、`model_q4.onnx`、
   fp32、两个 json 都送上去了，容器 healthy，`45b8bfb` 已推 main，CI + deploy 全绿。
   ~~② 权重站真的被用到一次~~ **✅ 线上实测**：d.gamestao.com 上导入同一课 →
   下 230.2 MB → 75 秒对完 45/45 句，confidence 与本机逐位一致。
   **剩下的两步**：
   ~~① 服务器真的算一课~~ **✅ 2026-09-21 他自己跑过一课**：`align.status` 变成 `ready`、
   常驻 **2.145 GiB / 4 GiB（53.6%）**（比原估的 1.3GB 高，已按实测改掉六处传抄），
   而且**闲置释放真的生效**：`09:31:58 2.145GiB/ready` → `09:33:00 56.27MiB/idle`。
   **fp32 一块几秒也量了**：加载权重 2.4 秒、满块（20 秒音频）**6.7 秒**、实时 **2.81×**
   —— 比 q4（4.1~4.5 秒、4.5×）**慢约 1.5 倍**，一课 6:16 从约 1.5 分钟变成约 2 分钟。
   对照表在 `deploy/README.md` §4b。（探针那句「每块平均 4.4 秒」要当心：25 秒音频切成
   20s + 7s，短块只要 2.2 秒，按每秒音频 0.33 秒算才对。）
   ~~② 出一个 0.5.0 的 iOS 包~~ **✅ 2026-09-21 已出、已上传 ASC、已装上真机**
   （`ios-v0.5.0`，构建号 10，run 35583768816，6m04s；`cap sync ios` 绿 = 插件删干净了）。
   **IPA 410.2 MiB → 32.7 MiB，装机 553.6MB → 95.2MB（少 458MB / 83%）。**
   **真机上还剩三条没看**：课程页上没有「在这台手机上算」+ 没登录时那段说明；
   登录后导入一课能自动送服务器算完；热更仍然生效（`/dict/` 那条符号链接现在是
   `linkBundledAssets` 唯一还在链的东西，断了不报错、只是查不到词）。

0g. **热更：0.4.0 的包已经发了（2026-09-21，构建号 9），剩装机那一步。**
   接了热更**仍然必须先发一次包**（热更客户端本身在包里），这一包顺带把积压的
   变更 36~40 一起带上手机（界面重做、学习记录、译文笔记、人耳确认、查词）。
   装上之后按 §10「热更新」那一段的四条真机项走：
   ① deploy 一次 → 杀进程重开 → 设置页「版本」里前端那行变成新的 buildId；
   ② **热更生效之后对齐与查词仍然可用**（这一条验的是符号链接 —— 断了的症状是
   对齐退回「从 HF 下载 200MB」、查词查不到，而不会报错）；
   ③ 再装一个新 IPA → 热更 bundle 被丢掉、回到随包那份，且 `/models/` 没变成断链；
   ④ 回滚演练：main 回退一个 commit 再 deploy → 手机下次启动退回旧版。
   **`minNative` 是手改的**（`scripts/build-ota.mjs`）：以后哪次前端改动用到了新的原生能力，
   记得把它提到那一版壳的版本号，否则包会推给一个没有那些原生代码的旧壳。

0f. **查词剩的两条**（顺手）：① 真断网（不是关掉那个开关）查一个内置词典里有的词 ——
   来源那一行应该写「内置词典 · de.wiktionary 没回应（离线？）」；
   ② 桌面上查词加进来的词，手机上同步之后在生词本里看得到（跟着 `vocab` 文档走，
   理论上不用改什么 —— 但和 0e② 一样，这条链路一次都没真跑过）。

0e. **FR-19 / FR-20 剩的两条**（顺手，不必单独开一次会话）：
   ① 在**真浏览器**里点一次「复制原文与提示词」，确认剪贴板主路径通
   （自动化环境里被拒，验的是退路那一条）；② 桌面贴一课译文 → 手机上
   同一课的通听里看得到中文（跟着 `lesson:<id>` 文档走，理论上不用改什么，
   但这条链路一次都没真跑过）。


0d. **FR-18 的手机侧验证**（和下面 0c、第 3 条那次装机并成一次做）：
   ① 在跟读页锁屏五分钟，回来后今天的时长**没有**涨五分钟（口径的关键一条）；
   ② 「保存 / 分享」在 iOS 上拉起系统分享面板，存进相册的是完整的 1080×1350；
   ③ 桌面练 10 分钟、手机练 5 分钟 → 同步之后两边都显示今天 15 分钟。

0c. **界面重做那一支：部署，然后在 iPhone 真机上看安全区。**
   **合并已经做完了**（2026-09-07，`3b4149c`），剩下的是 push（= 部署）与真机那一步。
   真机上只看三件事：① 底部标签栏与音频条各自让出 home indicator（`env(safe-area-inset-bottom)`），
   两者不并存所以要分别看：列表页看标签栏，课程页看音频条；② 顶栏在刘海下不被切；
   ③ 深色模式下状态栏是深的（`index.html` 里那两条带 `media` 的 `theme-color`）。
   这三条与第 3 条那次装机可以并成一次做。

0aa. **服务器对齐：真手机上走一遍**（0.3.0 已发布并部署，服务端那一半已经在 VPS 上跑过 ——
   数字见上面那张表）。剩下的是只有真机 + 真账号能答的那一步：
   手机（登录状态）导入一课 → 应该**自动**送到服务器算 → 期间锁屏 / 切走 App →
   回来看时间戳到没到。顺带确认课程页那两个按钮（「用服务器对齐」/「在这台手机上算」）
   的措辞在真机上读得通。

   另外一件顺手能做的：拿桌面浏览器对同一课算一遍，和服务器算的对比一下 ——
   三条路用同一份 q4 权重、同一套分块规则，**应该给出同一份时间戳**。
   探针打的那份指纹（frames + 前 5 个 log-prob + 全局均值）就是为这个对比准备的。

0. **真机第一次跑（0.2.1）答了一半：不是卡死，但一课估要 10–15 分钟 —— 于是手机端换了形状。**
   症状是「更新版本之后，自动对齐在开始的时候卡了几分钟没有进展」。查完**没有卡死的 bug**：
   那条路上原本只有一种进度事件（每块算完报一个比例），而它前面排着四件在手机上要几十秒到
   几分钟的事（9MB base64 过桥、原生解码、加载 241MB 权重、第一块推理），
   于是最需要耐心的那几分钟恰好是唯一没有反馈的几分钟。顺带也说明 `MatMulNBits` 那道题
   **大概过了**（没有出现「算子找不到」），但真正的凭据是块数往前走。

   补完进度之后真正的账露出来了：**一课 27 块、估 10–15 分钟，而手机不肯给这么长** ——
   （27 是算出来的：480 秒 / 18 秒步长。每块 20–30 秒是**估**的，依据是桌面 `wasm/q4`
   实测 1.4× 实时 —— 真机上还没跑完过一课，所以下面第 ② 条要的就是这个数。）
   Info.plist 里没有 `UIBackgroundModes`，锁屏或切走 App 之后进程被挂起，
   而 iOS 默认 30 秒到 2 分钟就自动锁屏。所以做了四件事（§0 变更 33、FR-15.15/16）：
   手机上**自动对齐不再启动**（主路径是「桌面算完同步过来」，课程页把这件事说清楚）、
   手动那一次**防锁屏**、**断点续算**（每块落盘，切走/被杀/按停止都能接着算）、
   进度改成三阶段 + 块数 + 秒表 + 「约还需」。

   **下一步是出 0.2.2，在真机上读三个数**：
   ① 「加载对齐模型」那一段多久 —— 进度条走到「第 0/27 块」的那一刻就是权重加载完；
   ② 每块多少秒 —— 块号一格一格跳，秒表在旁边；
   ③ 切走 App 再回来、以及按「停止」再点「接着算」，块号是不是从上次那一块继续。
   拿到 ② 之后才动 `EmissionsEngine` 里 `setIntraOpNumThreads(activeProcessorCount / 2)` 那行 ——
   它的注释说这是「大核数量」，**在 iPhone 上是错的**（A15 是 2 大 + 4 小，`6/2 = 3`
   会把一个小核算进去，而 ORT 的 intra-op 等最慢那个线程）。没有盲改：本机验不了 Swift。

0b. **新复习流程还差 iPhone 真机那一关**（桌面浏览器已经点过一轮，见上面）。三件事只有真机能答：
   ① **iOS 的自动播放手势链** —— 新交互是「点选项即判分」，与原来的「点显示答案」不同，
   而 §3.2 那条约束（只有用户手势链上的 `<audio>` 能开始播放）在新流程下要重测：
   自动播那一次仍然可能被拒，此时必须还能点大圆按钮救回来。
   ② **2×2 在 375px 的真机上按起来顺不顺手**（桌面按 430px 量过，2×2 落在拇指区、
   答错态不用滚动；375px 与真手指还没验），以及辨义题的释义在那个宽度下读不读得下去。
   ③ **报名第 4 档之后连着用三天**，确认每天真的只发 `newPerDay` 张、且课上标的词占额度。
   （单测覆盖了这条规则，但「跨天」这件事只有真的过一夜才算验过。）
1. **先部署一次（web 登录的修复还在工作区外面没上线），再真的用 Google 登录跑一遍，然后走完 §10 里标 ❌ 的那一串**，
   尤其是**恢复演练** —— 文档里写了「不做完不算验收通过」。链路本身已经全部接通（DNS、两个客户端 ID、Caddy、CI，
   见上面「大窟窿一」），**三件配置都不用做了**，控制台的重定向 URI 也已经补齐（见上面那段）；
   剩下的只有部署 + 登录这一下和跨设备恢复，而登录要在弹窗里输密码，只能他做。判据很直接：`sync.gamestao.com/v1/healthz` 现在还是
   `users:0`，登录成功它就不是 0 了。排错与验收清单在 `deploy/README.md`。
   附录 B.2 那三个 GitHub 待确认项**不用填了**，它们随 PAT 一起作废。
2. **实际用两周**，然后再回来改。§10 里标 🧪 的几条（跟读循环、多区间挖空）代码路径有测试，
   但没有人真的连着跟读过十分钟，手感问题只有用出来。
3. **在真机上跑一课，然后读「设置 → 对齐后端」。最新的包是 0.2.1 构建号 7**（run 33750498587，
   IPA 408MB，第一个带原生对齐插件的 —— 见第 0 条）；上一个是 0.2.0 构建号 4（run 33661964392，400MB）。
   （这个包**带上了**新图标、本地化 App 名、词典、四选一复习和同步 —— 0.1.1 那个包
   一样都没有。所以真机上要确认的事这一次是一批，不是一件。）
   这是那次崩溃修复的唯一验收：
   诊断页那一行写「分片取权重」= 修复生效；万一还是被杀，顶部黄条会说死在哪一步、哪套后端、
   多少字节 —— 这次不会再是黑箱。本机没有 Mac，所以只能靠设备自己回答。
   同一次装机还要确认另外两件：① 导出备份能不能落到「文件」App；
   ② 顶部安全区在真机刘海下对不对；顺便记一下真机上一课实际跑多久。
   **桌面壳（Electron）仍未做，也不急** —— 桌面就是浏览器。
4. ~~**手机端自动对齐要么换小模型、要么走原生插件**~~ —— **原生插件那条已经写完并出包**
   （2026-09-03，见第 0 条与现状那一段），真机验证是第 0 条。
   **换小模型那条仍然摆在桌上**，而且它解决的是原生插件解决不了的那一半：
   没有 WebGPU 的浏览器仍然要背 230MB 的 `wasm/q4`，Android 壳也还没有原生插件。
   改的是配置（`config.ts` 顶部写了换法：modelId / vocabSize / blankId + romanize 退化成恒等）
   而不是架构。**换模型之后 `REVIEW_MARGIN` 必须重标一次** —— 整条置信度分布会整体平移。
5. **砍权重 —— 方向被原生插件掀翻了，而且必须等第 0 条验过才能动。**
   原来的算盘是「iOS 走 `q4f16`，所以砍第 2 档那份 `q4`」。现在**原生插件用的正是 `q4`**，
   所以 iOS 包里的死重换成了 `q4f16`（196.7 MiB）—— 砍它能把装机压到 210MB 量级。
   **顺序不能反**：先砍权重再验插件，插件一旦不工作，那台设备连回退到 WebView 的路都没有了。
   （纯 web 版仍然必须两份都带 —— 用户设备什么都有。）
6. **在 iPhone 上确认预置卡到底有没有真人音**（见上）。设置页「词典」那一节会显示发音缓存
   条数与体积，以及系统有没有德语嗓音 —— 如果真机上几乎全是合成音，那就说明
   Lingua Libre 的 wav 覆盖率不够，届时要么接受合成音，要么在构建期把 ogg 转码
   （但那要引入 ffmpeg，与 §7.6「不为一次性构建产物装依赖」冲突，得单独权衡）。
7. Q7：存档列表页 `/de/alltagsdeutsch/s-9214` 是否也在 `__APOLLO_STATE__` 里给出完整的 400+ 期列表。
   若给了，L1 就能覆盖全部存档，大约 10 分钟的增量。

明确不要做（文档里已经论证过，不要重开讨论）：

- ~~不建任何后端、代理或本地 helper 进程~~ —— **2026-09-02 部分作废**：同步后端建了（`server/`）。
  R-1 管的是**内容获取通路**，抓 DW 仍然只从设备直连、无代理；同步后端在通路下游，
  只存用户自己那份私人复制。这条界线写进了 SPEC §3.1.1 的「同步后端与 R-1 的关系」。
  **仍然不做的是：代抓内容的代理、公共 CORS 代理、本地 helper 进程。**
- ~~不做 GitHub OAuth 登录~~ —— 整条 GitHub 路已删（SPEC §0 变更 25）。登录改用 Google，
  验签在自己的后端做，附录 B.1 那条「纯前端拿不到 token」的限制不再相关
- ~~不做自动强制对齐~~ —— **这条已作废，FR-15 做了**（用户 2026-08-31 明确要求集成进应用）。
  自动挖空难度判定、词形还原仍是 V2
- 不做内容分享或托管功能（§3.1 法律边界）
- 不做中文 → 德语方向的卡片（§7.5）
- 不做「一键导入全部期次」（§3.1.1 R-3）

几个容易踩的坑（前四条来自文档，其余都是实现期踩出来的；FR-15 与那次 iOS 崩溃各贡献了一批。全部写进了代码注释）：

- 全程复用同一个 `<audio>` 元素（iOS 手势链约束，§3.2）
- HTML → 纯文本转换必须同步维护 offset 映射，否则 Glossar 候选词会静默错位（§7.8）
- 排序去重用 `firstPublicationDate` 而不是 `pubDate`（DW 在重推旧期，附录 A.2）
- `Intl.Segmenter` 对 `z. B.` 一定会断错，切句强制要有手工修正 UI（FR-2.3、§7.1）
- **改 Lesson 一律走 `useLessonStore.patchLesson`**，不要拿组件闭包里的 lesson 直接 `saveLesson` ——
  连按 Enter 打点时后一次会把前一次静默抹掉。`saveLesson` 也必须**先 `set` 再 `await putLesson`**
- `Sentence.index` 一律等于数组下标，**排除不重排号**；合并/拆分才重排，且必须拿 `indexMap`
  同步 `VocabEntry.sentenceIndex`（SPEC §0 第 16 条）
- 跨句的整段引语会被 R-quote 合并成一句（最长见过 437 字符）。这是规则的正确后果，不是 bug；
  跟读嫌长就手工拆（附录 A.6）
- **同步后端第一次起来会报 `ERR_SQLITE_ERROR: unable to open database file`**：`./data` 是 docker
  头一次挂载时建的，属 root，而容器里跑的是非 root 用户。这台机器上 `sudo` 要密码，
  所以借一个 root 容器改属主：`docker run --rm -v "$PWD/data:/data" busybox chown -R 1001:1001 /data`。
  compose 里的 `user: "${SYNC_UID}:${SYNC_GID}"` 从同目录 `.env` 插值
- **`sync.gamestao.com` 的 DNS 必须是灰云（仅 DNS）**。橙云代理下 Caddy 拿不到 ACME 挑战，
  证书永远签不下来，症状是浏览器报 SSL 错而服务器日志里只有重试
- **「deploy 之后刷新还是旧版」不是部署失败**，是 Service Worker 从预缓存直出导航请求
  （`navigateFallback`），第一次刷新拿到的必然是旧壳。查这类症状**先看线上产物**
  （`curl` 拿 index.html 里那个 `index-*.js` 再 grep 里面的字符串），不要先怀疑 CI。
  变更 30 之后这件事自动了，但**原生壳与任何关掉 SW 的构建上这条推理仍然有效**
- **验 Service Worker 的行为只能用 `npm run preview`**：`vite dev` 下 `devOptions.enabled: false`，
  SW 压根不装。`.claude/launch.json` 里有个 `preview` 配置就是为此
- **Google 的 `webClientId` 在 Android 上填的也是 Web 那个客户端 ID**，不是 Android 客户端 ID。
  Android 客户端只在 Google 控制台登记（包名 + 签名 SHA-1），不进代码；填错的报错是
  `[28444] Developer console is not set up correctly`
- **仓库根 vitest 要排掉 `server/**` 和 `.claude/**`**：前者在 jsdom 里跑会让 jose 不认
  jsdom 的 `Uint8Array`（报密钥类型错），后者是 git worktree，会把同一批测试跑第二遍
- **FR-15 之前手打的时间戳没有 `timingSource` 字段**，所以「是不是人工的」必须判
  「`=== 'manual'` **或** 有 startTime 但 timingSource 缺失」。只判前者会让一次自动打点
  静默覆盖掉几十分钟手工活（`src/align/apply.ts` 的 `isManual`）
- **Web Audio API 在 Web Worker 里不存在**（`AudioContext`/`OfflineAudioContext` 只在主线程）。
  对齐必须在 Worker 里跑，所以解码留主线程、波形 transfer 进去
- **主线程不要 import `src/align/runtime.ts` 或 `emissions.ts`** —— 它们连着 transformers.js +
  onnxruntime-web，一 import 主包就从 497KB 涨到 1MB。纯配置在 `src/align/config.ts`
- transformers.js **默认从 jsdelivr CDN 拉 ORT 的 wasm**，不覆盖 `wasmPaths` 断网就起不来。
  现在走 Vite `?url`（`onnxruntime-web/ort-wasm-simd-threaded[.asyncify].{wasm,mjs}`）。
  **不要**改回复制到 `public/ort/` —— dist 里会出现两份同样的 23MB wasm
- Cloudflare 的 SPA fallback 给缺失路径返回 **200 + index.html**，所以探测本地模型
  不能只看 `res.ok` —— 要 GET 那份 `config.json` 并真的 parse 一遍。曾经是 HEAD + 验 content-type，
  原生壳里 dist 不由 HTTP 服务器托管，HEAD 和响应头都不保证（§7.10）
- 待校对阈值必须是**相对本课中位数**的。绝对阈值随模型漂：实测中位数 -1.11，
  按感觉写的 -0.6 会把 37/38 句全标黄。**而且标定之前先确认产生这条分布的代码是对的** ——
  那次量到的「最差 -3.50」其实是滑窗漂移的症状，却被当成素材的性质写进了标定注释；
  窗口修好后分布收窄，那个 0.8 的余量就够不着尾巴了（三课报 4/0/0），只好重标为 0.5。
  找 bug 的判据要用**物理上不可能的量**：这次指认出问题的不是置信度，是「一句 45~53 字符/秒」
  （德语朗读实测 12~15 字符/秒）
- **别拿 `int8` 当 WASM 的兜底**（302.6 MiB，加载即被杀，桌面 32GB 也一样）。门槛就是体积：
  ORT 的 wasm 堆里同时有三份左右（JS 缓冲 + protobuf 解析 + 权重张量），230MB 过得去、300MB 过不去。
  4-bit 那两份都跑得通（q4 230.3 / bnb4 212.2 MiB）
- **滑窗的窗口必须装得下最长的那句话还有余。** 窗内 token 数只能靠全局平均速率估，
  窗口一短，估计误差就与「一整句」同量级，而滑窗单调不回退于是一路累积。
  这条不变量钉在 `windowed.test.ts` 里（`LONGEST_SENTENCE_CHARS` / `SLOWEST_CHARS_PER_SECOND`）。
  **这个失败造不出可信的单测** —— 试过合成矩阵，匀速漂 0、加速率摆动仍漂 0（合成后验太确定），
  把正确 token 概率压到 0.25 才在 30 秒窗漂出 144 帧而 120 秒窗仍是 0，量级差太远。
  失败过程记在那个测试文件的注释里，不用再试一遍
- **「排除」这个默认值只能站在「音频里真的没有」那一边。** 把念过的句子排掉是在音频里挖空洞
  （对齐器必须把那段声音塞给邻句）；把没念的句子留着，最坏也只是那一句自己拿到低置信度并被标出来。
  两个方向的代价不对称，所以自动导入现在一句都不排（SPEC §0 变更 24）
- **`src/align/emissionMatrix.ts` 不许 import `@huggingface/transformers`。** 那是 emissions 与
  viterbi 之间那道缝的定义文件（FR-15.13），主线程、单测、将来的线缆编解码器都只需要它的类型
- **不要一次性整取权重。** Capacitor 的 iOS handler 对非媒体扩展名走 `Data(contentsOf:)`，
  加上 transformers.js 那次无谓的缓存回写，峰值接近 1GB，真机表现是应用被系统直接杀掉。
  走 `src/align/rangedFetch.ts` 的 8MB 分片（Capacitor 的 Range 分支用 `FileHandle.seek`）
- **判断服务端支不支持 Range 不能只看 206。** Vite dev server 会回
  `206 + Content-Range: bytes 0-196703175/196703176`（整份），只认状态码会让每片都拖回整份文件。
  要比对「回来的区间是不是真的就是我要的那一个字节」
- **进程被系统杀掉时 JS 跑不了任何收尾代码**，所以每个阶段都同步写一条 localStorage 面包屑
  （`src/align/journal.ts`）。localStorage 在 Worker 里不存在 —— 这就是后端选择挪到主线程的原因
- **`detectCrash()` 有副作用（把 running 记录归档），所以 `useAlignStore.init()` 必须真的只跑一次。**
  StrictMode 在开发模式下把 effect 调两遍，第二遍会用 null 把「上次被杀掉了」那条黄条覆盖掉 ——
  实测就是这样丢的，记录归档正常而界面上什么都没有

接原生壳（§7.10）踩出来的六条：

- **`base` 千万不要改成 `'./'`。** 上面那句「打包要配 base: './'」只对 **Electron 的 `file://`**
  成立；Capacitor 有真正的 URL 根（iOS `capacitor://localhost/`、Android `https://localhost/`），
  绝对路径原样就对。这条已经在 SPEC Q9 里改正
- **原生构建必须去掉 Service Worker**（`vite build --mode native`）。`autoUpdate` 的 SW 一旦缓存了
  旧壳的 index.html，`cap sync` 换掉 dist 也照样吃旧的，而原生壳里没有地址栏可以清它
- **`a[download]` 在 WKWebView 里静默失效** —— 不报错、不下载。存文件一律走 `src/lib/download.ts`
- **iOS 的 AppIcon 不能带 alpha 通道**（看的是通道在不在，不是有没有透明像素）。
  Xcode 本地构建完全不报错，第一次上传 App Store 才炸
- **Capacitor 8 的 iOS 走 SPM 不是 CocoaPods**，所以 xcodebuild 用 `-project` 而不是 `-workspace`、
  不跑 `pod install`；模板也不带共享 scheme（自动生成的那份在 gitignore 掉的 `xcuserdata/` 里），
  所以 `App.xcscheme` 手工写了一份入库。funny 停在 Capacitor 6，那边的 workflow 不能照抄
- **自己写的原生代码必须做成一个装在 `package.json` 里的 npm 包**，不能直接往 iOS 工程里加文件。
  `ios/App/CapApp-SPM/Package.swift` 由 `cap sync` 按已装插件整份重写（文件头就写着 DO NOT MODIFY），
  手工加的依赖会在下一次 sync 时被抹掉 —— 而 CI 每次出包都跑 `cap sync ios`。
  形状见 `native-plugins/align-native`（`file:` 依赖 + `capacitor.ios.src` + 根目录 `Package.swift`）。
  **包名与 SPM 名字的对应关系是算出来的**：`deutsch-align-native` 经 CLI 的 `fixName`
  变成 `DeutschAlignNative`，Package.swift 里的 `name:` 与库产物名必须逐字等于它，
  否则生成的那份引用不到这个包 —— 而报错发生在 Xcode 里，不在 sync 里

环境：Windows 11 + PowerShell。给我可执行命令时请用 PowerShell 语法（`curl.exe` 而非 `curl`，
`Select-String` 而非 `grep`，`Select-Object -First N` 而非 `head`）。
