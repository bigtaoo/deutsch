# 新会话开工提示

> 复制下面整段，粘贴到新会话即可开工。

---

项目：德语精听训练器（个人自用工具，工作目录 D:\deutsch）

**先读 SPEC.md。** 需求文档已定稿（约 1020 行），包含完整的功能需求、数据模型、技术决策、风险表和验收清单。附录 A 是 DW 接口的实测结果，附录 B 是 GitHub API 的实测结果——这些是真实探测出来的事实，不要重新假设，也不用重新验证（除非怀疑对方改版了）。

现在开工 §9 实现顺序里的第 1 步和第 1b 步：

1. **项目脚手架**：Vite + React + TypeScript + Tailwind + Vitest
2. **数据层**：IndexedDB 两层 schema（§2.3 缓存层 / 标注层，§6 数据模型），用 `idb`；启动时 `navigator.storage.persist()`
3. **导出 / 导入 / 合并**：§2.4 的合并规则，FR-11.11、FR-11.14
4. **GitHub 连接与自动备份**：FR-11.1 ~ FR-11.10。重点是「粘贴一次 PAT，其余全自动」——自动识别账号、一键创建私有仓库、校验 private 与写权限、监控 token 过期

第 1b 步紧跟第 1 步，在积累任何真实数据之前就要跑通。等攒了半年再补备份，那半年是裸奔的。

**明确不要做**（文档里已经论证过，不要重开讨论）：

- 不建任何后端、代理或本地 helper 进程（§3.1.1 R-1）
- 不做 GitHub OAuth 登录（附录 B.1 实测：端点无 CORS、不支持 PKCE，纯前端不可行）
- 不做自动强制对齐、自动挖空难度判定、词形还原（都是 V2）
- 不做内容分享或托管功能（§3.1 法律边界）
- 不做中文 → 德语方向的卡片（§7.5）

**几个容易踩的坑**，文档里都有细节：

- 全程复用同一个 `<audio>` 元素（iOS 手势链约束，§3.2）
- HTML → 纯文本转换必须同步维护 offset 映射，否则 Glossar 候选词会静默错位（§7.8）
- 排序去重用 `firstPublicationDate` 而不是 `pubDate`（DW 在重推旧期，附录 A.2）

**先给实现计划，不要直接开始写代码。**

环境：Windows 11 + PowerShell。给我可执行命令时请用 PowerShell 语法（`curl.exe` 而非 `curl`，`Select-String` 而非 `grep`，`Select-Object -First N` 而非 `head`）。
