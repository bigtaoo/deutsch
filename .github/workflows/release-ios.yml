name: release-ios

# 出 iOS 包并上传 App Store Connect（→ TestFlight）。
#
# 形状抄的是同账号下 funny 项目的 release-ios.yml，但有三处**必须不一样**：
#
#  1. **`-project` 而不是 `-workspace`。** Capacitor 8 的 iOS 模板走 SPM
#     （ios/App/CapApp-SPM/Package.swift），不再有 CocoaPods，所以既没有
#     `App.xcworkspace`，也不需要 `pod install`。funny 停在 Capacitor 6，那边是 Pods。
#  2. **不用 `agvtool`。** 这份 pbxproj 没有 `VERSIONING_SYSTEM = apple-generic`，
#     agvtool 会以「找不到版本号」失败。改成在 xcodebuild 命令行上覆盖
#     `CURRENT_PROJECT_VERSION` / `MARKETING_VERSION` —— Info.plist 里本来就是
#     `$(CURRENT_PROJECT_VERSION)`，覆盖会一路传下去。
#  3. **不带对齐权重。** 变更 42 之前这里有一步 `npm run stage:align`，把 418 MiB
#     的 CTC 权重放进 public/ 随包带走（装机 553.6MB）。iOS 上的本地对齐整条删掉之后
#     那一步连同 `deutsch-align-native` 那个 ONNX 插件一起没有了 ——
#     `cap sync ios` 不再往 Package.swift 里写 binaryTarget，SwiftPM 也不再需要
#     在 resolve 阶段联网拉 xcframework（那曾经是这条流水线唯一一处会莫名失败的地方）。
#
# 触发：推 tag `ios-v*`（如 ios-v0.1.0），或手动 Run。
#
# 需要在仓库 Settings → Secrets and variables → Actions 配置：
#   BUILD_CERTIFICATE_BASE64        Apple Distribution 证书（.p12）的 base64
#   P12_PASSWORD                    该 .p12 的密码
#   KEYCHAIN_PASSWORD               任意字符串；runner 上临时钥匙串的密码
#   BUILD_PROVISION_PROFILE_BASE64  App Store 类型的描述文件（.mobileprovision）base64
#   PROVISIONING_PROFILE_NAME       该描述文件在开发者后台显示的名字
#   APPLE_TEAM_ID                   10 位 Team ID
#   ASC_API_KEY_ID                  App Store Connect API key id
#   ASC_API_ISSUER_ID               App Store Connect API issuer id
#   ASC_API_KEY_CONTENT_BASE64      那份 .p8 私钥的 base64
#
# 上传成功不等于能装：ASC 那边要处理十几分钟，之后内部测试员自动可见，
# 外部测试组第一次需要过一次 TestFlight 审核。

on:
  push:
    tags:
      - 'ios-v*'
  workflow_dispatch:

concurrency:
  group: release-ios
  cancel-in-progress: false

jobs:
  build-and-publish:
    # 与 funny 的 release-ios.yml 用同一个 runner 镜像。
    runs-on: macos-26

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      # onnxruntime-node（@huggingface/transformers 带进来的）的 postinstall 在 linux x64 上会去
      # NuGet 下 CUDA EP —— 这里只用 CPU（随包自带），下了也用不上，却让安装看微软服务器的
      # 脸色：2026-09-23 deploy 就因为连 150.171.110.152:443 ETIMEDOUT 红过一次。
      # server/Dockerfile 早就用 --ignore-scripts 跳过了同一件事。
      - name: install
        env:
          ONNXRUNTIME_NODE_INSTALL: skip
        run: npm ci

      # tag 推上来时版本号取自 tag（ios-v0.1.0 → 0.1.0）；手动 Run 时取 package.json。
      # 构建号一律用 run_number：单调递增，不需要有人记得改，而 ASC 只要求它在同一个
      # 版本号下唯一。
      - name: resolve version
        id: version
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" && "${GITHUB_REF_NAME}" == ios-v* ]]; then
            VERSION="${GITHUB_REF_NAME#ios-v}"
          else
            VERSION="$(node -p "require('./package.json').version")"
          fi
          echo "marketing=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "版本 ${VERSION} 构建号 ${GITHUB_RUN_NUMBER}"

      - name: build web bundle (native mode)
        # 同步后端的地址与 Google 客户端 ID 是**构建期**注入的（见 .env.example）。
        # 不给的话构建出来的包里同步整块是关的 —— 而且不会报错，只在设置页显示
        # 「这个构建没有配置同步服务器」，很容易被当成代码坏了。
        # 放 Variables 不放 Secrets：客户端 ID 本来就会原样出现在下发的 JS 里，
        # 它不是秘密（秘密是服务器那边的 SESSION_SECRET，那个不进这个仓库）。
        env:
          VITE_SYNC_API_BASE: ${{ vars.SYNC_API_BASE }}
          VITE_GOOGLE_WEB_CLIENT_ID: ${{ vars.GOOGLE_WEB_CLIENT_ID }}
          VITE_GOOGLE_IOS_CLIENT_ID: ${{ vars.GOOGLE_IOS_CLIENT_ID }}
        run: npm run build:native

      # 把 dist/ 刷进 ios/App/App/public，并按 package.json 里的插件重写 Package.swift。
      # Capacitor 8 是 SPM，这一步不跑 pod install。
      - name: cap sync ios
        run: npx cap sync ios

      # 变更 52：热更曾经在真机上整个哑掉，最像的成因是 CapacitorUpdater 这个类没能
      # 链进最终二进制（SwiftPM 静态库剥掉了「看起来没人用」的符号）。这一步只能查出
      # 「配置有没有列它」，查不出「链接器最后留没留它」—— 但配置漏了它，后面必然是
      # 同一种哑法，所以先把这道最便宜的门槛卡住：两个源都得看得见 CapacitorUpdater。
      - name: 校验热更插件确实进了这次构建
        run: |
          grep -q CapacitorUpdaterPlugin ios/App/App/capacitor.config.json || {
            echo "::error::capacitor.config.json 的 packageClassList 里没有 CapacitorUpdaterPlugin —— 这次构建装上去热更会整个不生效"
            exit 1
          }
          grep -q CapgoCapacitorUpdater ios/App/CapApp-SPM/Package.swift || {
            echo "::error::CapApp-SPM/Package.swift 没有 CapgoCapacitorUpdater 依赖"
            exit 1
          }

      - uses: apple-actions/import-codesign-certs@v3
        with:
          p12-file-base64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
          p12-password: ${{ secrets.P12_PASSWORD }}
          keychain-password: ${{ secrets.KEYCHAIN_PASSWORD }}

      - name: install provisioning profile
        run: |
          PP_PATH="$RUNNER_TEMP/profile.mobileprovision"
          echo -n "${{ secrets.BUILD_PROVISION_PROFILE_BASE64 }}" | base64 --decode -o "$PP_PATH"
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp "$PP_PATH" ~/Library/MobileDevice/Provisioning\ Profiles/

      # 不入库：里面有 Team ID 和描述文件名，而且它完全由 secrets 决定。
      - name: write ExportOptions.plist
        run: |
          cat > "$RUNNER_TEMP/ExportOptions.plist" << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>app-store-connect</string>
              <key>teamID</key>
              <string>${{ secrets.APPLE_TEAM_ID }}</string>
              <key>uploadSymbols</key>
              <true/>
              <key>signingStyle</key>
              <string>manual</string>
              <key>provisioningProfiles</key>
              <dict>
                  <key>com.gamestao.deutsch</key>
                  <string>${{ secrets.PROVISIONING_PROFILE_NAME }}</string>
              </dict>
          </dict>
          </plist>
          EOF

      # CODE_SIGN_IDENTITY 用 "Apple Distribution" —— 2019 年之后签发的分发证书都叫这个名。
      # 如果这一步报「No signing certificate found」而钥匙串里那张是老证书，
      # 把它改成 "iPhone Distribution"（funny 那份 workflow 就是这么写的，它的证书更老）。
      #
      # **描述文件名走 DEUTSCH_PROFILE_NAME 这个自定义变量，不要直接给
      # PROVISIONING_PROFILE_SPECIFIER。** 命令行上的构建设置会套到**每一个** target 上，
      # 包括 SPM 依赖的包 target，而那些包不支持描述文件，报错形如
      #   「Alamofire_Alamofire does not support provisioning profiles」—— 一个包一条，
      # archive 直接失败（exit 65）。0.1.1 没撞上是因为当时 SPM 图里只有 Capacitor 自己的包；
      # @capgo/capacitor-social-login 带进来 GoogleSignIn / AppAuth / Facebook SDK /
      # Alamofire / GoogleUtilities / Promises / GTM* 一大片，全都会报。
      # 所以真正的设置写在 App target 的 Release 配置里（project.pbxproj 中
      # PROVISIONING_PROFILE_SPECIFIER = "$(DEUTSCH_PROFILE_NAME)"），这里只喂那个变量：
      # 包 target 照样收到 DEUTSCH_PROFILE_NAME，但 Xcode 不把它当签名设置，于是不报错。
      # 副作用也是想要的：本机用 Xcode 打开时这个变量是空的，App target 里
      # CODE_SIGN_STYLE = Automatic 会接手，不需要在开发机上装那份分发描述文件。
      - name: archive
        run: |
          xcodebuild \
            -project ios/App/App.xcodeproj \
            -scheme App \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath "$RUNNER_TEMP/App.xcarchive" \
            archive \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            DEVELOPMENT_TEAM="${{ secrets.APPLE_TEAM_ID }}" \
            DEUTSCH_PROFILE_NAME="${{ secrets.PROVISIONING_PROFILE_NAME }}" \
            MARKETING_VERSION="${{ steps.version.outputs.marketing }}" \
            CURRENT_PROJECT_VERSION="${{ github.run_number }}"

      - name: export ipa
        run: |
          xcodebuild \
            -exportArchive \
            -archivePath "$RUNNER_TEMP/App.xcarchive" \
            -exportOptionsPlist "$RUNNER_TEMP/ExportOptions.plist" \
            -exportPath "$RUNNER_TEMP/ipa"
          ls -lh "$RUNNER_TEMP/ipa"

      - uses: actions/upload-artifact@v4
        with:
          name: ios-ipa-${{ steps.version.outputs.marketing }}-${{ github.run_number }}
          path: ${{ runner.temp }}/ipa/*.ipa
          # 带权重的包约 250MB，留一份能装的产物就够，不用占着仓库配额三个月。
          retention-days: 14

      - name: upload to App Store Connect
        run: |
          mkdir -p ~/private_keys
          echo -n "${{ secrets.ASC_API_KEY_CONTENT_BASE64 }}" | base64 --decode \
            > ~/private_keys/AuthKey_${{ secrets.ASC_API_KEY_ID }}.p8
          xcrun altool \
            --upload-app \
            --type ios \
            --file "$(ls "$RUNNER_TEMP"/ipa/*.ipa | head -1)" \
            --apiKey "${{ secrets.ASC_API_KEY_ID }}" \
            --apiIssuer "${{ secrets.ASC_API_ISSUER_ID }}"
